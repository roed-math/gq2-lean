# Ticket board — step 2 (proving Theorem 1.2 from the axioms)

Source of truth for the proof phase.  Rationale, DAG, conventions, and wave schedule:
[`step2-plan.md`](step2-plan.md).  The **step-1 board** (statement formalization, ticket IDs
`T-xx` cited in module docstrings) is archived at [`tickets-step1.md`](tickets-step1.md).
Difficulty: ⭐ easy · ⭐⭐ medium · ⭐⭐⭐ hard/design-sensitive.  Model: **F** = Fable
(design-heavy), **O** = Opus (well-specified).  Status: ☐ open · ◐ in progress · ☑ done.

Rules: **no new `axiom`s without explicit user approval** (census at 13 — B10 added by explicit decision, P-06 escalation; B9 base-generalized + B11 added by explicit decision, P-15 escalation, user-approved 2026-07-03; **B11 split into B11a `hilbertSymbol_normCriterion_finiteDyadic` + B11b `unramifiedQuadratic_units_are_norms` by P-23, census 12→13, user-approved 2026-07-04** — `dyadicNormCriterion` kept as a same-name theorem, spectral-norm bridge isolated as a `def IsUnramifiedQuadraticSpectral`, not an axiom); statement tickets add their sorried theorems to
`SORRY_ALLOWLIST` in `scripts/check_axioms.sh` (same commit, entry cites the ticket), proof
tickets remove them; every ticket's new theorems satisfy `#print axioms` ⊆ std-3 ∪ the ticket's
**Ax** column; `GQ2/Foundations/Axioms.lean` is frozen; sessions claim a ticket by marking its
row ◐ before starting.  Paper: `paper/A_Profinite_Presentation_for_G__Q_2.pdf`.

| ID | Title | Diff | Model | Deps | Ax | Status |
|---|---|---|---|---|---|---|
| P-00 | Meta: step-2 plan + board + LeanBridge tooling import | ⭐ | **F** | — | — | ☑ 2026-07-03 (`docs/step2-plan.md`, `.claude/`) |
| P-01 | Meta: `docs/STATUS.md` refresh + repo-wide axiom ledger (batch `#print axioms` check vs App. D) | ⭐⭐ | O | — | — | ☑ 2026-07-03 (`GQ2/AxiomLedger.lean`, `docs/STATUS.md`) |
| P-02 | A: fill `exists_contSurj_of_card_le` (recipe at the sorry: cofiltered system + compactness) | ⭐⭐ | O | — | ∅ | ☑ 2026-07-03 (`GQ2/Reconstruction.lean`) |
| P-03 | A: t.f.g. — `FreeProfiniteGroup X` (finite `X`), quotients of t.f.g., `GammaA` t.f.g. | ⭐⭐ | O | — | ∅ | ☑ 2026-07-03 (`GQ2/FinitelyGenerated.lean`) |
| P-04 | A: universal marking admissible-in-the-limit (relator words ∈ `N_A`; wild pair's closed normal closure pro-2 in `Γ_A`) | ⭐⭐⭐ | **F** | — | ∅ | ☑ 2026-07-03 (`GQ2/AdmissibleLimit.lean`) |
| P-05 | A: Prop 2.3 — `Nat.card (ContSurj GammaA G) = admissibleCount G` | ⭐⭐⭐ | **F** | P-04 | ∅ | ☑ 2026-07-03 (`GQ2/Prop23.lean`) |
| P-06 | B: §3 statement extraction — Lemmas 3.4–3.8, Prop 3.2, Prop 1.1 as sorried statements + design note | ⭐⭐ | **F** | — | (statements) | ☑ 2026-07-03 (`GQ2/SectionThree.lean` + `GQ2/SectionThreeMarked.lean`, `docs/section3-extraction.md`) |
| P-07 | B: Lemmas 3.4/3.5 proofs (eq. (13) ledger: square-class basis, χ/ν rows, cup form `α²+βγ+γβ`) | ⭐⭐ | O | P-06 | B5, B7′ | ☑ **CLOSED 2026-07-03 (Opus)** — all four clauses proved, `SectionThree.lean` sorry-free & off the `SORRY_ALLOWLIST`, census unchanged (12). `lemma_3_5_hilbert_ledger` ✓ (B7′) **and `lemma_3_5_injective` ✓ (std-3)** — the latter via a self-contained pro-2-abelianization layer in `SectionThree.lean` (`isProP_two_topAb_D0`, `zpowZtwo` helpers, `D0ab_coord`: `D₀^ab ∋ z = Ā^aS̄^sȲ^y`, topological generation through `F₃↠D0Full↠D₀↠D₀^ab`); coordinate argument uses P-21 `η`-injectivity + a mod-4 unit reduction. `topAbelianization` profinite instances kept **`local`** (a global generic instance perturbs `AnabelianBridge`'s `K⧸M` synthesis; verified both build). **`b_decomposition` ✓ (std-3)** — `D₀^ab ≅ ℤ/2×ℤ₂×ℤ₂` via coordinate homs `τ,σ,γ` (local `d0LiftHom` universal-property replica + `abLift` descent through `abMk`), combined into `φ` shown bijective. **All 4 clauses now have proofs.** `lemma_3_5_marked_abelianization` (B5) is **proved modulo one clean lemma `markedHom_bijective`** (std-3 + sorryAx): the descent `markedPi` (`G_ℚ₂^ab→(G_ℚ₂(2))^ab`, all-lifts-agree), the marked hom `markedHom` (`Ā,S̄,Ȳ↦rec(−4),rec(1/2),rec(−3)`, relation `(−4)²·2⁻⁴=1` verified), and the generator matching are **all std-3**; the sole remaining sorry is `markedHom_bijective` (the pro-2 reciprocity iso: `{rec(−4),rec(1/2),rec(−3)}` coordinatize `(G_ℚ₂(2))^ab`). **Census-gating claim RETRACTED (Fable re-audit), then derivation EXECUTED (Opus):** B5 carries `denseRange_recip` + `continuous_recip`, so `markedHom_bijective` is provable from B5 as-is. **Now proved** (`#print axioms markedHom_bijective = std-3` since `R` is a param): injectivity via `nuT`/`chiT` descents (`abLiftG`) feeding the already-proved `lemma_3_5_injective`; surjectivity via `DenseRange (markedPi∘rec)` + the arithmetic lemma `units_gen` (`ℚ₂ˣ = ⟨−4,2,−3⟩·(ℚ₂ˣ)²`: valuation split + `hensel_sq` (Mathlib `hensels_lemma`) + `mod8_sq` (`toZModPow 3` casework) + `norm_one_unit`) + finite-2-Frattini `sq_generate` (coatom) + `eq_top_of_forall_map_eq_top`. `lemma_3_5_marked_abelianization` ✓ (B5); **P-10 `prop_1_1` thereby sorryAx-free** (std-3 + B3c + B8). The `norm_reciprocity`/Galois route was dominated and not taken. |
| P-08 | B: Lemmas 3.6–3.8 proofs (cyclotomic conjugation of peripherals; wild-relation shape) | ⭐⭐ | O | P-06 | B2, B8 | ☑ 2026-07-03 (Fable): all three theorems proved in `GQ2/AnabelianBridge.lean`, namespace `GQ2.SectionThree` — **`lemma_3_7`** (std-3 + B8), **`prop_3_8_lift`** (std-3 + B8; `α_{u,b} = θ_b ∘ ψ_u`: `ψ_u` from the B8 action pushed through `deltaLift`/`d0Lift` + Frattini surjectivity, `θ_b` the `Y ↦ S^bY` shear with `theta_relator`), **`prop_3_8_classification`** (**std-3 only, axiom-free**: `t`-torsion fixing + `η`-mod-4 kill of the `(−1)`-component + `D0ab_coord` rows; `u ∈ ℤ₂ˣ` forced via the same row for `ξ⁻¹`). B2 turned out unneeded. The three statements are **moved out of** `SectionThree.lean` (comment-pointers there; no sorried duplicates). B8 amended with the `hι_proj` pinning (`GQ2/PeripheralAction.lean` docstring; consistency `zhatProjTwo_omega2` proved). Certificates re-checked via `lake env lean` `#print axioms` |
| P-09 | B: Prop 3.2 proof — common tame quotient (`Γ_A` side: Lemma 3.1 ✓ + bridges; local side: B10 + Lemma 3.3 maximality) | ⭐⭐⭐ | O | P-06 | B5, B10 | ☑ 2026-07-03 (`GQ2/Prop32.lean`; in tree, commit pends SectionThree co-owner P-07) |
| P-10 | B: Prop 1.1 proof — marked dyadic Demushkin normalization, `ν_ur = (−2,1,0)` | ⭐⭐ | O | P-06, P-07, P-08 | B3c, B8 (B5 via param) | ☑ **CLOSED 2026-07-03** (Fable assembled, closed when P-07 landed): **`prop_1_1`** in `GQ2/PropOneOneAssembly.lean` (`GQ2.SectionThree.prop_1_1`), statement moved out of `SectionThree.lean` (comment-pointer). `#print axioms` = **std-3 + B3c(`dyadicOrientation`) + B8(`peripheralCyclotomicAction`)** — **sorryAx GONE** now that P-07's `markedHom_bijective` is proved; **nothing else**. Actual Lean axioms **B3c + B8** (not B4 — subsumed by B3c; not B5 — `R` is a param; not B7′ — Lemma 3.5's marked clause is B5-only). Assembly = B3c's `orientBundle.equiv` adjusted by the Prop-3.8-lifted (P-08) automorphism realizing the χ-preserving comparison `Θ = eab∘equivAb`, so `abMk(e.symm gen) = markedPi(rec …)`, then `ν_ur` reads off via the descent `nuUrBarAb`. New infra: generic `topAbCongr` (functorial topological abelianization), `abDescend`, `d0ab_hom_ext` (generator extensionality via `D0ab_coord`), χ-descents `chiD0`/`chiG`. `lake build GQ2.PropOneOneAssembly` green (8618); own-scope + check_axioms(census 12) pass |
| P-11 | B: §4 design — boundary-framed marked targets, exact-image counts, **Thm 4.2 statement** | ⭐⭐⭐ | **F** | — | (statements) | ☑ 2026-07-03 (`GQ2/BoundaryFrame.lean`) |
| P-12 | B: §5 design — Fox–Heisenberg word complex; 5.7/5.8/5.10/5.11/5.13/5.15 statements | ⭐⭐⭐ | **F** | P-11 | (statements) | ☑ 2026-07-03 (`GQ2/FoxHeisenberg.lean`) |
| P-13 | B: §5 proofs — **decomposed → P-13a–g** (parallelizable; see [`p13-ticket-split.md`](p13-ticket-split.md)) | ⭐⭐⭐ | O | P-12 | B6, B7 | ☑ CLOSED (2026-07-05) **all of P-13a–g done** — §5 deformation duality complete, std-3. `lemma_5_11` (P-13e, `GQ2/Devissage.lean`), `prop_5_16` (P-13g, `GQ2/LocalLiftingDuality.lean`), and `prop_5_15` (P-13f, `GQ2/DualityAssembly.lean` + `GQ2/DevissageInduction.lean`) all PROVED std-3, no sorry; `FoxHeisenberg.lean` is sorry-free. |
| P-13a | B: §5 wild-Fox + mixed-Hessian engines & split §5.13 (Stokes 5.6–5.10; wild row 5.4/5.5; Hessian 5.14 toolkit; `lemma_5_13_split` + `lemma_5_13_pairing_split`) | ⭐⭐⭐ | O | P-12 | — | ☑ 2026-07-04 (Opus; `GQ2/FoxHeisenberg.lean`, all std-3) |
| P-13b | B: §5.13 ramified normal form (`lemma_5_13_ramified`; V^T=0 ⇒ wild row S⁻¹d, unique x₀-supported rep) | ⭐⭐⭐ | O | P-13a | — | ☑ **DONE (Opus, 2026-07-04)** — `lemma_5_13_ramified` **sorry-free, std-3** (`{propext, Classical.choice, Quot.sound}`, no B-axioms = "Ax —"); library builds green (8591 jobs). **Statement amended** (both needed, mirror split's `hU`/`hVS`; supplied per-simple-factor by P-13d — `hTodd`+`hU` must both hold on each factor for P-13f): `hTodd : ∀ v, powOmega2 t.τ • v = v` (τ odd-order on V) + `hU : ∀ v, t.sigma2 • v = v` (σ₂ trivial). Nothing consumed it, so safe. **Assembly**: `(T−1)` surjective on finite V (`Finite.injective_iff_surjective` from `V^T=0`); `v=(T−1)⁻¹x₁`; wild row ⟹ `x₃=0`; reduced cocycle `x−d0 v` has zero `x₁`-slot so its tame row (`d1Fun_tame`) `σ⁻¹(T−1)x'₀=0` forces `x'₀=0`; `x−x0Supported(x₂)=d0 v`, `c=x₂` unique via the `x₂`-coord. **Full details below.** — **hyp gap RESOLVED + full ramified engine layer (all std-3)**: (1) **statement amended** — added `hTodd : ∀ v, powOmega2 t.τ • v = v` (τ's 2-part acts trivially = τ odd-order on V; ramified twin of split's `hU`, supplied per-factor by P-13d; nothing consumes the lemma yet so signature change is safe); (2) **4 engine lemmas** in the wild-Fox section — `sum_pow_smul_eq_zero` (general `P=0`: fixed-point-free σ + `σᴷ` fixes u ⟹ `∑_{i<K}σⁱu=0`), `norm_eq_zero_of_fixedPointFree` (orderOf corollary), **`powOmega2_u_of_oddFixedPointFree`** (the ramified twin of `powOmega2_u_of_trivial`: fixed-point-free odd-order base ⟹ `(powOmega2 p).u=0`, via `pow_u`+`powOmega2_pow_eq`+the ledger), `powOmega2_smul_of_trivial_mul` (ω₂-transparency of a trivial left factor, via `powOmega2_map` naturality — lets bases `x₁τ`/`x₀τ` inherit `hTodd` from τ). **FULL RAMIFIED WILD ROW LANDED (all std-3)**: statement further amended with `hU : ∀ v, t.sigma2 • v = v` (also needed — `g0 = sigma2²` in `h0`; mirrors split exactly), and the complete aux-word layer landed — `liftMarking_{u0,u1}_{u,g}_ramified` (offsets `=0`, bases trivial), `liftMarking_d0_{g,u}_ramified` (`d0.u = x₂`, the `P=0` `c`-survives/`b`-dies), `liftMarking_c0_u_ramified` (`=0`; `z0.g` reuses the split lemma), `liftMarking_h0_{g,u}_ramified` (`h0.u=0`, the `x₂+x₂+x₂+x₂` cancellation), and **`liftMarking_wildValue_u_ramified : wildValue.u = t.σ⁻¹ • x₃`** = the display-(53) wild row `S⁻¹·d`.  All mirror the split proofs with `u0.u=u1.u=0` (collapse) substituted. The `∃!`-assembly (see the header above) is now landed too — `lemma_5_13_ramified` is complete. See P-13b in `p13-ticket-split.md` |
| P-13c | B: §5.14 ramified mixed Hessian (`lemma_5_13_pairing_ramified`; λ((1+U+U⁻¹)c), U=σ₂ nontrivial) | ⭐⭐⭐ | O | P-13a | — | ☑ **DONE (Opus, 2026-07-04)** — `lemma_5_13_pairing_ramified` proved std-3, sorry-free (`heisMarking_{h0,c0,wildValue}_z_ramified` + `HeisLift.conjP_*_of_slice` + `elemDual_fixedPointFree_of`); adds `hTodd` per P-13b precedent. Build 8591 jobs; #axioms=12 |
| P-13d | B: §5 tameness rep-theory (central σ₂ 2-pow-order ⇒ σ₂=1; V^σ submodule ⇒ V^S=0; supplies hU/hVS to P-13f) | ⭐⭐⭐ | O | 5.12 (done) | — | ☑ 2026-07-04 (Opus; `GQ2/TameSimple.lean` std-3; `sigma2_smul_trivial`+`fixedPoints_sigma_eq_zero` via `central_pow2_smul_trivial`, the centrality-analogue of 5.12) |
| P-13e | B: §5.11 dévissage (mapping-cone 2-of-3 for `IsSelfDual` along a module SES) | ⭐⭐⭐ | F | — | — | ☑ 2026-07-04 (Opus/Fable) **CLOSED — `lemma_5_11` PROVED std-3, no sorry** (`GQ2/Devissage.lean`, same FQN `GQ2.FoxH.lemma_5_11`, relocated there since the proof needs the dévissage machinery). One hypothesis added vs the P-12 statement: `hgen : t.Generates` (identifies `ker d⁰` with `fixedPts C` via `H0w_eq_fixedPts`; admissible markings supply it). Full stack in `Devissage.lean`: nine-term LES (primal + dualized SES, arrows + all exactness), ElemDual pack (extension/separation/biduality/dual exactness), six χ-maps with free halves, all ladder squares (lemma_5_6 + eval + δ-cores), four-lemma, nine-window assembly ⇒ `selfdualW_two_of_three` (generation-free, word-internal form) ⇒ `lemma_5_11` via `isSelfDual_iff_W`. **Threaded:** `prop_5_15` (P-13f) and `cor_5_17_card` (incl. the P-13g worktree copy) gained `hgen`; `GQ2.lean` imports `GQ2.Devissage`. P-13f can invoke `lemma_5_11` directly. |
| P-13f | B: §5.15 duality assembly (`prop_5_15`; trivial-module Gram + simple via 5.12/5.13 + dévissage) | ⭐⭐⭐ | O | P-13b, P-13c, P-13d, P-13e | — | ☑ CLOSED (2026-07-05) — **`prop_5_15` PROVED std-3, no sorry** (`GQ2/DualityAssembly.lean` + `GQ2/DevissageInduction.lean`); §5 deformation duality complete, the ramified simple case **de-hU'd** (unconditional). ◐-history: part (i) `trivialSelfDual` COMPLETE — std-3, no sorry (`GQ2/{TrivialSelfDual,MixedBilinear}.lean`): `IsSelfDual t A` for any trivial `C`-action on a finite elementary-2 module. Cards + full degree-one pairing: `mixedB` bilinearity, general-offset wild `.z` peel (`heisMarking_{h0,x1sig,c0,wildValue}_z_cocycle`, `conjP_z_of_alzero`), closed form `mixedB_cocycle = y₂(x₂)+y₃(x₀)−y₀(x₃)+u₁.z` with ω₂ scalar confined to (3,3) (`heisMarking_u1_z_of_{x3,y3}_zero`) ⇒ Gram det = 1; descent via `Quotient.lift₂` + `elemDual_separates` nondegeneracy. **Assembly progress** (`GQ2/DualityAssembly.lean`, satellite importing Devissage, std-3): **simple-case card clauses DONE** — `card_H1w_of_normalForm` (`#H¹w=#A` from 5.13's normal form), `card_H0w_eq_one_of_nontrivial`+`card_fixedPts_elemDual_eq_one_of_nontrivial` (`#H⁰w=#(A^∨)^C=1`, no invariants), `card_H2w_and_Z1w_of_nontrivial_simple` (`#H²w=1`, `#Z¹w=#A²` via Euler char) ⇒ clauses 1–2 of `IsSelfDual` for every nontrivial simple. **Remaining for `prop_5_15`:** clause 3 (pairing) for nontrivial simples via `lemma_5_13_pairing` (split `λ(c)` perfect by `dualEval`; ramified `λ((1+U+U⁻¹)c)`); the split/ramified dispatch (`V^T` is `C`-stable via the tame relation `στσ⁻¹=τ²` ⇒ `V^T=⊥` (ramified) or `⊤` (split)); then the `lemma_5_11` dévissage strong-induction on `#A` (SES `0→W→A→A/W→0`). NB (resolved 2026-07-05): the ramified §5.13 engine (P-13b/c) and `prop_5_15` are committed & sorry-free; `FoxHeisenberg.lean` is sorry-free. |
| P-13g | B: §5.16 local lifting duality (`prop_5_16`; local Tate duality — invokes existing axioms B6/B7, no axiom-layer change) | ⭐⭐⭐ | O | — | B6, B7 | ☑ CLOSED (Opus, 2026-07-04) **ALL 6 CLAUSES PROVED & verified std-3+B6+B7**, own files `GQ2/LocalLiftingDuality.lean` + reusable `GQ2/CupSymmetry.lean` (both `lake build` green 8615/8586, sorry-free). Capstone `prop_5_16_bundle` = full 6-tuple with `prop_5_16`'s exact signature; `#print axioms` = std-3 + `GQ2.tateDuality` (B6) + `GQ2.Foundations.absGalQ2_localEulerCharacteristic` (B7), **NO sorryAx / no native_decide**. Clauses i–iii numeric (as before). iv–vi cups via NEW reusable infra: **cup graded-commutativity** `cup11_comm` (char-2 cochain homotopy) / `cup02_eq_cup20_flip` / `cup20_eq_cup02_flip`; **coefficient transport** `H0congr`/`H1congr`/`H2congr` (+`_mk` rfl bridges) — repo had ZERO cohomology functoriality; `dualAddEquiv : MuDual 2 A ≃+ ElemDual A` shown `G`-equivariant FROM `hpair`; and the **opposite-currying count** `bijective_cup` (B7 finiteness + 𝔽₂-functional separation `exists_addHom_ne_zero`) discharging B6's flagged one-sided-perfectness deviation. **`GQ2.FoxH.prop_5_16` NOW PROVED & CLOSED** (relocation done 2026-07-04, P-13f paused per user): moved `prop_5_16` + `cor_5_17_card` OUT of co-owned `FoxHeisenberg.lean` (import-cycle: `FoxHeisenberg` ← `LocalLiftingDuality` for `ElemDual`/`dualEval`) INTO `LocalLiftingDuality.lean` under a `namespace GQ2.FoxH` block — **qualified names unchanged** (`GQ2.FoxH.prop_5_16`/`cor_5_17_card`), pointer-comment left in `FoxHeisenberg` (P-15d pattern); `prop_5_15` (P-13f) stays. `#print axioms GQ2.FoxH.prop_5_16` = std-3 + B6 + B7, **NO sorryAx**; `cor_5_17_card` = that + sorryAx only via P-13f's `prop_5_15`. **Full-library `lake build` green + `check_axioms` ALL PASS** (census 13, sorries only in allowlist, no native_decide). ☑ **P-13g CLOSED.** |
| P-14 | B: §§6–7 design — 6.13 (D₈ class), 6.15→6.17 (Shapiro/cor), 6.8/6.9 (Gauss sign), 6.16→6.18 (Hilbert ledger), 6.21 (transgression) statements | ⭐⭐⭐ | **F** | P-11 | (statements) | ☑ 2026-07-03 (`GQ2/SectionSix.lean` + `GQ2/SectionSeven.lean` + `GQ2/QuadraticFp2.lean` + `GQ2/Corestriction.lean`, `docs/section67-extraction.md`) |
| P-15 | B: §§6–7 proofs | ⭐⭐⭐ | O | P-14 | B5, B6, B7, B7′, B9, B11 | ◐ (Fable+Opus, 2026-07-03): **§§6–7 essentially complete — only P-15f's 4 sorries remain** (`lemma_6_17_dim/vanish`, `prop_6_18_ramified/unramified`; SectionSix 16/20 theorems proved, SectionSeven sorry-free; sub-tickets P-15a–e, g, h, i all CLOSED std-3).  Original snapshot: (§6: 6.1; 6.13 D₈ + (96); **6.22 shear** via explicit coboundary `w(v,c)=f(v,a c)` — `f_cocycle`×4 + `f_polar` + `m_quad`, char-2 close through `CharTwo.two_eq_zero`; **6.15-square** on-the-nose — `graphPullback(squareOrbitDatum)=cor2Fun(α⌣α)` as raw functions via `congr 1`+`funext`+`finsum_congr`, key index identity `(mk' N g)⁻¹·u = g⁻¹•u` on `G/N`, NO cocycle needed [6.15-free/involution are NOT on-the-nose: the `ghat`-shift makes canonical `.out` reps differ, needs β's Z1 condition].  §7: **all of Lemma 7.1** — head + radical + dual; 7.3; **§7 block existence**; `frattiniLike` infra) **+ Prop 7.4 complete modulo its tame step-2 helper** (`lam_sq_vanish`, sorried; 7.4 verifies std-3+sorryAx through it, no B-axioms) **+ reusable infra `comm_bot_of_scalarChain`** (std-3): coprime odd action on a `Y`-central series is trivial (`⁅Ñ,S⁆=⊥`), the key step for 7.2 — Mathlib lacks coprime-action commutator theory, proved here by central-series induction (bottom layer `c 1 ≤ Z`, displacement hom `Ñ→G` into the central 2-layer, odd-coprime ⟹ trivial image).  Statement amendments: `f_cocycle`; `MinimalBlock.hL/h2L` (7.1-head false without — S₃/A₃ counterexample); **7.4 + framed-target head data** (soundness #3 — step 2 needs `H¹(H_V,V^∨) = 0`, tame-only; step 1 `b_λ(T₀,M)=0` proved abstractly as `lam_comm_vanish`).  **6.16 escalation RESOLVED**: B9 base-generalized + B11 added (census 12, user-approved) — `kummerClassK` layer in `GQ2/EvensKahn.lean`, history in `docs/review-packet.md` §2.  **SPLIT 2026-07-03 into sub-tickets P-15a–P-15i** (rows below; this row = umbrella + amendment history, stays ◐ until all sub-tickets land).  **Sub-ticket mechanics**: heavy work goes in each ticket's OWN new file; `SectionSix/Seven.lean` edits are one-line sorry-splices (`:= by exact GQ2.<OwnFile>.<thm> …`) — claim your row ◐ first; never `git checkout` the co-owned §-files (other tickets' closed lemmas live there); the `SORRY_ALLOWLIST` entry for a §-file is removed by the LAST sub-ticket zeroing its sorries; per-ticket gate = `lake build <own file> GQ2.SectionSix GQ2.SectionSeven` + `scripts/check_axioms.sh` (census 12).  (The earlier SectionEight/AnabelianBridge build-note is resolved — whole-library build was green at the first commit.) |
| P-15a | B: §6 quadratic engine — nonsingular `𝔽₂` zero-count + Lemma 6.6 (Wall, (86)) | ⭐⭐⭐ | O | — | ∅ | ☑ **CLOSED 2026-07-04 (Fable)** — **`lemma_6_6` FULLY PROVED + spliced; `#print axioms lemma_6_6` = exactly std-3 (no B-axioms, no sorryAx).**  **Wall's sign relation `g(q_U)=(−1)ᵏg(q)` proved in `GQ2/GaussCount.lean`** via the paper's Wall-form route made precise: (i) **`wall_count`** — the abstract Wall count `∑_{t,u∈W}(−1)^{ω(t,t)+ω(u,u)+ω(t,u)} = (−2)^k` for a biadditive right-nondegenerate `ω` on a finite elem-ab-2 `W` with a **2-power-order monodromy** `M` (`ω t u = ω u (M t)`) — proved by strong induction on `#W`: a nonzero `M`-fixed `a` (norm-trick `a := b + Mb` induction on the order) has row = column functional; if `ω a a = 1` split `W ≃ ZMod 2 × ker(ω a)` along `a` (factor `−2`); if `ω a a = 0` pick `t₀` with `ω t₀ a = 1`, shift-pairing by `a` kills every block with a `t₀`-coordinate (`sum_sign_shift_eq_zero`/`sum_neg_shift_eq_zero`), the surviving `ker(ω a)²`-block is `⟨a⟩`-blind (factor `4`), and the induction continues on `C = ker ψ` with the **corrected monodromy** `c ↦ Mc + ψ(Mc)·a`.  **The monodromy hypothesis is essential** (`ω=[[1,1],[0,1]]` on `𝔽₂²` gives `−8 ≠ 4`) — the paper's induction sketch is silent on it; flag for P-20 review.  (ii) **duality block**: `card_addHom_zmod2` (`#Hom(A,𝔽₂)=#A`, via `AddCommGroup.zmodModule`+`Module.card_eq_pow_finrank`+`Subspace.dual_finrank_eq`) ⟹ **`perp_ker_iff_mem_range`** (`K^⊥ = im N`, annihilator counting, both applications of the duality). (iii) **`gaussSum_qDouble`**: reindex `g(q_U)g(q) = ∑_{x,u}(−1)^{q(Nx)+q(u)+B(x,u)}`, fiber the `x`-sum over `N` (`Fintype.sum_fiberwise` + explicit `ker`-fiber equivs), kernel character sum `= #K·[u ∈ K^⊥]` (`charSum_eq_zero`), Wall form `ω t u := B(xrep t, u)` (choice-independent via `ker ⟂ im`), diagonal `ω t t = q t`, monodromy `U⁻¹\|_R` ⟹ `g(q_U)g(q) = #K·(−2)^k = (−1)^k#V = (−1)^k g(q)²`, cancel `g(q) ≠ 0`.  Engine + 6.6 both consumable by P-15b.  ~~◐ (Fable, 2026-07-03) —~~ **`GQ2/GaussCount.lean` built, all std-3, no axioms/sorries; the ZERO-COUNT ENGINE (what P-15b's `prop_6_9` consumes) is COMPLETE.** Better route than hyperbolic-splitting: the integer **Gauss sum** `g(q)=∑_v(−1)^{q v}` has **`gaussSum_sq : g(q)²=#V`** for nonsingular `q` (char-sum identity `g²=∑_u(−1)^{q u}∑_x(−1)^{B(x,u)}=(−1)^{q 0}#V`; inner sum vanishes off `u=0` by nonsingularity — NO splitting induction needed). Bridges: `gaussSum_eq` (`g=2·zeroCount−#V`), `arf_eq_zero_iff_gaussSum_pos`, `gaussSum_eq_pow` (`g=±2^m` when `#V=2^{2m}`), and the two headline lemmas **`zeroCount_of_arf_zero`** (`=2^{2m−1}+2^{m−1}`) / **`zeroCount_of_arf_one`** (`=2^{2m−1}−2^{m−1}`). **Lemma 6.6: 3 of its 4 pieces PROVED (std-3) + spliced.** `qDouble_eq_add` (`q_U(x)=q x+q((1+U)x)`), `polar_qDouble`/`polar_qDouble_eq` (`B_U(x,y)=B(x,(1+U+U⁻¹)y)`); **`qDouble_nonsingular`** (`ker(1+U+U²)=0` for 2-power `U` via `Function.IsPeriodicPt.gcd`: `U³y=y ∧ U^{2ⁿ}y=y ⟹ U^{gcd(3,2ⁿ)=1}y=y`); **`exists_card_range_eq_two_pow`** (rank `2^k`, `IsPGroup.iff_card`); **`gaussSum_ne_zero`** + **`arf_qDouble_of_gaussSum_sign`** (Arf reduction: `g(q_U)=(−1)^k g(q) ⟹ arf(q_U)=arf q+k`). **`lemma_6_6` SPLICED in `SectionSix.lean`** (imports GaussCount): nonsingularity + rank + Arf-reduction wired; **the ONLY remaining `sorry` is Wall's sign relation `g(q_U)=(−1)^k g(q)`** — Gauss sum of the DEGENERATE form `μ(x)=B(x,Ux)` (radical `ker(U+U⁻¹)`) with a linear twist (radical-split + descended nonsingular Gauss sum, ~300 ln; the hard core, own focused session). `#print axioms lemma_6_6` = std-3 + sorryAx (no new axioms; SectionSix stays allowlisted). Added reusable **`sum_sign_shift_eq_zero`** (twisted-sum vanishing, the level-0 building block of the Wall sign). **ASSESSMENT (2026-07-04):** the remaining Wall sign `g(q_U)=(−1)^k g(q)` needs the DESCEND machinery — twisted Gauss sum of the degenerate `q∘W` reduced through `V⧸radical` to a descended NONSINGULAR form + its `gaussSum_sq`, with two-level radical/`dim(K∩I)` bookkeeping (~300–400 ln quotient-quadratic-form theory absent from Mathlib); a focused (not unattended) session. GaussCount 0 sorries, in `GQ2.lean`; census 12. Defs: `GQ2/QuadraticFp2.lean`, `onePlusU` in `SectionSix.lean` |
| P-15b | B: Gauss signs — Lemma 6.8 (87)/(88) + Prop 6.9 (91) ×2 | ⭐⭐⭐ | O | P-15a | ∅ | ☑ **CLOSED 2026-07-04 (Fable)** — the entire §6.2 Gauss-sign pair PROVED, all std-3 (no B-axioms/sorryAx): **`prop_6_9_unramified`, `lemma_6_8`, `prop_6_9_ramified`** spliced in `SectionSix.lean`. Engine in `GaussSigns.lean` (Arf pinches `arf_eq_{one,zero}_of_dvd`, free-action divisibility `free_zeroCount_dvds`/`card_dvd_card_subtype_of_free`, operator crux `irreducible_operator_pow_ne_one` [cyclic `finrank=natDegree(minpoly)` bridge], `arf_eq_of_free`); ramified core in `GaussSignsRamified.lean` (`irreducible_operator_pow_card_sub_one` [`T^{2^{2m}−1}=1` via AdjoinRoot field] + **`arf_eq_s_ramified`** — the **⟨T⟩ route**, NO Hermitian model/involution/norm-one needed). REUSES **P-13d** (`IsSimpleModTwo`, `orderOf_powOmega2_dvd_two_pow` from `TameSimple.lean`). Key method: both Arf values fall out of the SAME free-action machinery (unramified pinch to 1; ramified dual pinch to parity of s), with tame inertia `⟨T⟩` itself as the free group. **Statement amendments (documented, P-20)**: `lemma_6_8`/`prop_6_9_ramified` gained `hWtsimple`(=`IsSimpleModTwo (zpowers cτ) Wt`), `hV2`/`hWt2` (exp-2), `hs1`, and **`hVU`/`hrank`** [(88a) `#V^U=2^{rs}` and (88b) `rank≡s` as isotypic DATA per extraction note 6 — deriving them needs the S-action/ω₂-cycle structure absent from `he` (covers only ⟨cτ⟩); genuine gap in `docs/p15b-field-core-scoping.md`]. `prop_6_9_unramified` fully SELF-CONTAINED (no data hyps). `GQ2.lean` gains `TameSimple`+`GaussSignsRamified`; SectionSix imports `GaussSignsRamified`+`Prop32`. Feeds P-15f (6.18) + P-16 (§8, not yet wired). |
| P-15c | B: Shapiro ledger — 6.15 free (104) + involution (105) | ⭐⭐⭐ | O | — | ∅ | ☑ **CLOSED 2026-07-04 (Fable)** — both clauses proved std-3, `Ax = ∅`, and **spliced into `SectionSix`** (`lemma_6_15_free` + `lemma_6_15_involution` both sorry-free; `ShapiroLedger.lean` sorry-free; `#print axioms lemma_6_15_involution_aux = [propext, Classical.choice, Quot.sound]`). **Free (104)**: `lemma_6_15_free_aux` (prior Opus session). **Involution (105)**: restructured off the banked monolithic-Λ plan onto the **paper-shaped transversal-change route** — (1) generic brick `cor2FunT_sub_cor2Fun_mem_B2` (corestriction along any transversal `T` differs from `cor2Fun` by `δ¹twistLambda`; needs only the raw char-2 cocycle identity + right-normalization; `cocycle_twist` = 5 cocycle instances + char-2 dup-kill via one `linear_combination … * (2=0)`); (2) the **compatible transversal** `invLift v := ((invIndexEquiv v).out).out` — lifts each `U₀`-coset through **`phi`'s own orbit-canonical base point**, killing the `uCorr` AND orientation layers by construction; alignment discriminant = literally `phi_inv_eq`'s ε-condition (`lWordT_invLift_mem_N_iff`); (3) word identities: aligned words **on the nose** (`W1`), flipped = canonical·ĝ·`shiftCorr` (`W2`), duality `sc(m·ḡ) = (ĝ·sc(m)·ĝ)⁻¹` collapsing all corrections to one read `dRead(m) = α(sc m)`; α-reads R1/R2 (evensAux) + R5/R6 (bS via `ghat_conj_lWord`); (4) **the position identity** `invPositionEval`: per position, `evensNormFun(ℓ^T_v γ, ℓ^T_{γ⁻¹v} η)` = `phi`'s Sum1 (on the nose) + Sum2 (ε·reads at the canonical shifted point via `invIndexEquiv_smul_out`) + the three coboundary terms of the **aligned-locus** `invLambda(σ) = Σ_{v aligned-for-σ} α(ℓ_{z_v}σ)·dRead(σ⁻¹•z_v)` — 2×2 aligned/flipped cells, each with char-2 pair-kills; (5) δ-assembly `graphPullback_sub_cor2FunT_mem_B2` (O-sums→v-sums via `finsum_comp_equiv`, η-sum reindex `sum_reindex_smul'`, `abel_nf` + `-1•x = x`); (6) chain `sub_add_sub_cancel` → `H2ofFun_eq_of_sub_mem_B2`. **Reusable**: the Step-1 brick is generic (any open finite-index `U`, any transversal); `IsLocallyConstant` continuity route for non-normal coset spaces (no `QuotientGroup.discreteTopology`). Gotchas: `set`-fvar folding vs rw (use body-form); `mk'`-vs-`↑`-form discipline (`simp only [mk'_apply]` breaks the O-TYPE — convert value-atoms only, `hcoe : ↑ghat = mk' N ghat` per cell); dependent-⟨v,proof⟩ rewrites via ∀-form helpers. |
| P-15d | B: Lemma 6.4 layer — representative/datum independence of `Q⁰_loc` ⟹ 6.14 (102) | ⭐⭐ | O | — | ∅ | ☑ **CLOSED 2026-07-04 (Opus)** — `GQ2.RepIndependence.lemma_6_14` **proved, `#print axioms = std-3` (no B-axioms, no sorryAx)**; `GQ2/RepIndependence.lean` sorry-free & OFF the allowlist; statement moved out of `SectionSix.lean` (comment-pointer, P-08/P-10 pattern — RepIndependence is downstream of §6's `Q0loc`/`graphPullback`).  Key: **inner-conjugation coboundary on `V⋊C`** (not a raw `linear_combination` — the direct route hit a genuinely multi-term coboundary).  `graphPullback(b) = φ_b^*κ⁰` (`φ_b(g)=(b g,ρ g) : SemiProd C W`); the `Quotient.out`-representative change is `φ_{b+δ⁰w₀} = c_s ∘ φ_b` (conj by `s=(−w₀,1)`); inner autos act trivially on `H²` via `innerConj` (`c_s^*κ⁰ − κ⁰ = δ¹η_s`, `η_s(x)=κ⁰(s,x)+κ⁰(sxs⁻¹,s)`, 3 instances of the cocycle identity `kappa0_cocycle`).  New reusable: `kappa0_cocycle` (κ⁰ ∈ Z²(V⋊C) from the factor-set axioms), `etaS`, `innerConj`.  **2-torsion hypothesis DROPPED** (structural proof works over any `AddCommGroup W`); amended hyps now just `hdatW`, `hiC` (C-module map), `hρW`.  ◐-history: reduction (`hStepA` comap on-the-nose via `hiC`; `mapCoeff1`/`Quotient.out` functoriality; `repIndep` = `H1mk`-equal ⟹ `B1`-extraction ⟹ `h2ofFun_eq_of_sub_mem_B2`) landed first, then the core.  `lake build GQ2.SectionSix GQ2.RepIndependence` green (8608).  Feeds P-15f (6.18). ~~◐ H2ofFun invariant under cohomologous change of the `Z1` representative~~ (6.22-style cochain identity + `H2mk` quotient plumbing, `GQ2/Cohomology.lean`/`GQ2/CtsCohBridge.lean`); note `graphPullback (datW.comap i) ρ b = graphPullback datW ρ (i ∘ b)` holds on the nose — ONLY the `Quotient.out` mismatch needs the 6.4 layer.  Own `GQ2/RepIndependence.lean`; splice `lemma_6_14`.  Also feeds P-15f (6.18 needs the same independence).  **STATUS (Opus, 2026-07-03): reduction DONE + verified, one cochain identity remains.**  `GQ2/RepIndependence.lean` builds green (allowlisted); `lemma_6_14` **proved mod one lemma** (`#print axioms = std-3 + sorryAx`, no B-axioms): on-the-nose `comap` step (`hStepA` via C-equivariance `hiC`), `mapCoeff1`/`Quotient.out` functoriality (`H1mk_out` + `QuotientAddGroup.map_mk'`), `repIndep` (`H1mk`-equal ⟹ `B1`-extraction ⟹ `h2ofFun_eq_of_sub_mem_B2`), and coboundary **continuity** all complete.  **Statement amended** (documented): `lemma_6_14` gains `hdatW : IsEquivariantFactorSet q datW`, `hiC : ∀ c v, i(c•v)=c•(i v)` (i a C-module map, eq. (77)), `hρW` (`G_ℚ₂` acts through ρ on W), `htorsW` (W a 𝔽₂-space — all §6 modules are).  **Sole remaining sorry** = `graphPullback_sub_mem_B2`: the conjugation cochain identity `dOne ψ = graphPullback(b+δ⁰w₀) − graphPullback(b)`, `ψ(g)=f(w₀,b g)+f(b g,ρg•w₀)+m(ρg,w₀)` (the `(w₀,1)`-conjugation phase) — a 6.22-style char-2 `linear_combination` over `f_cocycle`/`f_polar`/`m_quad`/`m_mul`, larger than 6.22 (~12 instances + arg-normalization; goal fully set up, subtractions cleared).  NOT yet spliced into `SectionSix` (splice + amend when the core lands). |
| P-15e | B: Hilbert ledger — Lemma 6.16 (110)–(114), deep-unit Evens norm | ⭐⭐⭐ | O | — | B7′, B9, B11 | ✅ **CLOSED 2026-07-04 (Fable) — `lemma_6_16` NOW SPLICED + PROVED.** `SectionSix.lemma_6_16` `#print axioms` = `{propext, Classical.choice, Quot.sound}` ∪ `{evensKahn_dyadic (B9), dyadicNormCriterion (B11)}` — exactly the declared column, no `sorryAx`; whole-scope build green (8629). Splice: `import GQ2.HilbertLedger` in SectionSix (HilbertLedger is import-independent of SectionSix); extract `b` from `IsDeepUnit`, build norm unit `n=Units.mk0(u²−d·v²)` (`≠0`: `A=β²≠0` + conjugate; `δ∉k` from `hindex=2` via `k.fs≤stab δ`⟹`subgroupOf=⊤`), convert `hunram` to the `x+yδ` form (via the NEW `hδL:δ∈L` hyp), transport `evensNormH2` args by `revert…;rw [hLδ];intro…`, `exact evensNorm_deepUnit_vanish`. **Statement amendment (P-20)**: added `hδL : δ ∈ L` (the P-15f consumer has `δ=√d∈L` concretely). `evensNorm_deepUnit_vanish` (HilbertLedger capstone) unchanged. Unblocks P-15f (6.17_vanish/6.18). (prior INFRA-done history:)  `evensNorm_deepUnit_vanish` (HilbertLedger, the capstone, self-contained over `stabilizer δ`) is proved; the SectionSix `lemma_6_16` (stated over `IsDeepUnit`/`L.fixingSubgroup`) is NOT yet wired to it. Splice route (HilbertLedger is import-independent of SectionSix ⟹ SectionSix can `import GQ2.HilbertLedger`, splice in place): extract `b` from `IsDeepUnit`, construct norm unit `n=u²−d·v²` (`≠0` via `A=β²≠0` + conjugate `u−vδ≠0`; `δ∉k` from `hindex=2`), `hβ':β²=u+vδ`, `hA:u+vδ=1+2b`, convert `hunram` (needs `δ∈L` — likely a statement amendment `hδL`, since the P-15f consumer has `δ=√d∈L` concretely), transport `evensNormH2` args along `hLδ`, `exact evensNorm_deepUnit_vanish`. Unblocks P-15f (6.17_vanish/6.18). ~~✅ **CLOSED (Opus, 2026-07-04)** — `GQ2/HilbertLedger.lean` is **fully sorry-free**; whole library builds green (8613 jobs); census 12; **removed from SORRY_ALLOWLIST**. `evensNorm_deepUnit_vanish` `#print axioms` = `{propext, Classical.choice, Quot.sound}` ∪ `{evensKahn_dyadic (B9), dyadicNormCriterion (B11)}` — exactly the declared axiom column, no `sorryAx`. **F-draft ☑ 2026-07-03**: tier structure + full sub-lemma interface fixed; **proved now, exactly std-3+B11**: `cup_of_normForm` (criterion.mpr), `cup_neg_self`, `cup_steinberg`, `cup_two_neg_one` (`−1 = 1²−2·1²`), `cup_unramified_unit` (B11 clause 2); derived `kummerClassK_inv`/`_mul_self`, `cup_self_eq_neg_one` (std-3+sorryAx via the sorried basics).  **O-finish PROGRESS (Opus, 2026-07-04): 5 of 6 O-items PROVED, all exactly std-3, whole library builds green (8613 jobs), census 12** — `h1_add_self`/`h2_add_self` (quotient induction + pointwise char-2), `kummerClassK_mul` (base-general cocycle mul via two new `ℚ̄₂`-level helpers `kcf_root_indep'` + `kcf_mul_of_fixed` off EvensKahn's `two_values_of_fixed`; `+` transported through `H1mk`/`map_add`+`Subtype.ext`), `kummerClassK_one` (`= [1·1]` + `add_left_cancel`), `trivialCupPairing_comm` (graded-comm in char 2: the two cup cocycles differ by `dOne(g↦−(a g·b g))` — identity holds over ℤ — `∈ B2`, then `eq_zero_iff`+`h2_add_self`), `norm_galois` (Mathlib `NormedAlgebra.norm_eq_spectralNorm ℚ_[2]` + `spectralNorm_eq_of_equiv`, 3 lines), **`sq_of_near_one` DONE** (hand-rolled quadratic Newton `w↦w−(w²−z)/(2w)` from 1, ~90 ln; `NormedField ↥k` restricts `ℚ̄₂`'s by `rfl`, `NormedSpace ℚ_[2] ↥k` infers, `CompleteSpace ↥k := FiniteDimensional.complete ℚ_[2] ↥k`; invariants `‖wₙ−1‖≤‖2‖` [ultrametric ⟹ `‖wₙ‖=1`] + `‖wₙ²−z‖≤‖2‖²·qⁿ⁺¹` [`q=‖z−1‖/‖4‖<1`, error identity `eₙ₊₁=eₙ²/(4wₙ²)` by `field_simp;ring`] ⟹ `cauchySeq_of_le_geometric` ⟹ `cauchySeq_tendsto_of_complete` ⟹ `tendsto_nhds_unique`; `‖2‖<1` via `norm_algebraMap' (𝕜':=↥k)`+`Padic.norm_p_lt_one`; API `IsUltrametricDist.norm_add_eq_max_of_norm_ne_norm`/`norm_add_le_max`; NB `Padic.hensels_lemma` ℤ_p-only, `ℚ̄₂` NOT complete).  **6th / capstone `evensNorm_deepUnit_vanish` DONE** (~135 ln, `set_option maxHeartbeats 2400000` — large context): step 1 `s'•δ=−δ` (`two_values_of_fixed hδ (fixingSubgroup_smul k s.2 d)` + `hs`/`mem_subgroupOf`+`mem_stabilizer_iff`); step 2 apply `s'` to `hA` (smul distributes via `smul_add`/`smul_mul'`; `s'•2=2` by `AlgEquiv.smul_def,map_ofNat`) ⟹ `(u:ℚ̄₂)=1+b+s'•b` (`linear_combination (hA+hArev)/2`), `(n:ℚ̄₂)=(1+2b)(1+2s'•b)` [diff-of-squares off `hnc`+`hδ`, `hnc` by `rw[hn];push_cast;ring`] ⟹ `‖u‖=1` + `‖n/(2u−1)−1‖=‖4‖‖b‖²<‖4‖` [`norm_galois`; numeral coe `((2:↥k):ℚ̄₂)=2` needs `norm_cast` NOT `push_cast`]; step 3 degree-1 B9 ⟹ `cor=[n]` [`kummerClassK_mul`/`_inv`/`h1_add_self`+`add_left_cancel`]; step-5-prep `[n]=[2u−1]` [`p:=Units.mk0(2u−1)`, `sq_of_near_one` on `n·p⁻¹`, `n=p·wU²` by `Units.ext` — beware `Units.val_mk0` unfolds `↑p` too, rewrite only `↑wU`]; step 4 expand degree-2 (`simp[map_add,AddMonoidHom.add_apply]`) kill `(2,u)+(u,2)`[`trivialCupPairing_comm`+`h2_add_self`], `(u,d)`[comm+`cup_unramified_unit`], `(d,n)`[`cup_of_normForm ⟨u,v,hn⟩`], `(u,u)=(u,−1)`[`cup_self_eq_neg_one`]; step 5 `[2]∪[n]+[u]∪[n]=[u]∪[−1]` via `[n]=[2u−1]=[−1]+[1−2u]` (`kummerClassK_mul`, `nth_rewrite 1` to avoid rewriting the `p` in `−p`) + `cup_steinberg(2u,1−2u)` + `cup_two_neg_one`; final collapse by `abel` (pure reorder) + `h2_add_self`×2 + `add_left_cancel`.  **`lemma_6_16` statement amended** (documented, extraction doc): `[FiniteDimensional ℚ_[2] k]` + Kummer presentation `(d, δ, hδ, hLδ)` + coordinates `(u, v, hAuv)` — consumers (P-15f) have these concretely; the splice transports `evensNormH2`-args along `hLδ` |
| P-15f | B: deep part + §6 headline — 6.17 dim/vanish + Prop 6.18 (115) ×2 | ⭐⭐⭐ | O | P-15a, P-15b, P-15d, P-15e | B6, B7 | ◐ (Fable, 2026-07-04) **📋 HANDOFF — read `docs/p15f-handoff.md` first (self-contained continuation guide).**  STARTED — deps P-15a/b/d/e all landed (P-15e ✅ CLOSED, 6.16 spliced ⟹ `lemma_6_17_vanish` unblocked). New file `GQ2/DeepPart.lean` (sorry-free, in GQ2.lean): **`card_H1_eq_card_of_H0_H2_trivial`** (Euler-char collapse: B7's `#H¹=#H⁰·#H²·2^{v₂}` + `#H⁰=#H²=1` ⟹ `#H¹=#V` — the Step-1 brick for BOTH 6.17_dim and 6.18) and `card_H0_eq_one_iff` (`#H⁰=1 ⟺ V^{G_ℚ₂}=0`). **PLAN for `lemma_6_17_dim` (`#X₊²=#H¹`)**: (1) `#H¹=#V` [DONE, Euler collapse]; (2) `#H⁰=#H²=1` for ramified simple V [H⁰=V^{im ρ}=0, H²=(V^∨)^{G}=0 via B6 `perfect20`]; (3) `#X₊=2^m` Lagrangian half-count (X₊ self-⊥ under B6 Tate pairing on H¹, deep-unit orthogonality (94), maximal) — the hard cohomological core. **⚠ STATEMENT GAP (P-20)**: frozen `lemma_6_17_dim`/`prop_6_18_*` lack `hc : Surjective c` — `V^{im ρ}` is C-stable (⟹ simplicity kills it) only when `im ρ ◁ C` (e.g. c surjective); parallels the `lemma_6_8` amendments. REMAINING: 6.17_dim (steps 2–3), `lemma_6_17_vanish` (needs P-15e/6.16), `prop_6_18_*` (assembly of 6.9[P-15b ✓]+6.16[P-15e]+6.17+6.4[P-15d ✓]). Own `GQ2/DeepPart.lean`; splice into SectionSix (P-15d pattern — statements move out). **★ KEYSTONE LANDED 2026-07-04 (Fable), std-3, sorry-free** (`DeepPart.lean` now `import GQ2.GaussCount`): **`arf_zero_of_lagrangian`** + **`gaussSum_eq_card_of_lagrangian`** — the combinatorial crux of step-3 / ramified 6.18: a totally-singular self-⊥ subspace `X` (hyp `hperp : X⊥ ⊆ X`) forces `g(q) = #X > 0`, hence `arf q = 0` (positive Gauss sign).  Proof = two-way eval of `∑_v ∑_{x∈X} (−1)^{q(v+x)}`: translation `v↦v+x` ⟹ `#X·g(q)`; polar-expand `q(v+x)=q v+B(v,x)` (`q\|X=0`) + `sign_add` + `charSum_eq_zero` (inner char-sum `= #X·[v∈X⊥]`) ⟹ `#X·#X`; cancel.  `#print axioms` = std-3 (no B-axioms, no sorryAx).  **This reduces `prop_6_18_ramified` to**: [Q0loc `IsQuadraticFp2`+`Nonsingular` on H¹ (Tate B6)] + [`#H¹ = 2^{2m}` (Euler collapse + `#H⁰=#H²=1`)] + [`lemma_6_17_dim` `#X₊²=#H¹`] + [`lemma_6_17_vanish` `Q0loc\|X₊=0`] → **`arf_zero_of_card_sq`** (the complete algebraic Lagrangian package) → `GaussCount.zeroCount_of_arf_zero`.  **★ `selfperp_of_card_sq` ✅ DONE 2026-07-04 (Fable), std-3** — `#X²=#V` + tot-sing + nonsing ⟹ self-⊥, via the injection `X⊥ ↪ (V/X)^∨` (descended polar functional `QuotientAddGroup.lift`, injective by nonsingularity) ⟹ `#X⊥ ≤ #(V/X)=#V/#X=#X`, sandwiched with `X⊆X⊥` (NO character-extension needed — cleaner than the `LinearMap.exists_extend` route). So the ENTIRE algebraic side of ramified 6.18 is now banked as **`arf_zero_of_card_sq`** (`#X²=#V` ⟹ `arf q=0`, folding in selfperp).  **Remaining for P-15f = purely cohomological**: (i) `Q0loc` is `IsQuadraticFp2`+`Nonsingular` on `H¹` (polar = perfect Tate cup pairing, B6, the (93) cochain computation off `RepIndependence.repIndep`); (ii) ~~`#H⁰=#H²=1`~~ ✅ DONE; (iii) `lemma_6_17_dim` — now reduces to [X₊ is an AddSubgroup] + [(93)] + [nondegeneracy via B6 `perfect11`] + [`X₊ = X₊⊥`, the (94)-orthogonality+maximality Kummer core] via `card_sq_of_selfperp`; `lemma_6_17_vanish` (`Q0loc\|X₊=0`, deep-unit orbits + 6.16); (iv) unramified 6.18 = Hermitian-line model (6.4/6.7).  **★★ AMENDMENTS + COHOMOLOGICAL BRICK SUITE LANDED 2026-07-04 (Fable)** — SectionSix statements amended (`hc : Function.Surjective ⇑c` ×4 [6.17dim/6.17vanish/6.18ram/6.18unram — with `B.tameF_surjective` gives `im ρ = C`], `hV2` ×2 [dim/vanish only — derivable from `hcard`+Cauchy in 6.18]; P-20-flagged docstrings; done in the P-15c-paused window per user greenlight).  `DeepPart.lean` (+`import GQ2.TateDuality`) gained, all `#print axioms`-verified: **std-3** — `card_H0_eq_one_of_surjective` (`V^G=V^C`, C-pointwise-fixed subgroup + simplicity + moving element), `card_H0_congr` (equivariant ≃+ transports `#H⁰`), μ₂ bricks (`muTwoGen`, `muTwo_eq_zero_or_gen` via `mul_self_eq_one_iff`, `zmodTwoEquivMuTwo` via `ZMod.lift`∘`zmultiplesHom`, `muTwo_smul_trivial` — additive automorphism of a 2-element group is id, NO action computation), `exists_polarSelfDual` (equivariant `V ≃+ MuDual 2 V` from nonsingular G-invariant polar; inj by `hns`, card by postcomp-ε + `card_addHom_zmod2`), `h2_add_self`, **`card_H2_eq_one_of_card_H0_eq_one`** (`#H² = #Hom(H²,𝔽₂) = #H⁰(MuDual 2 V) = #H⁰(V) = 1` — B6 `perfect02` at `M := V` through the `D`-param + self-duality; NO dual-simple lemma, NO H²-transport), `card_eq_two_pow_of_exp_two`, **`card_sq_of_selfperp`** (dim-clause skeleton: symmetric-biadditive nondegenerate `P`, `X = X⊥` ⟹ `#X² = #A`; counting halves `θ : A →+ Hom(X,𝔽₂)` ker=X ⟹ `#A ≤ #X²` and descended `X ↪ Hom(A/X,𝔽₂)` ⟹ `#X² ≤ #A`; NO perfectness/extension needed); **std-3+B7** — `card_H1_eq_card_of_simple` (assembly `#H¹ = #V`; B6 stays parametric).  Gate: `lake build GQ2.DeepPart GQ2.SectionSix` green; **`check_axioms.sh` fully green** (census 13, allowlist clean).  Gotchas: `set`-bound homs are defeq-opaque (route `ext`/`congr_fun` through `rw [hθ]`); `MuDual.ext` takes section-explicit `n M` (use `DFunLike.ext _ _`); `open scoped Classical` breaks `decide` when a non-decidable hyp is reverted; `congr 1` can close `Fintype.card (ZMod 2) = 2` definitionally.  **★★★ OVERNIGHT RUN 2026-07-04→5 (Fable): `prop_6_18_ramified` PROVED (modulo Lemma 6.17) + the ENTIRE `Q⁰_loc` structure layer.  SectionSix sorries 4 → 3** (`prop_6_18_ramified` statement MOVED to `GQ2/DeepPart.lean` per the P-15d pattern — DeepPart now imports `RepIndependence` (⊃ SectionSix), pointer-comment left in SectionSix; `#print axioms GQ2.DeepPart.prop_6_18_ramified` = **std-3 + B7 + sorryAx-through-6.17 only**, B6 parametric).  Landed in DeepPart, all `#print axioms`-verified: **(93) cochain identity** `graphPullback_add_sub_mem_B2` (std-3): `gp(b₁+b₂) − gp b₁ − gp b₂ − (b₂ ∪_B b₁) = δ¹(g ↦ f(b₁g, b₂g))` — 4×`f_cocycle` + `f_polar` + `m_quad`, one `linear_combination`, NO f-bilinearity needed; class level via `repIndep`: `Q0loc_add`, `polar_Q0loc` (polar = `ι_F`∘cup(`polarBihom`), swapped slots), **`isQuadraticFp2_Q0loc`** (std-3); **`nonsingular_Q0loc`** (std-3): `iotaF = D.inv ∘ mapCoeff2 muTwoOfF2` unwinds through **`mapCoeff2_muTwo_cup`** (cup coefficient-naturality — proof is `rfl` after double quotient-induction: `muDualPairing = id`, `polarMuDual := postPairing (polarBihom) muTwoOfF2`, all defeq) + `mapCoeff1_injective` (equivariant bijection ⟹ `H¹`-injective; coboundary pulls back along the inverse) + `polarMuDual_bijective` + B6 `exists_cup_ne_zero_of_ne_zero`; `muTwoOfF2_eq : SectionSix.muTwoOfF2 = zmodTwoToMuTwo := rfl` (the two μ₂-bridges are definitionally equal); **`deepPartSubgroup`** (std-3): X₊ is an AddSubgroup — `0 ∈` (witness `A = β = 1`; coboundaries die on `ker ρ`: `vanish_on_ker_of_H1mk_eq_zero`), `+`-closed (deep units multiply — `1+2b`-form + ultrametric `‖b₁+b₂+2b₁b₂‖ < 1`; Kummer cocycles multiply on `N`-fixed squares via HilbertLedger's `kcf_mul_of_fixed`; `kummerRestrict_hom`/`_mem_Z1` [`kummerCocycleFun_continuous` needs NO base-field hypothesis — clopen stabilizer], `phiRestrict_mem_Z1`, `H1ofFun_add`), `neg` trivial (`h1_add_self`); assemblies **`card_deepPart_sq_of_selfperp`** (dim ⟸ `hself`, std-3+B7) + **`card_Q0loc_zero_eq_of_dim_of_vanish`** (6.18ram ⟸ dim+vanish, std-3+B7) + `exp_two_of_simple_of_card` (`hV2` from `hcard`+Cauchy+simplicity).  Full `lake build GQ2` green (8651); guard ALL-green.  **REMAINING P-15f = the genuinely deep §6.3 Kummer cores**: `lemma_6_17_dim` (⟸ `hself` via `card_deepPart_sq_of_selfperp`: (94)-orthogonality + deep-unit maximality/half-count over the splitting field), `lemma_6_17_vanish` (orbit analysis: free/square/involution orbits through 6.14/6.15/6.16), `prop_6_18_unramified` (Hermitian-line model 6.4/6.7).  New gotchas: AbsGalQ2-vs-`Kummer.GaloisGroup ℚ₂` instance flavor — ℚ̄₂-smul statements MUST bind `g : Kummer.GaloisGroup ℚ_[2]` (defeq-cast at use sites via `(n : AbsGalQ2)` ascriptions, mirroring the `deepPart` def); flavor-mismatched `rw` fixed by re-typing the lemma instance with an annotated `have` (defeq-tolerant `:=`); `linarith` useless on `ZMod 2` (use `sub_eq_zero.mp`); `simp at hv` can close goals via `addOrderOf 0 = 1` (dangling `exact`s ⟹ "no goals").  **★ HERMITIAN-LINE COUNT LANDED 2026-07-05 (Fable), all std-3** — the complete computational core of **`prop_6_18_unramified`** (paper steps 4–5: `#Q⁻¹(0) = 1 + (2^m+1)(2^{m−1}−1) = 2^{2m−1} − 2^{m−1}`), in `DeepPart.lean` §HermitianCount as **`card_hermitian_trace_form_zero`**: on any `D = 𝔽_{2^{2m}}`, the form `x ↦ Tr_{ZMod(ringChar D)}(c·x^{2^m+1})` with `c^{2^m} ≠ c` (i.e. `c ∉ D₀` — the `D₀`-trace form `Tr_{D₀/𝔽₂}(a·N(x))` in **absolute-trace clothing**, no subfield/relative-trace plumbing in the statement!) has exactly `2^{2m−1} − 2^{m−1}` zeros.  Pieces (all std-3): generic `𝔽₂`-duality — `addHom_restrict_surjective` (restriction of functionals to a subgroup is onto, kernel = `Hom(A/X,𝔽₂)` counting) + **`mem_of_pairing_eq_zero`** (biadditive nondegenerate pairing + Lagrangian-count `#X² = #A` + vanishing-on-`X` ⟹ perp = `X` — REUSABLE for the eventual `X₊`-selfperp!); field-theoretic — `ringChar_eq_two_of_card`, `algebraMap_trace_eq`/`trace_eq_zero_iff` (Frobenius-sum form via `FiniteField.algebraMap_trace_eq_sum_pow`), **`trace_eq_zero_of_frobenius_fixed`** (`Tr\|_{𝔽_{2^m}} ≡ 0`: the `2m`-fold Frobenius sum doubles up, `Finset.sum_range_add` split — NO transitivity/relative trace needed), `card_pow_fixed` (`#{y : y^n = y} = 1 + #{u : u^{n−1}=1}` unit-split), `frobFixed` + `card_frobFixed` (`#𝔽_{2^m} = 2^m` via `IsCyclic.card_powMonoidHom_ker` gcd-count + `Nat.sq_sub_sq`), `exists_frobFixed_trace_ne_zero` (perp of the fixed field IS the fixed field), `card_filter_eq_of_mem_range` (hom fibres = kernel-cosets); assembly = 0-split + `Finset.card_eq_sum_card_fiberwise` over `range ψ` (`ψ = powMonoidHom (2^m+1)`, `#ker = 2^m+1`, `range = frobFixed`-units by gcd-count + `Set.eq_of_subset_of_ncard_le`) + the trace-kernel-half count (`2^{m−1}−1` nonzero).  Guard all-green.  **What remains for 6.18-unram = the model identification** (paper steps 1–3 + Lemma 6.7): `D := End_{H_V}(V)` is `𝔽_{2^{2m}}` (Schur), `H¹` is a `D`-line, `Q⁰_loc` is norm-one-invariant (via a `dat`-comap/6.14-style argument), and Lemma 6.7 pins `Q⁰_loc = Tr(c·x·x*)` — then `card_hermitian_trace_form_zero` finishes.  Lean gotchas: `omega` REJECTS nonlinear atoms (`(2^m−1)(2^m+1)` — route through `Nat.sq_sub_sq`, or substitute `x = k+1` + `ring`-lemma + omega); `Finset.card_bij` needs `symm` as a TACTIC before `refine` (Eq.symm-wrapping blocks s/t unification); inline `rw` in `card_eq_sum_card_fiberwise`'s H-lambda hits Finset-coe display forms — use `Finset.mem_filter.mp/mpr` terms.  **★★ LEMMA 6.7 PROVED 2026-07-05 (Fable), all std-3** — `hermitian_form_eq_trace_form` (DeepPart §HermitianCount): every nonsingular quadratic form on `D = 𝔽_{2^{2m}}` invariant under the norm-one circle `U = {u : u^{2^m+1} = 1}` IS `x ↦ Tr(c·x^{2^m+1})` with `c` outside the fixed field; capstone **`card_normOne_invariant_form_zero`**: any such form has `#Q⁻¹(0) = 2^{2m−1} − 2^{m−1}` — **the COMPLETE algebraic side of `prop_6_18_unramified`**, model-free (mirror of the ramified `arf_zero_of_card_sq`).  Proof route (paper 6.7 with a cardinality twist): polar `U`-invariance ⟹ the adjoint identity `B(dx,y) = B(x, d^{2^m}y)` holds on a SUBRING containing `U` ⟹ everywhere by **`subring_eq_top_of_normOne_le`** (NO minimal polynomials: a subring ⊇ U∪{0} has 2-power order `2^s ≥ 2^m+2`, its unit group gives `2^s−1 ∣ 2^{2m}−1` by Lagrange, and **`dvd_of_two_pow_sub_one_dvd`** (Euclidean on exponents, `Nat.sub_dvd_pow_sub_pow`) forces `s ∣ 2m`, so `s = 2m`); `ℓ = B(1,·)` trace-represented (`exists_trace_rep`, perfect trace pairing); symmetry + Frobenius-invariance (`trace_pow_two`/`trace_pow_pow` via the sum formula) force the coefficient Frobenius-fixed; `exists_add_pow_eq` (Artin–Schreier `im(c ↦ c+c^{2^m}) = frobFixed` by rank-nullity) produces `c` with matching polar; the difference is additive + `U`-invariant ⟹ trace-represented with `w(u+1) = 0` ⟹ `w = 0` (a nontrivial norm-one unit exists, `#U = 2^m+1 ≥ 3`).  **REMAINING for 6.18-unram = ONLY the cohomological model identification** (paper steps 1–3): `End_{H_V}(V) ≅ 𝔽_{2^{2m}}` (Schur), `H¹` a `D`-line (via `mapCoeff1`), `Q⁰loc` norm-one-invariant (6.14/dat-comap) — then `card_normOne_invariant_form_zero` finishes.  Gotchas: `add_pow_char_pow x y 2 m` (a b BEFORE p n as terms; rw-form self-unifies); `CharP (ZMod (ringChar D)) 2` needs explicit `hchar ▸ ZMod.charP _`; `Nat.card_option` doesn't exist (route `Fintype.card_option`); dite-Equiv `right_inv` needs `dsimp only` before `dif_pos/neg`.  **SPLIT 2026-07-05 → P-15f1–f3** (rows below; this row = umbrella + amendment history + landing records). |
| P-15f1 | B: `lemma_6_17_dim` — deep-half self-perpendicularity `X₊ = X₊⊥` (§6.3 Kummer core, eq. (94)) | ⭐⭐⭐ | O | P-15e ✓ | B6, B7 + B9/B11a/b budget; ⚠ new-leaf risk | ☑ **(Fable 2026-07-05) CLAIMED.** Goal = EXACTLY `hself` of the banked reducer `card_deepPart_sq_of_selfperp` (DeepPart.lean:1326): `∀ x, (∀ y ∈ X₊, polar (Q0loc D dat ρ) y x = 0) ↔ x ∈ X₊` — the whole quadratic/nonsingular/counting layer is DONE. Two halves: **(mpr) (94)-orthogonality** — deep×deep classes cup to 0 (unit-filtration duality `U^{(e+1)} ⟂ U^{(e+1)}`, `i+j = 2e+2 > 2e`) through the Kummer-coordinate expansion of `polar_Q0loc` = Tate cup; B9/B11a territory; **this brick is SHARED with P-15f2's free-orbit case — it lives HERE, f2 imports it**. **(mp) maximality** — perp-of-deep ⟹ deep; NB this is the V-ISOTYPIC statement, NOT K-level self-perpness (FALSE at K-level: `dim K^×/2 = d+2` can be odd — over ℚ₂ deep^⊥ ⊋ deep; `hram` is what kills the unramified/valuation directions inside the V-component); needs the local-Kummer identification `H¹(G_K, μ₂) ≅ K^×/2` as C-modules + unit-filtration count — the biggest remaining infra gap; **⚠ possible new-axiom escalation**: scope the minimal leaf from the paper's 6.17 proof and flag per protocol BEFORE adding. **⚠ SOUNDNESS FINDING 2026-07-05 (scoped, `docs/p15f1-scoping.md`)**: the frozen statement is FALSE without a self-duality hypothesis — the paper's §6.3 standing assumption (`q` invariant nonsingular ⟹ `V ≅ V^∨`) was dropped in extraction; refuted numerically by `H_V = C₇⋊C₃`, `V = 𝔽₈` (`#H¹ = 8` not a perfect square) and, WITH `hcard = 2^{2m}`, by `H_V = C₁₅⋊C₄`, `V = 𝔽₁₆` (`dim X₊ = 1 ≠ 2`) — amendment (add `q, hq, hns, hinv`, the vanish-clause package) PENDING USER GREENLIGHT. **Route decision**: paper filtration count (Route B) — `H¹ ≅ Hom_{H_V}(V^∨, M_K)` + graded duality by explicit inertia characters; NO H¹-pairing, `card_deepPart_sq_of_selfperp` unused; (94) stays f2-only. **Architecture**: parametric `DeepKummerData` (B6-pattern), leaf candidates L1 (Kummer bijectivity) / L2 ((93) filtration) / L3 ((94), for f2) pending census approval at the instantiation checkpoint; Lemma 6.11 projectivity (Clifford+Higman) = paper content, NOT leafable, structure-field-deferred (the mountain). Work order + effort map in the scoping doc §§3–4. **LANDED 2026-07-05**: (a) amendment user-approved + applied — `lemma_6_17_dim` gained `(q, hq, hns, hinv)` (P-20-flagged docstring with both counterexamples), consumer call in `DeepPart.prop_6_18_ramified` updated, `lake build GQ2.SectionSix GQ2.DeepPart` green; (b) `GQ2/LocalKummer.lean` created + registered in `GQ2.lean` — **Layer-1 counting core** proved std-3: `sum_eq_two_mul_tail` (duality-paired sums halve; `Finset.sum_Ico_consecutive` + `sum_Ico_succ_top` + `sum_nbij'` with `j ↦ 2e−j`, omega closes) + **`card_deepPart_sq_of_counts`** (the dim clause from the four count facts `htotal`/`hdeep`/`hpair`/`hmid` — Layer 2's production targets). Whole-library build green (8663), guard all-green (census 13). **LAYER 2a LANDED 2026-07-05 (Fable), all 15 decls std-3, file green, `lake build GQ2` green (8663)** — the FULL identification `H¹(ℚ₂,V) ≅ {admissible families}` modulo exactly TWO deferred cohomological inputs. In `GQ2/LocalKummer.lean`: scalar restriction `phiRes ρ x φ = [n ↦ φ((out x) n)] ∈ H¹(N,𝔽₂)` (+ `phiRes_def` rfl-lemma, `phiRes_of_rep` representative-independence via coboundary-death-on-ker, additivity in both slots); `deepClasses` + **`mem_deepPart_iff`** (the P4 bridge is DEFINITIONAL — `deepPart = {x \| ∀ φ, phiRes x φ ∈ deepClasses}`); `B¹(N,𝔽₂) = 0` package (`h1ofFun_eq_zero_iff`); local separation copy (decoupled from the FoxH/P-13f hot chain); conjugation action `conjAct` + computation rule + **`cocycle_conj`** (`b(g⁻¹ng) = g⁻¹•b(n)` on `N = ker ρ` — 3-step cocycle manipulation, no quotient types) + **`phiRes_conj`** equivariance; `AdmissibleFam` structure (additive + conjugation-equivariant families = ambient `Hom_{H_V}(V^∨, M_K)`) + `toFam`; **deferred inputs as named Props**: `InflationVanishes` (= `H¹(H_V,V) = 0` content: cocycles vanishing on N are coboundaries) and `FamiliesExtend` (= `H²(H_V,V) = 0` content: admissible families are hit) — BOTH to be produced from Lemma 6.11 projectivity; **`h1EquivFam`** (`Equiv.ofBijective toFam`) ⟹ **`card_H1_eq_card_fam`** + **`card_deepPart_eq_card_deepFam`** (deepPart ↔ families valued in deepClasses, via `Equiv.subtypeEquiv`). REMAINING f1 = instantiation-side only: (i) filtration bundle on `H¹(N,𝔽₂)` + family-counts `2^{Σ d_j}` (L1/L2 leaves + Hom-exactness), (ii) discharge the two deferred inputs (6.11/P2), (iii) `d_pair`/`d_middle` productions, then splice through `card_deepPart_sq_of_counts`. Gotchas: `unfold phiRes` delta-exposes the `H1`-quotient in TYPE args ("target not type-correct under instances") — use the rfl-lemma + `rw`; `h1ofFun_eq_zero_iff` must bind `N : Subgroup AbsGalQ2` (NOT the defeq `Kummer.GaloisGroup ℚ_[2]` flavor) or `rw` silently fails on display-identical terms; `Quotient.out_eq` needs a type-ascribed `have` (`H1mk` ≠ `⟦·⟧` syntactically); `DistribMulAction.toAddMonoidHom` deprecated → `DistribSMul.toAddMonoidHom`. **LAYER 2b LANDED 2026-07-05 (Opus), std-3** — the reduction is now COMPLETE: `structure DeepKummerData` (B6/TateDuality pattern, over `ρ`) bundles `e`/`d` + the two deferred inputs (`hinf`/`hext`) + `hpair`/`hmid` + the two family counts (`card_fam = 2^{Σ range(2e+1) d}`, `card_deepFam = 2^{Σ Ico(e+1)(2e+1) d}`); **`dim_deepPart_of_data`** proves `#X₊² = #H¹` PARAMETRICALLY (chains 2a's `card_H1_eq_card_fam`/`card_deepPart_eq_card_deepFam` into Layer-1 `card_deepPart_sq_of_counts`). All of f1's mathematical content is now `dim_deepPart_of_data`; **ALL that remains is constructing one `DeepKummerData` instance**. NO axiom declared (structure, not axiom); census 13. **INSTANTIATION CHECKPOINT REACHED** (scoping doc §3): the instance needs (a) the L1/L2 leaves [PENDING USER APPROVAL] and (b) the Lemma-6.11 projectivity discharge of `hinf`/`hext` [paper content, the mountain]. **CHECKPOINT REFINED 2026-07-05 (Fable) → `docs/p15f1-axiom-proposal.md`** (QUEUED behind P-25's census motion, paste-ready blocks included, NOT executed): reading the paper's proof of (78) (p. 25) showed `hinf`/`hext` need NO projectivity — coprime averaging (Brown III.(10.2), line-verified) + `V^I = 0` collapse, in-repo provable, ~30–50 ln for H¹; 6.11 feeds ONLY the deep-count multiplicativity `Ext¹(V^∨,U_{e+1}) = 0`. Proposed leaves shrink to TWO: **B∘1 `kummerClassK_surjective`** [NSW (6.2.1)+(6.2.2) + Ch. VI §2 display, verified verbatim; injectivity already PROVED — `kummerClass_eq_zero_iff`] and **B∘2 `dyadicUnitFiltration`** [Serre LF IV §2 Prop. 6 + Prop. 7, verified verbatim in the UPDATED scan; the (93) square-class computation, Hensel top (= `sq_of_near_one` ✓), −1 ∈ U_e, graded duality, and Lemma 6.10 all EXCLUDED as provable]. (94)/L3 deferred to f2 (candidate B11a+conductor route; sharp (94) not numbered in any provided PDF — O'Meara §63 / Fesenko–Vostokov flagged). Curtis–Reiner replaced by Webb (free PDF, Clifford Ch. 5 §5.3, Higman Ch. 11) + originals (Clifford Ann. Math. 38 (1937); Higman Duke 21 (1954)). **⚠ CITATION FIX found: review-packet's B11a says XIV §2 'Prop. 7 iii (p. 209)' but the updated scan has it as Prop. 4 iii (pp. 206–207); B11b re-checked, correct. Fix block included in the proposal §6.6.** **★ B12 + B13 LANDED 2026-07-06 (Fable), census 13 → 15, user-approved** (P-25 had strengthened B10 in place, so no index shift): `axiom kummerClassK_surjective` (B12 — surjectivity-only, injectivity already proved) + `axiom dyadicUnitFiltration : DyadicUnitFiltration k` (B13) in `GQ2/Foundations/Axioms.lean`; NEW defs file **`GQ2/UnitFiltration.lean`** (own file, upstream of Axioms — `normUnits`, `depthUnits` subgroups in spectral-norm vocabulary + `depthUnits_zero`/`_antitone`, all PROVED, plus the `DyadicUnitFiltration` structure: uniformizer data, `‖2‖ = ‖π‖^e`, residue counts `2^f − 1`/`2^f` per Serre IV §2 Prop. 6); the proposed (F2) inertia-twist clause was found DERIVABLE during statement design (exact ℚ̄₂-algebra + `he`) and deliberately excluded — to be proved in-repo. Updated: `check_axioms.sh` (census 15; NB atomic-replace drops the exec bit — chmod +x after), `AxiomLedger.lean` (B12/B13 entries, ALARM 0), `GQ2.lean`, `literature-axioms.md` (B12/B13 §B entries + B10–B11 backfill note + §E lines + B11a **Prop. 4 correction** + tallies 'fifteen'), `review-packet.md` (§2 rows, counts, census bullet 13→15, deviations rows, classification rows [B12 direct, B13 classical+encoding], B11a correction). Gates: whole-library `lake build GQ2` green (8668), guard all-green, AxiomLedger 0-unknown. **★ `hinf` + `hext`-reduction LANDED 2026-07-06 (Opus), std-3** — both deferred cohomological inputs of Layer 2a discharged WITHOUT the projectivity mountain: **`inflationVanishes_of_oddNormal`** (std-3) proves `InflationVanishes` parametrically from `(I ◁ C, \|I\| odd, V^I = 0)` — the coprime-averaging argument (Brown III (10.2)), Hochschild–Serre-FREE: descend `b` to `b̄ : C → V` (ρ surjective + ker acts trivially), average over `I` (`odd_nsmul_eq_self`: odd multiple = id in 2-torsion, so `w₀ = Σ_{i∈I} b̄ i` with NO division), then the two-way coset evaluation `r(ic) = i•r(c) = r(c)` forces the error `r c ∈ V^I = 0`. **`familiesExtend_of_card_le`** (std-3 + B7) reduces `hext` to a CARDINALITY bound: `toFam` is injective (banked `phiRes_injective` ← hinf), so `#AdmissibleFam ≤ #H¹` + finiteness ⟹ bijective ⟹ `FamiliesExtend` — the degree-2 H² cochain-extension construction is NEVER needed; at instantiation this `#AdmissibleFam` is the same count B12/B13+filtration already supply. Both inputs now factor through (a) the tame-arithmetic verification `I odd`/`V^I=0` (odd inertia + ramified-simple fixed-point-freeness — supplied at instantiation) and (b) the `#AdmissibleFam` filtration count. `lake env lean GQ2.LocalKummer` green; `#print axioms` = std-3 (hinf) / std-3+B7 (hext-reduction); guard all-green (census 15). **hinf tame-arithmetic bricks LANDED 2026-07-06 (Opus), std-3**: **`fixedByNormal_eq_bot`** (`V^I=0` from `I ◁ C` normal + `hsimple` + a moving vector — the `hVI` producer, propext+Quot.sound only) and **`odd_orderOf_tameInertia`** (`Odd (orderOf (c tameTau))` for any `c : ContinuousMonoidHom Ttame D`, D finite — applies `c` to the proved `tame_relation` `τ^σ=τ²`, so `c τ` is `SemiconjBy`-conjugate to `(c τ)²`, `SemiconjBy.orderOf_eq` + `orderOf_pow`/gcd forces odd; the `Odd (Nat.card ↥I)` producer). Both abstract hypotheses of `inflationVanishes_of_oddNormal` now have producing lemmas. REMAINING f1 = the `DeepKummerData` instantiation over B12/B13: (i) hinf instantiation — identify `I = zpowers (c tameTau)` + prove it Normal in `C` (needs `C = closure{c σ, c τ}` from `hc` surjective + tame generation; the one remaining hinf gap, deferred to instantiation where the concrete generation facts are in scope), then splice `fixedByNormal_eq_bot` + `odd_orderOf_tameInertia` into `inflationVanishes_of_oddNormal` — **DONE: `tameInertia_normal` (std-3) proves `⟨c τ⟩` normal via the normalizer argument (`c σ` sends `τ→τ²` by `tame_relation`, `zpowers(τ²)=zpowers(τ)` by odd order via `exists_pow_eq_self_of_coprime`, `MonoidHom.map_zpowers`+`mem_map_iff_mem`+`normalizer_eq_top_iff`), and the FOUR-BRICK ASSEMBLY `inflationVanishes_ramifiedTame` (std-3) bundles all four into `InflationVanishes` for the tame ramified-simple setup — leaving ONLY the two standard profinite plumbing facts `hsurj` (`im ρ = C`) + `hgen` (`C = closure{c σ, c τ}`) as inputs, discharged at the `lemma_6_17_dim` proof site from `hc`/`B.tameF_surjective`/tame generation**; (ii) the filtration `Hom`-count (`card_fam`/`card_deepFam`) + `familiesExtend_of_card_le` for hext. **DIMENSION COUNT SCOPED 2026-07-06 (Opus) → `docs/p15f1-dimcount-scoping.md`**: the final piece `#X₊ = 2^m` splits into a CHEAP upper bound `#X₊ ≤ 2^m` (isotropy: **`card_sq_le_of_isotropic` LANDED std-3** — isotropic subgroup under a nondegenerate 𝔽₂-pairing has `#X² ≤ #A`, via `X ↪ Hom(A/X,𝔽₂)`; remaining = the cup=Hilbert-symbol bridge on deep Kummer classes + (94)-minimal `(U_{e+1},U_{e+1})=0`, **SHARED with P-15f2's free-orbit vanishing**, candidate no-new-axiom via B11a+conductor) and an EXPENSIVE lower bound `#X₊ ≥ 2^m` (**the sole genuine need for Lemma 6.11 projectivity** — the coprime trick provably fails here since `(V⊗U_{e+1})^I ≠ 0` at deep depths; Higman+Clifford, the isolated mountain, or leaf `Ext¹(V^∨,−)=0` per proposal §4.2). Both routes A/B converge on this same count. **6.11 ROUTE DECIDED + LANDED 2026-07-06 (Fable, user-approved): SORRIED PAPER NODE** — 6.11 needs NO axioms (pure finite rep theory, std-3 from Mathlib: Maschke ✓, PID structure ✓ in Mathlib; Clifford weight-orbit + norm-criterion + relative trace to build, est. 1200–2500 ln) but has NO precise literature reference (it is the paper's OWN §6.3 Lemma 6.11, pp. 29–30, assembled from Clifford 1937 + Higman 1954; only the Higman fragment is Webb-citable) — so per census hygiene it is carried as a sorried paper node, the P-14→P-15 pattern. NEW FILE **`GQ2/RegularSummand.lean`** (allowlisted): `lemma_6_11` (V an equivariant split summand of `Fin N → C → ZMod 2` with INLINE left-translation action — no bespoke instances; std-3+sorryAx) + **`equivariant_lift_of_regular_summand` PROVED std-3 sorry-free** (the Hom-exactness consequence: equivariant maps out of V lift along equivariant surjections of 2-torsion modules; via `regBasis` indicator decomposition + choice-lift on identity-based basis vectors + `G F = Σ F n x • (x•w n)` with translation-equivariance by `Equiv.mulLeft`-reindex) + `regBasis_decomp`/`regBasis_translate` (std-3). Discharge plan in the file docstring; ideal parallel-agent ticket (zero arithmetic context). **★ f1 REDUCERS LANDED 2026-07-06 (autonomous, both verified) — the two gaps now cleanly isolated as hypotheses/nodes:** **`dim_deepPart_of_balance`** (std-3): `#X₊² = #H¹` ⟺ the single balance `#X₊ = #(H¹/X₊)` (Lagrange on finite H¹) — the whole graded `DeepKummerData` scaffold is bypassed; **`card_deepPart_sq_le_of_isotropic`** (std-3+B7): the UPPER bound `#X₊² ≤ #H¹` from the isotropy hypothesis `hiso` (deep×deep polar-cup = 0 — the (94) content as a clean hypothesis, via the new abstract `card_sq_le_of_isotropic`). So f1 = (i) discharge `hiso` = the (94)/cup=Hilbert-symbol orthogonality [**shared with f2**, `docs/p15f2-scoping.md`]; (ii) the LOWER bound `#X₊ ≥ 2^m` [needs `lemma_6_11`/exactness, P-15f4] — or equivalently the balance `hbal`. Architectural note: `lemma_6_17_dim` lives in the UPSTREAM SectionSix; its machinery (LocalKummer) is downstream, so the final splice needs the statement moved out (P-15d/6.18ram pattern) OR the machinery cited via the sorried nodes — deferred to the discharge of (i)+(ii). ⚠ **P-15f2 SCOPED** (`docs/p15f2-scoping.md`): `lemma_6_17_vanish` gated on the SAME (94) gap (free/square orbits) + `lemma_6_11` (regular embedding) + an UNBUILT monomial-expansion layer (~200–400 ln combinatorics); involution orbits close via 6.16 (banked). Recommendation: land the (94)/cup=Hilbert orthogonality FIRST — it unblocks both f1's upper bound and f2's free/square orbits. (Whole-lib build transiently RED on parallel agents' untracked SectionNine.lean/AffineTLift.lean — orthogonal; my gate green.) (P-15f3 later added `UnramifiedModel.lean` to the allowlist; guard now all-green.) NEXT: Layer-2 `DeepKummerData` design (fields = L1/L2/L3 leaf candidates + flagged 6.11-consequence), then P4 `deepPart` bridge → P3 inf-res → P1 → instantiation checkpoint (leaf approval). Own file `GQ2/LocalKummer.lean`; SectionSix splice = one-liner through `card_deepPart_sq_of_selfperp`. ⚠ co-owns the frozen 6.17 signatures with P-15f2 — cross-flag any amendment. **★ (94) LEAF DISCHARGED AS THEOREMS 2026-07-06 (Fable) — NO NEW AXIOM (census 15)**: user directive = no exercise-grade axioms; checked all provided texts — FV Ch. VII §4 has the general (94) only as **Exercises 4c/5b**, O'Meara §63 assembles it from the defect propositions, Serre LF XIV is base-ℚ₂-only — no numbered theorem exists, so PROVED instead. `HilbertLedger.lean` **Tier 5**: `normForm_mul`/`normForm_inv` (Brahmagupta composition) + `normForm_of_deep`/`normForm_neg_one_of_deep` (**std-3 sorry-free**: contraction descent — exact solve `x₀=(b+1)/2, y₀=(b−1)/2`, error `u=c/b` with `‖u−1‖ ≤ ‖b−1‖·(‖a−1‖/‖2‖)`, endgame `sq_of_near_one` = O'Meara **63:1** Local Square Thm) ⟹ `cup_deep_deep`/`cup_deep_neg_one`/`cup_deep_self` (**std-3 ∪ {B11a} exactly** — also sharpened `cup_of_normForm` to consume B11a directly, dropping unused B11b from every downstream trace). Consumer bridge `norm_sub_one_lt_of_isDeepUnit` (LocalKummer, std-3). **f1's `hiso` leaf + f2's free/square-orbit leaves BANKED**; remaining for f1: monomial-expansion bridge + lower bound — both gated on `lemma_6_11` (P-15f4). **★ deepClasses↔kummerClassK BRIDGE LANDED 2026-07-06 (Fable), std-3, NO axiom** (`LocalKummer.deepClass_eq_kummerClassK`): a deep Kummer class over `k.fixingSubgroup` (finite `k`) is `kummerClassK k a` for a genuine deep unit `a∈kˣ` — the `k.fixingSubgroup`-fixed deep unit lands in `k` by `InfiniteGalois.fixedField_fixingSubgroup` (IsGalois ℚ_[2] ℚ̄₂), cocycle matched up to sign (`kummerCocycleFun_neg`). Consequently `cup_deepClasses` (std-3∪{B11a}): **two deep classes in H¹(G_k,𝔽₂) cup to 0** — the (94) orthogonality now in `deepClasses` vocabulary, the exact scalar leaf f1's isotropy and f2's free/square orbits consume. **HONEST REMAINING SCOPE (mapped from paper §6.3 + 2 deep explorations):** f1's DIM clause uses the GRADED route (NOT isotropy) — `DeepKummerData` `card_fam`/`card_deepFam`/`hpair`/`hmid` = the graded-Hom-over-filtration count (`Hom_{H_V}(V^∨,M_K)` exact via 6.11 + B12/B13 + isotypic multiplicities + Hilbert duality j↔2e−j + 6.10 middle-empty), ~300-500 ln module theory, LEAST banked. f2's VANISH = monomial expansion (6.14 banked, 6.15-free banked, 6.16 banked; involution coboundary ~150 ln + G_ℚ₂→K symbol decomposition ~200 ln UNBUILT). Both ~600-850 ln, gated on 6.11; multi-session. The deepClasses-orthogonality (this landing) + the transport `ker ρ = (fixedField)ⁱ` at the consumer are the connectors from those expansions to the banked scalar leaf. **SPLIT 2026-07-06 (Fable) into P-15f5–P-15f8** (rows below; the graded-route decomposition per the p. 34 route correction — dim clause is the `DeepKummerData` count, NOT isotropy): f5 = Hom-exactness counting engine (pure module theory, std-3 standalone), f6 = Kummer transport + graded counts (B12/B13), f7 = the two symmetry inputs `hpair`/`hmid` (⚠ leaf decisions), f8 = `DeepKummerData` assembly + statement-move splice. This row stays ◐ as umbrella + amendment history.  **STATUS CORRECTION + CLOSE-OUT (Fable, 2026-07-08): stale claim (2026-07-05) cleared; ground-truth audit — BOTH halves discharged.**  (mpr) isotropy = banked **`LocalKummer.cup_deepClasses`** (std-3+B11a; consumed by `DeepDualityK` + the f2 free-orbit lane).  (mp) maximality = **SUPERSEDED** by the f6/f7/f8 balance-duality route (`card_deepPart_sq_of_duality` → `ResidueLift.lemma_6_17_dim_final`, sorry-free, std-3+B6/B7/B11a/B12/B13); `card_deepPart_sq_of_selfperp` retains NO live consumer (banked infra).  Nothing remains under this umbrella; the two `SectionSix` 6.17 interface sorries are tracked by the deferred joint f2+f8 statement-move (f8 row).  **→ ☑ CLOSED (absorbed).** |
| P-15f2 | B: `lemma_6_17_vanish` — `Q⁰loc∣X₊ = 0` (orbit decomposition over 6.14/6.15/6.16) | ⭐⭐⭐ | O | P-15d ✓, P-15e ✓, P-15f1 (the (94) brick) | B9, B11a/b (through 6.16); B6/B7 parametric | ◐ **📋 HANDOFF — read `docs/p15f2-handoff.md` first (self-contained continuation guide: the f2a/f2b/f2c1/f2c2/f2d tree, the assembly chain with exact names, landed infra, and gotchas).** **CLAIMED (Fable 2026-07-07) — INCREMENT 1 LANDED: the per-orbit corestriction-of-coboundary bridge, std-3.**  New leaf `GQ2/OrbitVanish.lean` (registered): **`H2ofFun_cor2Fun_coboundary_eq_zero`** (cochain form — `cor2Fun U (δ¹c) = 0` in `H²(G_ℚ₂)` for continuous `c : ↥U → 𝔽₂`) + **`H2ofFun_cor2Fun_eq_zero_of_H2_eq_zero`** (class form — `H²ofFun ↥U inner = 0 ⟹ H²ofFun G_ℚ₂ (cor2Fun U inner) = 0`, the exact shape the 6.15 orbit outputs feed), both `#print axioms` = std-3.  This is the "continuity → B²" step the scoping doc flagged as still-needed: `cor2Fun_dOne` (✓) + `smul_zmodTwo` (trivial 𝔽₂-action) + `continuous_lTrans'` (the `cor1Fun` continuity) + `H2ofFun_eq_of_sub_mem_B2`; the subgroup-`H²` extraction (feared per the P-15f handoff §4 view-clash) resolved cleanly.  So EACH banked per-orbit leaf (free/square = `cup_deepClasses`, involution = `lemma_6_16`) now corestricts to a vanishing `Q⁰loc`-contribution through this bridge.  **INCREMENT 2 LANDED (Fable 2026-07-07): the vanishing ASSEMBLY, std-3** — **`Q0loc_vanish_of_orbit_sum`** (same file): given the monomial expansion `hexp` (`Q⁰loc x = ∑_orbit iotaF(H2ofFun(cor2Fun(U_o)(inner_o)))`) + per-orbit `H²ofFun ↥(U_o) inner_o = 0`, concludes `Q⁰loc x = 0` (`Finset.sum_eq_zero` through the increment-1 bridge + `iotaF`-`map_zero`).  This is the scoping-doc-recommended `Q0loc_vanish_of_orbit_data` verified reduction.  **INCREMENT 3 LANDED (Fable 2026-07-07): the full f2 reducer down to the RAW cochain decomposition, all std-3** — the additivity backbone **`H2ofFun_sum_of_mem_Z2`** (`H²ofFun` additive on a finite sum of cocycles), the `hexp`-producer **`Q0loc_eq_orbit_sum_of_decomp`** (`Q⁰loc x = ∑ iotaF(H2ofFun(cor2Fun …))` from `graphPullback dat ρ (out x) = ∑_o φ_o` + the 6.15 cohomologies, via `iotaF`'s `map_sum`), and the capstone **`Q0loc_vanish_of_decomp`** composing all: given the raw per-orbit decomposition `hdecomp` + 6.15 `hcoh` + deep-class `hvanish`, concludes `Q⁰loc x = 0`.  **f2's ENTIRE cohomological reduction is now built — the SOLE remaining input is `hdecomp`: `graphPullback dat ρ (out x) = ∑_o φ_o`**, the `q∘p` monomial expansion over the regular module (`lemma_6_11` ✓ + `lemma_6_14` naturality + the datum decomposition `datW = Σ orbitDatum`; the `freeOrbitDatum`/`invOrbitDatum`/`squareOrbitDatum` defs ✓ exist in `OrbitData.lean`, but no decomposition lemma yet — that is gap 2, the pure combinatorics).  `lake build GQ2.OrbitVanish` green (8637), gate green (census 15).  **INCREMENT 4 LANDED (Opus 2026-07-07, 4b595a5): the datum-additivity assembly, std-3** — `sumDatum` (pointwise/block sum of factor-set datums = the datum-level `datW = Σ_o datum_o`), **`graphPullback_sumDatum`** (`graphPullback (sumDatum s datf) ρ b = ∑_o graphPullback (datf o) ρ b` — the paper's "multi-orbit assembly = additivity of graphPullback in the datum", SectionSix:646), and the datum-level reducer **`Q0loc_vanish_of_datum_decomp`** (`dat = sumDatum s datf` + per-orbit banked 6.15 cohomologies `hcoh` + deep vanishing ⟹ `Q⁰loc x = 0`; per-orbit Z² auto via `graphPullback_mem_Z2`).  Lands the remaining input on the **datum-level** identity `datW = Σ_o datf_o` (the natural form of Lemma 6.2, cleaner than the raw cochain `hdecomp`) + *literal* 6.15 outputs.  `#print axioms {graphPullback_sumDatum, Q0loc_vanish_of_datum_decomp}` = std-3.  **CRISP REMAINING INTERFACE (the §6.2/§6.3 monomial-expansion build — a MAJOR separate effort, NOT plumbing)**: via `lemma_6_14` transport to the regular module `W`, produce (a) `datW` on `W` with `datW.comap ι = dat` [6.11 ✓], (b) `datW = sumDatum (orbit datums ext-by-0 to W)`, (c) each orbit datum's pullback = its 6.15 corestriction [banked], (d) scalar coords of a deep class are deep units [feeds `hvanish` via 6.16/(94)].  **Remaining = ONLY the §6.2 datum decomposition (a)–(d)** (feeding `Q0loc_vanish_of_datum_decomp`) + the 6.18ram statement-move splice (coordinate the shared SectionSix exit with f1's f8).  ⚡ **BANKED SCAFFOLDING (Opus 2026-07-07 discovery)**: `GQ2/KappaNormalForm.lean` (P-17e5, std-3) already has `PermW H K` (the regular module `𝔽₂[H]^K` w/ block left-translation), `permBas`/`permBas_support_decomp` (basis + coordinate decomp), **`quadratic_eq_double_sum`** (THE monomial expansion `Q F = ∑_{p,p'} F_p F_{p'} f₀(p,p')`), the involution-datum transport (`invBlockSquare`/`exists_invBlock_datum`), `exists_datum_finset_sum` (datum for a Σ of square maps — existence analog of `sumDatum`), and `exists_datum_of_invariant_quadratic` (invariant quadratic ⟹ equivariant datum).  BUT KappaNormalForm took a DELIBERATE **non-orbit-decomposed** route ("single invariant biadditive β-refinement", for `kappa0_exists`), so it does NOT hand f2 the orbit-sum datum its per-orbit vanishing needs — the orbit grouping (β-monomials → free/square/involution 6.15 forms) is the genuine remaining build, now sitting on top of banked scaffolding.  **Alt route worth weighing**: reroute f2 vanishing through the β-datum directly (bypass orbit decomposition), coordinating with P-17e5.  **Prior design notes (history):** ☐ — orbit/monomial expansion of `graphPullback` at a deep class: free orbits die by (94) [IMPORT the P-15f1 brick], square orbits by `−1 ∈ U_e` + 6.15-square ✓ (on-the-nose, SectionSix), involution orbits by `lemma_6_16` ✓ (spliced, B9+B11). The expansion layer (q∘p monomials → orbit sum over `C/⟨involution⟩`) is UNBUILT — design from the paper's 6.17-vanish proof before coding. Own file `GQ2/OrbitVanish.lean` (imports `GQ2/LocalKummer`); splice one-line. ⚠ co-owns the frozen 6.17 signatures with P-15f1 — cross-flag any amendment. **(94) LEAVES BANKED 2026-07-06** — scoping gap 1 CLOSED as theorems, no new axiom: free orbits = `cup_deep_deep`, square orbits = `cup_deep_self`/`cup_deep_neg_one` (HilbertLedger Tier 5, std-3∪{B11a}). Remaining: `lemma_6_11` (P-15f4) + the monomial-expansion layer. **deepClasses-orthogonality LANDED 2026-07-06** (`LocalKummer.cup_deepClasses`, std-3∪{B11a}): free/square orbit scalar leaf now in `deepClasses` vocab (via `deepClass_eq_kummerClassK` bridge, std-3). Remaining = the monomial-expansion assembly (6.15 involution coboundary ~150 ln + symbol decomposition), gated on 6.11; 6.14/6.15-free/6.16 banked. **cor2Fun_dOne LANDED 2026-07-06** (`GQ2/CorestrictionCohomology.lean`, std-3, no axiom): corestriction commutes with δ¹ — the cochain heart of 'cor descends to H²', enabling per-orbit vanishing (6.15 output = H2ofFun(cor2Fun(cup)) → 0 via cup_deepClasses). **6.15 FULLY BANKED** (free+square+involution; a prior agent finished lemma_6_15_involution_aux). f2 now needs only: the monomial expansion (q∘p → orbit sum over regular module) + continuity plumbing (cor1Fun continuous from G⧸U discrete). 6.14/6.15/6.16/(94)/deepClasses-bridge/cor2Fun_dOne all banked.  **★ SPLIT 2026-07-07 (Opus) into P-15f2a–P-15f2d (parallelizable; rows below).** The landed cohomological backbone (`Q0loc_vanish_of_datum_decomp` + `Q0loc_datum_indep_of_core`, `GQ2/OrbitVanish.lean`, std-3) makes each remaining prerequisite hypothesis-isolated behind a fixed compiling interface, so the four subtickets parallelize: **f2a** (datum-independence) + **f2b** (isometric embedding) are INDEPENDENT — start now; **f2c** develops against f2b's interface; **f2d** is the capstone splice.  Full decomposition + dependency graph: `docs/p15f2-subtickets.md`; DI-core analysis: `docs/p15f2-option1-scoping.md`.  This row stays ◐ as the umbrella. |
| P-15f2a | B: **`Q⁰loc` datum-independence** — DI-core (Lemma 6.1/6.4): the graph pullback of a zero-form equivariant factor set is a `2`-coboundary, hence any two equivariant factor sets for the same `q` give equal `Q⁰loc`. | ⭐⭐⭐ | — | P-15f2 backbone ✓ | ∅ (pure cohomology/𝔽₂ linear algebra) | ✅ **DONE (Opus, 2026-07-07, std-3 — `GQ2/OrbitVanish.lean` §DatumIndependence).** Deliverable **`Q0loc_datum_indep`**: two equivariant factor sets `dat1`, `dat2` for the SAME `q` + `hV2` + `(I ◁ C, hodd : Odd #I, hVI : V^I=0)` ⟹ `Q⁰loc dat1 ρ x = Q⁰loc dat2 ρ x`; `#print axioms` = **std-3, NO B-axioms**. Chain (all std-3): **increment 1** cochain heart `graphPullback_mem_B2_of_refinement` (a refinement `Δφ` with polar `hQ` + defect `hE` ⟹ `graphPullback = δ¹(Δφ∘b) ∈ B²`) + `Q0loc_datum_indep_of_refinement` (8b18bf8); **A** `exists_refinement_of_zero_form` (0235bb8 — the twisted extension `ZFExt V` = elementary abelian 2-group via `AddGroup.ofLeftAxioms`+`AddCommGroup.zmodModule`, `base`-projection surjective 𝔽₂-linear, `exists_rightInverse_of_surjective` gives `Δφ` with `(Q)`); **B** `exists_equivariant_refinement` (be94b06 — the banked f1 averaging pattern: defect `D c v` additive+right-cocycle, `L₀=Σ_{i∈I} D i` kills it on odd `I` (`mulRight` reindex + `\|I\|` odd in 𝔽₂), normality ⟹ corrected defect `I`-invariant, `Σ_{i∈I} i•v ∈ V^I=0` kills it globally — **NO general `H¹(C,V*)` theory**); **C** capstone (be94b06). **f2d consumes**: `Q0loc_datum_indep` with `I = zpowers(c tameTau)`, `hIn`←`tameInertia_normal`, `hodd`←`odd_orderOf_tameInertia`, `hVI`←`fixedByNormal_eq_bot` (all banked). **Option-2 (fix canonical datum in 6.17) REJECTED** — consumers get datums existentially via `kappa0_exists`, so it relocates rather than removes the requirement. Build green (8637), gate green (census 15). Full plan: `docs/p15f2-subtickets.md`, `docs/p15f2-option1-scoping.md` §P0. |
| P-15f2b | B: **isometric regular embedding** — the `C`-equivariant split embedding `ι : V →+ W = PermW C N` into the regular module carrying the **orbit-sum form** `Q_W`, with `Q_W ∘ ι = q` (isometric) + retraction `r`. Upgrades `lemma_6_11` to an isometry vs. the orbit-sum form. | ⭐⭐⭐ | — | `lemma_6_11` ✓, KappaNormalForm ✓ | ∅ (finite rep theory) | ✅ **DONE — orbit route COMPLETE (Opus 2026-07-07; I0 `4a3ca0c`, I1 `6d3c3cc`, I2 `cbf4700`, I3 `677ae4a`, I4 `55ee2cd`; foundation `cfbbe96`).** Full interface delivered by **`GQ2/RegularIsometry.lean`'s `regular_isometric_embedding_orbit`** (std-3): the `C`-equivariant (through `e : C ≃* G⧸N`) isometric split embedding into `Fin K → RegRep N` carrying the **§6.2 orbit-sum datum** `sumDatum (orbitIndexSet Q_W) orbitDatum` for `Q_W := q∘r`, *definitionally* the orbit sum + isometry + equivariance + retraction. Built on the new **`GQ2/OrbitDecomp.lean`** (`isEquivariantFactorSet_orbitSumDatum` — the orbit decomposition, I1–I3) + I0's de-privatized `KappaNormalForm` generic layer + the foundation `regular_isometric_embedding`. **Scoping finding** (`docs/p15f2b-foundation-notes.md`): the ticket's difficulty split was inverted — the isometry is FREE (via `q∘r`), the real content was clause (2) `datW = sumDatum orbit-datums` (which `exists_datum_of_invariant_quadratic` deliberately avoided via the single β-refinement). The datum is *literally* the orbit sum, so f2c's per-orbit `hcoh`/`lemma_6_15` matching needs zero bridging. `e` kept abstract → f2d instantiates at `C ≅ AbsGalQ2⧸ker ρ`. Consumers: `OrbitVanish.Q0loc_vanish_of_datum_decomp` (dat=sumDatum shape); remaining for `lemma_6_17_vanish` = f2a (datum-indep, proof paper-verified) + f2c + f2d. |
| P-15f2c | B: **Shapiro coordinates + scalar deepness** — for `x ∈ deepPart ρ`, the transported cocycle `out(ι∗x)`'s block coordinates are (cohomologous to) `shapiroFun (ker ρ) α_r` for scalar Kummer coordinates `α_r`, each `α_r` a deep unit. | ⭐⭐⭐ | — | P-15f2b interface, `lemma_6_15` ✓, `lemma_6_16` ✓, `cup_deepClasses` ✓ | B9, B11a (via 6.16/(94)) | ◐ **SPLIT 2026-07-07 → P-15f2c1 (Shapiro `hcoh` read, keystone) + P-15f2c2 (involution Kummer field-data); this row now tracks the LANDED portion + coordinates the two children.** f2b LANDED (OrbitDecomp sorry-free) → f2d UNBLOCKED; DEEPNESS TRANSPORT + REINDEX INFRA LANDED (Opus, 2026-07-07): `RegularIsometry.regular_isometric_embedding_orbit` bakes in `datW = sumDatum orbitDatum` (the `Q0loc_vanish_of_datum_decomp` shape); the free-orbit conjugation-stability caveat is now **banked** (`AdmissibleCount.conjAct_deepClasses`); 4 **f2d infra bricks LANDED (std-3, `ShapiroDeepness.lean`)**: **`deepPart_mapCoeff1`** (deepness across the Lemma-6.14 embedding `ι`, via `phiRes_mapCoeff1`+`mem_deepPart_iff`); **`FactorSet.reindexHom`**/**`graphPullback_reindexHom`**/**`Q0loc_reindexHom`** (the C↔`AbsGalQ2⧸kerρ` bridge — reindex the datum's acting group along `φ:C'→C`, so `Q0loc (dat.reindexHom φ) ρ' = Q0loc dat (φ∘ρ')`; with `φ=e`, `e∘ρ=mk'N` it rewrites the C-level datum as the `mk'`-level orbit map for `lemma_6_15_*`). f2d path = datum-indep (DI-core, f2a) → `RepIndependence.lemma_6_14` → reindex → `Q0loc_vanish_of_datum_decomp`. **Remaining, split across children/siblings**: `hcoh` Shapiro coordinate read → **P-15f2c1** (keystone); involution `lemma_6_16` field-data → **P-15f2c2**; DI-core → **P-15f2a**; final composition → **P-15f2d** (now thin — the reindex/transport infra is landed here). Design `docs/p15f2c-design.md` ⟨2026-07-07⟩. **hvanish CORES LANDED (Opus, 2026-07-08)** — `GQ2/ShapiroDeepness.lean` (green, tree 8638): **`hvanish_cup`** (square+free `hvanish`: deep classes `H1mk a, H1mk b ∈ deepClasses k.fixingSubgroup` ⟹ `H2ofFun (cup11Fun mul a b) = 0` via `cup_deepClasses`(94) + B²-extraction; std-3 + B11a), **`hvanish_evensNorm`** (involution cochain bridge: `evensNormH2 = 0` ⟹ `H2ofFun (evensNormFun) = 0`, std-3 — f2d composes `lemma_6_16`), **`H2ofFun_eq_zero_of_H2mk`** (shared tail).  Design: `docs/p15f2c-design.md`.  **Key find**: the deepness extraction is BANKED (`LocalKummer.mem_deepPart_iff`: `x ∈ deepPart ↔ ∀φ, phiRes ∈ deepClasses (ker ρ)`) and the `hcoh` matching is direct `lemma_6_15_*` (f2b makes the pullbacks their syntactic inputs).  **Remaining**: (i) `hcoh` instantiation at f2b block coords + the deepness/`ker ρ = k.fixingSubgroup` field bridge → **f2d**; (ii) the involution `lemma_6_16` field-data construction (`k`,`L`,`δ`,`u`,`v` from `deepPart`'s deep-unit witness) — the deep §6.3 residue; (iii) free-orbit deep-class conjugation-stability (small).  Original spec: Produce the two per-orbit hypotheses of `Q0loc_vanish_of_datum_decomp`: **`hcoh`** (each `graphPullback (orbit datum) ρ (out ι∗x) ~ cor2Fun` — match banked `lemma_6_15_square/free/involution`) and **`hvanish`** (each inner cocycle `= 0` in the subgroup `H²` — scalar `α_r ∈ U_{e+1}` ⟹ `cup_deepClasses`(94)/`lemma_6_16`). **P3 + P4**. SEMI-INDEPENDENT: develop against f2b's fixed interface, instantiate when f2b lands. On `permBas_support_decomp` + `shapiroFun` + `deepPart`/`deepClasses`. ~200–300 ln. |
| P-15f2c1 | B: **Shapiro H¹ coordinate read** (`hcoh`, the f2c keystone) — for the transported deep class `ι∗x` on `W = Fin K → RegRep N`, show each orbit's block coordinate of a `Z¹`-representative is cohomologous to `shapiroFun (ker ρ) α_j` for a scalar `α_j : Z1 (ker ρ) 𝔽₂`, so the banked `lemma_6_15_{square,free,involution}` (stated at `shapiroFun`, acting map `mk' N`) fire and give `hcoh : H2ofFun (graphPullback (orbitDatum o) ρ (out ι∗x)) = H2ofFun (cor2Fun (U o) (inner o))` for all three orbit types. Genuine core = **Shapiro's H¹ surjectivity** `H¹(G, Coind_N 𝔽₂) ≅ H¹(N, 𝔽₂)` (eval-at-base-coset ↔ `shapiroFun`; **NOT banked** — repo has `shapiroFun` + forward props only, not the extraction) + `graphPullback` cohomology-invariance (partly banked `DeepPart.graphPullback_add_sub_mem_B2`) + the `mk' N`↔`ρ` reindex (**banked** `ShapiroDeepness.Q0loc_reindexHom`/`graphPullback_reindexHom`). Unblocks all three orbits' `hcoh`. ⚠ touches shared Shapiro infra (`Corestriction`/`ShapiroLedger`). | ⭐⭐⭐ | F | P-15f2b ✓, `lemma_6_15` ✓, `Q0loc_reindexHom` ✓, Shapiro infra | ∅ (std-3) | ☑ **CLOSED (2026-07-07, committed `7da3fc0`) — `GQ2/ShapiroRead.lean` LANDED, sorry-free, registered, in the green tree; `#print axioms hcoh_involution` = std-3.**  Delivers `shapiroCoord`/`shapiroFun_shapiroCoord_eq` (the explicit-witness Shapiro read), the three per-orbit `hcoh_square`/`hcoh_free`/`hcoh_involution`, and `shapiroCoord_mem_deepClasses` (the f2d deep-coordinate feed).  Board was stale-marked OPEN; corrected on the P-16d6e3 board sweep. |
| P-15f2c2 | B: **Involution deep-unit Kummer presentation** (the `lemma_6_16` field-data → involution `hvanish`) — from `deepPart ρ`'s deep-unit witness at the involution block coordinate, construct the concrete Kummer data `SectionSix.lemma_6_16` consumes: `k ≤ L` (index 2), generator `(d : kˣ, δ, δ²=d, δ∈L, fixingSubgroup rel)`, coordinates `(u : kˣ, v : k, A = u+vδ)`; then compose the banked `ShapiroDeepness.hvanish_evensNorm` (`evensNormH2 = 0 ⟹ H2ofFun (evensNormFun) = 0`). Self-contained char-≠2 Kummer field theory (complete-the-square √-adjoin + degree-2 power basis). mathlib scaffolding: `IntermediateField.adjoin.powerBasis`, `Algebra.IsQuadraticExtension.finrank_eq_two` — but the √-adjoin **generator is not in mathlib**. Needs the `AbsGalQ2 = Kummer.GaloisGroup ℚ_[2]` + `ker ρ = L.fixingSubgroup` bridges (DeepDualityK `kerToFixing`/`hker`). | ⭐⭐ | F+O | `lemma_6_16` ✓ (stmt), `deepPart`/`IsDeepUnit`, `hvanish_evensNorm` ✓, mathlib power-basis | ∅ construction (enables B9/B11a via `lemma_6_16`) | ◐ **SPLIT 2026-07-07 → P-15f2c2a (abstract Kummer presentation package, F) ∥ P-15f2c2b (spine: tower dictionary + assembly, O); `hunram` DEFERRED as future c2c — do NOT spawn until c2b's Step 0 pins its route** (the reducer quantifies over ALL involution orbits: either structure forces `L/k` unramified — deepness? the `C`-action? — or ramified involution orbits need a different vanishing route / an upstream hypothesis; possible statement amendment).  Split principle: `lemma_6_16`'s signature is landed/frozen, so bricks statable purely against it (c2a) parallelize with ZERO f2b coupling; `hunram` is not yet statable.  ⚠ **Tower dictionary CORRECTED 2026-07-07** (the design doc's "`k = K` = fixed field of `ker ρ`, `L = K(√A_j)`" was misassigned; this row's bridge note inherited it): matching the reducer's involution `hvanish` (`H2ofFun ↥U₀ (evensNormFun (N.subgroupOf U₀) ĝ α)`) against `lemma_6_16`'s conclusion (evensNorm over `↥(k.fixingSubgroup)`, index-2 `(L.fixingSubgroup).subgroupOf`) forces `k.fixingSubgroup = U₀`, `L.fixingSubgroup = N = ker ρ`: **`k = fixedField U₀` (lower), `L = fixedField (ker ρ)` (upper)**, `Gal(L/k) = U₀/N = ⟨ĝ⟩`; the deep unit `A_j ∈ L`; `β = √A_j` generates a FURTHER unnamed extension (`L ≠ k(√A_j)`); the witness transport `IsDeepUnit (ker ρ) A → IsDeepUnit L.fixingSubgroup A` is an EQUALITY rewrite (`ker ρ` open ⟹ closed ⟹ `fixingSubgroup (fixedField (ker ρ)) = ker ρ`, InfiniteGalois — the DeepDualityK `kerToFixing`/`hker` pattern; likewise open `U₀ ⊇ ker ρ` ⟹ `k.fixingSubgroup = U₀`) — NO monotonicity helper needed.  Design §"involution field-data" REWRITTEN (corrected dictionary + split interfaces). |
| P-15f2c2a | B: **Abstract Kummer presentation package** (c2's field-theory heart) — ONE exported existence lemma, statable purely against the frozen `lemma_6_16` signature (NO f2b, NO `ρ`): for `k ≤ L` intermediate fields of `ℚ̄₂/ℚ_[2]`, `[FiniteDimensional ℚ_[2] k]`, degree 2 (interface takes DEGREE-2 data, e.g. `Algebra.IsQuadraticExtension`-shaped — the fixing-index-2 → degree-2 bridge is c2b's), `A ∈ L`, `IsDeepUnit L.fixingSubgroup A` ⟹ `∃ (d : (↥k)ˣ) (δ : ℚ̄₂) (u : (↥k)ˣ) (v : ↥k), δ² = d ∧ δ ∈ L ∧ hLδ ∧ A = u + vδ`.  Three lemma groups: **(i) `hLδ`** — `(L.fixingSubgroup).subgroupOf (k.fixingSubgroup) = (MulAction.stabilizer _ δ).subgroupOf _`, the **√-adjoin-generator mathlib gap** (⟸: `g` fixes `k` pointwise + `δ` ⟹ fixes `k⟮δ⟯ = L` — `δ` integral, `PowerBasis.algHom_ext`-style adjoin-simple ext; ⟹: trivial from `δ ∈ L`); **(ii) complete-the-square** — primitive `θ ∈ L∖k` (`k⟮θ⟯ = L`: degree 2 is prime), minpoly `X² + aX + b`, `δ := θ + a/2`, `d := a²/4 − b` (`≠ 0` else `δ = 0 ⟹ θ ∈ k`); **(iii) coordinates** — `A = u + vδ` in the `{1, δ}` power basis (`IntermediateField.adjoin.powerBasis`); `u` is a UNIT by deepness: `u = 0 ⟹ σA = −A` (`σ` = the nontrivial `L ≃ₐ[k] L`, `σδ = −δ`; lift to `ℚ̄₂` by `AlgEquiv.liftNormal`) ⟹ `‖2‖ = ‖(A−1) + (σA−1)‖ ≤ max(‖A−1‖, ‖σA−1‖) < ‖2‖` by spectral-norm Galois-invariance (idiom banked in `AdmissibleCount.conjAct_deepClasses`) — contradiction.  NEW FILE `GQ2/QuadraticAdjoin.lean`, write-disjoint from c1/c2b (shared-worktree safe). | ⭐⭐ | F | `lemma_6_16` ✓ (frozen stmt), `IsDeepUnit`, mathlib `IntermediateField.adjoin.powerBasis` + `AlgEquiv.liftNormal` | ∅ construction (std-3 target) | ☑ **CLOSED 2026-07-07 (Fable) — `GQ2/QuadraticAdjoin.lean` LANDED (340 ln): all 7 declarations PROVED, `#print axioms exists_kummer_presentation` = exactly std-3 (gap lemma too); file gate green; registered in GQ2.lean; tree green (8719).**  **Exports `exists_kummer_presentation`**: `(hkL : k ≤ L) (hdeg : Module.finrank ↥k ↥(extendScalars hkL) = 2) (hAL : A ∈ L) (hA1 : ‖A − 1‖ < ‖(2:ℚ̄₂)‖) ⟹ ∃ d δ u v, δ² = ↑d ∧ δ ∈ L ∧ hLδ ∧ A = ↑u + ↑v·δ` — shapes match `lemma_6_16` verbatim.  **Refinements vs spec**: deepness input is the NORM INEQUALITY, not `IsDeepUnit` (c2b converts via `norm_sub_one_lt_of_isDeepUnit` + `A ∈ L` from `N`-fixedness/`fixedField`); single repo import `GQ2.HilbertLedger` (for `norm_galois`); index-2 → degree-2 bridge stays c2b's.  **Routes SIMPLER than spec — NO `powerBasis`/`liftNormal`/minpoly-identification**: (i) `fixingSubgroup_adjoin_simple` (the mathlib gap, GENERAL over `F`/`E`, ~15 ln, upstreamable): Galois connection `le_iff_le` at `zpowers σ`; `subgroupOf` packaging via `fixingSubgroupEquiv` (function-`rfl`) + `mem_extendScalars` (`Iff.rfl`); (ii) `exists_conj`: σ moving δ from `InfiniteGalois.fixedField_fixingSubgroup ⊥` + `fixingSubgroup_bot` (`IsGalois ↥k ℚ̄₂` auto via `tower_top_intermediateField`), then `(σδ)² = δ²` forces `σδ = −δ`; (iii) complete-the-square in DISCRIMINANT form `δ := 2θ + a`, `d := a² − 4b` (no division; θ via `SetLike.exists_of_lt`, `k⟮θ⟯ = L'` by `eq_of_le_of_finrank_le`, relation by `aeval_eq_sum_range'` + `Monic.coeff_natDegree`); (iv) coordinates via `Algebra.adjoin_singleton_eq_range_aeval` + `modByMonic` (`natDegree_modByMonic_lt`) — no `δ ∉ k` needed; (v) unit coordinate: `u = 0 ⟹ σA = −A ⟹ ‖2‖ ≤ max = ‖A−1‖ < ‖2‖` (`norm_galois` at `restrictScalars`, `IsUltrametricDist.norm_add_le_max`), uniform in `v`.  **Gotchas**: `σ.commutes`/`Algebra.smul_def` give `algebraMap`-form vs goals' `↑` — defeq but NOT syntactic: coercion-ascribed `have` or `exact`/`calc` (never `rw`); `push_cast` misses `↑((4:↥k))` — write `b+b+b+b`; `(pow_eq_zero_iff two_ne_zero).mp` needs parens; `modByMonic_add_div` takes BOTH polys, no `Monic`; `adjoin_simple_toSubalgebra_of_integral` DEPRECATED → `_of_isAlgebraic`; `(2:ℚ̄₂) ∈ k` closes by bare `simp` (`ofNat_mem`).  ⚠ this cell was clobbered once by a parallel `tickets.md` commit — restored. |
| P-15f2c2b | B: **Involution spine — tower dictionary + `lemma_6_16` assembly** — (1) **Step 0**: read f2b's involution block coordinate (`ĝ`, `hs : ĝ ∉ N.subgroupOf U₀`), pin the corrected tower (`k = fixedField U₀`, `L = fixedField (ker ρ)` — umbrella row), and **decide the `hunram` route → scope c2c**; (2) **witness plumbing**: `mem_deepPart_iff` → `⟨A, β, hdeep, hβ, hβ0, heq⟩`; `A ∈ L` from `IsDeepUnit`'s `N`-fixedness + `fixedField`; the two EQUALITY transports (`fixingSubgroup (fixedField (ker ρ)) = ker ρ`, `k.fixingSubgroup = U₀` — InfiniteGalois, open ⟹ closed); `hkL` by antitone `fixedField`; the fixing-index-2 → degree-2 bridge feeding c2a's interface; (3) **mechanical side-conditions** `htriv`/`hUo`/`hs`/`hα`/`hαc` (banked: `kummerCocycleFun_mul` `GQ2/Kummer.lean:179`, `Continuous (kummerCocycleFun _)` `:148`; `hUo` from open `ker ρ`); (4) the `α_j = kummerCocycleFun β` **cochain-level match** — `heq` is CLASS-level; confirm the `H2mk`/`H2ofFun` layer absorbs a `B¹` discrepancy (else borrow the `graphPullback_…_mem_B2` machinery); (5) **assemble** `lemma_6_16` ∘ `hvanish_evensNorm` with c2a's package + `hunram` SORRIED for an early end-to-end typecheck; discharge as they land.  SEMI-INDEPENDENT of c2a (develop against its fixed ∃-interface).  Owns the c2 umbrella row + the design doc. | ⭐⭐ | O | P-15f2c2a (interface only — sorry until it lands), `hvanish_evensNorm` ✓, `mem_deepPart_iff` ✓, f2b involution coordinate | ∅ assembly (inherits B9/B11a via `lemma_6_16`) | ☑ **CLOSED 2026-07-08 (Fable, committed `aaa9458`) — the involution `hvanish` is now self-contained, c2a package DISCHARGED.**  Two-part landing: **(a) the assembly** `ShapiroDeepness.hvanish_involution_of_deepClass` (parallel session, earlier) — steps (2)–(5): unpacks the deep class, derives `hα` (`kummerCocycleFun_hom_on`) / `hαc` (continuity), applies the abstract package + `hvanish_involution` = `hvanish_evensNorm ∘ lemma_6_16`; **(b) the discharge** (this session, NEW file `GQ2/InvolutionVanish.lean`, sorry-free, leaf green 8641): **`hvanish_involution_of_deepClass'`** removes the abstract `hc2a` hypothesis, proving it via **`kummer_presentation_of_index_two`** ⇐ `QuadraticAdjoin.exists_kummer_presentation` (P-15f2c2a): `A ∈ L` from `IsDeepUnit`'s fixedness + `InfiniteGalois.fixedField_fixingSubgroup L`, `‖A−1‖<‖2‖` from `LocalKummer.norm_sub_one_lt_of_isDeepUnit`, and `hdeg` from the one real brick **`finrank_extendScalars_eq_two`** (the fixing-index-2 → degree-2 Galois bridge, **std-3**): `index_extendScalars_fixingSubgroup` transports `L.fixingSubgroup.subgroupOf k.fixingSubgroup` along `fixingSubgroupEquiv k` (`index_map_of_bijective`), then `InfiniteGalois.normalAutEquivQuotient` (`Gal(ℚ̄₂/↥k)⧸G_L ≃ Gal(L/↥k)`, index-2 ⟹ `normal_of_index_eq_two` ⟹ `normal_iff_isGalois`) ∘ `IsGalois.card_aut_eq_finrank`.  `#print axioms hvanish_involution_of_deepClass'` = std-3 + {B9 (`evensKahn_dyadic`), B11 (`hilbertSymbol_normCriterion_finiteDyadic`), B11b (`unramifiedQuadratic_units_are_norms`)} via `lemma_6_16` — exactly the predicted `⊆ {B9,B11}`.  **`hunram` stays a hypothesis** (correct interface): f2d supplies it via c2c4's `UnramifiedBridge.hunram_involution` + the marking data `(R,B,c,ρ,horient,hLfix)` — f2d-level context, not c2b field theory.  Gotcha banked: `Module.Finite ℚ_[2] ↥(extendScalars hkL)` needs a LinearEquiv transport from `↥L` (identity on the shared carrier; `map_smul'` closes by `simp [coe_smul, RingHom.id_apply, SetLike.val_smul]` — the ℚ_[2]-actions align after `coe_smul`), then `Module.Finite.right` gives the `↥k`-version.  Design `docs/p15f2c-design.md` §"involution field-data".  (⚠ tree was red at close from a PARALLEL session's uncommitted `Prop89Close`/`Phase140GammaA`/`VanishClose` WIP — `stageR136_local` undefined — unrelated to this leaf, which builds clean.) |
| P-15f2c2c | B: **the analytic `hunram`** — discharge the equal-value-groups hypothesis (`∀ x∈L, x≠0 → ∃ y∈k, y≠0 ∧ ‖x‖=‖y‖`) that c2b threads into `hvanish_involution` and `lemma_6_16`/`cup_unramified_unit` consume, for the involution tower `k = fixedField U₀ ≤ L = splitField ρ`, `[L:k] = 2`.  **(A) value-group half ✓**: B13 discreteness ⟹ `‖x‖ = ‖π‖^n` (`norm_eq_zpow`), so `‖π_L‖ = ‖π_k‖ ⟹ hunram` (`hunram_of_uniformizer_norm_eq`, + the `e_L = e_k` form).  **(B) ramification bridge [DERIVE — user decision 2026-07-08: NO new axiom]**, 5-step route (`docs/p15f2c2c-scoping.md` §half-(B)): (1) `m := e_{L/k} ∣ 2` — quadratic ĝ-norm trick (`x·ĝx ∈ k`, `norm_galois`), axiom-free; (2) `e_L = e(L/F₀)·e_{F₀}` with `e(L/F₀) ∣ r = ord(c tameTau)` **odd** (`⟨t⟩`-coset norm, `tame_odd_order`), `F₀ := L^{⟨t⟩}`; (3) **keystone: B5 `norm_reciprocity` is the full finite-level norm-group correspondence** (`Reciprocity.lean:216`) and `Gal(F₀/ℚ₂) = C/⟨t⟩` is cyclic ⟹ `e_{F₀} = [U : U∩N(F₀ˣ)] = #(unit-image)` (`Algebra.norm_eq_prod_automorphisms` + `v₂`-SES index bookkeeping); (4) unit-image is **odd**: it factors through a continuous `Ttame → 2-group` killing `τ`, which factors through `nuT` (`tame_odd_order` + T-05 `proPKernel_le_ker`), and B10′ `nuT_recip_unit` kills the 2-part — orientation threaded as ONE added hypothesis of the moved 6.17-vanish (hc/hV2 precedent), discharged at `boundaryMapsWitness`; (5) `e_L` odd + `m ∣ 2` ⟹ `m = 1` ⟹ (A).  Bricks: N1 Galois coset-norm kit → N2 CFT index → N3 `nuT`-factoring → N4 assembly (~3–5 sessions). | ⭐⭐⭐ | F+O | half (A) ✓, B5 `norm_reciprocity`, B10′ `nuT_recip_unit`, B13, `ResidueLift.splitField`+`fixingSubgroup_splitField` ✓, `Tame.tame_odd_order` ✓, `norm_galois` ✓ | ∅ NEW — trace std-3 + {B5, B10′, B13} (existing; census unchanged; 6.17-vanish docstring budget gains B5/B10′/B13 — P-20 deviation note) | ☑ **CLOSED 2026-07-08 — the analytic `hunram` is FULLY DERIVED (no new axiom, as decided): `UnramifiedBridge.hunram_involution`, `#print axioms` = std-3 + B13 exactly** (B5/B10′ enter only at the consumer's `R`/`horient` instantiation — parametric, leaner than the predicted budget).  All four lanes landed: **c2c1** ☑ (`GaloisCosetNorm`, std-3) ∥ **c2c2** ☑ (`UnitNormIndex.card_unitImage_eq_e`, std-3 parametric-B5) ∥ **c2c3** ☑ (`TameTwoQuotient` + `TameOrientationWitness`) → **c2c4** ☑ (`UnramifiedBridge`, the e-chain + `odd_e_inertiaField` + finale).  **Half (A)** (commit `6e0ea24`): `GQ2/UnramifiedNorm.lean`.  📋 History/recipes: `docs/p15f2c2c-handoff.md`, `docs/p15f2c2c-scoping.md` §half-(B).  **Consumer (c2b → f2d)**: feed `hunram_involution` `(k, L, hkL, hindex, hLfix)` + the frozen 6.17 data + `TameUnitOrientation localReciprocity B.tameF` (the moved 6.17-vanish's ONE added hypothesis — `hc`/`hV2` precedent, P-20 flag; discharged at the witness by `tameUnitOrientation_witness`). |
| P-15f2c2c1 | B: **N1 — the Galois coset-norm kit** (axiom-free).  NEW FILE `GQ2/GaloisCosetNorm.lean`, write-disjoint from all f2 files.  For `H ≤ K ≤ Kummer.GaloisGroup ℚ_[2]` with `Finite (K ⧸ H.subgroupOf K)`: `cosetNorm H K x := ∏_{c : K ⧸ H.subgroupOf K} ((Quotient.out c : ↥K) : _) • x`.  Deliverables: (i) per-coset well-definedness on `x ∈ fixedField H` (`g•x` depends only on `gH`); (ii) `cosetNorm H K x ∈ fixedField K` (left-multiplication permutes cosets; field commutativity reorders the finite product); (iii) `‖cosetNorm H K x‖ = ‖x‖ ^ Nat.card (K ⧸ H.subgroupOf K)` (`HilbertLedger.norm_galois` factorwise).  Plus the relative-`e` bookkeeping over B13-data-as-hypotheses (the half-A idiom, building on `UnramifiedNorm.norm_eq_zpow`): `relE`-extraction (for towers `F ≤ L` with filtrations `FF`, `FL`: the unique `m ≥ 1` with `‖FF.π‖ = ‖FL.π‖^m`), tower multiplicativity (`e_L = m·e_F` via zpow-injectivity on `‖2‖ = ‖π‖^e`), and **`relE ∣ n` whenever `∀ x ∈ L^×, ‖x‖^n ∈ ‖F^×‖`** — with (iii) this gives `relE(fixedField H / fixedField K) ∣ Nat.card (K ⧸ H.subgroupOf K)`.  Consumed by c2c4 at two instances: c2b's index-2 pair (`m ∣ 2`) and the `⟨t⟩`-preimage pair (`e(L/F₀) ∣ r`). | ⭐⭐ | O | half (A) ✓ (`UnramifiedNorm.norm_eq_zpow`), `norm_galois` ✓, InfiniteGalois `fixedField`/`fixedField_fixingSubgroup` ✓ | ∅ (std-3; B13 data threaded as hypotheses) | ☑ **CLOSED (Opus 2026-07-08, commits 964b472+36a7119)** — `GQ2/GaloisCosetNorm.lean` registered in the root import, gate-covered, axiom-free (std-3), build green (8651), census 15.  ALL N1 deliverables: Part 1 `cosetNorm`+(i)`smul_eq_of_quot_eq`+(ii)`cosetNorm_mem`+(iii)`norm_cosetNorm`+`exists_mem_fixedField_norm_pow`; Part 2 `relE`/`relE_spec`/`e_eq_relE_mul`/`relE_pos`/`relE_dvd` + the **c2c4 capstone `relE_dvd_index`** (`relE(fixedField K / fixedField H) ∣ [K:H]`, combining (iii)+`relE_dvd`).  Ready for c2c4 (instantiated at the index-2 pair `∣2` and the `⟨t⟩`-preimage pair `∣r`). |
| P-15f2c2c2 | B: **N2 — the CFT unit-index = ramification index** (the keystone B5 consumer; `ρ`-free, fully parallel).  NEW FILE `GQ2/UnitNormIndex.lean`.  For finite Galois `F/ℚ₂` (`IntermediateField ℚ_[2] ℚ̄₂`, `[FiniteDimensional]`, `[IsGalois]`) with `hab : ∀ σ τ : F ≃ₐ[ℚ_[2]] F, στ = τσ` and B13 data `FF`: **`Nat.card ((restrictAb F hab ∘ localReciprocity.recip)-image of `unitEmbed`-units) = FF.e`**.  Chain (design pinned): `[ℚ₂ˣ : N] = #Gal = finrank` (B5(a) surjectivity + `Subgroup.index_ker` + `IsGalois.card_aut_eq_finrank`); `v₂(N(Fˣ)) = (n/e)·ℤ` (`Algebra.norm_eq_prod_automorphisms`, lift automorphisms along `AlgEquiv.restrictNormalHom_surjective`, `norm_galois` ⟹ `‖(Norm x : ℚ̄₂)‖ = ‖x‖^n`, then `norm_eq_zpow` + the `‖(y:ℚ̄₂)‖ = 2^{−v₂ y}` bridge — n/e ∈ ℤ by the full-Galois coset norm); the `v₂`-SES index factorization `[ℚ₂ˣ : N] = [ℤ : v₂(N)]·[U : U∩N]` (`U := ker v₂ = unitEmbed.range` via the `PadicInt`-‖·‖≤1 description; 2nd-iso + index multiplicativity — small hand-rolled index lemma if mathlib's `relindex` calculus resists); conclude `[U : U∩N] = FF.e` and `= #(unit-image)` (B5(a) kernel + 1st-iso on `U`). | ⭐⭐⭐ | F+O | B5 `norm_reciprocity` ✓ (`Reciprocity.lean:216–220`), half (A) ✓, mathlib `Algebra.norm_eq_prod_automorphisms`/`restrictNormalHom_surjective` | std-3 + B5 | ☑ **CLOSED 2026-07-08 (Fable session 2, commits `964b472`(analytic core)+`e8b3dc8`; registered in root imports, `lake build` green 8731).**  `GQ2/UnitNormIndex.lean` (336 ln, sorry-free): analytic core `norm_val`/`norm_two`/`e_mul_val_norm`; `v₂`-calculus (`v2_mul`/`v2_zpow`/`v2_inv`/`v2_uniformizer`/`v2_unitEmbed`, **`mem_unitEmbed_range_of_v2_eq_zero`** — ker v₂ ⊆ `unitEmbed.range` via the `PadicInt`-‖·‖≤1 description + `PadicInt.isUnit_iff` — and `eq_zpow_uniformizer_mul_unit`, the `2^v·u` decomposition); `f₀`-layer (`piUnit`, `normValPi := v₂(N π_F)`, `e_mul_normValPi` `e·f₀ = n`, `normValPi_pos`, `normValPi_dvd_v2_of_mem` `v₂(N(Fˣ)) ⊆ f₀ℤ`, `exists_mem_normSubgroup_v2_eq` ⊇); **the count `card_unitImage_eq_e`**: `Nat.card (((restrictAb F hab).comp R.recip).comp unitEmbed).range = FF.e`.  **GATE: `lean_verify card_unitImage_eq_e` = std-3 EXACTLY** — beats the budgeted std-3+B5: fully parametric in `R : LocalReciprocity` + B13-data-as-hypothesis (half-A idiom); B5/B13 enter only at c2c4's instantiation.  ROUTE deviation-of-record (simpler than this row's pinned chain): NO `Subgroup.index_ker`/SES/`relindex`/iso-theorems — `Gal ⧸ Gu` is cyclic generated by `q := φ(2)` (B5(a) surjectivity + the `2^v·u` decomposition), `q^k = 1 ↔ f₀ ∣ k` (B5(a) kernel = `normSubgroup` + the `f₀`-layer), so `[Gal:Gu] = orderOf q = f₀` (`Nat.card_zpowers`) and `#Gu·f₀ = #Gal = n = e·f₀` (`Subgroup.card_mul_index` + `IsGalois.card_aut_eq_finrank`, now `Nat.card`-stated).  → UNGATES c2c4 increment 2: consume `UnitNormIndex.card_unitImage_eq_e` to convert unit-image-odd into `Odd e_{F₀}`. |
| P-15f2c2c3 | B: **N3 — the tame 2-quotient factoring + the B10′ orientation package**.  NEW FILE `GQ2/TameTwoQuotient.lean`.  (i) *τ-death in 2-groups* (pure finite group theory): any `φ : Ttame →* Q`, `Q` a finite 2-group, has `φ tameTau = 1` (`Tame.tame_odd_order` on the pushed `tame_relation`: odd ∧ 2-power order ⟹ trivial).  (ii) **the factoring**: any continuous `φ : Ttame → Q` (finite discrete 2-group) factors through `nuT : Ttame → Ztwo` — route: `T₂ := maxProPQuotient 2 Ttame`; T-05 `proPKernel_le_ker` factors `φ` through `T₂`; identify `T₂ ≅ Ztwo` compatibly with `nuT` (both directions by universal properties: `nuT` descends `T₂ → Ztwo` since `Ztwo` is pro-2; `Ztwo → T₂` from the `Zhat` generator-classifier killing the pro-2 kernel; roundtrips = id on the topological generator, `monoidHom_eq_of_topGen` pattern).  FALLBACK if `T₂ ≅ Ztwo` fights: the direct procyclic-closure argument in `ℤ₂ × Q` (scoping §half-(B) step 4).  (iii) `def TameUnitOrientation (R : LocalReciprocity) (tameF : ContinuousMonoidHom AbsGalQ2 Ttame) : Prop := ∀ u g, toAb g = R.recip (unitEmbed u) → nuT (tameF g) = 1` + **the witness discharge**: `boundaryMapsWitness.tameF` satisfies `TameUnitOrientation localReciprocity` (unpack the witness's `tameF` against `tameQuotient.nuT_recip_unit` — read `BoundaryMapsWitness.lean`'s construction). | ⭐⭐⭐ | F | T-05 `proPKernel`/`maxProPQuotient`/`proPKernel_le_ker` ✓, `tame_relation` ✓, `Tame.tame_odd_order` ✓, B10′ `nuT_recip_unit` (for (iii) only) | (i)+(ii) ∅ (std-3); (iii) std-3 + {B10′ + B5-via-bundle} | ☑ **CLOSED 2026-07-08 (Fable, two increments).**  **(i)+(ii)+(iii-def)** landed `b43be7a` (`GQ2/TameTwoQuotient.lean`, registered `c4e2118`): `map_tameTau_eq_one` (i); `maxProPMk_tameTau` + **`ker_nuT_le_proPKernel`** + `map_eq_one_of_nuT_eq_one(_finite)` (ii — via the SIMPLER kernel-inclusion route: `maxProPMk = ρ'∘nuT` by the `Zhat`-power hom `ρ'` + `monoidHom_eq_of_topGen` on `{σ,τ}`, NO `T₂ ≅ Ztwo` iso needed; the `_finite` form is the c2c4 consumer shape); `TameUnitOrientation` def (iii-def).  `lean_verify`: (i)/(ii) = **std-3 exactly**.  **(iii) witness discharge** landed this increment: NEW leaf `GQ2/TameOrientationWitness.lean` (registered) — `tameUnitOrientation_tameFHom` (**std-3 + {B5 `localReciprocity`, B10′ `tameQuotient`} exactly**, the handoff-§7 predicted trace; proof is verbatim `tameQuotient.nuT_recip_unit` through the `locTame`-repackaging defeq, the `tame_recip_unitNeg3` pattern) + `tameUnitOrientation_witness` (the `B := boundaryMapsWitness` consumer shape; trace adds the witness bundle's B3c+B8, = P-25's `prop_3_14` budget; no `sorryAx`).  Leaf-file placement: keeps `TameTwoQuotient` light for c2c4 AND dodges the `maxProPMk_tameTau`/`ker_nuT_le_proPKernel` name collision between the `GQ2` (TameTwoQuotient) and `GQ2.SectionThree` (BoundaryMapsWitness) scopes.  → c2c4 consumes `map_eq_one_of_nuT_eq_one_finite` + `TameUnitOrientation`; the P-19/P-20 assembly consumes `tameUnitOrientation_witness`. |
| P-15f2c2c4 | B: **N4 — assembly: `hunram` for the involution tower** (capstone).  NEW FILE `GQ2/UnramifiedBridge.lean`, gated on c2c1–c2c3 (+ half (A) ✓).  Tower setup: `t := c tameTau`, `⟨t⟩ ◁ C` (tame relation; conj-by-`s` is into, finite ⟹ onto), `C/⟨t⟩` cyclic (`hc`-surjectivity: `C = ⟨c tameSigma, t⟩`), `r := ord t` odd; `F₀ := fixedField (kerGal-preimage of ⟨t⟩)` (the `ResidueLift.splitField` idiom), `IsGalois ℚ_[2] F₀` (normal fixing subgroup), `Gal(F₀/ℚ₂)` abelian (`restrictNormalHom` kills the fixing subgroup ⟹ quotient of the cyclic `C/⟨t⟩`).  Unit-image odd: the 2-part quotient `Gal(F₀/ℚ₂) ↠ Q` composed back to `AbsGalQ2` kills `ker B.tameF` and `τ` (`hfac`), so factors as a continuous `Ttame → Q` ⟹ c2c3(ii) factors it through `nuT` ⟹ `TameUnitOrientation` kills the unit-image in `Q` ⟹ image lands in the odd part ⟹ c2c2 gives **`e_{F₀}` odd**.  The `e`-chain (c2c1): `e(L/F₀) ∣ r`, `m := relE(L/k) ∣ 2` (c2b's `hindex`/`s`/`hs` — `x·(s•x) ∈ k` since `L.fixingSubgroup` is index-2-normal in `k.fixingSubgroup`), `e_L = m·e_k` and `e_L = e(L/F₀)·e_{F₀}` ⟹ `e_L` odd ⟹ `m = 1` ⟹ `‖π_L‖ = ‖π_k‖` ⟹ half (A) `hunram_of_uniformizer_norm_eq` ⟹ **`hunram` verbatim in `hvanish_involution`'s shape** (`ShapiroDeepness.lean:187`).  Interface: consumes c2b's `(k, L, hkL, hindex, s, hs)` + the frozen 6.17 data `(ρ, c, hc, B, hfac)` + `TameUnitOrientation localReciprocity B.tameF` (threaded; discharged at the witness by c2c3(iii)) + c2b's `L.fixingSubgroup = kerGal ρ` identification.  ⚠ flags: the moved 6.17-vanish gains the orientation hypothesis (`hc`/`hV2` amendment precedent, P-20); docstring axiom budget gains B5/B10′/B13 (existing axioms; census unchanged). | ⭐⭐ | F+O | P-15f2c2c1, P-15f2c2c2, P-15f2c2c3, half (A) ✓, `ResidueLift.splitField`/`fixingSubgroup_splitField`/`splitField_finiteDimensional` ✓ | std-3 + {B5, B10′, B13} | ◐ **STARTED (Fable 2026-07-08) — INCREMENT 1 LANDED: the complete `e`-chain, SORRY-FREE, all std-3 exactly** (`GQ2/UnramifiedBridge.lean`, registered; `lake build` green 8657).  **Leg 1**: `relE_dvd_of_index` (`e(L/k) ∣ [k.fs : L.fs]` — c2c1's `relE_dvd` + `exists_mem_fixedField_norm_pow`, memberships transported along `InfiniteGalois.fixedField_fixingSubgroup`; avoids filtration casts entirely) + `relE_dvd_two` (at c2b's `hindex`, the quotient `Finite` from `index = 2 ≠ 0` via `Nat.card_ne_zero`).  **Leg 2**: `preimGal ρ S` (the `kerGal` two-views idiom at an arbitrary `S ≤ C`) + `kerGal_le_preimGal`/`preimGal_isOpen`/`inertiaField := fixedField (preimGal ρ ⟨t⟩)`/`fixingSubgroup_inertiaField`/`inertiaField_finiteDimensional` (the `splitField` mirrors) + `inertiaField_le` (`F₀ ≤ fixedField (kerGal ρ)`, hand-rolled antitonicity) + **`card_quot_preimGal`** (`[T₀ : ker ρ] = orderOf t` — first iso at the restricted `ρ' : T₀ →* ⟨t⟩` + `Nat.card_zpowers`).  **Assembly capstone `hunram_of_odd_eF0`**: given `e(L/k) ∣ 2`, `e(L/F₀) ∣ r` odd, and `hodd : Odd F₀F.e` (the c2c2/c2c3-gated input, THREADED), concludes the verbatim `hunram` — divisor-of-odd is odd (`Int.odd_mul`), tower product odd, odd positive divisor of 2 is 1 (`Int.le_of_dvd` + omega + `Int.odd_iff`), `relE_spec` at 1 ⟹ `‖π_L‖ = ‖π_k‖` ⟹ half (A).  Gotcha: `x ∈ inertiaField` is defeq-opaque to `rw [mem_fixedField_iff]` — re-view via an explicitly-typed `have` first.  **☑ CLOSED (Fable 2026-07-08) — INCREMENT 2 LANDED: `hodd` DERIVED + the finale BUILT; c2c4 (and the whole c2c derivation) COMPLETE.**  `lake build` green (8659), guard all-pass (census 15), file sorry-free.  New pieces (all in `UnramifiedBridge.lean`): **§Descend** `descendHom`/`descendHom_apply` (factor a hom through a surjection with kernel inclusion, `surjInv`-based); **§OddPart** `odd_of_dvd_odd`, `oddTorsion hab` (odd-order elements, subgroup via `Commute.orderOf_mul_dvd_lcm` — commutativity threaded as `hab`, not `[CommGroup]`, matching the Galois-side shape), `odd_card_oddTorsion` (Cauchy contrapositive via `exists_prime_orderOf_dvd_card`), **`isPGroup_quotient_oddTorsion`** (`g^{2^a}` has odd order for `a := factorization 2`, via `orderOf_pow` + `Nat.ordProj_dvd`/`Nat.not_dvd_ordCompl`); **§TameKill** `unit_dies_in_two_group` (`ξ' (ρ g) = 1` for any `ξ' : C →* Q`, `Q` finite 2-group, `g` a reciprocity-unit lift: `ξ'∘ρ = (ξ'∘c)∘B.tameF` by `hfac`, `ξ'∘c` continuous from discrete `C` with the ⊥-topology `letI`'d on abstract `Q` — no Krull-instance interference — then c2c3's `map_eq_one_of_nuT_eq_one_finite` + `TameUnitOrientation`); **§OddE `odd_e_inertiaField`** (`Odd F₀F.e`, **std-3 exactly**): `⟨t⟩ ◁ C` (`Tame.zpowers_normal_of_tame` — already proved in Tame.lean!), preimage normal ⟹ **`IsGalois ℚ_[2] F₀` via mathlib's `InfiniteGalois.normal_iff_isGalois`** (the flagged plumbing risk — RESOLVED, it exists), `κ : C →* Gal(F₀/ℚ₂)` = `descendHom` of `restrictHom F₀` (ker inclusion via the BANKED term-mode `IntermediateField.restrictNormalHom_ker` — `restrictHom` is defeq-opaque to `rw`), `κ` surjective + kills `t` ⟹ `hab` via `κ̄` on `C⧸⟨t⟩` = `zpowers (mk s₀)` (map_closure + `closure_union`/`closure_singleton_one`/`zpowers_eq_closure`), the unit-image ≤ `oddTorsion hab` (the kill at `Q := Gal⧸oddTorsion`), `Subgroup.card_dvd_of_le` + c2c2's `card_unitImage_eq_e` ⟹ odd; **§Finale `hunram_involution`** (THE c2c DELIVERABLE, **`#print axioms` = std-3 + B13 exactly** — LEANER than the predicted budget: B5/B10′ enter only at the consumer's `R`/`horient` instantiation since both are parameters): takes c2b's `(k, L, hkL, hindex)` + frozen `(B, c, hc, ρ, hfac)` + `horient : TameUnitOrientation R B.tameF` + `hLfix : L.fixingSubgroup = kerGal ρ`, internal B13 filtrations, and concludes `∀ x ≠ 0 ∈ L, ∃ y ≠ 0 ∈ k, ‖x‖ = ‖y‖` — verbatim `hvanish_involution`'s `hunram`.  Lean gotchas banked: (i) `set t := c tameTau` REWRITES argument types (`F₀F`) creating shadowed copies — use `revert F₀F; generalize hteq : c tameTau = t; intro F₀F` instead; (ii) `rw [← hgt]` on `κ t = 1` fails the motive (`κ`'s TYPE mentions `t`) — prove `κ t = κ (ρ gt)` by rewriting the OTHER side (`rw [hgt]`, motive only abstracts the argument); (iii) `Nat.not_dvd_ordCompl` takes `Nat.Prime` (namespace-`Prime` inside `Nat`), not `Prime`.  **Consumer note (c2b/f2d owner, cross-flagged)**: instantiate `R := localReciprocity`, `horient := tameUnitOrientation_witness`-style at `B := boundaryMapsWitness` (or thread `TameUnitOrientation localReciprocity B.tameF` as the moved 6.17-vanish's added hypothesis — the `hc`/`hV2` amendment precedent; P-20 flag stands). |
| P-15f2d | B: **final assembly + SectionSix splice** — compose f2a (swap `dat → datW.comap ι`) + `lemma_6_14` transport + f2b (`datW = sumDatum`) + f2c (`hcoh`/`hvanish`) through `Q0loc_vanish_of_datum_decomp`, then remove the `lemma_6_17_vanish` sorry. | ⭐⭐⭐ | — | P-15f2a, P-15f2b ✓, P-15f2c1, P-15f2c2 | (inherits B9/B11a/B6/B7) | ☐ OPEN (capstone). **NARROWED (2026-07-07): the reindex/transport infra is LANDED** in `ShapiroDeepness.lean` — `Q0loc_reindexHom`/`graphPullback_reindexHom` (the "swap `dat → datW.comap ι`" step, now a one-liner) + `deepPart_mapCoeff1` (deepness across `ι`); remaining = compose them with f2a (DI-core) + f2c1/f2c2 + `lemma_6_14` + the `e:C≃AbsGalQ2⧸kerρ` instance plumbing. Skeleton buildable NOW against a/b/c signatures. ⚠ **co-owns the frozen 6.17 signatures + the shared SectionSix exit with P-15f1/P-15f8** — statement-move pattern (6.18ram/P-15d), cross-flag. Wiring only. ~100–200 ln. **◐ STARTED (Opus, 2026-07-07) — the FRONT-HALF is VERIFIED; `GQ2/VanishClose.lean` registered.** The full `lemma_6_17_vanish` → `Q0loc D datWC ρ (ι∗x) = 0` reduction COMPILES (f2a `Q0loc_datum_indep` + Lemma 6.14 `RepIndependence.lemma_6_14` + f2b `regular_isometric_embedding_orbit` + all W-instance plumbing); verbatim drop-in code + the reducer plan in **`docs/p15f2d-handoff.md`**. ⚡ **KEY FINDING**: the P-15f handoff's `RegMod`-global-trivial-action warning does NOT apply to `W = Fin K → RegRep (ker ρ)` — `RegRep` is an opaque `def`, so `DistribMulAction AbsGalQ2 W` fails to synthesize by default (verified `#synth`), and the intended `mk'`-pullback action `letI`s cleanly (no synonym). Committed this session: the sorry-free wiring bricks (`reindexHom_sumDatum`, `isEquivariantFactorSet_reindexHom`, `eOfSurj`, `eOfSurj_rho`, all std-3) NOW REGISTERED (were orphaned) + the handoff. **REMAINING** (handoff §3–§4): (i) the reducer application `Q0loc_vanish_of_datum_decomp` — per-orbit `hcoh` (mechanical, `graphPullback_reindexHom` + `eOfSurj_rho` + f2c1 `hcoh_{square,free,involution}`) + `hvanish` (square/free = `hcup` via `hvanish_cup`+splitField transport; involution = c2's `hvanish_involution_of_deepClass`); (ii) ⚠ **the one genuine gap = the Evens-norm cohomology-invariance bridge** `evensNormFun α - evensNormFun α' ∈ B²` for cohomologous scalar `α~α'` — needed because f2c1's involution inner is `shapiroCoord`-based while c2's vanishing is `kummerCocycleFun β`-based (same deep class, different cochain); ~50–100 ln in `EvensKahn`; (iii) the SectionSix statement-move — **JOINT with P-15f8** (dim proof `ResidueLift.lemma_6_17_dim_final` LANDED): relocate `prop_6_18_ramified` downstream citing both `_final` proofs, delete the two SectionSix sorries, drop from `SORRY_ALLOWLIST`. |
| P-15f3 | B: `prop_6_18_unramified` — cohomological model identification (paper steps 1–3 + Lemma 6.7 splice) | ⭐⭐⭐ | O | P-15d ✓ (6.14 naturality); **NO 6.17 dependence** (statement standalone — verified) | B6, B7 | ✅ **PROVED — CLOSED modulo splice** (2026-07-05; status verified Fable 2026-07-07).  `GQ2.UnramifiedModel.prop_6_18_unramified` is sorry-free in `GQ2/UnramifiedModel.lean`; `#print axioms` = **std-3 + B7** (`absGalQ2_localEulerCharacteristic`; **B6 via the `D : TateDuality 2` parameter**), **no sorryAx** — matches the declared Ax column.  All five design bricks below are ✅ done.  **☑ FULLY CLOSED 2026-07-07 (Fable)**: the `SectionSix.lean:914` sorried copy is MOVED OUT to a comment-pointer (the `prop_6_18_ramified`/P-15d pattern; a direct `exact` splice was impossible — `UnramifiedModel → DeepPart → … → SectionSix` would cycle); zero external references verified before removal; library + §8 leaves build green.  `SectionSix.lean` now carries only the two P-15f1/f2 sorries (`lemma_6_17_dim`/`_vanish`) and leaves the `SORRY_ALLOWLIST` when they clear.  ⚠ **NOT** completed by the 2026-07-07 B6 base-generalization — proven 2 days prior; the board row was simply stale.  **Design bricks (history, all done):** (1) **C cyclic**: `hunram`+`hfaith` ⟹ `c tameTau = 1`; `hc` + tame topological generation ⟹ `C = ⟨c tameSigma⟩` ⟹ abelian. (2) **Schur**: the 𝔽₂[C]-image in `End V` is a finite integral domain (kernels/images are C-stable + `hsimple`) = a field `F`; `V ≅ F` as an F-line ⟹ `#F = #V = 2^{2m}`. (3) **H¹ a D-line**: `mapCoeff1` is additive + compositional in the C-equivariant coefficient map ⟹ H¹ an F-space; `#H¹ = #V = #F` (Euler collapse ✓ banked; `V^C = 0` from simplicity+faithfulness+`C ≠ 1` [`C = 1` forces `#V = 2`, contra `hcard` with `m ≥ 1`]) ⟹ dim 1. ⚠ do NOT confuse the coefficient-only action with the conjugation-acts-trivially fact — only the COMPOSITE (conj on argument ∘ action on coefficients) is id on H¹; argument-conj alone is not even a cochain map, so the coefficient action is genuinely nontrivial. (4) **U-invariance of the transported form**: needs [a C-INVARIANT variant of `hermitian_form_eq_trace_form` — replace `hU` by invariance under a subgroup `Γ ⊆ D^×` with 𝔽₂[Γ] = D; the polar adjoint `d ↦ d*` is then a ring automorphism = Frobenius power, involutive, ≠ id (else `Γ` is 2-torsion in odd-order `D^×` ⟹ trivial) ⟹ `* = Frob^m` ⟹ `Γ ⊆` norm-one AUTOMATIC; DeepPart amendment, hU-version currently consumer-free] + [**`Q⁰loc` datum-independence for fixed q** (`Q0loc_{u^*dat} = Q0loc_{dat}` when `q∘u = q`) — CHECK availability in the 6.4/6.5 layer (P-15a); if absent, this is the one genuinely new cohomological brick; then `lemma_6_14`-style naturality with `u` as the equivariant map gives `Q0loc(u_* x) = Q0loc(x)`]. (5) Transport along the D-line iso + splice `card_normOne_invariant_form_zero`. Statement MOVES out of SectionSix (6.18ram/P-15d pattern). Own file `GQ2/UnramifiedModel.lean` (imports DeepPart). Likely FIRST to finish of the three. |
| P-15f4 | B: discharge `lemma_6_11` — ramified simple `V` is an equivariant split summand of a regular `𝔽₂[H_V]`-module (projectivity) | ⭐⭐⭐ | O | — (self-contained; no arithmetic context) | ∅ (NO axioms — pure finite rep theory) | ☑ **CLOSED via P-17e4 (2026-07-06).** ~~OPEN (created 2026-07-06).~~ The sole `sorry` of `GQ2/RegularSummand.lean` (`lemma_6_11`, allowlisted paper node §6.3 pp. 29–30). Its consequence `equivariant_lift_of_regular_summand` (Hom(V,−)-exactness = the deep-count lower bound input) is ALREADY PROVED std-3 sorry-free, so discharging this one sorry removes `sorryAx` from the whole f1 chain with zero consumer churn. **Discharge plan (file docstring)**: (1) Sylow-2 `P ≤ C` cyclic (`\|⟨c τ⟩\|` odd via `LocalKummer.odd_orderOf_tameInertia`; `P ↪ C/⟨cτ⟩`-closure cyclic); (2) `V\|_P` free over `𝔽₂[P]` — Maschke on the odd inertia (Mathlib `MonoidAlgebra.Submodule.exists_isCompl`) + Clifford weight-orbit over `𝔽_{2^T}` (weight-stabilizer acts by 2-power-order scalar = 1, faithfulness) + norm-criterion descent (`V` `𝔽₂[⟨p⟩]`-free ⟺ `N_p V = V^p`, `N_p=(1+σ)^{2^a−1}`; Mathlib `Module.equiv_directSum_of_isTorsion`); (3) free-over-`P` ⟹ split-summand-over-`C` via the odd-index `[C:P]` relative trace (sibling of `inflationVanishes_of_oddNormal`'s averaging, `odd_nsmul_eq_self`, no division). Est. 1200–2500 ln, ZERO arithmetic — **ideal isolated parallel-agent ticket**. Own file `GQ2/RegularSummand.lean` (already exists, allowlisted). **Execution plan banked (Fable 2026-07-06): `docs/p15f4-plan.md`** — full brick decomposition A–G with an 𝔽₂-native route (no 𝔽̄₂/Clifford: conj-by-Frobenius on the inertia subring + eigenvector-orbit stabilizer kill + jump-dimension norm criterion + explicit odd-transversal trace), verified-name ledger, risk register. Start there; do not re-derive the route. **→ MERGED INTO P-17e4 (cross-checked 2026-07-06, Fable):** the 6.11 *assembly* landed there (odd-index Sylow relative trace `regular_summand_of_subgroup_summand`, std-3 sorry-free, + kernel toolbox), but the sorry is **NOT discharged** — `#print axioms GQ2.lemma_6_11` still shows `sorryAx`, entering through the single re-posed kernel `sylow_free_of_ramified` (RegularSummand.lean: `V\|_P ≃+ 𝔽₂[P]^r` equivariantly — the pp. 29–30 weight-orbit freeness, the true long pole). Do NOT work this ticket independently — track the kernel under P-17e4. **KERNEL LANDED (P-17e4 CLOSED 2026-07-06): `#print axioms GQ2.lemma_6_11` = exactly std-3, `RegularSummand.lean` sorry-free & off the allowlist — this cross-reference row is CLOSED too.** |
| P-15f5 | B: f1-dim **Hom-exactness counting engine** — given a regular-summand package `(ι, r)` for `V` (hypothesis; 6.11's output shape), `Hom_C(V, −)` is surjective along equivariant surjections of finite 2-torsion modules (= banked `equivariant_lift_of_regular_summand`), hence for a finite filtration `M = F₀ ⊇ … ⊇ F_n = 0`: `#Hom_C(V, M) = ∏ⱼ #Hom_C(V, Fⱼ/Fⱼ₊₁)` (each step: `Hom(V,Fⱼ) ↠ Hom(V,grⱼ)` with kernel `Hom(V,Fⱼ₊₁)`, count by fiber). Pure module theory, NO arithmetic; **std-3 sorry-free standalone** (the package is a hypothesis — sorryAx enters only at f8 through the 6.11 node). | ⭐⭐ | F | — (consumes banked `equivariant_lift_of_regular_summand`) | ∅ | ☑ **DONE (Fable, 2026-07-06, `GQ2/HomCounting.lean` — NEW file, registered in GQ2.lean; library green 8673, guard green, census 15).** All six declarations **std-3 sorry-free** (`#print axioms` verified — the package-as-hypothesis design keeps `sorryAx` out entirely): **`equivHoms C V W : AddSubgroup (V →+ W)`** (equivariance closed under 0/+/−) + `mem_equivHoms_iff`; **`postCompHom`** (post-composition as an additive map between equivariant-Hom groups) + eval rule; **`card_ker_postCompHom`** (kernel identification: `ker (π∘−) ≃ Hom_C(V, W')` along an equivariant injection `j` with `range j = ker π` — choice-lift `Function.invFun j` through the injection, additivity/equivariance by `j`-injectivity); **`card_equivHoms_of_exact`** (THE step: `#Hom_C(V,W) = #Hom_C(V,W')·#Hom_C(V,W'')` for an equivariant SES presentation of finite 2-torsion modules — Lagrange `AddSubgroup.card_eq_card_quotient_mul_card_addSubgroup` + first iso `QuotientAddGroup.quotientKerEquivOfSurjective`, surjectivity from banked `equivariant_lift_of_regular_summand`); **`card_equivHoms_of_subsingleton`** (endpoint `#Hom = 1` at the filtration bottom); **`card_equivHoms_congr`** (transport along an equivariant `≃+` — for swapping in concrete graded models). Iterated ∏-over-filtration intentionally left to f6/f8 (one step-application per graded piece at the consumer's concrete presentation). f6 can now start. |
| P-15f6 | B: f1-dim **Kummer transport + graded counts** (`card_fam`/`card_deepFam`) — identify `AdmissibleFam ρ` with `Hom_{H_V}(V^∨, H¹(N,𝔽₂))` and transport the unit filtration `Uᵢ(K)` across B12 (`kummerClassK_surjective`) using the `deepClass_eq_kummerClassK` bridge (banked std-3); compute `#Hom(V^∨, grⱼ)` by isotypic multiplicities against B13's graded sizes; feed f5's engine to get the two counts as `2^{Σ d j}`. ⚠ likely needs the **B13-(F2) inertia-action clause** (inertia acts on `grⱼ` by the `j`-th tame-character power — Serre LF IV §2 Prop 7 `[✓ verified in the provided scan]`, deferred from the original B13; **user approval before any census change**). | ⭐⭐⭐ | F+O | P-15f5, B12, B13 | B12, B13 (+ possible approved extension) | ◐ **📋 HANDOFF — read `docs/p15f-handoff.md` first (self-contained continuation guide, in-repo).** **CLAIMED (Fable, 2026-07-06).** Plan: (i) the depth-filtration skeleton `kummerDepthSubgroup k π j` (AddSubgroup of `H¹(G_k,𝔽₂)` = `kummerClassK`-image of `depthUnits π j`; subgroup structure from banked `kummerClassK_mul`/`_one` + 2-torsion; monotone; `⊥` at `j = 2e+1` via `sq_of_near_one` + `kummerClassK_mul_self`) — π/`‖2‖=‖π‖^e`/discreteness as HYPOTHESES so the skeleton stays axiom-free, B13 supplies them at the consumer; (ii) B12 top identification + graded models; (iii) AdmissibleFam ≃ equivHoms bridge + f5 iteration. Leaf checkpoint before any B13-(F2) use. **Brick (i) LANDED (Fable, 2026-07-06): `GQ2/KummerFiltration.lean`** (NEW file, registered; all six declarations **std-3 sorry-free axiom-free** by `#print axioms`; guard green census 15; whole-lib transiently red on the P-17e4 agent's in-flight RegularSummand WIP — orthogonal, my gate green): **`kummerDepth k π j : AddSubgroup (H¹(G_k,𝔽₂))`** (kummerClassK-image of `depthUnits π j`; subgroup via `kummerClassK_mul`/`_one` + 2-torsion `h1_add_self`), `mem_kummerDepth_iff`, **`kummerDepth_antitone`**, `kummerClassK_eq_zero_of_sq`, **`kummerDepth_eq_bot`** (dies at `j ≥ 2e+1` via `sq_of_near_one` — the f5-iteration endpoint), **`kummerClassK_mem_deepClasses`** (deep unit ⟹ deep class; converse of the bridge), **`coe_kummerDepth_deep`** (`(kummerDepth k π (e+1) : Set) = deepClasses k.fixingSubgroup` — stage e+1 IS the deep classes, so `deepClasses` is an AddSubgroup; forward via `hstep`, backward via the bridge + `hπ_max` discreteness upgrading `< ‖π‖^e` to `≤ ‖π‖^{e+1}`). B13 inputs (π∈k, ‖2‖=‖π‖^e, e≥1, ‖π‖<1, discreteness) all HYPOTHESES — consumer pulls them from `dyadicUnitFiltration`. NEXT: brick (ii) B12 top identification (#H¹ vs the unit part) + graded models; brick (iii) AdmissibleFam ≃ equivHoms bridge + f5 iteration. **Brick (ii) LANDED (Opus, 2026-07-06): the `C`-module structure on `H¹(N,𝔽₂)`** (LocalKummer `ConjAction` section; all std-3 axiom-free by `#print axioms`; guard green census 15): the `conjAct` algebra — `conjMap_mul`, `conjAct_add`, `conjAct_zero`, `conjAct_comp` (left action, contravariant conjMap composition), `conjAct_one`, **`conjAct_inner`** (inner conjugation by `m∈N` is trivial on `H¹(N)`: cocycle is a hom, `f(m⁻¹nm)=f(n)` in char 2), `conjAct_ker` (`ρg=ρg' ⟹ conjAct g = conjAct g'`) — packaged into **`conjModule ρ hρsurj : DistribMulAction C (H¹(N,𝔽₂))`** (`c•ξ := conjAct ρ (surjInv c) ξ`; one_smul/mul_smul via conjAct_ker+comp+surjInv_eq; @[reducible] for letI use) + `conjModule_smul_of_lift` (`c•ξ = conjAct ρ g ξ` for ANY lift g). This is the acting-group structure making `AdmissibleFam = equivHoms C V^∨ (H¹(N))`. NEXT brick (iii): the `AdmissibleFam ≃ equivHoms` bridge (under `letI conjModule`) + the U_{e+1} SES (deepClasses = kummerDepth(e+1), an AddSubgroup by brick i) + ONE f5 `card_equivHoms_of_exact` application ⟹ reduces the balance `#X₊=#(H¹/X₊)` to the duality (f7). **Brick (iii-a) LANDED (Opus, 2026-07-06): the `AdmissibleFam ≃ equivHoms` bridge** (`GQ2/AdmissibleCount.lean` NEW file, registered; all std-3 axiom-free by `#print axioms`; guard green census 15): **`dualModule : DistribMulAction C (V →+ 𝔽₂)`** (dual action `(c•φ)v = φ(c⁻¹•v)`; @[reducible], letI'd to beat the trivial codomain instance) + `dualModule_smul_apply`; **`fam_equivariant`** (the core: `ξ.fam` intertwines dualModule and conjModule — converts `AdmissibleFam.equiv'`'s G_ℚ₂-form to the C-form via `surjInv c` + `hρ`); **`admissibleFamEquiv : AdmissibleFam ρ ≃ equivHoms C V^∨ (H¹N)`** (letI conjModule + dualModule in the type; forward = fam_equivariant, converse `equiv'` re-derived by conjAct_ker+hρ calc); **`card_admissibleFam_eq`** (#AdmissibleFam = #equivHoms — feeds f5). NEXT brick (iii-b): apply f5 `card_equivHoms_of_exact` to the U_{e+1} SES (deepClasses = kummerDepth(e+1) AddSubgroup, brick i) ⟹ reduces the balance #X₊=#(H¹/X₊) to the duality (f7). Gotcha banked: `•` (letI instance) vs `.toSMul.smul` (explicit) don't rw-match — use a calc through defeq + conjAct_ker instead of rw-at-hyp. **Brick (iii-b) PARTIAL (Opus, 2026-07-06): SES substrate** (`AdmissibleCount.lean`, both std-3): **`deepClassesSubgroup N : AddSubgroup (H¹(N,𝔽₂))`** (deepClasses closed under 0/+/neg — deep-unit product A₁A₂ + `kcf_mul_of_fixed` + `H1ofFun_add`, reusing the deepPartSubgroup pattern) + **`kcf_conj`** (`κ_β(g⁻¹ng) = κ_{g·β}(n)` via `inv_smul_eq_iff`). **BLOCKED on `conjAct_deepClasses`** (conjModule-invariance of deepClasses, needed for f5's W'=↥(deepClassesSubgroup) C-action): genuine AbsGalQ2/GaloisGroup instance-annotation incompatibility — `deepClasses`/`conjMap`/the ℚ̄₂-action use `Kummer.GaloisGroup ℚ_[2]` (`AlgEquiv.aut`) while `ρ.toMonoidHom.ker : Subgroup AbsGalQ2` uses `Field.instGroupAbsoluteGaloisGroup`; the two are defeq but `rw` at `instances` transparency won't bridge `ρ.ker`, and `g • x` for g:AbsGalQ2 has no HSMul instance. Reverted; NEXT: resolve the instance bridge (a `show`/`change` to the GaloisGroup view, or a small transport lemma `deepClasses (ρ.ker) ↔ deepClasses (ρ.ker as GaloisGroup-subgroup)`), then conjAct_deepClasses → subgroup/quotient C-actions → f5 `card_equivHoms_of_exact` on the U_{e+1} SES ⟹ balance. **§4 GATE RESOLVED + bricks landed (Opus, 2026-07-06, branch `p15f6-conjact-deepclasses`, NOT merged): `conjAct_deepClasses` (std-3, the gate — take `g` in the reducible GaloisGroup view + build the witness before touching `conjAct`, close via `calc`), `conjModuleDeep`/`conjActQuotHom`/`conjModuleQuot` (f5's W'/W'' C-actions), `deepFamEquiv`/`card_deepFam_eq` (`#deepFam = #equivHoms C V^∨ deepClassesSubgroup`) — all std-3 in `GQ2/AdmissibleCount.lean`, builds green. Steps 3+5 then CLOSED the assembly: **`card_equivHoms_quotient_ses`** (the view-normalization brick — the SES count stated over a plain-fvar subgroup, so `.Normal`/`AddCommGroup`/`Finite` resolve against fvars) + **`card_equivHoms_deepSES`** (instantiation at `H¹(N)`/`deepClassesSubgroup` with the conjModule actions passed as NAMED instance args) + **`card_deepPart_sq_of_duality`** (the f6 output: `#X₊² = #H¹(ℚ₂,V)` given `hduality` (→f7), the `V^∨` regular-summand package (6.11, proved by P-17e4), and `hinf`/`hext` (banked dischargeables, →f8)). All `#print axioms` = std-3 exactly; `GQ2.AdmissibleCount` builds green. Detail: `docs/p15f-handoff.md` §7.** ☑ **CLOSED (Fable, 2026-07-07)** — the original graded scope (`card_fam`/`card_deepFam` by B13-isotypic multiplicities, and the B13-(F2) extension question) is **superseded by the balance route** (handoff §2: ONE SES at `U_{e+1}` + the f7 duality replaces the full graded count — no (F2) clause, no census question); bricks i–iii (KummerFiltration skeleton, conjModule, AdmissibleFam/deep bridges) are the surviving components of the final chain. Consumers: P-15f7 (`hduality`), P-15f8 (assembly + splice). |
| P-15f7 | B: f1-dim **the two symmetry inputs** — (i) `hmid` (`d e = 0`) = Lemma 6.10: `e` odd + the (F2) clause ⟹ inertia trivial on `gr_e` ⟹ no ramified copy of `V` (shares f6's B13-(F2) need); (ii) `hpair` (`d j = d (2e−j)`) = graded Hilbert duality + self-duality `V ≅ V^∨` (via the invariant `q`, banked `polarMuDual`): needs the **sharp/nondegeneracy half of eq. (94)** (`Uᵢ^⊥ = U_{2e−i+1}`, both inclusions — the "⊆" half is banked as `cup_deepClasses`/Tier-5). Candidate no-leaf route: total nondegeneracy of the Hilbert pairing from B11a + counting via B12/B13; else a precise-theorem leaf (FV Ch. IV §5 **Thm (5.2)** `[✓ numbered, in references/]`) — **user approval before any census change**. | ⭐⭐⭐ | F | P-15f6 | B11a/B12/B13 budget; ⚠ possible new-leaf decision | ☑ **(Fable, 2026-07-06, branch `p15f6-conjact-deepclasses`) — ABSTRACT LAYER COMPLETE, all `#print axioms` = std-3 exactly** (`GQ2/DeepDuality.lean`, registered, builds green). Deliverable = the f6 capstone's `hduality`. Minimal route (strictly less than the paper's p. 34 graded computation): ONE sharp instance `Deep^⊥ ≤ E` ((94)@(e+1)-⊆, E = U_e-classes `‖A−1‖ ≤ ‖2‖`, π-free) + nondegeneracy + banked Tier-5 isotropy + Hom-symmetry + 6.10 middle-kill; NO per-level (93). **LANDED std-3**: `stabSubAction`/`stabQuotAction`; `card_equivHoms_eq_one_of_conjSmulTrivial` (inertia-kill); `equivHomsCurry`; prod/source-congr counts; `exists_functional_ne_zero`/`dualHom_surjective_of_injective` (𝔽₂ substrate); `evalDualEquiv`; split-off machinery (`splitProdEquiv`, `exists_section_of_epi`, `exists_retraction_of_mono`); **`card_equivHoms_comm`** (Hom-symmetry: strong induction on #W, U-copies split off either side — the "self-duality ⟹ equal multiplicities" content); `pairPerp`/`perpEquivDualQuot`; **`card_equivHoms_deep_eq_quot`** = the abstract hduality (consumer-shaped named instances). **REMAINING (instantiation; full list handoff §8)**: midClasses mirrors; `eU` from the invariant form; `ht₀U` from `hram`; hmid via the DERIVABLE twist `conjAct_mid_sub_mem_deep` (no leaf) + `t₀`-lifts residue-trivial (BoundaryMaps unpacking); **leaf decision RESOLVED (user-approved 2026-07-07, `docs/p15f7-axiom-proposal.md`): B6 BASE-GENERALIZED in place, census-neutral (B9/B11 pattern)** — `axiom tateDualityAt (G) … (hloc : IsLocalDualizingGroup G n) : TateDualityG G n` (NSW (7.2.6) already covers arbitrary `p`-adic `k`); old `tateDuality` re-derived as the `G_ℚ₂`-member `def` (zero consumer churn; traces now show `tateDualityAt`); `AxiomLedger` B6 entry swapped; `review-packet`/`literature-axioms`(±onepage) updated with the FV IV §5 (5.1)/(5.2) + O'Meara 63:13 induced-Hilbert cross-refs; guard green, census 15. This supplies `B := inv_K∘cup` + (H2) nondegeneracy at `G_K`; invariance free (`Aut(ℤ/2) = 1` + cochain-level cup equivariance). **(H4) `hsharp` = in-repo proof, approved plan**: perp-count (banked) + `(U_e,U_{e+1}) = 1` via Tier-5 descent extension + B13 sizes; fallback single-clause leaf only on a second explicit approval. Package std-3 via P-17e4-closed 6.11. **K-LEVEL PAIRING LANDED (Fable, 2026-07-07, `GQ2/DeepDualityK.lean` NEW file, registered)**: `ker_isLocalDualizingGroup` (std-3; subtype embedding, finite index from `Finite C`) → **`tateDualityK`** (the FIRST consumer of `tateDualityAt`); `zmodMuDualEquiv` coefficient bridge + **`pairingK`** (= `inv_K∘cup` on `H¹(ker ρ, 𝔽₂)`); **(H2) `pairingK_nondeg`** (std-3 + tateDualityAt exactly, from `perfect11`-injectivity); **(H1) `pairingK_conjAct`/`pairingK_conjModule`** (conjugation invariance — the cup cocycle precomposes on the nose with `conjMap × conjMap` under the trivial coefficient actions, `comp_conjMap_mem_B2_iff` transports coboundaries, and `ZMod 2`-two-valuedness + `inv`-injectivity force equality; the equation-hypothesis form `inv_H2mk_eq_of_comp_conjMap` avoids the higher-order-unification timeout). **§H U-side inputs (DeepDuality.lean)**: `polarSelfDual` (`V ≃+ V^∨` from `(q,hq,hns)` + `card_addHom_zmod2`) + equivariance from `hinv`; **`dualSelfDual`/`dualSelfDual_equivariant`** (= the `eU`/`heU` inputs, via banked `evalDualEquiv`); **`exists_dualModule_smul_ne`** (= `ht₀U`, functional separation). REMAINING: (H4) sharp (approved counting route), hmid twist `conjAct_mid_sub_mem_deep` + `t₀`-residue-triviality, isotropy `k`-splice, final instantiation. **(H5)+(H3)+(H4)-easy LANDED (Fable, 2026-07-07, session 4)**: ① **hmid twist DONE, pure std-3** (`DeepDuality.lean` §G′): `IsResidueTrivial` + `.conj` (conj-stability via `norm_galois`) + **`conjAct_mid_sub_mem_deep`** (Lemma 6.10 — 2-torsion + `kcf_mul_of_fixed` turn the difference into `[κ_{(g•β)β}]`; the PRODUCT `(g•A)·A = 1+2(g•b+b+2(g•b)b)` is deep via the inertia estimate at `x := b`; `p=2` turns the paper's division into a product, NO leaf) + **`conjAct_surjInv_conj_mid_sub_mem_deep`** (the literal `hmid` at `conjModule`: one residue-trivial lift of `t₀` covers all `d·t₀·d⁻¹` via `conjAct_ker`); `t₀`-lifts-residue-trivial stays hypothesis-side (f8, BoundaryMaps unpacking). ② **isotropy `k`-splice DONE** (`DeepDualityK.lean` §IsotropySplice, std-3+B11a+tateDualityAt): `kerToFixing` under POINTWISE `hker : x ∈ ker ρ ↔ x ∈ k.fixingSubgroup` (no subgroup-equality cast) + **`pairingK_deep_deep`** (cocycle-level splice of Tier-5 `cup_deepClasses`: same cup cocycles, `B²`-witness transported along `kerToFixing` + through `muNTwoEquiv.symm`) + wrapper **`deepClassesSubgroup_le_pairPerp_pairingK`** = `hiso`. ③ **(H4) easy half DONE — `(U_e, U_{e+1}) = 1` PROVED, no leaf** (`HilbertLedger.lean`): `normForm_of_mid_aux`/**`normForm_of_mid`** (mid-base Brahmagupta descent — with `‖a−1‖ ≤ ‖2‖` the error contracts by the CURRENT depth, self-referential budget `‖b−1‖(‖b−1‖/‖2‖)^j < ‖4‖`; FV VII§4 Ex. 4c at `(e,e+1)`, exercise-grade hence PROVED) + `cup_mid_deep` (std-3∪{B11a}); (`DeepDualityK.lean`): `norm_sub_one_le_of_isMidUnit` + **`midClass_eq_kummerClassK`** (`≤`-mirror bridge) + `cup_midClasses_deepClasses` + **`pairingK_mid_deep`** (same splice) + wrapper **`midClassesSubgroup_le_pairPerp_pairingK`** = `E ≤ Deep^⊥`. **REMAINING: (H4) counting half** (`#Deep^⊥ = #(M⧸Deep)` via banked `perpEquivDualQuot`+`card_addHom_zmod2`+Lagrange, vs `#E·#Deep ≥ #M` via B12/B13 + O'Meara-63:9-style square-depth bookkeeping ⟹ `Deep^⊥ = E` by `eq_of_le_of_card_le` ⟹ `hsharp`) + **final `card_equivHoms_deep_eq_quot` instantiation** → `hduality` (f8 is waiting on exactly this handle). **(H4) COUNTING HALF COMPLETE (Fable, 2026-07-07, session 5, `GQ2/DeepCount.lean` NEW, 5 increments, ~1000 lines)**: perp-side banked as **`card_pairPerp`** (`#S^⊥ = #(M⧸S)`) + **`pairPerp_le_of_card_le`** (easy-inclusion + card ⟹ sharp; both std-3, `DeepDuality.lean` §E); structural side **`card_quot_deep_le_card_mid`** (`#(M⧸Dc_{e+1}) ≤ #Dc_e` over `k` — **std-3 + B12 exactly**, no B13-clause extension needed): (K) `exists_sq_of_kummerClassK_eq_zero` (Kummer kernel via `B¹ = 0`), `coe_kummerDepth_mid` (stage `e` = midClasses), square-parity dichotomy (squares skip odd levels < 2e), graded squaring `grSq` + `kummerDepth_even_collapse` (equal-card bijectivity), `classGrMap` + odd-level fullness (`card_classGr_odd` = 2^f / `_even` = 1 / `_le`), tail survivor `exists_kummerDepth_ne_zero` (the `[−1]`-kernel at `i = e` ⟹ non-surjective ⟹ escaping class nonzero by (K)), ℕ-valuation `exists_nat_val` + level-0 collapse (odd-order squaring) + head `card_quot_kummerDepth_one_le_two` (≤ 2, `Fin 2`-surjection, first B12 use), and the **paired descent** `R(s)` closing head-vs-tail at `s = e−1`. **REMAINING (pure plumbing, no open mathematics — handoff §8 items α/β/γ)**: (α) group-transport of the count `k.fixingSubgroup → ker ρ` (with trivial coefficients `B¹ = 0` so `H1 ≃ Z1` and the transport is Z1-precomposition along `kerToFixing`; ~100 lines + subgroup-matching), (β) `hsharp` := `pairPerp_le_of_card_le` wiring, (γ) the final letI-instantiation → `hduality`. **☑ DELIVERABLE BUILT (Fable, 2026-07-07, session 6)**: α = `fixingToKer`/`h1KerToFix`/`h1FixToKer` (Quotient.out-based, `B¹ = 0` makes representatives exact) → **`h1KerFixEquiv : H¹(ker ρ) ≃+ H¹(G_k)`** (std-3) + `h1KerToFix_mem_{deep,mid}_iff` (the (A,β)-tuples transport verbatim) + **`card_quot_deep_le_card_mid_ker`** (the count in `ker ρ`-vocabulary; ambient/deep/mid cards via subtypeEquiv through `coe_kummerDepth_{deep,mid}`, quotient card by Lagrange-cancellation; std-3+B12); β+γ = **`hduality_of_data`** (`GQ2/DeepCount.lean` §Finale) — the abstract engine instantiated at `M := H¹(ker ρ)`@`conjModule`, `U := V^∨`@`dualModule`, `B := pairingK`, `Deep/E := deep/mid-ClassesSubgroup`, `hsharp` := `pairPerp_le_of_card_le`∘(easy half + transported count), `hmid` := `conjAct_surjInv_conj_mid_sub_mem_deep`, `instDeep/instQ := conjModuleDeep/Quot` (rfl-compatibilities); conclusion = VERBATIM the f6-capstone `hduality`-hypothesis. **`#print axioms` = std-3 + {B11a, B12, tateDualityAt} EXACTLY** (the plan-of-record budget). Remaining inputs are all f8-side producers: the `V^∨` 6.11-package (`hsimple`/`hnt`/`ι`/`r`/…, DimAssembly has the bricks), `eU`/`heU`/`ht₀U` from the 6.17 form-package via §H, the residue-trivial tame lift `(g₀, hg₀, hg₀rt)` (BoundaryMaps unpacking), and the B13 bundle instantiation (`dyadicUnitFiltration` + the `hker` identification). f7's own scope is COMPLETE.  **STATUS CORRECTION (2026-07-08): stale claim cleared — scope was already COMPLETE per this row's own tail.**  `GQ2/DeepDuality.lean` sorry-free, registered, all `#print axioms` = std-3; `hduality` consumed by the sorry-free f8 chain (`DimClose` → `ResidueLift.lemma_6_17_dim_final`).  **→ ☑ CLOSED.** |
| P-15f8 | B: f1-dim **assembly + splice** — build the `DeepKummerData ρ` instance from f5+f6+f7 outputs; discharge `hinf`/`hext` via banked `inflationVanishes_ramifiedTame` + `familiesExtend_of_card_le` (needs the `hsurj`/`hgen` plumbing from `hc : Surjective c` + `B.tameF_surjective`); apply banked `dim_deepPart_of_data`; close `lemma_6_17_dim` by the 6.18ram **statement-move pattern** (statement upstream in SectionSix, machinery downstream in LocalKummer — move the statement out or reroute `prop_6_18_ramified`'s citation). ~~Carries `sorryAx` through the 6.11 node until P-17e4's kernel lands~~ (6.11 node std-3 since P-17e4 CLOSED 2026-07-06 — the closed chain is now sorryAx-free, std-3+B). | ⭐⭐ | F+O | P-15f5, P-15f6, P-15f7, P-17e4 (6.11 node) | B6, B7, B12, B13 (+ f6/f7 outcomes) | ◐ **CLAIMED (Fable 2026-07-07) — INCREMENT 1 LANDED: the parametric close is BUILT, `#print axioms` = exactly std-3.**  New leaf `GQ2/DimAssembly.lean` (registered in `GQ2.lean`): **`lemma_6_17_dim_of_hext_hduality`** = `lemma_6_17_dim`'s conclusion from its own hypothesis set + exactly TWO parameters (`hext : FamiliesExtend`, `hduality` in the f6-capstone's letI'd shape = f7's deliverable).  Landed: profinite plumbing (`rho_surjective` = `c∘tameF` onto; `gen_of_surjective` via `SectionThree.gen_ttame_quotient` + discrete-is-topological; `tame_rel_image` via `tame_relation`), **`hinf` discharged** (`inflationVanishes_ramifiedTame`), and the **`V^∨` regular-summand package** = `lemma_6_11_of_tame_pair` at `dualModule` with new 𝔽₂-dual transport bricks `dual_faithful`/`dual_ram` (separation via `exists_functional_ne_zero`) + **`dual_simple`** (annihilator C-stable ⟹ `⊥` by V-simplicity; evaluation `V ↪ W^∨` + `card_addHom_zmod2` ×2 forces `#W = #V^∨` ⟹ `⊤`) + `dual_add_self` — the letI-diamond navigated by stating the bricks POINTWISE (handoff-§5 idiom), the capstone application closing by defeq.  `lake build GQ2.DimAssembly` green (8641), gate green (census 15).  **Remaining**: ~~(i) `hext` production~~ **✅ INCREMENT 2 LANDED (Opus 2026-07-07): `hext` DISCHARGED, all std-3** — new leaf `GQ2/ShapiroExtend.lean` (imported by `DimAssembly`, not separately registered — builds transitively): **`familiesExtend_of_package`** (`#print axioms` = exactly std-3) proves `FamiliesExtend ρ` from the **`V`-side** 6.11 package alone, NO counting/filtration — (a) **inverse Shapiro at the free module**: explicit coinduced-coefficient extension cocycle `b(g)(n,x) = u_n(s(x)⁻¹·g·s(ρ(g)⁻¹x))` (`shapiroWord` middle-insertion factorization = the cocycle identity; normalized section `sec1`; seeds `u_n = Quotient.out (ξ(ev_{n,1}))`), the family recovered on all of `R^∨` via the `conjAct`-equivariance bootstrap (`ρ` surjective) + the finite `ev`-span `addHom_eq_sum_evReg`; (b) **retract transfer**: `phiRes_mapCoeff1` naturality (`mapCoeff1_H1mk` is `rfl`) + pull-back along `mapCoeff1 r`.  ⚠ Lean gotcha banked: the repo's **global trivial `DistribMulAction AbsGalQ2 (ZMod 2)`** (`SectionSix.lean:70`) lifts pointwise over raw function types and BEATS proof-local `letI`s — solved with the **instance-opaque synonym `RegMod C Nr`** (plain `def`; instance search can't unfold it, so the left-translation instances declared on it are unique); defeq-fill (`have`s with explicit RegMod-typed statements, term-mode `exact` across the synonym boundary) replaces `rw` at the seam.  **`DimAssembly.lemma_6_17_dim_of_hduality`** (`#print axioms` = exactly std-3, NO sorryAx — the 6.11 chain is clean since P-17e4): `lemma_6_17_dim`'s conclusion from its own hypothesis set + ONE parameter (`hduality` = f7's deliverable; the `V`-side package from `lemma_6_11_of_tame_pair` feeds `familiesExtend_of_package` in-proof); increment-1's two-param close kept unchanged for compatibility.  `lake build GQ2.DimAssembly` green (8642), guard green (census 15).  ~~(ii) `hduality` ← f7~~ **✅ LANDED (f7 CLOSED: `DeepCount.hduality_of_data`)**.  **✅ FINALE (Opus 2026-07-07): the full assembly is BUILT** — new leaf `GQ2/DimClose.lean` (imports `DimAssembly`+`DeepCount`): **`lemma_6_17_dim_of_residueLift`** feeds every input of `hduality_of_data` from `lemma_6_17_dim`'s own hypotheses (the `V^∨` 6.11 package via the `dual_*` bricks; the self-duality `eU`/`heU`/`ht₀U` from `(q,hq,hns,hinv)` via `DeepDuality.dualSelfDual`/`_equivariant`/`exists_dualModule_smul_ne`; the **B13 unit-filtration bundle from the `dyadicUnitFiltration` axiom** — already in the census, no new axiom) and hands it to `lemma_6_17_dim_of_hduality`, **reducing `#X₊²=#H¹` to EXACTLY ONE input: a residue-trivial lift of tame inertia** `(g₀, hg₀ : ρ g₀ = c tameTau, hg₀rt : IsResidueTrivial (ker ρ) g₀)` (+ the standard Galois-correspondence `k`-plumbing `k`/`FiniteDimensional`/`hker`, threaded as everywhere in `LocalKummer`/`DeepDualityK`).  `#print axioms` = std-3 + B6 + B11a + B12 + B13, **NO sorryAx, NO new axiom**; `lake env lean` green, gate pass (census 15).  ~~The one genuine gap~~ **✅ THE RESIDUE-TRIVIAL TAME LIFT IS PROVED — NO NEW AXIOM (Fable 5, 2026-07-07)**: new leaf `GQ2/ResidueLift.lean` derives it from the existing budget by a **commutator trick** — the tame relation `σ⁻¹τσ = τ²` makes `τ = [σ⁻¹,τ]` a commutator, so `tameF`-surjectivity lifts it to a commutator `g₀ = s⁻¹tst⁻¹` of `G_ℚ₂`; commutators are residue-trivial because the residue action is abelian, proved in pure spectral-norm terms: **Teichmüller approximation** (`exists_rootOfUnity_near`: `‖y‖=1 ⟹` a root of unity within `<1` — B13 at `k := ℚ₂(y)` gives `‖y^{2^f−1}−1‖ ≤ ‖π‖ < 1`, then `X^m−1` splits over `ℚ̄₂` and the ultrametric **pigeonhole** on `∏‖y−ζᵢ‖ = ‖y^m−1‖` finds the close root — no Hensel) + **commuting Galois action on `⟨ζ⟩`** (`IsPrimitiveRoot.eq_pow_of_pow_eq_one` power maps).  Also the full **splitting-field plumbing** from Mathlib's infinite Galois correspondence: `kerGal` (the `AbsGalQ2`/`GaloisGroup` view-repackage via the `toAbs` identity bridge — exact-level default-defeq, the §4-gate idiom), `splitField := fixedField (kerGal ρ)`, `fixingSubgroup_splitField` (`InfiniteGalois.fixingSubgroup_fixedField` at the `ClosedSubgroup`), `splitField_finiteDimensional` (`isOpen_iff_finite`).  **`lemma_6_17_dim_final`** = the SectionSix conclusion from its OWN hypothesis set + only `[Finite (H¹(ker ρ, 𝔽₂))]` (the `K^×/2`-finiteness instance, a B12/B13 consequence pending splice).  `#print axioms`: the lift = **std-3 + B13 exactly**; `lemma_6_17_dim_final` = std-3 + B6 + B11a + B12 + B13 — the §6.3 budget, **no sorryAx**.  Gate green (census 15).  **✅ ITEM (a) DONE — `[Finite (H¹(ker ρ,𝔽₂))]` DISCHARGED via the SHAPIRO ROUTE, no new axiom (Fable 5, 2026-07-07)**: new leaf `GQ2/ShapiroFiniteness.lean` (globbed by `check_axioms.sh`; imported by `ResidueLift`) proves **`finite_H1_open (U) (hU : IsOpen U) [Finite (G⧸U)] : Finite (H¹(U,𝔽₂))`** for any open `U ≤ G_ℚ₂`: build the finite **coinduced/permutation module** `PermMod U = 𝔽₂[G/U]` (instance-opaque synonym so the coinduced action `(g•f)x = f(g⁻¹•x)` is not shadowed by the global pointwise-trivial `Pi`-action; `ContinuousSMul` via `continuous_prod_of_discrete_right` + fiberwise-open `IsLocallyConstant`), and the **Shapiro map** `θ = mapCoeff₁(ev)∘res₁ : H¹(G,𝔽₂[G/U]) → H¹(U,𝔽₂)`, proved **surjective** by the explicit section `σ(c)(g)(x)=c(w(g,x))` with transversal word `w(g,x)=sect(g⁻¹•x)⁻¹·g⁻¹·sect x ∈ U` (cocycle identity `w(g₁g₂,x)=w(g₂,g₁⁻¹•x)·w(g₁,x)` = pure `group` calc; collapses to `u⁻¹` at the base coset ⟹ round-trip = id, `𝔽₂`-inversion trivial); source finite ⟹ **(B7 `finite_H1`)** target finite via `Finite.of_surjective`.  `#print axioms finite_H1_open` = **std-3 + B7 exactly**.  Wired into `ResidueLift.lemma_6_17_dim_final`: `ρ.ker` is open (preimage of `{1}`, `C` discrete) with `Finite (G⧸ρ.ker)` (injects into finite `C`), so the binder is **dropped** — `lemma_6_17_dim_final` now needs **NO finiteness hypothesis**.  `#print axioms lemma_6_17_dim_final` = **std-3 + B6 + B7 + B11a + B12 + B13**, NO sorryAx, NO new axiom (B7 already in the deep-part budget; census 15).  `lake build GQ2.ResidueLift` green (8654 jobs).  **✅ REGISTERED + FULL BUILD GREEN (Opus 2026-07-07): `DimClose` + `ResidueLift` now imported in `GQ2.lean`** (after `DimAssembly`); `lake build` green (8718 jobs), guard green (census 15, no new axiom — the lone guard FAIL `Phase140Local.lean:727` is a PRE-EXISTING unregistered paused-swarm WIP sorry, unrelated to f8).  `#print axioms lemma_6_17_dim_final` = std-3 + B6 + B7 + B11a + B12 + B13, NO sorryAx.  **f8's mathematical content is COMPLETE and in the library.**  **Remaining (DEFERRED — user decision 2026-07-07): (b) the 6.18ram statement-move.**  The only open item is cosmetic: the allowlisted `SectionSix.lemma_6_17_dim` sorry stays (its sole consumer `DeepPart.prop_6_18_ramified` is a leaf with no Lean consumers, still `sorry`-blocked by `lemma_6_17_vanish`/f2).  Per user decision, DEFER to a single joint f2+f8 move — when f2 proves `lemma_6_17_vanish` downstream, remove BOTH 6.17 lemmas from SectionSix and rewire `prop_6_18_ramified` to both real proofs at once (no signature churn now).  **STATUS CORRECTION (2026-07-08): NO active agent (claim stale) — proof chain COMPLETE**: `ResidueLift.lemma_6_17_dim_final` proves `lemma_6_17_dim`'s exact statement sorry-free (`#print axioms` = std-3 + B6/B7/B11a/B12/B13; whole chain `DimAssembly`/`DimClose`/`ResidueLift` registered + sorry-free).  ONLY remaining: the user-deferred **joint f2+f8 statement-move splice** (remove both 6.17 interface sorries from `SectionSix` + rewire `prop_6_18_ramified` to the two real proofs at once, when f2d's vanish lands).  UNCLAIMED — natural tail of the f2d close. |
| P-15g | B: §7 Lemma 7.2 — tame-free route + block-module infra | ⭐⭐⭐ | O | — | ∅ | ☑ **CLOSED 2026-07-04 (Opus)** — `lemma_7_2` **proved, `#print axioms GQ2.SectionSeven.lemma_7_2 = [propext, Classical.choice, Quot.sound]` (std-3, no B-axioms, no sorryAx)**; gate green (census 12); tame hyps `π,hπ,hkerπ,cH,hcH` **unused** (tame-free route confirmed). `SectionSeven` stays allowlisted only for `lam_sq_vanish` (P-15h). Prototyped in scratch `GQ2/WF72.lean` then spliced (`set_option maxHeartbeats 1000000 in`); scratch deleted. **Assembly** (~230 ln): `Ñ:=zpowers y` (odd via `exists_odd_moving_general`), `⁅Ñ,S⁆=⊥` (`comm_bot_of_scalarChain` + `B.scalar_below`, coprime `orderOf y` ⊥ `#S` 2-pow via `IsPGroup.iff_card`/`Nat.coprime_two_right`), `⁅Ñ,R⁆=⊥` (`R≤S`), `D=K⊓C_Y(R)` `Y`-normal, three-subgroup `commutator_commutator_eq_bot_of_rotate`, `K₁=normalClosure⁅K,Ñ⁆`, chief dichotomy (`=S`⟹`[y,ks]∈S` contra existence; `=P`⟹`B.minimal K₁=K`⟹(a) `K≤C_Y(R)`); class-2 (b)(c): `[k,l]²=1` (central-comm expand), `(kl)⁴=k⁴l⁴` (via `[l,k]k²l²` + pairwise-central rearrange), `Kf={k∈K\|k⁴=1}` `Y`-normal ⊇`⁅K,Ñ⁆` (`f[k,n]=k⁴·n(k⁴)⁻¹n⁻¹=1`, Ñ fixes `R∋k⁴`) ⟹`K≤Kf`⟹(c); (b) `r²=1` by `closure_induction` (`R` abelian). **Lean gotcha banked: `group` does NOT expand `x^(4:ℕ)` — insert `hp4 : x^4 = x*x*x*x` (`pow_succ×3+pow_one`) before every `^4`-vs-product `group`.** INFRA (banked std-3, wired in): new `GQ2/BlockModule.lean` (sorry-free, in `GQ2.lean` before SectionSix; block-agnostic so no cycle) exports — `comm_bot_of_scalarChain` (relocated public, was private in SectionSeven; that copy REMOVED, `import GQ2.BlockModule` added there, SectionSeven still green), `conjHom`/`conjHom_compat`, `blockAction` (the `Y`-conj MulAction on `↥P⧸S.subgroupOf P`, `@[reducible]`), `blockPerm : Y →* Perm (P/S)` + `@[simp] blockPerm_apply_mk` (**built as a DIRECT perm rep from `QuotientGroup.map`, NOT `MulAction.toPermHom` — the latter's `•` diamonds hopelessly against `blockAction.smul`; `map_mk'` is `rfl`, reduce via defeq `show`, never `rw`/`simp`**), and **`exists_odd_moving_general`** (std-3: odd-order `y` with `∃p∈P,[y,p]∉S`, via `¬goal ⟹ φ.range=blockPerm.range` is `IsPGroup 2` [odd part `y^ordProj[2]` acts trivially] ⟹ `IsPGroup.card_modEq_card_fixedPoints` on `φ.range` ⟹ `\|V\|` even ⟹ nonzero fixed coset ⟹ the fixed subgroup `W={p∈P\|∀y [y,p]∈S}` is `Y`-normal `S<W≤P` ⟹ `B.chief` ⟹ `W=P` ⟹ contradicts `nontrivial_action`).  Key API: `Nat.ordProj_dvd`, `Nat.not_dvd_ordCompl`, `orderOf_pow'`, `orderOf_dvd_natCard q` (arg!), `MulAction.compHom Q φ.range.subtype`, `Finite.one_lt_card_iff_nontrivial`.  **REMAINING = the `lemma_7_2` assembly in SectionSeven** (route below, all tools ready): `Ñ := Subgroup.zpowers y` (odd, coprime to `#S` 2-power); `⁅Ñ,S⁆=⊥` via `comm_bot_of_scalarChain` fed `B.scalar_below`'s chain ⟹ `⁅Ñ,R⁆=⊥` (`R≤S` by `lemma_7_1_head`); `D := K ⊓ centralizer R` proved `Y`-normal inline; three-subgroup `Subgroup.commutator_commutator_eq_bot_of_rotate` (H₁=K,H₂=Ñ,H₃=R: h1 `⁅⁅Ñ,R⁆,K⁆=⊥` from `⁅Ñ,R⁆=⊥`; h2 `⁅⁅R,K⁆,Ñ⁆⊆⁅R,Ñ⁆=⊥` since `⁅R,K⁆≤R`) ⟹ `⁅K,Ñ⁆≤D`; `K₁:=normalClosure ⁅K,Ñ⁆≤D`, and `K₁⊔S` is `Y`-normal in `[S,P]` so `B.chief` ⟹ `=S`(⟹`⁅K,Ñ⁆≤S`⟹ via `mul_normal` factor `p=k·s` and `[y,ks]=⁅y,k⁆·k⁅y,s⁆k⁻¹∈S` ⟹ contra existence) or `=P` ⟹ `B.minimal K₁ = K` ⟹ `K≤centralizer R` = conclusion (a); then class-2: `k²∈R≤Z(K)` ⟹ `[k,l]²=[k²,l]=1`, the 4th-power `Y`-equiv hom `f k=k⁴` kills `⁅K,Ñ⁆` (`f[k,n]=f(k)·(n·f(k⁻¹)·n⁻¹)=1`, Ñ fixes R∋f(k)), normalClosure=K minimality ⟹ `f=1` ⟹ `K⁴=1`=(c), `R` elem ⟹ (b). |
| P-15h | B: §7 `lam_sq_vanish` ⟹ Prop 7.4 fully std-3 | ⭐⭐⭐ | O | P-15g | ∅ | ☑ **CLOSED 2026-07-04 (Opus)** — `#print axioms GQ2.SectionSeven.prop_7_4 = [propext, Classical.choice, Quot.sound]` (**std-3, NO sorryAx, no B-axioms**); SectionSeven compiles clean & is **SORRY-FREE**. The entire §7 Prop 7.4 is proved. Three tiers: (1) **module core** — `avg_dual_zero` (F1 odd averaging, no Maschke), `dual_vanish_concrete` (F1 bridge `V^Ctil=0 ⟹ (V∨)^Ctil=0`), `fixed_zero_of_moves` (A simplicity via `fixSub`+`chief`); (2) **P1** `L` acts trivially on `V=P/S` — `L_le_blockPerm_ker` (`exists_L_fixed_coset` p-group count + chief upgrade); (3) **tame construction** (`by_cases` on tame inertia `⟨cH τ⟩` moving `V`): tame structure via `import GQ2.Prop32` (`SectionThree.gen_ttame_quotient` closure=⊤, `tame_relation` `s⁻¹ts=t²`, `Tame.zpowers_normal_of_tame`+`Tame.tame_odd_order`); **ramified** `Ctil=π⁻¹⟨cH τ⟩` odd via `odd_preimage_quot` (`π⁻¹⟨t⟩/(YV∩·)` quotient of `π⁻¹⟨t⟩/ker π≅⟨t⟩` odd); **unramified** `Ctil=⊤`, `Y/Y_V` odd via `unram_odd` [`cyc_YV` IsCyclic(Y/Y_V) from `cyclic_quot`(H/mapπYV, `cH τ` dies)+iso transport; then by_contra 2∣card, Cauchy order-2 `gbar`, `blockPerm(C̃)` 2-group, `exists_normal_fixed_coset` image-count ⟹ `fixSub⊋S` ⟹ chief ⟹ `C̃≤Y_V` ⟹ `gbar=1` contra] + `top_quot_card` (⊤-quotient card). **New std-3 infra banked in SectionSeven**: avg_dual_zero, dual_vanish_concrete, fixSub/fixed_zero_of_moves, exists_L_fixed_coset, L_le_blockPerm_ker, cyclic_quot, exists_normal_fixed_coset, odd_preimage_quot, unram_odd, cyc_YV, top_quot_card. **Gotchas banked**: `ḡ`(combining macron) invalid Lean id→`gbar`; `IsCyclic.commGroup` mul NOT defeq canonical→derive comm from generator; `exists_prime_orderOf_dvd_card` wants `Fintype.card`; `Subgroup.normal_top`; `QuotientGroup.map_mk`(↑x,rfl) not `map_mk'`; act-based avg (not DistribMulAction, AddAut friction); `ofMul_mul`/`toMul_ofMul` root-ns. SectionSeven now sorry-free (can be dropped from SORRY_ALLOWLIST; left for parallel-run safety). |
| P-15i | B: Lemma 6.21 — transgression splitting (116-consequence) | ⭐⭐⭐ | **F**+Opus | — | ∅ | ☑ **DONE (Fable design + close, 2026-07-04)** — the isolated gap was a *statement-extraction* gap: the paper's “fixed equivariant class κ⁰_q” hypothesis had been dropped in the consequence-form extraction (see `docs/p15i-transgression-gap.md`).  **Amended (reviewed, user-approved)**: `lemma_6_21` gains `(dat : FactorSet C V) (hdat : IsEquivariantFactorSet q dat)` (Lemma 6.1 vocabulary, same form `lemma_6_22` consumes); `splitting_of_global_cocycle` gains the raw cochain avatar `(t, ht_quad, ht_mul)`.  New std-3 lemmas: `symm_cocycle_is_coboundary` (symmetric zero-diagonal 2-cocycle on elem-ab V is δθ; twisted product 𝔽₂×_S V is an exponent-2 abelian group ⟹ 𝔽₂-linear section — reusable), `mixedA_defect` (D_c formula, 3 cocycle instances), `equivariant_lift_of_factorSet` (m-family transported along θ), and the closed assembly: `Ã c := mixedA c + t c⁻¹` is additive with δÃ = δ(mixedA) = B♭f ⟹ `bflat_bijective` yields the primitive g.  **`lemma_6_21` PROVED & SPLICED, std-3, no B-axioms; `GQ2/Transgression.lean` sorry-free, off SORRY_ALLOWLIST.** |
| P-16 | B: §8 — half-torsor count 8.6 + closed recursion Prop 8.9 (eqs. (136)–(142)) | ⭐⭐⭐ | **F** draft, O finish | P-11, P-14 | B6, B7, B9 | ◐ **DECOMPOSED 2026-07-04 (Fable) → P-16a–d** (`docs/p16-ticket-split.md`; heterogeneous gates: 8.6-local un-gated, 8.6-gammaA/8.9 behind P-13f).  Parent inventory (all std-3): F-draft ☑; engines 8.4/8.5; scalar twisting; `lemma_8_2_gammaA`/`lemma_8_2_local` (amended: `BoundaryMaps`+compactness); **`lemma_8_3`** eight-lift partition (124) (torsor core + two `masterLifts` fibrations + corestriction layer; `hfg` amendment); **`two_mul_card_fiber`** ((127)/(139) half-count core); generalized `charEquiv`/`cmhEquivFun`; `𝔽₂`-topology binder cleanup.  Remaining sorries 8.6×2 + `prop_8_9` are the sub-tickets; detailed O-half work order stays in `docs/section8-extraction.md`.  **Update 2026-07-05: P-16a ☑ + P-16b ☑ (`lemma_8_6_local` PROVED); remaining sorries: `lemma_8_6_gammaA` (P-16c, gated on P-13f) + `prop_8_9` (P-16d).** |
| P-16a | B: §8 central-obstruction engine (def-layer → `RadicalEdgeData`; `MLifts` torsor, obstruction `o` ↔ `ContCoh.H2`, affine variation) | ⭐⭐⭐ | F+O | — | ∅ | ☑ **DONE (Fable, 2026-07-05)** — relocation: `GQ2/RadicalEdgeData.lean` (`CentralCover`, `polarMul`, `RadicalCoverData`, `NoDescent`, `two_mul_card_fiber`, `MLifts`(+`.Central`) moved; SectionEight imports it). Engine: `GQ2/CentralObstruction.lean`, all std-3 sorry-free — kernel-sign calculus (`zsign`), commutator/polar ledger `mul_comm_z` (extraspecial from `hq` alone), linear complement `TComplement`+`tComplement_nonempty` (𝔽₂-functional split of `p⁻¹(T)`), **edge cocycle** `edge`/`edgeQ` with `edge_spec`/`edge_mul`/`edge_add`/`edge_M_zero`/`edge_coset` + **descent** `not_noDescent_of_edge_trivial`; Γ-layer: `TCocycle` crossed T-cocycles, `twist` **involution**, obstruction class `ob` with `central_iff_ob_eq_zero`, lift-family independence, **exact (129)** `ob_twist` (`ob(u·f) = ob f + [varCoc u]`, f-independent variation), flip lemma `two_mul_card_of_swap`, and the assembled **`half_count`** (needs: nonzero variation class + `#H²(Γ,𝔽₂)=2` + `[Finite (MLifts)]`). Design refinement vs the split doc (paper-faithful): the count twists by **T**-valued cocycles (`q\|_T=0` ⟹ linear variation) and the M-torsor/orbit machinery collapses to the single twist-involution — simpler than planned. Build 8651; census 13 ✓ |
| P-16b | B: `lemma_8_6_local` close ((d) `ℓ≠0 ⟺ NoDescent` via B6 + P-13g `CupSymmetry` toolkit + `two_mul_card_fiber`) | ⭐⭐ | O | P-16a | B6, B7 | ☑ **DONE (Fable, 2026-07-05)** — **`lemma_8_6_local` PROVED & SPLICED**, `#print axioms` = std-3 + `GQ2.tateDuality` (B6) + `absGalQ2_localEulerCharacteristic` (B7, via `card_H2_zmod2_eq_two`), NO sorryAx.  Own file `GQ2/RadicalEdgeLocal.lean`: `exists_good_twist` (minimal-context B6 step: the shifted edge `φ(γ)(s):=ε̄(ργ)(γ⁻¹•s)` is an exact `Z¹(MuDual 2 A_T)`-cocycle; `[φ]=0` would trivialize the edge by ρ-surjectivity ⟹ descent, contra `NoDescent`; `perfect11` then yields `w` with `inv(cup[φ][w])≠0`, and on the nose `cup11Fun φ w = muNTwoEquiv.symm ∘ varCoc u_w`) + `half_torsor_local` (finitizes `MLifts` via `hfg`+`finite_continuousMonoidHom`, bridges the raw nonvanishing to the `H²(Γ,𝔽₂)`-class, closes by P-16a `half_count`).  **Statement amendment (documented)**: `lemma_8_6_local` gains `[CompactSpace] [TotallyDisconnectedSpace]` + `hfg` (the B1-shaped t.f.g. input) per the `lemma_8_2_local`/`lemma_8_3` precedents.  Ax note: B7 enters through `#H²(𝔽₂)=2` finiteness — row budget updated B6→B6,B7.  Lean-lessons: local `DistribMulAction _ (ZMod 2)` letI + compactness binders blow up μ₂-dual instance search (hence the minimal-context helper); class-typed defs need `@[reducible]` (`tCommGroup`); quotient-inverses need type ascription under `Quotient.out`. |
| P-16c | B: `lemma_8_6_gammaA` close — **decomposed → P-16c1–c5** (plan + dependency diagram in `docs/p16-ticket-split.md` §P-16c; rows below) | ⭐⭐ | O | P-16a, P-13f | B6, B7 *(plan: ∅)* | ☑ **CLOSED 2026-07-06 (Opus) — `lemma_8_6_gammaA` PROVED std-3; all of P-16c1–c5 landed; `lake build GQ2` exit 0 (8678 jobs, 0 errors); `#print axioms lemma_8_6_gammaA = {propext, Classical.choice, Quot.sound}` (∅ extra axioms — the plan's predicted budget).** New files `GQ2/{MixedBObs,LedgerGammaA,HalfTorsorGammaA}.lean` + one-line splice into `SectionEight.lean`.  The Phase-3 "main risk" ledger identity DE-RISKED: `HeisLift` = central-𝔽₂ ext of `WordLift` ⟹ `mixedB` IS a `relZPair` (`mixedB_eq_relZPair`); the edge is already packaged in c3's `φf` ⟹ the edge-unfold `varCoc u = kappaHeis(H·,H·)` is near-definitional, NOT the feared edge=Fox.  ◐-history: ⚠ SCOPING (Opus, 2026-07-05) — **not a mechanical port of P-16b; a real infra gap surfaced.** The engine `CentralObstruction.half_count` is generic over the source group `Γ` ✓, so `lemma_8_6_gammaA` needs exactly two `GammaA`-specific **continuous-cohomology** inputs: (5) `#H²(GammaA, 𝔽₂) = 2` and (4) a nonzero variation class in `ContCoh.H² GammaA (ZMod 2)` from `NoDescent` (P-16b gets these from B7's `card_H2_zmod2_eq_two` + B6's `perfect11`). **But `prop_5_15` lives on the Fox–Heisenberg WORD complex (`H1w`/`mixedB`) and `prop_5_16` on `H*(AbsGalQ2)` — neither is `ContCoh.H*(GammaA)`, and there is NO formalized bridge `H*(GammaA) ≅ H*w` (only `cor_5_17_card`'s *cardinality* equalities word↔AbsGalQ2).** So (d′) "via 5.15/5.16" is not a direct substitution: it needs either (i) a continuous-cohomology↔word-complex quasi-iso for `Γ_A = F₄/N_A` (a foundational piece — arguably its own ticket), or (ii) re-expressing the whole `MLifts`/obstruction machinery in the word complex + a word-level `half_count`. `lemma_8_6_gammaA` is currently consumed by nothing (only P-16d3's `half139` will). **PLAN LANDED (Fable, 2026-07-05)** — full 4-phase route in `docs/p16-ticket-split.md` §P-16c: the repo's Γ_A dictionary (`Marking.push/descend` [Prop23], relator-death + cofinality [AdmissibleLimit], `topGen_gammaA` [P-25]) makes a **degree-≤2 presentation comparison** cheap, no full quasi-iso needed. Phase 1 `z1Equiv` (continuous crossed cocycles ≃ `Z1w t_ρ`, via `Marking.descend` at `WordLift`); Phase 2 `theta` (finite-level relator evaluation) + `theta_injective` ⟹ `#H²(Γ_A,𝔽₂) ≤ 2` (target size from `trivialSelfDual`; = 2 follows from Phase 3's class, no surjectivity pass); Phase 3 port P-16b's source-generic twist prefix + `prop_5_15`'s perfect pairing + the Θ–mixedB ledger identity (**main risk, pinned**: `mixedB` = traced SUM `tame.z+wild.z` while the `H2w`-detector = WILD-z mod τ-column `im d1_triv = span{(1,0)}` — they agree on §5.13 x₀-supported normal forms via `heisMarking_tameValue_z_eq_zero`); Phase 4 `half_torsor_gammaA` + splice (no `hfg` amendment — `topGen_gammaA` + `finite_continuousMonoidHom`). **Correction to the (d′) sketch: `prop_5_16_bundle` is NOT needed Γ_A-side; expected Ax = ∅** (std-3, under the declared B6,B7 budget). ~3–4 sessions; Phases 1–2 are independently landable and reusable (the degree-2 comparison is Thm-4.2-ward infrastructure). **Decomposed 2026-07-05 → P-16c1–c5** (rows below; phase↔row mapping: c1 = Phase 1, c2 = Phase 2, c3+c4 = Phase 3 with the crux isolated in c4, c5 = Phase 4). |
| P-16c1 | B: Γ_A degree-≤1 bridge (`GQ2/WordCohBridge.lean`): `z1Equiv` — continuous crossed cocycles `Γ_A → A` (action through a finite admissible quotient `q`, marking `t_ρ = Marking.push`) ≃ `Z1w t_ρ`, forward = eval at the 4 marked generators (injective: `topGen_gammaA` density + continuity), backward = crossed cocycle ↔ hom to `WordLift A C` over `q` via `Marking.descend` (admissibility of the lifted marking: `liftMarking` relations ⟺ `d1 x = 0` by the §5.13 ledger; `Pro2Core` via central-2-extension `isPGroup_normalClosure_image_inf`; `Generates` by passing to the generated subgroup + `cmhInclude` back); `B¹ ↔ B1w`; corollary `h1Equiv : ContCoh.H1 GammaA A ≃+ H1w t_ρ` | ⭐⭐ | O | — (all inputs landed: Prop23 dictionary, AdmissibleLimit, `topGen_gammaA`, §5.13 ledger) | ∅ | ☑ **DONE & verified std-3** (Opus, 2026-07-05) — `GQ2/WordCohBridge.lean` (sorry-free, imported into `GQ2.lean`; `lake build GQ2` green 8665 + `check_axioms` census 13; `#print axioms {z1Equiv,h1Equiv,toZ1wHom_ofZ1w,ofZ1w_toZ1wHom,NA_le_ker_classify,eval_dZero} = std-3`, Ax=∅ ✓). **Landed:** `wordHom` (cocycle `z ↦` the `WordLift = A⋊C` hom `γ↦⟨z γ, q γ⟩`), `gammaGen`/`markC`/`markC_map`, `eval` + `eval_add`, `liftMarking_eval`(+`_univ`), relator-death `liftMarking_eval_{tame,wild}Rel`, **`eval_mem_Z1w`** (eval lands in `Z1w` — both relators die in Γ_A), bundled forward `toZ1wHom : Z1 GA A →+ Z1w t_q`. **Two structural findings (resolved, documented in-file):** (1) built over the RAW `GA := F₄⧸N_A` (not the `GammaA = ProfiniteGrp.of` wrapper — its fresh instances break `q.comp (quotientMk NA)` unification; carriers defeq, so transports to the `GammaA` statement at the end); (2) the wild coordinate uses `wildValue` (finite ω₂) which lacks source-finite naturality over the infinite `GA` — routed through **`map_wildRelator_eq_one_iff`** (the profinite Hat-word `map_h0Hat`/`map_u1Hat`/`map_c0Hat` naturality, continuous, no `Finite` source). **BACKWARD + EQUIV DONE:** gateway `NA_le_ker_classify` (`ker c` admissible via `isAdmissibleU_iff_NA_le`; `Pro2Core` via **`IsPGroup.comap_of_injective`** — wild core `≤ gHom⁻¹(base core)` transferred into `F₄⧸ker c` along the injective `kerLift`, **NOT** `isPGroup_normalClosure_image_inf`), backward `ofZ1w` (`.u` of the descended hom; cocycle from `liftHom_g`), both round trips + **`z1Equiv`** + coboundary descent `eval_dZero` + **`h1Equiv`**. **Route refinements vs the c-plan:** (a) **`hA₂ : ∀ a, a+a=0` (A elementary-2) is REQUIRED** — the backward `Pro2Core` (wild core `A⋊⟨t.x₀,t.x₁⟩` pro-2) holds iff the A-part is 2-torsion; correct for §8's 𝔽₂-coefficients, threaded through `ofZ1w`/`z1Equiv`/`h1Equiv`. (b) **No `topGen_gammaA` needed** — both round trips factor through `quotientMk` surjectivity + `Marking.toHom_hom_univMarking_map` uniqueness, so BoundaryConstruction/Prop32 are **not** imported (clean dependency; one `haveI` supplies the `OpenSubgroup`-quotient `DiscreteTopology` for the `let`-bound `U`). **Unblocks P-16c2 (θ) + P-16c3 (`h1Equiv` consumer).** |
| P-16c2 | B: Γ_A degree-2 comparison (`WordCohBridge.lean` cont.): small compactness lemma "a continuous 2-cocycle `Γ_A² → 𝔽₂` factors through a finite level" (upgrade to an admissible level via `exists_isAdmissibleU_le` + `isAdmissibleU_inf`); `theta : Z²cont(Γ_A,𝔽₂) → 𝔽₂² ⧸ im d1_triv` = relator-`z` pair of a generator-lift into the finite-level `κ`-twisted central extension `C̃_κ`, well-defined (central-shift = `d1` at trivial coefficients) + level-independent (cofinality); **`theta_injective`** — `θκ = 0` ⟹ shifted marking of `C̃_κ` admissible ⟹ `Marking.descend` splits it over Γ_A ⟹ the splitting's `𝔽₂`-component trivializes `κ` ⟹ `κ ∈ B²`; `card_H2_gammaA_le_two` (target size 2 = `trivialSelfDual` at the trivial-group marking). NO surjectivity pass — `#H² = 2` follows from c4's nonzero class | ⭐⭐ | O | P-16c1 | ∅ | ☑ **DONE & verified std-3 (Opus, 2026-07-06)** — full card bound `card_H2_gammaA_le_two` landed; see final update at end of cell.  Foundation notes retained: (Opus, 2026-07-05) **FOUNDATION DONE, std-3** — new file `GQ2/WordCoh2.lean` (sorry-free, `lake build GQ2.WordCoh2` green 8601; NOT yet in `GQ2.lean` — WIP, and the c3 agent is editing the aggregator). **Mapping finding:** `SectionSix.CentralExt` has an ADDITIVE base + concrete-only (`decide`) group instance — unusable for the multiplicative level `L=F₄/U`; built my own (like `WordLift` for c1). **Landed:** `TwoCocycle L` (κ:L→L→𝔽₂, `norm : κ 1 1 = 0`, `cocyc`) with derived `κ_one_left/right`/`κ_inv`; **`CentExt c = L ×_κ 𝔽₂`** (product `(l,z)(l',z')=(l l', z+z'+κ l l')`) with full `Group` instance (mul_assoc by `linear_combination c.cocyc`, inv via `κ_inv`+`CharTwo`), `Finite`/discrete instances, base `proj : CentExt c →* L`, central `incl : 𝔽₂ → CentExt c`, `base_eq_one_iff`; `liftMark t c` (mark `CentExt` by `(t.genᵢ, 0)`), `liftMark_map_proj` (=`t`), `liftMark_{tame,wild}Value_base` (relator value projects to base's, via `Marking.map_{tame,wild}Value`; wild needs `[Finite L]`✓ since `CentExt` finite), **`relZPair t c`** (the 𝔽₂² obstruction pre-quotient), `liftMark_{tame,wild}Value_eq_incl` (relator dies into the central fibre when `t` satisfies the relation). **REMAINING (~250 ln):** the factoring lemma (`κ∈Z²cont` ⟹ finite admissible level; the θ target `𝔽₂²/im d1_triv = H2w(t_triv)` has card 2 = `card_H2w_trivial` at `A=ZMod 2` ✓ EXISTS), `theta : Z²cont →+ H2w(t_triv)` from `relZPair` + additive + kills B², `theta_injective` (via `Marking.descend`), `card_H2_gammaA_le_two`. θ-injectivity = the reusable degree-2 half of the presentation comparison (Thm-4.2-ward infrastructure). **UPDATE 2026-07-06 (Opus, std-3, `lake build GQ2.WordCoh2` green 8601): the ENTIRE INJECTIVITY DIRECTION is built.** Whole shifted-lift mechanism (`NA_le_ker_shiftLift`/`sectionHom`/`projC_comp_sectionHom`/`cocycle_mem_B2`) + **§ShiftLaws** (`shiftCompare` comparison hom into `WordLift (ZMod 2)(CentExt c)`; `shiftLiftMark_{tame,wild}Value_fib = (liftMark).{…}.fib + a 1` — BOTH fibres shift by `a 1`; `exists_shift_of_relZ_eq`) + **§Injectivity keystone** `inflated_cocycle_mem_B2` (balanced obstruction `tame.fib=wild.fib` ⟹ inflated `κ=c.κ∘(levelProj×levelProj)` ∈ `B2 (F₄/NA)(ZMod2)`). **GROUND-TRUTH CORRECTION: `im d1_triv = Δ` (DIAGONAL, `d1Fun_of_trivial=(x1,x1)`), NOT `span{(1,0)}` — the "convention trap" is superseded; detector = SUM `tame+wild`.** REMAINING (fresh session): (C) **factoring** + (B) card assembly. **UPDATE 2026-07-06 #2 (Opus, std-3, build green 8601): ★ (C) DONE — flagged topology gap CLOSED — plus ALL hard algebra of (B).** `exists_openNormalSubgroup_factor_two` (elementary compactness: box-per-point via `isOpen_prod_iff`+`exist_openNormalSubgroup_sub_open_nhds_of_one`, `elim_finite_subcover`, `Finset.inf'`), `exists_twoCocycle_factor` (κ → `U=comap NA V` + `TwoCocycle(F₄/U)` via `Quotient.liftOn₂`, inflates back), `mem_B2_of_factor_balanced` (**injectivity direction fully assembled**), `relZPair_comap` (level-independence via `projExt`), `relZPair_add` (**additivity** via `FiberProd=L×𝔽₂²` + `pr1/pr2/prSum`, d1Fun_add pattern). **REMAINING = card bookkeeping ONLY (~150 ln, NO math gaps): `obs:Z²cont→+𝔽₂` (tame.fib+wild.fib of factored normalized κ) + `ker obs⊆B²` (= mem_B2_of_factor_balanced) ⟹ `#H²=#(Z²/B²)≤#(Z²/ker obs)=#im obs≤2` [quotient-monotonicity — avoids the well-def direction]. Recipe in memory.**  **★★ UPDATE 2026-07-06 #3 (Opus — `#print axioms {card_H2_gammaA_le_two, obs, obsFun_add, obs_ker_le} = {propext, Classical.choice, Quot.sound}` std-3 ✓; `lake build GQ2.WordCoh2` green 8601): CARD ASSEMBLY DONE — P-16c2 COMPLETE.** `LevelFactor`/`LevelFactor.obs` (relZPair sum of a factorization) + level-independence `obs_eq_comap` (`relZPair_comap`) + well-definedness `obs_congr` (common refinement `U₁⊓U₂` via `QuotientGroup.map` projections; the two pulled-back cocycles agree since both inflate to κ) → `obsFun` (obstruction of the *normalized* factored cocycle) + additivity `obsFun_add` (common refinement + `relZPair_add`) → **`obs : Z²cont(Γ_A,𝔽₂) →+ 𝔽₂`** (`AddMonoidHom.mk'`) with **`obs_ker_le : ker obs ⊆ B²`** (`mem_B2_of_factor_balanced` on the balanced normalized cocycle + `const2_mem_B2` restores the normalization constant) ⟹ **`card_H2_gammaA_le_two : Nat.card (H2 (F₄/NA)(ZMod 2)) ≤ 2`** via `#H² = #(Z²/B²) ≤ #(Z²/ker obs) ↪ #𝔽₂ = 2` (`kerLift`-injective + `QuotientAddGroup.map`-surjective + `Nat.card_le_card_of_{injective,surjective}`; discrete/canonical `ZMod 2` topology, not the abstract binder). Still NOT in `GQ2.lean` (WIP; c3 aggregator). **For P-16c4/c5: c2's `≤2` is the `hcard` input; the `=2` (≥2 direction) needs `B²⊆ker obs` (coboundaries balanced — the direction c2 skipped) + `obs`(varCoc)≠0 from c3's `mixedB≠0`.** |
| P-16c3 | B: the Γ_A twist, duality half (`GQ2/RadicalEdgeGammaA.lean`): port `RadicalEdgeLocal.exists_good_twist`'s **source-generic prefix** verbatim for `Γ = GammaA` (the `act`/`φf`/`hφZ1`/`not_noDescent_of_edge_trivial` blocks use nothing about `AbsGalQ2`; copy-adapt — do NOT churn the closed P-16b file); bridge `[φf] ≠ 0` through c1's `h1Equiv` + P-13g `dualAddEquiv` (`MuDual 2 ↔ ElemDual`) to a nonzero `H1w`-dual class; `prop_5_15` at `A_T := Additive ↥D.T` (conjugation `C`-action — P-16b's `letI` pattern; `hA₂ = D.helem`; `hgen` from `hρ`+`Marking.push_admissible`) perfect pairing `P` ⟹ `w` with `mixedB t_ρ x_w y_φ ≠ 0`; pull `w` back through c1 to the `TCocycle` `u_w` | ⭐⭐ | O | P-16c1, P-16a | ∅ | ☑ **DONE & verified std-3** (Opus, 2026-07-05) — `GQ2/RadicalEdgeGammaA.lean` sorry-free; `lake build GQ2` green (8669); `#print axioms exists_good_pairing_gammaA = std-3` (no B6/B7 — **Ax ∅** confirmed); `check_axioms` green (census 15, no allowlist entry needed — file is sorry-free). **Deliverable = the nonzero mixed pairing** `∃ x y : Z1w (markC ρ), mixedB (markC ρ) x.val y.val ≠ 0` (headline in `C`-module form so the signature needs only the file-level `C`-action; `x_w`/`yφ`/`φf` live inside as `have`s for c4 to consume). **Structural pivot vs the plan (docs/p16c3-recon.md):** `MuDual`'s action is hardcoded to `AbsGalQ2` (`TateDuality`), so `dualAddEquiv` does NOT apply — the shifted edge is built directly in `ElemDual (Additive ↥D.T)` (generic contragredient `DistribMulAction C (ElemDual A)`); the one new lemma is `hsmulD : (γ•l) a = l (γ⁻¹•a)` (replaces `smul_muN_two_trivial`). **Route:** file-level `cActT` (C-conjugation) + `AddCommGroup (Additive ↥D.T)` instances; `GA`-action = `compHom ρ` (so `hcompat`/`hactGA` = `rfl`); `φf`/`hcrossZ`/`hφne` port from `exists_good_twist` (edgeQ-only, `not_noDescent_of_edge_trivial`); bridge = `prop_5_15 (markC ρ)` (admissibility from `markC_admissible`) right-nondeg + `h1Equiv` transport of `[φf]≠0`. **Unblocks P-16c4** (needs c2's `θ` + this `mixedB≠0`). |
| P-16c4 | B: the Θ–mixedB comparison (the c-plan's crux): (i) `varCoc u_w` = the cup 2-cochain of `(φ, w)` pointwise (P-16b's `hbridge`, source-generic); (ii) `theta [varCoc u_w]` = the relator-`z` **pair** of `heisMarking t_ρ x_w y_φ` (κ-twisted extension = pushout of the `HeisLift` extension — ledger identity over the `liftMarking`/`heisMarking_*_z` apparatus); (iii) the ⚠ convention resolution: `mixedB` = traced **SUM** `tame.z + wild.z` (FoxHeisenberg:885) vs the `H2w`-class detector = **WILD**-z mod `im d1_triv = span{(1,0)}` (τ-column; forced by `#H2w = 2`) — they agree on §5.13 **x₀-supported normal forms** (`heisMarking_tameValue_z_eq_zero` needs σ/τ-slots zero); conclusion: `mixedB ≠ 0` ⟹ `theta [varCoc u_w] ≠ 0` ⟹ (θ linear, kills B²) `varCoc u_w` not a continuous coboundary — `hvar` ✓ and `#H²(Γ_A,𝔽₂) = 2` ✓ (with c2's ≤) | ⭐⭐⭐ | O (F design ✓ = the c-plan) | P-16c2, P-16c3 | ∅ | ☑ **COMPLETE & verified std-3 (Opus, 2026-07-06)** — both (A) `H²(Γ_A,𝔽₂)↪𝔽₂` and (B) the ledger `obs(varCoc)=mixedB` + `hvar` + `#H²=2` landed; `lake build GQ2.HalfTorsorGammaA` exit 0.  History below.  ~250 ln. **The risk row.** Fallback documented in the plan: derive the wild-z functional's nondegeneracy directly from `IsSelfDual`'s clauses if the normal-form route fights.  **★ UPDATE 2026-07-06 (Opus — std-3, `lake build GQ2.WordCoh2` green 8601): the convention trap (iii) is ALREADY RESOLVED by c2's architecture** — c2 shipped `obs : Z²cont(Γ_A,𝔽₂) →+ 𝔽₂` as the traced **SUM** `relZPair.1+relZPair.2`, and its ground-truth correction pinned `im d1_triv = Δ` (DIAGONAL), so the H2w-detector IS the sum functional (kills Δ); `mixedB` is ALSO the traced SUM `tame.z+wild.z`. **⟹ SUM=SUM, NO x₀-supported normal-form grind needed.** c4 now = TWO concrete lemmas. **(A) `obs_B2_eq_zero : B²⊆ker obs`** (⟹ `obs` descends to **`H²(Γ_A,𝔽₂) ↪ 𝔽₂`**, the degree-2 presentation-comparison injection, reusable Thm-4.2-ward). **Pure content DONE & verified** (new §CoboundaryObstruction in `WordCoh2.lean`): `trivialMarking_{tame,wild}Value` (`⟨1,1,1,1⟩` kills both relators, `powOmega2 1=1`), `zeroCocycle`+`fibHom0` (κ=0 ⟹ fib is a hom) → **`relZPair_zero t=(0,0)`**, `coboundaryCocycle λ` (κ=δ¹λ; char-2 cocyc via `abel_nf`+`CharTwo.two_eq_zero`), `Psi λ : CentExt(δ¹λ)→*CentExt 0` (`(l,z)↦(l,z+λl)`), `map_Psi_liftMark` (→ `shiftLiftMark t (λ∘gens) 0`), **`obs_coboundary_eq : (relZPair t (coboundaryCocycle λ)).1+.2 = λ(t.tameValue)+λ(t.wildValue)`** (via `Psi`+shift laws+`relZPair_zero`). **(A) COMPLETE & verified std-3 (Opus, 2026-07-06 — `#print axioms {obs_B2_eq_zero, obs_ker_eq_B2, obsH2_injective, exists_oneCochain_factor, card_H2_gammaA_le_two} = {propext, Classical.choice, Quot.sound}`, `lake build GQ2.WordCoh2` green 8601):** the ContCoh threading landed — **`exists_oneCochain_factor`** (1-var factoring, reusing `exists_openNormalSubgroup_factor_two` on `ψ∘fst` + `comap N_A` transport), **`obs_B2_eq_zero : B²⊆ker obs`** (κ∈B² ⟹ `normalizeCochain κ = δ¹ψ'` [`ψ'=ψ-const(ψ1)`; the LHS/RHS char-2 identity closes by `simp[…,CharTwo.sub_eq_add]; abel`] → `coboundaryCocycle λ` factor of `normalizeCochain x.1` via `exists_oneCochain_factor` → `obsFun_eq`+`obs_coboundary_eq` → `λ(t_U.tameValue)+λ(t_U.wildValue) = λ1+λ1 = 0`, both relators dying by `isAdmissibleU_of_NA_le hU`), **`obs_ker_eq_B2 : ker obs = B²`**, and the bundled **`obsH2 : H²(Γ_A,𝔽₂) →+ 𝔽₂` + `obsH2_injective`** — the degree-2 presentation-comparison injection **`H²(Γ_A,𝔽₂) ↪ 𝔽₂`** (reusable Thm-4.2-ward). **(B) the ⭐⭐⭐ ledger identity `obs(varCoc u_w) = mixedB t_ρ x_w y_φ`** (the real remaining risk): match the finite-level `relZPair` SUM to `heisMarking`'s traced-z SUM (factor `varCoc` at the ρ-level / common refinement). With (A)+(B): `obs(varCoc)=mixedB≠0` (c3) + obs kills B² ⟹ `[varCoc]≠0∈H²` ⟹ `#H²≥2`, with c2 `≤2` ⟹ **`#H²=2`** + `hvar`.  **★★ UPDATE 2026-07-06 (Opus — (B) STRUCTURAL CORE LANDED & VERIFIED std-3, `lake build GQ2.MixedBObs` exit 0): the feared "HeisLift↔CentExt correspondence" (the ⭐⭐⭐ crux) is DONE.** `HeisLift A C` IS a central-𝔽₂ ext of `WordLift(A×A^∨)C`; new generic file **`GQ2/MixedBObs.lean`** ships `kappaHeis : TwoCocycle(WordLift(A×ElemDual A)C)` (`κ p q=p.u.2(p.g•q.u.1)`), `PhiHeis : CentExt kappaHeis →* HeisLift A C` (`⟨((a,l),g),z⟩↦⟨a,l,z,g⟩`, rfl homs), and **`mixedB_eq_relZPair : mixedB t x y = (relZPair (mBaseMarking t x y) kappaHeis).1+(…).2`** — so **`mixedB` IS a WordCoh2 relator-`z` pair** (`#print axioms {kappaHeis,PhiHeis,mixedB_eq_relZPair}=std-3`). **REMAINING = the edge-specific ASSEMBLY (~150ln, de-risked, reuses WordCohBridge):** reconstruct c3's `φf` (RadicalEdgeGammaA:165-217); `w=ofZ1w x`+package `u:TCocycle`; graph hom `wordHom ρ (pair(w,φf))`; **ledger** `obs(varCoc u)=mixedB(x,eval φf)` via the NEAR-DEFINITIONAL edge unfold `varCoc u(γ,δ)=(φf γ)(γ•w δ)=kappaHeis(wordHom γ,wordHom δ)` + `relZPair_comap`+`liftMarking_eval_univ`+`mixedB_eq_relZPair` (NOT the hard edge=Fox — the edge is already packaged in c3's φf!); nonvanish transfer (c3's `y`≈`eval φf` in H1w ⟹ needs mixedB-kills-B1w-in-2nd-slot); `hvar` via `obsH2_injective`, `hcard` via c2. Assembly + P-16c5's `half_torsor_gammaA` want a NEW file importing WordCoh2+CentralObstruction+RadicalEdgeGammaA+MixedBObs.  **★★★ UPDATE 2026-07-06 (Opus — the entire OBSTRUCTION SIDE of (B) is DONE & verified std-3, `lake build GQ2.LedgerGammaA` exit 0):** `GQ2/MixedBObs.lean` adds **`obs_inflation`** (obs of a cocycle factoring pointwise as `κ(H a)(H b)` through finite `L` = `relZPair (gammaGen.map H) κ`; the whole `LevelFactor`/`relZPair_comap` computation, generic; `NA ≤ ker` automatic since `H∘quotientMk` kills `NA`).  New `GQ2/LedgerGammaA.lean`: `pairHom` (graph hom of `(w,φf)`), **`obs_varCoc_eq_mixedB`** (THE LEDGER `obs(varCoc u) = mixedB (markC ρ)(eval w)(eval φf)`, via edge unfold `varCoc u(a,b)=kappaHeis.κ(H a)(H b)` + `obs_inflation` + `mixedB_eq_relZPair`), **`varCoc_class_ne_zero`** (`hvar`: `mixedB(eval w)(eval φf)≠0 ⟹ [varCoc u]≠0` via `obs_B2_eq_zero`).  All std-3.  **REMAINING = input plumbing only (~150 ln, mechanical):** (1) reconstruct `φf`+`hφZ1` (verbatim c3 RadicalEdgeGammaA:165-280); (2) `w=ofZ1w x`, `u` from `w`; (3) nonvanish descent (c3's `y`≈`eval φf` in H1w ⟹ `mixedB` kills `B1w` in 2nd slot); (4) `#H²=2` via c2.  **☑☑☑ COMPLETE 2026-07-06 (Opus — P-16c4 DONE end-to-end, verified std-3, `lake build GQ2.HalfTorsorGammaA` exit 0, 8634 jobs):** the input plumbing landed cleanly (the nonvanish descent avoided by reconstructing `φf` in `exists_phiF` and invoking `prop_5_15`'s `hright` directly at `[φf] ≠ 0`, with `h1wMk⟨eval φf⟩ = h1Equiv[φf]` holding by `rfl`).  New `GQ2/HalfTorsorGammaA.lean`: **`exists_nonzero_varCoc_gammaA (D)(S)(hedge)(ρ)(hρ)(htriv) : ∃ u : TCocycle D ρ, H2mk GA ⟨varCoc D ρ S u, …⟩ ≠ 0`** (the P-16c4 `hvar` deliverable — builds T-modules via `compHom ρ`, `exists_phiF`, `prop_5_15`, `w = ofZ1w x`, `u` from `w`, `varCoc_class_ne_zero`) and **`card_H2_gammaA_eq_two : Nat.card (H2 GA (ZMod 2)) = 2`** (the `hcard` — `obsH2_injective` + the nonzero class ⟹ `obsH2` bijective).  Both `#print axioms = {propext, Classical.choice, Quot.sound}`.  **Ready for P-16c5** (`half_torsor_gammaA` = `CentralObstruction.n D ρ S htriv u hvar hcard` + `Finite (MLifts)` + splice `SectionEight.lemma_8_6_gammaA` sorry at SectionEight:1288). |
| P-16c5 | B: `half_torsor_gammaA` assembly + splice: `Finite (MLifts D ρ)` via `topGen_gammaA` + `finite_continuousMonoidHom` (**no `hfg` statement amendment**, unlike the local side; `GammaA` is a `ProfiniteGrp` — compactness instances exist); `half_count` with c4's `hvar` + `hcard`; one-line splice into `SectionEight.lemma_8_6_gammaA` (claim the co-owned row) | ⭐ | O | P-16c2, P-16c4, P-16a | ∅ | ☑ **COMPLETE & verified std-3 (Opus, 2026-07-06, `lake build GQ2` exit 0, 8678 jobs, 0 errors).** `GQ2/HalfTorsorGammaA.lean` **`half_torsor_gammaA (D)(hedge)(ρ : ContinuousMonoidHom GammaA (Bg⧸M))(hρ) : 2 * Nat.card {f : MLifts D ρ // f.Central} = Nat.card (MLifts D ρ)`** = `CentralObstruction.half_count D ρ0 S htriv u hvar hcard` (fed `exists_nonzero_varCoc_gammaA` + `card_H2_gammaA_eq_two`); `Finite (MLifts)` via `finite_continuousMonoidHom hfg Bg` + `gammaA_topologicallyFinitelyGenerated` (no `hfg` amendment, as predicted). **GammaA↔GA transport handled** (the ticket's flagged wrinkle): retype `ρ` to the raw `F₄ ⧸ N_A` (`let ρ0 := ρ`, defeq to `↑GammaA`) so the `GA`-machinery instances resolve, and transport `TotallyDisconnectedSpace (F₄⧸NA) := inferInstanceAs (… (GammaA : Type))`.  **`SectionEight.lemma_8_6_gammaA` sorry FILLED** (`:= LedgerGammaA.half_torsor_gammaA D hedge ρ hρ`, `import GQ2.HalfTorsorGammaA` added); `#print axioms lemma_8_6_gammaA = {propext, Classical.choice, Quot.sound}` — **∅ extra axioms, beats the B6,B7 budget.** **Unblocks P-16d3's gammaA instance.** **⟹ ENTIRE P-16c LANE (c1–c5) CLOSED.** |
| P-16d | B: `prop_8_9` assembly (136)–(142) — **decomposed → P-16d1–d6** (`docs/p16-ticket-split.md`) | ⭐⭐⭐ | O | P-16b, P-16c, P-13f | B6, B7, B9 | ◐ **(Fable 2026-07-05)** skeleton ☑ (`RecursionInputs`+`prop_8_9_aux`; eq138 ← `lemma_8_3`); item 1 ☑ core (`liftB`+`stageR136_of`; residue = obstruction-module datum → P-16d2); item 2 ☑ **FULLY** (`partition137_of` std-3; **eq137 discharged in the aux**; two statement corrections documented: `zBC` re-encoded to the ∃-lift form — the pair-with-`g` encoding overcounts ×8 and contradicts (139) — and eq137’s sum restricted to `J ↠ C` per the paper; deviation ledger in `docs/section8-extraction.md`).  Remaining decomposed as P-16d1–d6 below. |
| P-16d1 | B: §8 frame-enrichment layer (per-λ `RadicalCoverData` square-form data on `M_B`/`T_Bsub` + the κ⁰_q/6.3 base-class datum for the descended `V⋊C`) | ⭐⭐⭐ | F+O | prop_7_4 ✓, §7 block | ∅ | ☑ **DONE (Fable 2026-07-05)** — all std-3, build+gate green.  (i) **Derived layer facts** (`SectionEight.lean`, `RecursionFrame` ns): `MB_normal`/`MB_elem`/`MB_comm` (elementarity forced by `ker π_B = Φ(K)`), `TBsub_eq_mapKS` (the `R`-factor dies in `B`), `TBsub_normal`, `TBsub_le_MB` (via `lemma_7_1_head`), `ker_piBC = M_B`, `piBC_surj`.  (ii) **`RecursionFrame.Enrichment`**: per-λ square-form fields (`q/hq/hrad/hTzero` on `M_B`/`T_B`) + descended-module κ⁰-datum fields (`Vmod/descend/qbar` quad+ns+inv + `FactorSet`/`IsEquivariantFactorSet` — the `lemma_6_21` relative hypothesis, for P-16d4); `Enrichment.radData : RadicalCoverData RF.YB` (cover `p_λ`, layers `M_B`/`T_B`; normality/elementarity discharged from frame+block) + `radData_noDescent_iff` = **`Iff.rfl`** vs the (139)/(140) case split — P-16d3 consumes `lemma_8_6_local ✓` directly.  (iii) **Block-level constructibility** in new `GQ2/FrameEnrichment.lean`: `blockT_map_le_blockM_map` + **`mForm_of_qbar`** — from Prop 7.4's output package the `M_B`-form `q_M(π_B k) := λ(k²)` is well defined with both radical clauses (route: `R ≤ K∩S` (`lemma_7_1_head`) puts `π_B`-fibres in single `S`-cosets; everything reads off `q̄` via `hspec`); Φ-membership lemmas (`sq_mem_frattiniLike`/`comm_mem_frattiniLike`) added to `FrattiniNongen.lean` (now root-reachable via SectionEight→FrameEnrichment).  (iv) ⚠️ **`prop_8_9` amended (documented, review-flag)**: gains `(En : RF.Enrichment)` — the bare-frame `∀ RF` statement quantifies over junk covers for which (139) fails; the paper's §7.4/§6.1 standing data must enter as hypothesis (deviation ledger #3, `docs/section8-extraction.md`).  **d3 (local lane) and d4 now unblocked.** |
| P-16d2 | B: R-stage obstruction module — the `(W, o, e, hmB, hobs, hfib)` datum for `stageR136_of` (+ `z_R` torsor count from the 5.15/5.16 `Z¹`-numerics) | ⭐⭐⭐ | O | P-13f (gammaA numerics; local via `prop_5_16_bundle`) | B6, B7 | ☑ **ABSTRACT MODULE DONE (Opus, 2026-07-06)** — (136) reduced to two source residues (`hsep_hom`+`hZcount`) for the d6 concrete instantiation; std-3 sorry-free.  *(History: REDUCTION LANDED 2026-07-05 + design escalation.)* `GQ2/RStageObstruction.lean` — **`stageR136_ofObstruction` std-3, sorry-free**: repackages `stageR136_of`'s `W`/`o`/`e` interface into the natural shape (obstruction = linear functional `obs : BoundaryLifts(B) → D_Rᵛ`; `W := D_Rmodᵛ`, `e` via finite-dim double-dual `evalEquiv`), so a caller supplies only `obs`/`hmB`/`hobs`/`hfib`. **⚠ ESCALATION** (`docs/p16d2-plan.md`): the datum *witness* needs the compatibility that the abstract `scalarCover l` really is the `λ`-pushout `Y/ker λ ↠ Y/R` of the single radical extension `Y↠B` — which the bare frame + `Enrichment` **document but do not carry**. Without it, obstruction-linearity (Lin) and the `hobs` separation (Sep) are underivable. Recommend adding a `cover_map` compat family to `Enrichment` (light touch, no bare-frame edit) OR building the witness in the concrete-`𝒴` d5/d6. Numeric for `hfib` identified: `#Z¹(Γ,R)=\|R\|²·#(ElemDual R)^C=z_R` is exactly `prop_5_16_bundle` cl.2 (local) / `prop_5_15` `IsSelfDual` cl.2 (gammaA) + `card_DR`. **Option A APPROVED + STARTED (user 2026-07-05):** `GQ2/RStageObstructionBuild.lean` (std-3) — `RCoverData RF` compat structure (`coverMap_λ : Y →* (scalarCover λ).cover`, `p_λ∘coverMap_λ=π_B`) + `lifts_scalarCover_of_liftB` (easy `hobs` dir) + **`trivialRCD`** (the `M=⊥` `RadicalCoverData` unlocking the whole `CentralObstruction` engine — `ob`/`central_iff_ob_eq_zero` — for bare covers; the `mB⟺ob` reuse hinge). `R=Φ(K)` confirmed elem-abelian (`lemma_7_2`). **5-step core progress** (`RStageObstructionBuild.lean`, all std-3 sorry-free): **(1)✅ bridge** (`trivialRCD`+`central_iff_ob_eq_zero`⟹`liftsThroughCover_iff_homOb`; `cardTwoLinEquiv` for `H²≅𝔽₂`). **(2)✅ obs + hmB DONE** — `RObstructionData` (adds `D_Rmod≃D_R` + `pair : D_Rmod →ₗ (R→+𝔽₂)` + `pair_coverMap`), `rDefect`, the connection `homOb=H2mk(pair d∘rDefect)` (`homOb_eq_H2mk_pair`), the functional `obs : D_Rmod →ₗ 𝔽₂` (`obsMapAdd`+`cardTwoLinEquiv`), and **`hmB_holds`** = `stageR136_ofObstruction`'s `hmB` exactly. **(3)◐ hobs** — ⟸ **DONE** (folded into the assembler); ⟹ = hard separation: **Frattini/framing wrapper DONE** (`liftB_fibre_nonempty_of_homLift`, commit `10522ef`, std-3 — a bare hom lift `φ:Γ→Y` of `g` lands in the fibre via `surj_of_piB_surj`+`TB_head`/`TB_theta`), so **residual = the separation core** (`obs g=0 ⟹ ∃ hom lift`), needing a new `H²(Γ,R)` layer + `(R^∨)^C`-injectivity + pushout-kernel field. **(4)✅ hfib abstract torsor DONE** (commit `41afee3`, std-3): `RCocycle` (=`Z¹_{Γ,ρ}(R)`), `R_le_ker_piY`/`R_le_ker_thetaY` (needs `hE2`), `surj_of_piB_surj` (Frattini), **`fibreCocycleEquiv`** (`{f//liftB f=g} ≃ RCocycle`), **`hfib_holds`** (`#fibre=z_R`, reduced to source count `#RCocycle=z_R` — d6 citation of 5.15/5.16+`card_DR`). **(5)✅ assemble DONE** (`stageR136_ofRObstructionData`). **✅✅ ABSTRACT MODULE FINISHED (commit `9687aca`)** — **`stageR136_ofRSepData`** (std-3, sorry-free): (136) fully derived, every abstractly-provable ingredient proven (obstruction map, `hmB`, easy `hobs`, the hfib fibre-torsor, hsep's Frattini/framing wrapper, **`homLift_of_split`** = the constructive splitting-cochain⟹continuous-hom-lift), reduced to exactly **two irreducible source residues** d6 supplies: `hsep_hom` (`obs g=0 ⟹ g` has a hom lift — the `(R^∨)^C`-detection of `H²(Γ,R)`, a *concrete* R+C-action property, **provably not** abstractly derivable — the assembly hits an irreducible `H¹`-vanishing even with pair-injectivity) + `hZcount` (`#RCocycle=z_R`, the 5.15/5.16 numeric+`card_DR`), plus `hE2`. **Net: P-16d2's abstract R-stage obstruction module is COMPLETE**; d6 constructs the concrete `RObstructionData` and discharges the two arithmetic residues, then calls `stageR136_ofRSepData`. ~19 commits; leaves await the d6 splice (not yet imported by `GQ2.lean`). |
| P-16d3 | B: `zBC ↔ MLifts` bridge + `half139` (fibration over `TC`-lifts; fibres = central `M`-lifts; consume 8.6-local ✓ / 8.6-gammaA = P-16c) | ⭐⭐ | O | P-16d1; P-16c (gammaA instance) | B6, B7 | ☑ **DONE (Opus 2026-07-05)** — new `GQ2/RadicalEdgeBridge.lean` (sorry-free, **all std-3, Ax ∅** in-file; B6/B7 only via the consumed 8.6; root build 8666 + gate green).  **`half139_of`**: `2·zBC = \|M_B\|²·e_Γ(C)` by fibring `zBC` over the `C`-exact-image `ρ` (`Equiv.sigmaFiberEquiv`; the fibre = `CentralOver ρ`, `IsBoundaryLift` redundant via `isBoundaryLift_of_over` from d4) and summing — the two per-source numerics enter as **hypotheses** `hlem86` (source's Lemma 8.6) + `hMcount` (`2^{2 dim M}=\|M_B\|²` count), so **no P-13f needed here** (source-generic).  The genuine bridge = **`liftsOver_equiv`/`centralOver_equiv`**: `LiftsOver ρ ≃ MLifts (E.radData l h) ρ'` (central ↔ central) via `piBCiso : B/M ≅ C` + transport `ρ' := piBCiso⁻¹∘ρ`, so d6 discharges `hlem86` from `lemma_8_6_local`/`gammaA` and `hMcount` from the 5.15/5.16 numerics.  ⚠ **Lean note (ledger #7)**: the transport iso/quotient are parametrized by the `RadicalCoverData` `D` (not `RF.MB`) so `QuotientGroup.mk` uses `D.M`'s normality instance and matches `MLifts` on the nose — else the `RF.MB`-vs-`(E.radData l h).M` instance-diamond breaks the fibre-equiv `rw`.  **d6 now has every `half139`/`phase140` input** (only P-16c + P-16d2 remain before the final splice). |
| P-16d4 | B: state+prove Lemma 8.7 (affine T-lifting via `lemma_6_21` ✓ + κ⁰_q datum) and Prop 8.8/(135) (via `lemma_8_5` ✓ + `lemma_6_22` ✓) — statement-layer additions, review-flagged | ⭐⭐⭐ | F+O | P-16d1 | ∅ | ☑ **DONE (Fable F-plan + Opus O-close, 2026-07-05)** — new `GQ2/AffineTLift.lean` (sorry-free, **every theorem std-3, Ax ∅**; root build 8664 + gate green; not in sorry-allowlist). Plan `docs/p16d4-plan.md`. **Deliverables** over `(D : RadicalCoverData)` + a bundled `Descent`/`DescData` (d6 unpacks from `E.radData`/`radData_noDescent_iff` + `Enrichment`): (2.1) `sectN`+`edge_zero` (the `N`-complement's edge vanishes — normal `N` ⟹ no `z`-defect); (2.2) **`descended_splitting`** = `∃ σ : C₀ →* B/T` sectioning `piQbar` — `lemma_6_21` at `iV`/`ξ` where `iV : V ↪ B/T` (via the `descend`-quotient iso, char. `iV_spec`) and **`ξ`** is the descended central double cover `B̃/N ↠ B/T`'s defect sign-cocycle (`descKerCases`/`ccZsign`/`xi_cocycle`/`xi_diag = q̄`); (2.4) **`central_twist_iff`** (zero-edge: `varCoc ≡ 0` via `edge_zero` ⟹ `ob_twist` collapses ⟹ twist preserves `Central`) + **`tcocycle_torsor_equiv`** (`red_T`-fibre ≃ `Z¹(T)`) + **`lemma_8_7_count`** (`#{central lifts in a fibre} = #Z¹_{Γ,ρ}(T)` = the (132) `μ`); (2.5) **`exists_polar_inverse`** (`a_{χ,κ}` supplier; `B♭` bijective by nonsingularity+finrank); (2.6) **`prop_8_8_target`** (edge-killing shear via `lemma_6_22`, total phase `DeltaScalar`); (2.0) `RecursionFrame.headBC`/`thetaBC`/`isBoundaryLift_of_over` exports in `SectionEight.lean` (+ `partition137_of` deduped). ⚠️ **Review-flagged deviations** (ledger #4/#5 in `docs/section8-extraction.md`): 8.7 stated cocycle-level not class-level (μ=`#Z¹(T)`, `\|B¹\|` absorbed into d6's `V`-coord base); 8.8 target-side only (Γ-(135) = d6 behind P-13f) with a `6.22`-normalized `Δ` (has `γ⌣a`; safe — phase family `∃`-bound in `prop_8_9`). **2.3** explicit `graphEquiv` folded into the `red_T`-fibration; **2.7** `centralCoverOfCocycle` deferred to d5. |
| P-16d5 | B: shared witness `(μ, G⁰, D_T, phase)` — μ closed formula, `G⁰` from `prop_7_4` ✓, `D_T`/`phase` = (134) `Δ_{χ,κ}`-covers (needs 8.7’s `e`-class) | ⭐⭐ | O | P-16d1, P-16d4 ✓ | ∅ | ☑ **DONE (Opus 2026-07-05)** — **`centralCoverOfCocycle`** built (`GQ2/AffineTLift.lean`, std-3, Ax ∅; root build + gate green): the twisted product `𝔽₂ ×_δ C₀` of a **normalized** `𝔽₂`-2-cocycle `δ` as a genuine `CentralCover C₀` (mult `(s,c)(t,d)=(s+t+δ(c,d),cd)`, `z=(1,1)`, `ker = ⟨z⟩` — full `Group` instance + all 5 cover fields incl. `ker_eq`; multiplicative analog of P-15i `Transgression.Twisted`).  **`phaseFamily`** wraps it to the `DT → CentralCover C₀` shape of `prop_8_9`'s `phase` field.  With this the shared witness is fully constructible by d6: **μ** = `lemma_8_7_count`'s `Nat.card (TCocycle D ρ)` (= `#Z¹(T)`), **G⁰** = `gaussSum E.qbar`, **D_T** = the `(T^∨)^C` index, **phase** = `phaseFamily (fun ζ => DeltaScalar …)`.  The witness *assembly* + the `eq140` proof (needs `μ` source-independence, props 5.15/5.16 = **P-13f**) is the d6 splice — ledger #6 in `docs/section8-extraction.md`. |
| P-16d6 | B: `phase140` + final two-source splice of `prop_8_9` (aux ×2; per-source `hfg`/`hscalar`/`hhead` discharge; `prop_8_9` **already amended** by P-16d1 with `(En : RF.Enrichment)` — the splice consumes it, incl. the concrete `𝒴`-instance of `En` with P-16d5) | ⭐⭐⭐ | O | P-16d3 ✓, P-16d4 ✓, P-16d5 ✓, P-16d2 ✓, P-16c ✓, P-13f ✓ | B6, B7, B9 | ◐ **BACKBONE + half139-bridge LANDED (Opus 2026-07-06)** — new leaf `GQ2/RecursionSplice.lean` (off co-owned `SectionEight.lean`), std-3 sorry-free: **`prop_8_9_of`** reduces `prop_8_9`'s conclusion to witness + 2 `RecursionInputs` + `hfg`/`hhead` per source (via `prop_8_9_aux` ×2; **`hscalar` discharged** from `lemma_8_2_gammaA`/`_local`); **`half139_via_radData`** strips the d3 bridge reducing `half139` (both sources) to the pure `MLifts` facts `2·#central=#MLifts` (Lemma 8.6) + `#MLifts=\|MB\|²` (5.15/5.16); **`phase140_ofPhaseData`** — the **(140) reducer** (`lemma_8_5`/8.7 analog of `stageR136_of`) — reduces (140) to the phase datum `hfib : zBC=μ·M` + `hgauss` (`lemma_8_5` aggregated) by `linear_combination`.  **So all three displays (136)/(139)/(140) now have clean reducers.**  **Second pass (Opus 2026-07-06): the (140) engine is COMPLETE and fully `En`-supplied** — μ-partition `central_card_eq_reductions_mul_tcocycle` (+ `centralOver` form), `zBC_eq_mu_mul_reductionCount` (`hfib` ⟸ μ-independence alone), `lemma_8_5_aggregated`, **`phase140_of_gaussCorrespondence`** (the capstone (140) reducer, analog of `stageR136_ofRObstructionData`), `polarInverseL`(+spec)/`phase140_of_nonsingular` (the `a_χ` polar data discharged from `En.hns` via `exists_polar_inverse` ✓), `enrichment_card_Vmod` (`\|V\| = \|M_B\|/\|T_B\|` from `En.descend`) — all std-3 sorry-free.  **Scope correction**: `Enrichment` already carries `Vmod`/`qbar`/`hquad`/`hns`/`dat`, so the (140) engine + witness are `En`-constructible (previously mis-scoped as blocked).  **`prop_8_9` NOT closed** — the remaining CONCRETE work is **split into P-16d6a–e** (rows below; exact obligations with `En` instantiation in `docs/p16d6-concrete-spec.md`; narrative in `docs/p16d6-plan.md`): **a** = (136) R-stage datum + residues (the `Enrichment`-extension framing was corrected — built against `blockFrameImpl` in a leaf), **b** = μ-independence, **c** = Prop-8.8 core (decomposed → c1/c2/c3), **d** = (139)/`G_ℚ₂`, **e** = assembly + splice.  **Lane roll-up (Fable, 2026-07-06)**: **a ☑** (general-Γ layer complete; d6e consumes `blockStageR136_ofSplitCriterion`), **b ☑** (μ-indep proved, conditional on the fed per-source counts `κ_M`/`κ_I` → e), **c ☑ (2026-07-07: keystone `hkey` + `DeltaChi` phase data landed — see the c1c/c2 rows)** (history: c3 μ-half ✓; **c1 = the deep Prop-8.8 group-theory↔quadratic core** — ⚠ two engine-spec bugs found by the c-verification pass (2026-07-06), split → c1s/c1a/c1b/c1c, see the P-16d6c row; c2 gated on c1c's keystone; **c3 G0-half route DECIDED — §6.2 pinning via P-15b's `prop_6_9`/`lemma_6_8`, no `En` amendment, O-ready**), **d ◐** (`half139_local` plumbing ✓; 2 scoped sorries `hfix`/`htorsor`, recipes in `docs/p16d6d-hMcount-handoff.md`), **e ☐** (scope enumerated in `docs/p16d6a-handoff.md` §3: per-source R-stage residues — local = one `prop_5_16` invocation + plumbing, candidate = the (2,0)+`WordCoh2` gaps, optional `P-16d6e-A` split — plus b's `κ_M`/`κ_I` and d's shared `#MLifts`).  **a–d are ALL ☑ (2026-07-07) — only e remains** (residue package + fed counts + final splice). |
| P-16d6a | B: **(136) R-stage** — build `RObstructionData (blockFrameImpl T Blk hE2)` against the **concrete frame** (P-17c ✓ realizes `scalarCover` as `Y/l ↠ Y/R`, so **NO `Enrichment` edit needed** — earlier "co-owned extension" scoping CORRECTED), then discharge residues **`hsep_hom`** (the `(R^∨)^C`-separation `obs g=0 ⟹ ∃ hom lift`, NOT abstractly derivable) + **`hZcount`** (`#RCocycle = z_R`, 5.15/5.16); `hE2` free (input to `blockFrameImpl`).  Consumer: `stageR136_ofRSepData` ✓ (P-16d2).  Spec §3.  **Decomposed (Opus 2026-07-06, P-17d-scale)**: **a-covers ✓** (`blockRCoverData`: `coverMap l h = mk' l.1`, `coverMap_lifts` = `rfl`; std-3, `GQ2/BlockRStage.lean` — git mv from `ScratchP16d6a`); **a-DRmod ✓** (the whole `(R^∨)^C` character duality + assembly, std-3: `RCharSub` = D_Rmod = Y-inv 𝔽₂-chars of `R`; forward `RCharKer` χ↦ker + normal/≤R/relIndex≤2; inverse `RCharOf` R'↦[r∉R']; `blockToDR` = the bijection; **`blockRObstructionData`** = the full `RObstructionData (blockFrameImpl …)` incl. `pair_coverMap` `zsign`-match); **a-assemble ✓ (Opus 2026-07-06)** — **`blockStageR136`** (std-3): the general-Γ (136) identity for `blockFrameImpl`, `stageR136_ofRSepData`-instantiated at `blockRObstructionData`, `hE2` discharged from the frame arg, source residues `htriv`/`hcard`/`hfg`/`hZcount`/`hsep_hom` threaded as hypotheses; + `blockRChar_card` (`#(R^∨)^C = #D_R`, the `z_R`-`#D_R`-factor bridge). **Residue architecture (CORRECTION)**: `hZcount`/`hsep_hom` are **not** general-Γ dischargeable — the absolute crossed-cocycle count `#RCocycle` needs prop_5_16/5.15, which are **Γ-specific** (`Γ ≠ AbsGalQ2` blocks the invocation; cf. `PhaseMuIndep.lean:21`), and the (139) lane's analogue `hMcountM` is likewise a **source hypothesis fed at assembly** (`RecursionSplice.half139_via_radData`).  So both stay hypothesis-side at P-16d6a (like `hcard`/`hfg`), discharged **per-Γ at P-16d6e/P-17i** (matching the explicit `hfg` "reserve B1 to P-17i" instruction).  **`hsep_hom` re-scoped (Opus 2026-07-06, read paper Prop 8.9 pp. 42–43): it is ALSO the Γ-specific 5.15/5.16 duality, NOT a separate abstract build.**  `obs g d = ⟨d, ob(g)⟩` pairs `d ∈ D_R` with the full `R`-obstruction `ob(g) ∈ H²(Γ,R)`; `ob(g)=0 ⟺ g` lifts (abstract obstruction theory); so `obs g = 0 ⟺ ob(g) ∈ (D_R)^⊥`, and the separation IS the perfect pairing `D_R = (R^∨)^C ≅ H²_{Γ,ρ}(R)^∨` (paper p. 42) — same duality as `hZcount`, discharged from ONE package at assembly.  (`mapCoeff2` `Cohomology.lean:429` DOES exist — coeff functoriality present; earlier "missing character-separation / dévissage infra / `[Y,R]≠⊥` worry" retracted — the duality detects all of `H²(Γ,R)`.)  **No separate P-16d6a-sep abstract build.**  **Fable double-check + interface (2026-07-06)**: conclusions verified against the Lean duality statements — **local package COMPLETE in `prop_5_16`** (cl. 3 = `hcard` verbatim, cl. 2 = the `Z¹` count shape, cl. 6 `cup20`-bijectivity = the separation, since `cup20 c φ = [φ∘c]` on `H⁰`); **candidate `IsSelfDual` has cards + (1,1) only — NO (2,0) clause** ⟹ the `Γ_A` separation needs a (2,0)-corollary (right-nondeg + card ⟹ perfect, finite lin. alg.) and a twisted degree-2 `WordCohBridge`/`WordCoh2` extension; `hcard_A` = WordCoh2's `≤2` injection + a nonzero class (routes: handoff §3).  **NEW std-3 interface**: `hsep_hom_of_splitCriterion` + `blockStageR136_ofSplitCriterion` — frame plumbing pre-discharged; a source owes only the cochain-level **split criterion** + `hZcount`/`hcard`/`hfg`/`htriv`.  Citation precision: the p. 42 duality display is for the phase module `T`; the `R`-instance is implicit in Prop 8.9 (`z_R` display + Fourier inversion). | ⭐⭐⭐ | O | P-16d2 ✓, **P-17c ✓** | ∅ (this layer is std-3; B6/B7 enter per-Γ at d6e) | ☑ 2026-07-06 (Opus+Fable) — **general-Γ layer COMPLETE** (`blockRObstructionData`, `blockStageR136`, `blockRChar_card`, `hsep_hom_of_splitCriterion`, `blockStageR136_ofSplitCriterion`; all std-3, leaf `GQ2/BlockRStage.lean`, root-import wiring deferred to d6e); per-Γ residues re-homed to **P-16d6e** with enumerated routes (handoff §3) |
| P-16d6b | B: **(140) μ-independence** — `hμ : ∀ ρ, #(TCocycle (En.radData l h) (rhoPrime …ρ)) = μ`: the crossed `Z¹_{Γ,ρ}(T)` count is `ρ`-independent (5.15/5.16 content — the `ρ`-twisted conjugation actions on `T` give equal counts).  Consumer: `zBC_eq_mu_mul_reductionCount` ✓ (which then yields the whole (140) `hfib`).  Spec §2 | ⭐⭐ | O | P-13f ✓, P-16d4 ✓ | ∅ (file std-3; B6/B7/B9 deferred to the P-16d6e assembly as the fed per-source counts) | ☑ **CLOSED 2026-07-06 (Opus)** — `GQ2/PhaseMuIndep.lean` **sorry-free, std-3 (Ax ∅), off the `SORRY_ALLOWLIST`**; `lake build GQ2.PhaseMuIndep` green (8656), `check_axioms` pass (census 15).  **`tcocycle_mu_indep` + `tcocycle_card_indep` PROVED std-3** (`#print axioms` = `{propext, Classical.choice, Quot.sound}` for all three theorems, incl. the new `mlifts_card_eq_image_mul_tcocycle`).  The `#Z¹_{Γ,ρ}(T)` count is `ρ`-independent, reduced through the new **`Γ`-generic torsor identity `mlifts_card_eq_image_mul_tcocycle`**: `#MLifts = #(red_T image)·#Z¹(T)` (every `red_T`-fibre is a `Z¹_{Γ,ρ}(T)`-torsor via `tcocycle_torsor_equiv`; the all-lifts analog of `central_card_eq_reductions_mul_tcocycle`, simpler — no `Descent`/`lemma_8_7_count`), so `#Z¹(T) = #MLifts / #(red_T image)` is `ρ`-independent by cancellation.  **⚠ §6 RISK RESOLVED (handoff option 3, user-approved 2026-07-06):** the handoff's Route B is **unusable in this file** — `prop_5_16_bundle` / `card_Z1_eq` / `card_H2_eq_fixedPts` are stated over `AbsGalQ2` only (they carry B6/B7: Tate duality + local Euler char for `G_ℚ₂`), but `tcocycle_mu_indep` and its consumer `zBC_eq_mu_mul_reductionCount` are **generic over `Γ`**, and the count *value* is genuinely source-specific, so no single generic Route-B proof exists (structural, verified).  μ-independence is therefore delivered **conditional on the two per-source counts** `#MLifts` (= `half139_via_radData`'s `hMcountM`, the `\|M_B\|²` count) and `#(red_T image)` (the `B/T`-lift count) as fed constants `κ_M`/`κ_I`, to be discharged per source at the **P-16d6e** assembly (`prop_5_16_bundle` for `G_ℚ₂`; the `Γ_A`/`prop_5_15` analog in `HalfTorsorGammaA`/`RadicalEdgeGammaA` for `Γ_A`) — the exact idiom the sibling (139) count already uses.  Handoff `docs/p16d6b-handoff.md` updated with the resolution.  Prior ◐-history: **STARTED (Fable 2026-07-06)** — new own leaf `GQ2/PhaseMuIndep.lean` (untracked WIP; the earlier plan was Route B via `prop_5_16_bundle` + a `TCocycle ≃ Z1` bridge — superseded by the torsor reduction once the `Γ`-genericity blocker was confirmed). |
| P-16d6c | B: **(140) Prop-8.8 core** — one coupled build: the C-descent data `Lin : V →ₗ Efp`/`hLin`/`κ_ρ`/`ε_ρ`; **`hM`** (the (135)/Prop 8.8 count: central-liftable `T`-reduction ⟺ `Qx = ε_ρ ∧ Lin x = κ_ρ`, via `En.q`/`hq` + `lemma_6_21`/`6_22` ✓ + `prop_8_8_target` ✓); **`hphase`** (the character↔phase-cover reindex `Σ_χ Σ_ρ sign(χκ_ρ+ε_ρ+Q(a_χ)) = Σ_ζ (2·nPhase − e_Γ(C))`); the **witness** `phase = phaseFamily (DeltaScalar (En.dat l h) γ_ζ δ_ζ a_ζ)` (the 2-cocycle normalization proofs + per-character edge-killing shear) + `μ`/`G0` **`l`-independence** (Arf).  `0 < Nat.card DT` free (`DT := V^∨ ∋ 0`).  Consumer: `phase140_of_nonsingular` ✓ — engine fully `En`-supplied (`a_χ = polarInverseL` ✓, `hWV = enrichment_card_Vmod` ✓, `hDT`/`hG0` = `rfl`).  Spec §§1–2 | ⭐⭐⭐ | O | P-16d4 ✓, P-16d5 ✓, P-16d6b | ⊆ {B6, B7, B9} (audit at close) | ☑ **CLOSED (Fable 5, 2026-07-07) — all sub-tickets ☑ (c1s/c1a/c1b/c1c/c2/c3)**; **DECOMPOSED (Opus 2026-07-06) → P-16d6c1/c2/c3** (rows below); scoping handoff `docs/p16d6c-handoff.md` written against the proved `phase140_of_nonsingular` interface.  **c3 ☑ CLOSED (Opus 2026-07-07)** — both halves done: μ (`GQ2/PhaseLIndep.lean` `tcocycle_card_l_indep`, std-3) + **G0** (`GQ2/PhaseGaussLIndep.lean` `gaussSum_qbar_l_indep_unramified`/`_ramified`, std-3, §6.2 Arf-pinning, structural hyps threaded to d6e; `prop_6_9_unramified`/`lemma_6_8` confirmed std-3).  **Remaining:** **c1** = `Lin`/`κ`/`ε` + `hM` (the deep Prop-8.8 group-theory↔quadratic count, largest); **c2** = `hphase` + the `phase`/`DeltaScalar` witness (one coupled build on `prop_8_8_target` ✓); **c3** G0-half = `gaussSum(qbar l h)` `l`-independence — **route DECIDED 2026-07-06: §6.2 pinning via P-15b's `prop_6_9` pair, `En` amendment demoted to fallback** (analysis + decision on the c3 row).  Own leaves off co-owned `RecursionSplice.lean`/`SectionEight.lean`.  **⚠ FABLE VERIFICATION PASS (2026-07-06): plan architecturally CONFIRMED, but two engine-spec bugs found** (derivations in handoff §⚠, which supersedes conflicting statements): **(Bug 1, gates c1c)** `phase140_of_*`'s `hM`/`μ` are miscalibrated by `\|B¹_{Γ,ρ}(V)\| = #V` — the central `red_T`-image is stable under the **free** `B¹`-translation (conj by `m ∈ M_B` over the same `ρ'`: `redT(mfm⁻¹) = δb̄·redT f`; free since `V^C = 0`), so `hM`'s LHS is a multiple of `#V`, while its RHS `≤ #W = #V` with equality impossible ⟹ **the reducer is vacuous except when `zBC = 0`**.  Paper (132) keeps `\|B¹\|` inside `μ = \|B¹(V)\|·\|Z¹(T)\|`; the engine's `hμ`-pinned `μ = #Z¹(T)` does not.  Repair (c1s) is shape-preserving: `hM` gains a `#W·` factor, the two conclusions' `μ`-slot ↦ `#W·μ`; `eq140`/`prop_8_9`/`zBC_eq_mu_mul_reductionCount`/P-16d6b all stay as-is (`μ` existential; final witness `#V·#Z¹(T_B)`).  **(Bug 2, doc-level)** spec §1's `DT := (En.Vmod)^∨` is the wrong index — the paper's phase index is `D = (T^∨)^C` (dim r; (140) prefactor `2^{r+1} = 2·#D`), `Efp ≅ D^∨`; engine agnostic, docs corrected.  **Hidden obligations surfaced**: the **P-13g-deferred** cor-5.17 adjoint `⟨∂b,χ⟩ = ⟨b,ρ*γ_χ⟩` (first line of the paper's (135) proof) → c1b; `∂`-surjectivity (LES fragment + 5.16 numerics) → c1b; source-Gauss = target-Gauss `G(Q⁰_{Γ,ρ}) = G(q̄)` (**Γ-side** — `Q⁰_{Γ,ρ}` lives on `H¹_{Γ,ρ}(V)`, so no frame-level `En` field can carry it; routes on the c1c row) → c1c.  **c1 split → c1s/c1a/c1b/c1c rows below, F/O per row.** |
| P-16d6c1 | B: **(140) C-descent + Prop 8.8 count** (umbrella) — the **repaired** `hM` (`#(central red_T image)(ρ) = #B¹·N(κ_ρ, ε_ρ)`, Bug 1) + the C-descent data `Lin : V →ₗ Efp`/`hLin`/`κ_ρ`/`ε_ρ` (`Efp = D^∨`, `D = (T^∨)^C`; `κρ(χ) = ι_Γ(ρ*(χ_*e))`, `ερ = ι_Γ(ρ*δ_κ)` — canonical, per Bug-2 fix).  The deep group-theory↔quadratic bridge.  **Split → c1s/c1a/c1b/c1c** (rows below).  Handoff `docs/p16d6c-handoff.md` §⚠+§c1; **design record `docs/p16d6c-keystone-design.md` (Fable 5, 2026-07-07 — authoritative for the keystone)** | ⭐⭐⭐ | F+O (per sub-row) | P-16d4 ✓, P-16d5 ✓, P-16d6b ✓ | ⊆ {B6, B7, B9} (audit at close) | ◐ decomposed (Fable 2026-07-06); **c1a ☑ CLOSED (Opus 2026-07-07** — `GQ2/VCocycle.lean`: `VCocycle`/`vcocycleEquivLifts`/`vCob`+`vCob_injective`/`mConj`+`mConj_central`/`cocycleOf_mConj`, std-3, validates c1s' `\|B¹\|=#V`); c1s/c1b/c1c open |
| P-16d6c1s | B: **(140) engine-spec repair (Bug 1)** — in co-owned `RecursionSplice.lean`: `phase140_of_gaussCorrespondence` + `phase140_of_nonsingular` get `hM ↦ #W·N(κρ,ερ)` and conclusion `μ`-slot ↦ `#W·μ` (algebra re-check in situ: `zBC = μ·#W·ΣN` + `2#DT·ΣN = #W·e + G0Σζ` ⟹ same shape).  NO other engine change (`eq140`/`prop_8_9`/`hμ`/P-16d6b untouched).  d6a/d6d don't touch these theorems — low collision, but co-owned: coordinate on the board | ⭐ | **F** | — | ∅ (statement-level) | ☑ **CLOSED 2026-07-07 (Fable)** — in `RecursionSplice.lean`, ONLY the two `phase140_of_*` theorems touched: `hM` RHS ↦ `Nat.card W * N(κρ,ερ)`, conclusion μ-slot ↦ `(Nat.card W * μ : ℕ)` (cast ℕ-atom — matches the `RecursionInputs.phase140` field at witness `μ = #V·μ₀` by `exact`); proof: `hfib` now `zBC = (#W·μ)·Mcount` (fold `Finset.mul_sum` + `ring`), `phase140_ofPhaseData` applied at `#W·μ` — abstract engine untouched (μ-generic), `hgauss`/`hμ`/`zBC_eq_mu_mul_reductionCount`/`eq140`/`prop_8_9` all unchanged, algebra re-verified in situ (same shape ✓).  Bug-2 doc fix was already landed 07-06; docs now also updated: spec §1 μ-row = (132) value `#B¹(V)·#Z¹(T)` + §2 sketch `μ₀`-note, handoff ⚠/c1s ✅-markers.  Gates: LSP clean; `lake build GQ2.RecursionSplice GQ2.Half139Local` green (8659 jobs, only foreign pre-existing warnings); `#print axioms` BOTH = `{propext, Classical.choice, Quot.sound}`; census 15; guard green.  **c1c's target statement is now satisfiable** (`LHS(ρ) = #B¹·N(κρ,ερ)`, `#B¹ = #V = #W`) |
| P-16d6c1a | B: **crossed `V`-cocycle layer** — `VCocycle` over `ρ` mirroring `TCocycle`; bijection {cts homs `Γ → B/T` over `ρ'`} ≅ `Z¹_{Γ,ρ}(V)` via `descended_splitting`'s `σ` (`B/T ≅ V ⋊ C`); the **`B¹`-translation facts** (conj by `m ∈ M_B` ⟹ `redT ↦ δb̄·redT`, same `ρ'`, Central preserved; free when `V^C = 0`) — the mathematical core of Bug 1, landing it validates c1s.  Own leaf; well-specified (mirror `TCocycle` API) | ⭐⭐ | **O** | P-16d4 ✓ | ∅ expected | ☑ **CLOSED (Opus, 2026-07-07)** — new leaf `GQ2/VCocycle.lean` (off `AffineTLift`), **all sorry-free, std-3** (`#print axioms` = `{propext, Classical.choice, Quot.sound}` for `vcocycleEquivLifts`/`cocycleOf_mConj`/`vCob_injective`/`mConj_central`/`qOfCocycle_conj`; file OFF the `SORRY_ALLOWLIST`); `lake build GQ2` green (8688), `check_axioms` all-pass (census 15).  **Delivered**: (1) **`VCocycle`** = continuous crossed `Z¹_{Γ,ρ}(V)` mirroring `TCocycle` (additive, valued in `V=M/T`; continuity stored via the `iV∘ofAdd` embedding into the discrete `Q=Bg/T`, since `DescData`'s `V`/`C₀` are topology-free) + `Zero`/`Add` + `c_one`/`ext`; **`rho0`** = the `C₀`-valued lower map (descent of `ρ` through `piC₀`).  (2) **`vcocycleEquivLifts`** = the semidirect bijection `Z¹_{Γ,ρ}(V) ≃ {g : Γ→ₜQ // piQbar∘g = ρ'}` via a splitting `σ` of `piQbar` (`descended_splitting` ✓); forward `qOfCocycle` (`γ↦iV(cγ)·σ(ρ'γ)`, hom via `iV_conj`/`sigma_iV_comm`), inverse `cocycleOfQ` (`iV⁻¹(gγ·σ(ρ'γ)⁻¹)`, choice-extracted).  (3) **`vCob`** = the principal coboundary `V→Z¹` (`γ↦ρ'γ·v−v`), `vCob_add`; **`vCob_c_eq_zero_iff`** (kernel = `ρ'`-fixed vectors) + **`vCob_injective`** (`hfix : V^{im ρ'}=0 ⟹` injective — the `\|B¹\|=#V` freeness, the `V^C=0` clause).  (4) **B¹-translation facts** — `mConj` (conj `M`-lift by `m∈M`, still over `ρ`), **`mConj_central`** (Central preserved via a cover-lift of `m`), `redTLift`, and the payload **`qOfCocycle_conj`** (conj by `iV(v)` = `+vCob v`, the semidirect action) ⟹ **`cocycleOf_mConj`**: `cocycleOfQ(redT(mConj m f)) = cocycleOfQ(redT f) + vCob(descend m)`.  **c1s VALIDATED**: `V` acts freely (via `descend`) on the central `red_T`-image by `B¹`-translation, so its cardinality carries the missing `\|B¹\|=#V` factor — the arithmetic close (`= #V·N(κρ,ερ)`) is c1c.  ⚠ `vCob` uses the standard sign `ρ'γ·v−v`; the conjugation-cocycle computation naturally gives `v−ρ'γ·v`, reconciled in `cocycleOf_mConj`'s `hcob` via `Vmod_exp2` (char 2). |
| P-16d6c1b | B: **connecting map + Lemma 8.7 (131)** — cocycle-level `∂_{Γ,ρ}` for `0→T→M→V→0`, the class `e` (pullback of `B → V⋊C` along the zero section), `actual-lift ⟺ ∂c = ρ*e`; the **P-13g-deferred cor-5.17 adjoint** `⟨∂b,χ⟩_Γ = ⟨b,ρ*γ_χ⟩_Γ`; `∂`-surjectivity onto the `H²(T)`-model (LES fragment + 5.16 numerics).  New subsystem — crossed-`H²(T)` encoding choices are design-sensitive | ⭐⭐⭐ | **F** | P-16d6c1a | ∅ (generic layer std-3; B6/B7 enter via the threaded residues at d6e) | ☑ **CLOSED (Fable 5, 2026-07-07)** — TWO new leaves, all sorry-free std-3, `lake build GQ2` green (8699), census 15: **`GQ2/PhaseObstruction.lean`** = the `𝔽₂`-cover obstruction calculus (`iotaB` = ι_Γ with `iotaB_add` from threaded `hH2 : #H²(Γ,𝔽₂) = 2`; `pullCoc` + `Z2`/`B2`-membership; **`centralCover_lift_iff`** — lift through a section-equipped central cover ⟺ `f^*δ ∈ B²`, consuming the new `centralCoverOfCocycle_exists_section` export in `AffineTLift`; `phaseSign`/`sum_phaseSign` = the (141) count; **`phase140_of_phaseObstruction`** = a PAPER-FAITHFUL (140) reducer beside the engine: per-ρ `2\|D_T\|·#(central redT image) = \|V\|(\|V\| + G0·Σ_ζ ±1)` ⟹ eq140 at μ-slot `\|V\|·μ₀` — `Lin`/`κρ`/`ερ`/`lemma_8_5` eliminated from this route, F-decision superseding spec §2's shape).  **`GQ2/VLiftCount.lean`** = the (131) layer over c1a: `TCharC D` (the `(T^∨)^C`-model, an `AddSubgroup` — this IS the `D_T` index, `hDT` trivial), `CountSections`, **`tDef`** (the cocycle-level `∂c + ρ*e` in one piece — the class `e` is `uDef` in `KeystoneDelta`), `chiDef_mem_Z2` (χ-conj-invariance kills the crossed twist), `betaChi := ι_Γ(χ_* tDef)`, `TLiftable` + generic direction `betaChi_of_tliftable`, `betaXi := ι_Γ(g_c^*ξ)` + **`central_iff_betaXi`** (the `CentralObstruction.ob`-bridge through `mk_N`, via `descP_lift_sign_mem_B2_iff` well-definedness), **`mem_centralImage_iff`** = the full (131), and **`two_mul_card_centralImage`** = the master count delivering `hMobst` from the threaded Γ-residues {`hsep` (the separation — d6a `hsep_hom` idiom), `haff`, `hpartial` (∂-surjectivity form), `hZcard` (#Z¹(V)=#V²), `hGaussZ` (source-Gauss transport `Σ_c sign(Q⁰ c) = #V·G0`), `hH2`} + the keystone data (`Δ`, `sh`, `hkey` — c1c).  The P-13g-deferred cor-5.17 adjoint DISSOLVES at this cochain level (design §6); ∂-surjectivity enters only as `hpartial`.  Full design + derivations: **`docs/p16d6c-keystone-design.md`**. |
| P-16d6c1c | B: **(135)-Γ keystone + `hM` close** — the per-`(χ,ρ)` exponent identity `ι_Γ(ρ*Δ_{χ,κ}) = χ(κρ) + ερ + q̄(a_χ)` (SHARED with c2's `hphase`); `central ⟺` scalar-quadratic on the `V`-coordinate; **source-Gauss = target-Gauss `G(Q⁰_{Γ,ρ}) = G(q̄)`** — ⚠ `Q⁰_{Γ,ρ}` lives on **`H¹_{Γ,ρ}(V)`**, NOT `V` (correcting the c3-row analysis's clause (ii)), so this is Γ-side and **no `En` field can carry it**; route options (F-design): (a) pin BOTH sides to the same structural constant — target via §6.2 as in c3, source via a `prop_6_9`-style dual pinch on `(H¹, Q⁰_{Γ,ρ})` (nonsingularity of its polar = 5.16 cl.-4 cup-perfectness ✓ proved; the tame/free structure on `H¹` is the open design item), or (b) a direct source↔target Arf transport along the completed square / cor-5.17 parity; assemble the **repaired** `hM = #B¹·N(κρ,ερ)` via (126) on both sides.  Pins `Lin` concretely.  Watch-item: the un/ram dichotomy (§6.2 (83): `Q⁰_A = q` vs `q_U`) may make the correct V-side count form `q̄` or its ω₂-double — settle in the keystone analysis before freezing `hG0`'s instantiation | ⭐⭐⭐ | **F**-design → **O**-close | P-16d6c1s ✓, P-16d6c1b ✓ | ∅ expected (Γ-residues threaded to d6e) | ☑ **CLOSED (Fable 5, 2026-07-07)** — **F-DESIGN COMPLETE + Stage A LANDED (Fable 5, 2026-07-07)** — the full keystone derivation is worked and recorded in **`docs/p16d6c-keystone-design.md`** (authoritative; supersedes the handoff §c1c where they conflict): the `Lin`/`κρ`/`ερ`-interpolation is GONE (paper-faithful reducer, see c1b row); the remaining `hkey` is the (135) cochain identity `betaChi χ c + betaXi c = QZero(c + sh χ) + ι_Γ(ρ'^*Δχ)`, derived in the doc via (§2) the EXPLICIT `ω_χ = kappa0 datχ + inf e_χ` decomposition (no abstract extraction — `datχ` an equivariant factor set for the ZERO form built from the three `T`-defect atoms), (§3) the symmetric-zero-diag `V`-splitting lemma, (§4) the `Θ = κfull + κ⁰` four-chase extraction with explicit `γκ/δκ/uκ`, (§5) the cover-commutator-=-polar lemma, (§6) the `prop_8_8_target`-assembly with `sh χ := aχ∘ρ'` and the dual-crossed law forced abstractly by `κgen`-cocycle-ness; `haff` falls out of §2 (γ''-additivity).  **Stages A+B+§2+haff LANDED (Fable 5, 2026-07-07, second pass)**: `GQ2/KeystoneDelta.lean` (std-3, sorry-free, `lake build GQ2` green 8700, census 15) now carries: (A) the raw semidirect calculus (`pmul`/`jmap`/`Jmap` + multiplicativity), the atoms `mDef`/`conjDef`/`uDef` (∈ T; `uDef` = the class `e`), **`JDefT_eq`** (the product formula `JDef = conjDef·uDef·mDef`); (B) **`exists_splitting_of_symm_zero_diag`** (design §3 — the `𝔽₂ ×_φ V`-extension splits linearly over the field); (§2 COMPLETE) the `mDef`/`conjDef`-identity packs (`mDef_cocycle` via a **file-local `CommGroup ↥D.M`** + ac-simp; `conjDef_add`/`conjDef_mul` via `M_comm_T`-sorted calcs), **`datChi`** + **`isEquivariantFactorSet_datChi`** (zero-form equivariant factor set) and **`chiJDef_eq`** (`χ∘JDef = kappa0 datχ + inf e_χ` — the explicit (130)-normal form of the χ-pushout, no abstract extraction), the graph ties `graph_pmul`/`tDef_eq_JDefT` + `chiDef_decomp` (cup + `g`-coboundary + scalar); and **`betaChi_affine` = the master count's `haff`, PROVED** (`#print axioms` = std-3) — one of `two_mul_card_centralImage`'s six threaded Γ-residues is now discharged generically (only `hsep`/`hpartial`/`hZcard`/`hGaussZ`/`hH2` remain per-source).  **THIRD PASS (Fable 5, 2026-07-07): §§4–5 COMPLETE + §6 through the `Ψ_χ`-normal form** (`lake build GQ2.KeystoneDelta` green 8662, all std-3, sorry-free; full-tree `lake build GQ2`/`check_axioms` currently red on FOREIGN in-flight files `SectionTen.lean`/`RStageLocal.lean` — not this lane): **(§5)** `xi_polar` (the cover-commutator = polar lemma, via the `s0`-square ledger on `covQ`) + `xi_one_left/right`/`ker_sq_one`/`xi_diag_sq`; **(§4)** `kfull`/`theta`/`kappa0_serre`/`pcob_serre`, `gkappa` (the `V×V`-splitting), `theta'` and **the four-chase extraction `theta'_decomp`** (`Θ' = Γγκ + inf δκ + ∂uκ`, raw values `gkraw`/`ukap`/`dkap`), **`gammakap_add`** (E5) and **`gammakap_dual_crossed`** (E6); **(§6, structural half)** `gchi` (the per-χ `f_χ`-splitting), `gamma2` + additivity + dual-crossed, `gammatot` + laws, `polar_smul_inv`/`exists_polar_inverse'`/`polar_inj`, **`achi`** (the shear family) with **`achi_crossed`** (`ha`, from the dual-crossed law + polar equivariance — `hinvQ : IsInvariant DD.C0 DD.qbar` threaded, supplied by `En.hinv` at e) + `achi_kill` (`hkill`), `kappa0_datChi_decomp`, `deltatot`/`wtot`, and **`psi_decomp`** — the complete `Ψ_χ = κ⁰ + Γγtot + inf δtot + ∂W_χ` pointwise normal form.  **FOURTH PASS — CLOSED (Fable 5, 2026-07-07): the `hkey`-assembly is LANDED** (`GQ2/KeystoneDelta.lean` Stage E, `section Keystone`; `lake build GQ2.KeystoneDelta` green 8662, zero new warnings, all std-3): **`shChi`** (`a_χ∘ρ'` as a crossed `VCocycle`; continuity through the discrete `Bg⧸M`, crossed law = `achi_crossed`), **`DeltaChi`** (`:= DeltaScalar DD.dat γtotHom δtotλ (achi …)`), the two NEW memberships **`graphPullback_mem_Z2_of_cocycle`** (generic-Γ Lemma 6.1/(62): continuity via the `invFun`-through-`iV`-and-`ρ` factorization, cocycle identity via `m_quad`+`m_mul`+`f_cocycle`) and **`graphCob_mem_B2`** (graph-coboundary of any pair potential), and **`keystone`**: `betaChi S hσ χ c + betaXi hσ Dsc c = QZero DD ρ (c + shChi S Dsc hσ hinvQ χ) + ι_Γ(pullCoc ρ' (DeltaChi S Dsc hσ χ))` — EXACTLY `two_mul_card_centralImage`'s `hkey` at `Δ := DeltaChi S Dsc hσ`, `sh := shChi S Dsc hσ hinvQ`; Γ-residues `htriv`/`hH2` only.  Proof: `prop_8_8_target` at the sheared cocycle `c₀ := c + sh_χ` (exponent-2 collapse `s_a(graph c₀) = graph c`), `psi_decomp` at `graph c`, pointwise `linear_combination`, then `iotaB_add`-split — `ρ'^*Δχ ∈ Z2` obtained by SUBTRACTION from the pointwise identity (the planned Δ-cocycle-law route was not needed for the membership; the Δ-cocycle law itself landed separately under c2).  d6e consumes `keystone` verbatim. |
| P-16d6c2 | B: **(140) `hphase` + phase witness** — the character↔phase-cover reindex `Σ_χ Σ_ρ sign(χκ_ρ+ε_ρ+Q(a_χ)) = Σ_ζ (2·nPhase − e_Γ(C))` **and** the witness `phase = phaseFamily (DeltaScalar (En.dat l h) γ_ζ δ_ζ a_ζ)` — one coupled build (the `DeltaScalar` 2-cocycle normalization `hcoc`/`hl`/`hr` + the sign-sum↔`2·nPhase−e` identity) on top of `prop_8_8_target` ✓'s edge-killing shear.  **Post-verification (Fable 2026-07-06)**: index is `ζ ∈ DT = (T^∨)^C` (Bug-2 fix, NOT `V^∨`); the sign-conversion runs through c1c's per-`(χ,ρ)` keystone — don't start the `hphase` half before it; the `hcoc`/`hl`/`hr` normalization half + the (141) unobstructed/obstructed count split are independent and O-ready.  Own leaf.  Handoff §c2 | ⭐⭐⭐ | **F**-reindex → **O**-counts | P-16d5 ✓, P-16d6c1c (keystone; Δ-family def is joint) | ⊆ {B6, B7, B9} (audit at close) | ☑ **CLOSED (Fable 5, 2026-07-07)** — **RESHAPED by the c1b architecture**: `hphase`/the character↔phase reindex are **ELIMINATED** — the paper-faithful reducer `phase140_of_phaseObstruction` consumes `phaseSign` directly, and the (141) split is **LANDED** (`sum_phaseSign`, `GQ2/PhaseObstruction.lean`, std-3).  Remaining c2-content is folded into keystone stage D (design doc §6): the `Δχ := DeltaScalar dat γtot δtot aχ` family with normalizations `hcoc`/`hl`/`hr` (hcoc = the inf-restriction of a cocycle-sum; hl/hr by evaluation — recipes in the doc), the witness `phase ζ := centralCoverOfCocycle (Δ ζ) …`, and the e-side per-(ζ,ρ) sign bridge `phaseSign (phase ζ) ρ = sign(ι_Γ(ρ'^*Δζ))` (one-liner via `centralCover_lift_iff` + the new `centralCoverOfCocycle_exists_section` export).  **LANDED (same day)**: the folded content is DONE in `GQ2/KeystoneDelta.lean` Stage F (`section PhaseData`), all std-3: **`DeltaChi_cocycle`** (`hcoc` — via the NEW pair-level **`chiJDefT_serre`** (the `J`-defect associativity chase, mirror of `chiDef_mem_Z2` at pair level) + **`bundle_serre`** (Serre for `κ⁰+Γγtot+inf δtot`, by `psi_decomp`×4 + `kfull_serre` + `pcob_serre` subtraction), evaluated on the `(0,·)`-section against four `prop_8_8_target`-squares + `kappa0_serre` + `pcob_serre`), and **`DeltaChi_one_left`/`DeltaChi_one_right`** (`hl`/`hr` — via the new normalization atoms `achi_one`, `gammatot_one`, `gammatot_zero`, `deltatot_one_left/right`, `uDef_one_left/right`, `theta'_pone_left/right`).  Remaining at d6e: only the witness line `phase ζ := centralCoverOfCocycle (DeltaChi S Dsc hσ ζ) (DeltaChi_cocycle …) (DeltaChi_one_left …) (DeltaChi_one_right …)` — the per-(ζ,ρ) sign bridge is ALSO LANDED generically (`sign_iotaB_pullCoc_eq_lift_sign`, see the d6e row). |
| P-16d6c3 | B: **(140) `G0`/`μ` `l`-independence** — **μ ✅ DONE** (`GQ2/PhaseLIndep.lean`, `tcocycle_card_l_indep` std-3: `#TCocycle(radData l h)` is `l`-independent, since the `M`/`T` layers and `rhoPrime` are `l`-constant).  **G0 ⚠**: `gaussSum(qbar l h)` `l`-independence is **not** derivable from `Enrichment` (no cross-`l` field) — needs an `Enrichment.hGaussConst`/`hArf` amendment (owner sign-off); scoped in handoff §c3.  **Fable note (2026-07-06)**: c1c's source-Gauss = target-Gauss obligation is the same Arf-transport family — spec ONE `En` amendment serving both (F specs, O applies after sign-off).  **⚠ Fable analysis (2026-07-06) — the `En` amendment may be AVOIDABLE; flagged for the c-agent to weigh against its own plan.**  `prop_6_9_unramified`/`prop_6_9_ramified` (P-15b, `SectionSix.lean:306/368`, both ✓ std-3) pin `zeroCount q` — hence `gaussSum q` via `gaussSum_eq` (`g = 2·zeroCount − #V`, P-15a) — of **ANY** nonsingular `C`-invariant quadratic form on `V` to a value fixed by `(V, tame structure, m)` **alone, λ-free**.  So (i) **c3** G0-half `l`-independence = two applications (`qbar l h` vs `qbar l' h'`, both qualify via `En.hquad`/`hns`/`hinv`); (ii) **c1c** `G(Q⁰_{Γ,ρ}) = G(q̄)` = one application to each of the two nonsingular invariant forms on the same `V` *[(ii) CORRECTED by the c-agent below: `Q⁰_{Γ,ρ}` lives on `H¹_{Γ,ρ}(V)`, not `V` — see the c1c row]*.  No cross-`l` `En` field needed — the λ-free structural hypotheses (tame cover, faithfulness, simplicity, un/ram dichotomy + its ramified data `hVU`/`hrank`/`hs1`) thread **hypothesis-side**, discharged at d6e (the c3-μ / d6b / d6a idiom).  Two caveats for the c-agent: the recursion needs the Gauss **value** anyway (§9 determinism), so wiring §6.2 is mandatory regardless — an `En` field would duplicate it and multiplies across every stage of the P-17i induction (`blockEnrichment`); and `prop_6_9_ramified`'s data hyps (`hVU`/`hrank`, flagged in the P-15b row as a genuine gap deriving them from `he`) must be supplied at the frame, so if that wiring is heavier than the amendment the trade may still favor a field — the c-agent should judge with its c1b/c1c context.  Board `P-15b` row already flags "Feeds P-16 (§8, not yet wired)".  **✅ ROUTE DECIDED (Fable c-agent, 2026-07-06) — adopt the §6.2 pinning; `En` amendment demoted to FALLBACK.**  Verified against the actual signatures, the route is even cleaner than flagged: **unramified** = `prop_6_9_unramified` ×2 (pins `zeroCount q` with NO data hyps beyond the tame structure) + `gaussSum_eq`; **ramified** = `lemma_6_8` ×2 whose **first conjunct pins `arf q = s` directly** (no detour through the `qDouble`) + the P-15a `arf`↔`gaussSum` bridges (`gaussSum_eq_pow` etc.) ⟹ `G(q̄_λ) = G(q̄_λ')`, λ-free, both cases.  Accepted caveats: the λ-free structural hyps (tame cover `c`/`hc`/`hfaith`/`hsimple`, dichotomy, and the ramified data `s`/`r`/`a`/`hs1`/`Wt`/`e`/`he` + the P-15b-flagged `hVU`/`hrank` gap) thread hypothesis-side to **d6e's residue list** (c3-μ/d6b/d6a idiom); the §9-determinism argument (Gauss **value** needed anyway; an `En` field would duplicate §6.2 and multiply across P-17i's `blockEnrichment` stages) is sound and sealed the decision.  Clause (ii) of the analysis (c1c) is corrected on the c1c row — `Q⁰_{Γ,ρ}` lives on `H¹`, Γ-side, no `En` field possible there either way.  Own leaf | ⭐ | **O** (`prop_6_9`/`lemma_6_8` ×2; amendment = fallback only) | P-16d6b ✓ | ∅ (μ) / ∅ (G0) — both std-3; B-leaves enter only via the threaded structural hyps at d6e | ☑ **CLOSED (Opus, 2026-07-07)** — **G0 DONE**, new leaf `GQ2/PhaseGaussLIndep.lean` (imports `PhaseLIndep`+`SectionSix`), all std-3 sorry-free, `lake build` green (8657), `check_axioms` pass (file NOT on `SORRY_ALLOWLIST`).  Deliverables `gaussSum_qbar_l_indep_unramified`/`_ramified` (`gaussSum (En.qbar l h) = gaussSum (En.qbar l' h')`, the (140) `hG0` transport) via the per-`l` Arf pins `arf_qbar_eq_one_of_unramified` (Prop 6.9, Arf=1) / `arf_qbar_eq_s_of_ramified` (Lemma 6.8 first conjunct, Arf=s) + reduction `gaussSum_eq_of_arf_eq` (Arf⇒gaussSum, both `±2^m`) + encoding bridge `gaussSum_eq_quadraticFp2` (`SectionEight.gaussSum = QuadraticFp2.gaussSum`).  Wiring: `Hf := RF.YC` via `En.actV`, `hinv := En.hinv`; the tame cover `c : Ttame ↠ RF.YC` + `hfaith`/`hsimple`/dichotomy/ramified-data + `#V=2^{2m}` thread hypothesis-side to d6e (c3-μ/d6b/d6a idiom).  **`#print axioms` = std-3 (no B6/B7, no sorryAx)** for all 6 theorems — confirmed the "∅ expected": `prop_6_9_unramified`/`lemma_6_8` are themselves std-3 (the §6 header's "B5/B6/B7′/B9" is for 6.13/6.16/6.18, not these).  **μ DONE (Opus 2026-07-06)** (`PhaseLIndep.tcocycle_card_l_indep`).  **So c3 is fully closed** (μ + G0). |
| P-16d6d | B: **(139) for `G_ℚ₂`** — discharge `half139_via_radData` ✓'s two hypotheses from **`lemma_8_6_local`** ✓ (`SectionEight.lean:1301`) + **`prop_5_15`/`prop_5_16`** ✓ (the `Γ_A` twin is P-16c's ✓, not here).  Spec §4 | ⭐⭐ | O | P-16d3 ✓, P-13f ✓ | ⊆ {B6, B7} (B9 unneeded) | ☑ **CLOSED (Opus, 2026-07-07)** — new leaf `GQ2/Half139Local.lean` (off co-owned files), **all sorry-free**: **`half139_local`** (the P-16d6e deliverable, exact `RecursionInputs.half139` shape) `#print axioms` = **std-3 + B6 (`tateDualityAt`) + B7 (`absGalQ2_localEulerCharacteristic`), NO sorryAx**; `lake build GQ2.Half139Local` green (8659 jobs), `check_axioms` all-pass (file OFF the `SORRY_ALLOWLIST`). **`hMcountM_local` (`#MLifts = \|M_B\|²`) FULLY PROVED** = Step 1 (additive `M_B`-module + `ρ'`-conjugation action, reusable atoms `conj_eq_of_mk_eq_M`/`mCommGroup`) · Step 3 (`card_Z1_eq`: `#Z¹ = \|M_B\|²·#fixedPts`) · Step 4 (`hfix : #fixedPts = 1` via the `lemma_7_1_dual` bridge — char `φ = lam∘(Blk.K↠M_B) : Blk.K →* μ₂`, `X = φ.ker.map`, normality via `conj_eq_of_mk_eq_M` + C-invariance, index-2 via `quotientKerEquivOfSurjective`) · Step 2 (`MLifts ≃ Z¹` torsor equiv **+** `Nonempty (MLifts)` by **extension-splitting**: continuous section `s = Quotient.out∘ρ'`, factor-set 2-cocycle `c = s·s·s⁻¹ ∈ Z²`, coboundary since `#H²=1`, lift `f = (toMul ψ)⁻¹·s`). Also **`hlem86M_local`** (`= lemma_8_6_local` on `rhoPrime …ρ`) + **`rhoPrime_surjective`** proved. The `#MLifts` count is the shared deep input P-16d6b's `hML`/`κM` defers — now available. Full record in `docs/p16d6d-hMcount-handoff.md`; the crux `(M^∨)^C=0` is `lemma_7_1_dual` (minimality of `K` + `V=P/S` chief factor). **Reusable byproduct**: a continuous-cohomology extension-splitting proof (`#H²(G,A)=1 ⇒ lift exists`) inline in the `Nonempty` step. |
| P-16d6e | B: **final assembly** — build the two `RecursionInputs` from a–d (+ P-16c for `Γ_A`'s `half139`), construct the shared witness value from `En`, then `exact prop_8_9_of …` into `prop_8_9` (co-owned one-line edit; remove from `SORRY_ALLOWLIST`).  Coordinate the **P-17i strengthening** `0 < Nat.card DT` in the same statement touch (still free after the Bug-2 index fix: `DT = (T^∨)^C ∋ 0`, NOT `V^∨` — see P-16d6c's ⚠ row).  **Contract change from c1s (Bug 1)**: the `phase140_of_*` `μ`-slot becomes `#W·μ`, so the shared witness here is `μ := #V·#Z¹(T_B)` (paper (132)), not `#Z¹(T_B)` alone.  **B1 accounting**: keep `hfgF` hypothesis-side (first consumption reserved to P-17i).  Spec §5.  **Scope grew (d6a re-home + d6b's fed counts, 2026-07-06)**: per source `Γ ∈ {Γ_A, G_ℚ₂}`, also discharge the R-stage (136) residues via **`blockStageR136_ofSplitCriterion`** (`GQ2/BlockRStage.lean`; wire it into the root import here) — `htriv`/`hcard`/`hfg`, `hZcount` (`RCocycle ≃ Z1` bridge, d6b's `TCocycle` pattern, + duality-cl.-2 count + `blockRChar_card`), and the **split criterion** (local: ONE `prop_5_16` invocation cl. 2/3/6 + the `compHom` `C=Y/K`-action via `lemma_7_2` `K`-centrality + `B²`-extraction; candidate: **two named §5-lane gaps** — the (2,0)-corollary + the twisted degree-2 `WordCoh2` extension, and `hcard_A` = `≤2`-injection + nonzero class — **split a `P-16d6e-A` sub-ticket if heavy**) — plus d6b's fed per-source constants `κ_M`/`κ_I` and d6d's shared `#MLifts` input.  Full routes: `docs/p16d6a-handoff.md` §3.  **(140)-route UPDATE (Fable 5, 2026-07-07)**: consume the paper-faithful chain — `RecursionInputs.phase140 := fun l h hN => phase140_of_phaseObstruction …` (`GQ2/PhaseObstruction.lean`) with `DT := ↥(TCharC (En.radData l h))`, `phase ζ := centralCoverOfCocycle (DeltaChi S Dsc hσ ζ) (DeltaChi_cocycle …) (DeltaChi_one_left …) (DeltaChi_one_right …)` (**phase data ALL LANDED** — `GQ2/KeystoneDelta.lean` Stage E/F), `hMobst := two_mul_card_centralImage …` (`GQ2/VLiftCount.lean`) at `Δ := DeltaChi S Dsc hσ`, `sh := shChi S Dsc hσ hinvQ`, **`hkey := keystone S Dsc hσ hinvQ htriv hH2` (LANDED 2026-07-07** — `hinvQ : IsInvariant DD.C0 DD.qbar` from `En.hinv`); the per-(ζ,ρ) sign bridge is **LANDED generically (Fable 5, 2026-07-07)**: **`sign_iotaB_pullCoc_eq_lift_sign`** (`GQ2/PhaseObstruction.lean`, std-3) — `sign ι_Γ(f^*δ) = (if ∃ lift through centralCoverOfCocycle δ … then 1 else -1)` for any bundled `f : Γ →ₜ* Y₀` and normalized raw 2-cocycle `δ`; at e, instantiate `δ := DeltaChi S Dsc hσ ζ` and `f :=` the bundled `rho0`-hom of `rhoPrime …ρ` (coercion defeq to the master count's `fun γ => rho0 DD ρ′ γ`), matching `phaseSign`'s if-form — assembly sketch in `docs/p16d6c-keystone-design.md` §8.  Its Γ-residues to discharge here per source: `hsep`, `hpartial`, `hZcard` (#Z¹(V)=#V²), `hGaussZ` (`Σ_c sign(Q⁰ c) = #V·G0`), `hH2` (#H²(Γ,𝔽₂)=2) — plus c3-G0's threaded tame-structure package (`c`/`hc`/`hfaith`/`hsimple`/dichotomy/ramified data/`#V=2^{2m}`, see the c3 row) and d6b's `κ_M`/`κ_I`.  P-16d6d's `#MLifts=\|M_B\|²` supplies `κ_M` (local). | ⭐⭐ | F/O per e-row | P-16d6a ✓, P-16d6b ✓, P-16d6c1–c3 ✓, P-16d6d ✓, P-16c ✓ | B6, B7, B9 | ◐ **ASSEMBLY IN PROGRESS (Fable 5, 2026-07-07/08)** — plan: `docs/p16d6e-assembly-plan.md` (authoritative). **(1) Statement surgery DONE**: per-λ phase family in `ClosedRecursion`/`RecursionInputs`/`prop_8_9_aux` (SectionEight) + `prop_8_9_of` (RecursionSplice) + the PROVED `count_eq_of_closedRecursion` (SectionNine) adapted; `prop_8_9` RELOCATED to the new capstone leaf `GQ2/Prop89Close.lean` (allowlisted), restated at `RF := blockFrameImpl T Blk hE2` with hypothesis ledger {hE2, hfgF (B1), hheadA, hheadF, hsimple, hfaith, hVne, hG0indep} + `0 < #DT` (P-17i); SectionEight keeps a pointer.  Amendment rationale (Bug-3, per-λ `Δ_{χ,κ_λ}`) recorded in the plan doc §1A. **(2) Generic (140) assembly DONE** (`GQ2/Phase140Assembly.lean`, std-3, tree 8708): `Enrichment.descData` (DescData wrapper), `descSigma`/`descSections` (6.21 splitting + sections), **`phaseChi`** (the per-(l,h) phase family from `DeltaChi`+laws), `card_TCharC_pos`, `rho0_descData_rhoPrime` (the roundtrip `rho0∘rhoPrime = ρ.1.1`), **`hMobst_of_residues`** (master count at the keystone data + sign bridge → `phaseSign`), **`phase140_from_residues`** (the `phase140` field verbatim, parametric over per-source residues {hsep, hpartial, hZcard, hGaussZ, hH2, hμ}).  **(3) Latent-breakage fixes at root-wiring**: `RStageLocal.lean` forward-reference (`htriv_local` relocated above `SepHom`) and the `BlockDR` cross-file duplicate (BlockRStage copy → `BlockDRsub`) — both files were outside the root import closure until now; `RStageLocal`/`Half139Local`/`BlockRStage`/`Phase140Assembly`/`Prop89Close` now root-wired, all gates pass.  **Remaining lanes → rows P-16d6e3–P-16d6e7 below** (F/O assignments per row; ☑-history rows P-16d6e1/P-16d6e2).  Prior ◐-history (Opus, local (136) package): new leaf `GQ2/RStageLocal.lean` (imports BlockRStage + LocalLiftingDuality): the **local (136) R-stage residues**.  Landed: (i) the `C = Y/K` conjugation action on `Additive R` (`rCommGroup`/`conj_eq_of_mk_eq_K`/`conjC`/`conjC_smul_of_mk`, std-3 — lemma_7_2's outputs thread as `hRK`/`hR2`); (ii) **`hZcount_local` PROVED** (`#print axioms` = std-3 + B6 + B7, no sorryAx): `#RCocycle = z_R` per boundary lift, via the `RCocycle ≃ Z¹(G_ℚ₂, R_{f₀})` bridge (d6b `TCocycle` pattern) + `card_Z1_eq` (5.16 cl. ii) + the invariant-character bridge `fixedPts(Y/K)(R^∨) ≃ RCharSub` + `blockRChar_card` + `zR`-rfl.  **⚠ scoping finding (recorded in the file docstring)**: `hsep_hom` must be supplied DIRECTLY to `blockStageR136`, NOT through `hsep_hom_of_splitCriterion` — the latter's `hsplit` quantifies over arbitrary continuous `g : Γ → Y/R`, but the prop_5_16 clause-(vi) separation needs `Γ ↠ C` surjectivity, which only `BoundaryLifts` carry (`ContSurj` bundles it); for non-surjective `g` the Y-invariant characters may underdetect `H²(Γ, R_{ρ_g})`.  **✅ LOCAL PACKAGE COMPLETE (Opus 2026-07-07, increments 3–4)**: `htriv_local` (any action on 𝔽₂ trivial), `hcard` (`card_H2_zmod2_eq_two`), **`hsep_hom_local` PROVED** (std-3 + B6 + B7, no sorryAx — `θ := mk' K ∘ slift ∘ g` continuous surjective hom [defect ∈ R ≤ K]; defect ∈ Z²(G_ℚ₂, R_ρ) [middle-insertion + one `mul_comm`]; paired classes = `cup20`-values on `H⁰ = (R^∨)^C = RCharSub` [Γ-inv = Y-inv via θ-surjectivity]; `bijective_cup20_dualEval` ⟹ `[rDefect] = 0`; `B²`-extraction [exp-2 kills the `dOne` signs] + `homLift_of_split`), and **`stageR136_local`** (std-3 + B6 + B7, no sorryAx) — the (136) identity with ALL per-source residues discharged, the local `RecursionInputs.stageR136` field verbatim (hyps to the splice: `hRK`/`hR2` = lemma_7_2, `hfg` = B1 → P-17i).  B1 → P-17i).  Then the candidate source (P-16d6a §3's two named §5-lane gaps) + fed counts `κ_M`/`κ_I` + the final `RecursionInputs` splice (gated on c1/c2). |
| P-16d6e1 | B: **`prop_8_9` statement surgery** — per-λ phase family (⚠ Bug-3, c-lane family: the paper's (134) classes are `Δ_{χ,κ_λ}`, κ the λ-cover's scalar-pushout class; the shared-family draft would force an unproven `zBC`-l-independence) through `ClosedRecursion`/`RecursionInputs`/`prop_8_9_aux` (SectionEight), `prop_8_9_of` (RecursionSplice), and the **proved** `count_eq_of_closedRecursion` (SectionNine); **relocate** `prop_8_9` → capstone leaf `GQ2/Prop89Close.lean` at `RF := blockFrameImpl T Blk hE2` (the only (136)-provable frame = the only intended consumer, P-17c/P-17h), hypothesis ledger {`hE2`, `hfgF` (B1 → P-17i), `hheadA`, `hheadF`, `hsimple`, `hfaith`, `hVne`, `hG0indep`} + conclusion `0 < #DT` (P-17i).  Record: `docs/p16d6e-assembly-plan.md` §1 | ⭐⭐ | **F** | P-16d6a–d ✓ | ∅ (statement layer) | ☑ **CLOSED (Fable 5, 2026-07-08)** — all four files amended and green (incl. the proved SectionNine consumer, mechanically adapted); `prop_8_9` relocated (SectionEight keeps a pointer comment); root-wiring surfaced + fixed two latent breaks (`RStageLocal.lean` forward-reference — `htriv_local` relocated above `SepHom`; `BlockDR` cross-file duplicate — BlockRStage copy → `BlockDRsub`); `RStageLocal`/`Half139Local`/`BlockRStage`/`Prop89Close` now root-imported; tree 8708, `check_axioms` all-pass (`Prop89Close.lean` allowlisted for the interim `sorry`) |
| P-16d6e2 | B: **generic (140) assembly** — the source-generic chain from the P-16d6c keystone to the `RecursionInputs.phase140` field | ⭐⭐ | **F** | P-16d6c ✓, P-16d6e1 ✓ | ∅ (all std-3) | ☑ **CLOSED (Fable 5, 2026-07-08)** — `GQ2/Phase140Assembly.lean`, every theorem `#print axioms` = std-3: **`Enrichment.descData`** (the `DescData (En.radData l h)` wrapper; `ker_piBC` ⟹ `hkerC0`), `descSigma`/`descSections` (chosen 6.21 splitting + normalized sections), **`phaseChi`** (the per-(l,h) phase family `ζ ↦ centralCoverOfCocycle (DeltaChi …)` from the landed c2 data), `card_TCharC_pos` (P-17i supplier), **`rho0_descData_rhoPrime`** (the roundtrip `rho0 ∘ rhoPrime = ρ.1.1` — aligns the master count's `ι_Γ(ρ'^*Δ)`-signs with `phaseSign` on the nose), **`hMobst_of_residues`** (per-ρ: `two_mul_card_centralImage` at `Δ := DeltaChi`, `sh := shChi`, `haff := betaChi_affine`, `hkey := keystone`, each `±1` rewritten via `sign_iotaB_pullCoc_eq_lift_sign`), **`phase140_from_residues`** (the `phase140` field verbatim at μ-slot `#V·μ₀`, family `phaseChi` — parametric over the per-source residues {`hsep`, `hpartial`, `hZcard`, `hGaussZ`, `hH2`, `hμ`}).  Elaboration gotchas recorded: `descended_splitting` takes `Dsc` FIRST; `pullCoc`'s phantom `C0`-implicit must be crossed by an explicit funext-equation (not `rw` on the function) before the sign bridge at `Y0 := RF.YC` | 
| P-16d6e3 | B: **local (140) residues** — for `Γ = G_ℚ₂` at the chosen `(descSigma, descSections, Dsc)`: **`hsep`** (the `(T^∨)^C`-separation: all `betaChi χ c = 0` ⟹ `TLiftable c` — the `hsep_hom_local` pattern at the `T`-module through the `M`-lift obstruction; `prop_5_16` clause (vi) `cup20`-bijectivity + `B²`-extraction), **`hpartial`** (∂-surjectivity of the `χ`-components — cup-duality clauses (iv)/(v)), **`hZcard`** (`#VCocycle = #V²` — `VCocycle ≃ Z¹(G_ℚ₂, V_ρ)` bridge (d6b `TCocycle` pattern) + clause (ii) `#Z¹ = #V²·#(V^∨)^C` + `#(V^∨)^C = 1` from `hsimple`/`hfaith` (dual of a nontrivial simple module has no invariants)), (NOTE: `hGaussZ` is NOT an e3 residue — it is P-16d6e4's `gaussZ_reduction` ⊗ P-16d6e4a, threaded via the `GaussZResidue` ledger hypothesis).  **`κ_I`** (`#(red_T image) = κ_M/μ₀` via `mlifts_card_eq_image_mul_tcocycle` — don't re-count; `μ₀` from clause (ii) at `A := Additive T`, ρ-free since the `C`-action on `T` is frame-level).  Already ✓: `hH2` (`card_H2_zmod2_eq_two`), `κ_M` (`hMcountM_local` = `\|M_B\|²`) | ⭐⭐ | O (recipes established: `hsep_hom_local`/`hZcount_local`/`hMcountM_local` are the worked patterns) | P-16d6e2 ✓ | ⊆ {B6, B7} | ☑ **CLOSED 2026-07-08 (Fable) — all four local (140) residues PROVED; `phase140_local` is sorry-free, `#print axioms` = std-3 + B6 (`tateDualityAt`) + B7 (`absGalQ2_localEulerCharacteristic`), exactly `⊆ {B6, B7}`; committed (`af45034`, `29c62ef`), whole tree green (8731).**  Leaf **`GQ2/Phase140Local.lean`** (imports `Phase140Assembly` + `Half139Local`).  **Plumbing VALIDATED end-to-end**: `phase140_local` (the `RecursionInputs.phase140` display for `AbsGalQ2`) type-checks against `phase140_from_residues` with `htriv`/`hH2` discharged locally (inlined `htriv_local'` + `card_H2_zmod2_eq_two`, both std-3) and the residues threaded; `lake build GQ2.Phase140Local` green (8665 jobs).  **PROVED**: `htriv_local'`, **`vFixedPts_eq_one`** = `#(V^∨)^{YC}=1` (std-3, via `card_fixedPts_elemDual_eq_one_of_nontrivial`), and — the two **counting** residues — **`hZcard_local`** (`#VCocycle=#V²`) and **`tcocycle_card_local`** (the `hμ` supplier, `#TCocycle=#T²·#(T^∨)^{YB/M}`, ρ-free closed form): both `#print axioms` = **std-3 + B6 (`tateDualityAt`) + B7 (`absGalQ2_localEulerCharacteristic`), no sorryAx** — exactly the predicted `⊆ {B6, B7}`.  Route: mirror `hMcountM_local` (module setup + `card_Z1_eq`); `hZcard`'s `VCocycle≃Z¹` bridge handles the `iV∘ofAdd` continuity via `IsLocallyConstant.desc` and the `En.Vmod`/`(descData).Vmod` defeq split via coercion+`exact`-bridging.  **⚠ STATEMENT FINDING**: `hZcard` is FALSE from `hsimple`/`hfaith`/`hVne` alone — the corner `#V=2 ∧ YC=1` satisfies the ledger yet gives `#(V^∨)^{YC}=2`; it needs the **nontrivial-action** hyp `hnt : ∃ g v, g•v≠v` (≡ `Nontrivial YC` via `hfaith`), added to `hZcard_local`/`phase140_local` — **the capstone ledger (`Prop89Close.prop_8_9`) must gain `hnt`** (discharged from the block's ramified regular summand; `hfaith` becomes redundant for `hZcard` once `hnt` is present).  **✅ `hsep_local` PROVED (Opus, 2026-07-07)** — `#print axioms` = std-3 + B6 + B7, no sorryAx — all 7 stages landed (~230 lines): (1) `Additive T` module [reuse `tcocycle_card_local`], (2) `tDef∈Z²(Γ,A)` [from `chiDef_mem_Z2`'s `hraw`], (3) dual+`hpair`, (4) `cup20 [tDef] n = H2mk(chiDef χ_n c)` via `hconjinv` (the `n↔χ_n∈TCharC` invariance transport) then `hc χ_n`⟹`chiDef∈B²`⟹`=0`, (5) `[tDef]=0` via `bijective_cup20_dualEval` injectivity, (6) B²-extract `ψ`, (7) **direct `M`-lift** `f γ=ψγ·fLift γ` (`hsplit`+T-abelian/exp-2 give the hom property; `ψ∈T` gives over-ρ' + `redTLift=qOfCocycle`).  **Gotcha banked**: the `RF.YB⧸M` quotient `Inv` fails bare-`⁻¹` synthesis (unfolded `Quotient (leftRel M)` head ≠ registered `G⧸N` instance) — fixed by `letI : Inv (RF.YB⧸M) := inferInstance` (**`letI` not `haveI`** — must stay defeq-transparent to `smul_apply`'s inverse); `set_option synthInstance.maxHeartbeats 4000000`+`maxHeartbeats 1600000` needed.  **Final residue `hpartial_local`** — the nondegeneracy statement (`∀χ≠0, ∃c, betaChi χ c ≠ betaChi χ 0`), **NOW PROVED (see landing note below)**.  **✅ DESIGN (Fable, 2026-07-07) — statement TRUE AS STATED, NO amendment** (contrast the `hZcard` `hnt`-finding): the a-priori kill scenario (χ in the image of `res_T : (M^∨)^C → (T^∨)^C`, which makes `betaChi χ` CONSTANT in `c` — e.g. any χ≠0 when the `C`-action is trivial, since `0→T→M→V→0` then splits) is EXCLUDED for the radical frame because **`(M_B^∨)^{YC} = 0`** — the paper's Lemma 7.1/`Hom_C(M,𝔽₂)=0` (Prop 7.4's proof, p. 38), refuted-by-`lemma_7_1_dual` as in `hMcountM_local`'s `hfix`.  **Route (paper (127)/(129)/(133), contrapositive)**: `hall` ⟹ (1) `gχ`-split `χ∘mDef` (`exists_splitting_of_symm_zero_diag`); (2) `iotaB (cupChi gχ χ c) = 0` ∀c (`chiDef_decomp` + `gPart_mem_B2` + `cupChi_zero` + `iotaB_add`/`iotaB_of_mem_B2` — inflated scalar cancels in the difference); (3) `cupChi gχ χ c (p) = ξ(p.1)(c.c p.2)` for the dual-connecting cochain `ξ(γ)(w) := χ(conjDef(ρ₀γ, (ρ₀γ)⁻¹•w)) + gχ(w) + gχ((ρ₀γ)⁻¹•w)` (paper (133)'s `γ_χ`, inflated; inverse-twist matches `cup11Fun`'s `p.1 • b p.2` convention); (4) `ξ ∈ Z¹(Γ, ElemDual Vmod)` via `isEquivariantFactorSet_datChi`'s `m_mul`/`m_quad` (+ `hg`-telescoping); (5) all cup values vanish over the `hZcard_local` `VCocycle ≃ Z¹` bridge (`:287`); (6) `[ξ] = 0` by right-slot separation (below); (7) B¹-extract `ξ = ∂n`; (8) **`M`-character** `ψ(m) := χ(t-part) + (gχ+n)(v-part)` (coordinates via `descend`/`hdesc_conj` + `S.mV`): HOM by `hg`+`n`-additivity, conj-INVARIANT by `ξ = ∂n` + `rhoPrime_surjective` (`Half139Local:42`, paper (129) "because ρ is surjective"); (9) `ψ ≠ 0` (`ψ\|_T = χ`) ✗ `(M_B^∨)^{YC} = 0` ⟹ `χ = 0` ✗ `hχ`.  Expected axioms: std-3 + B6 + B7 (unchanged `⊆ {B6,B7}`).  **✅ `hpartial_local` LANDED (Fable, 2026-07-08) — the design above executed as stated, ~470 ln.**  Support lemmas: **(i) `cup11_dualEval_right_separating`** (Phase140Local, std-3 + B6): the stage-6 right-slot separator, mirroring `bijective_cup11_dualEval`'s internals (`H1congr dualAddEquiv` pullback + `cup11_comm` slot swap + `(tateDuality 2).perfect11` injectivity); **(ii) `Half139Local.mchar_conj_invariant_eq_zero`** (EXTRACTED public, exactly std-3): the `(M_B^∨)^{conj} = 0` refutation (Lemma 7.1 via `lemma_7_1_dual`), refactored out of `hMcountM_local`'s `hfix` block into a shared theorem taking raw `∀ bb : RF.YB` conjugation-invariance — so both `hMcountM` and `hpartial` reuse it (a superseded private duplicate in Phase140Local was removed in `29c62ef`).  **Stage-8 realization**: the invariant `M`-character `ψ(m) = χ⟨tpart m⟩ + gχ(vco m) + n(vco m)`, with `tpart m := m·mV(vco m)⁻¹ ∈ T` (`hdesc_ker`); **additivity** from the T-part product law `tpart(mm') = tpart m · tpart m' · mDef(vco m, vco m')` (M abelian — 5-step `hcomm`-calc) + `hg` on the `mDef` χ-term + char-2; **conjugation-invariance** by the collapse `bb·mV(v)·bb⁻¹ = uσ(cc)·mV(v)·uσ(cc)⁻¹` (write `bb = uσ(cc)·k`, `k = uσ(cc)⁻¹·bb ∈ ker piC0 = M` since `piC0(uσ cc) = cc` via `piQbar_mk` + `descSigma_spec`; `M` abelian kills the `k`-conjugation), reducing to `hkey cc v`.  **Gotchas banked**: the `(En.descData l h).Vmod` vs `En.Vmod` DEFEQ SPLIT recurs at every `gχ`(descData.Vmod)/`n`(En.Vmod) boundary — `rw`/`toAdd_one` fire on the `gχ` side but NOT the `n` side (implicit `Multiplicative.toAdd {α}` differs); route the `n`-side through `congrArg n hvc` + `congr 1` (defeq-tolerant `exact`) or a `conv_lhs => rw [hdesc1]` + `map_zero` (uses `toAdd 1 ≡ 0` defeq), NOT `rw`; char-2 linear closers via `∀ … : ZMod 2, … := by decide` (NOT `linear_combination`, which leaves `2·x` that `ring` can't kill); `toAdd_one` (root namespace, not `Multiplicative.toAdd_one`); `map_one'`/`ψ 1 = 0` via `add_left_cancel` (no `self_eq_add_right`).  ⚠ **Shared-file casualty note**: this row's design paragraph was clobbered once by a parallel session's `tickets.md` commit (stale-read overwrite); board editors during swarm periods should re-read rows before writing. |
| P-16d6e4 | B: **source-Gauss transport `hGaussZ`** (BOTH sources) — `Σ_{c ∈ Z¹_{Γ,ρ}(V)} sign(Q⁰(c)) = #V · gaussSum(q̄)`, the paper's (126)-step feeding `G0 := gaussSum(q̄ l₀ h₀)` (shared across sources — the whole point of the twin dualities).  Route sketch (plan doc §4): (i) `Q⁰` is `B¹`-invariant — the coboundary shift of the graph pullback is a `B²`-difference (`lemma_6_14`/`graphPullback_sub_mem_B2` machinery, RepIndependence) ⟹ `Σ_{Z¹} = #B¹ · Σ_{transversal}` with `#B¹ = #V` free (`V^C = 0` from `hsimple`+`hfaith`); (ii) the transversal/`H¹`-Gauss = `gaussSum(q̄)` — a `prop_6_9`-style dual pinch on `(H¹_{Γ,ρ}(V), Q⁰)` (cf. c3-G0's §6.2 Arf pinning; both sides pinned to the same structural constant).  ⚠ **re-read paper p. 41 ((126)–(128)) before freezing the design** — the c1c row's surviving watch-item (un/ram `q` vs `q_U` dichotomy) lands here.  The deepest remaining P-16 mathematics | ⭐⭐⭐ | **F**-design → O-close | P-16d6e2 ✓; per-source duality bundles (`prop_5_16` / `prop_5_15`) | ⊆ {B6, B7, B9} | ◐ **LAYER (I) + LEDGER RESHAPE LANDED (Opus, 2026-07-08); only P-16d6e4a remains** — F-DESIGN COMPLETE (Fable 5, 2026-07-08) — `docs/p16d6e4-gauss-design.md` (authoritative; supersedes this row's route sketch).  **Key finding (paper pp. 40–41 reread)**: the (140) factor is `G(Q⁰)` on `H¹_{Γ,ρ}(V)`, whose value comes through the §6.2 evaluation **(83)** — `Q⁰ ≅ q̄` (unramified) or `≅ qDouble q̄ (U•·)` (ramified, SAME space `V`) — which the repo took as the *evaluated shape* (the P-12 seam).  So: (a) the e1-ledger hypothesis `hG0indep` is **wrong-shaped** in the ramified case — reshape per design §4 (`(G0 : ℤ)` + per-source `hGaussZA`/`hGaussZF`, + NEW `Nontrivial RF.YC`; drop `hG0indep`); (b) **`G0 = −2^m` unramified / `+2^m` ramified**, both PINNED by the landed `prop_6_9_unramified` (`zeroCount = 2^{2m−1} − 2^{m−1}`) and `lemma_6_8` clause 4 (`arf (qDouble q̄ (U•·)) = 0`) — frame-level, hence source-shared ✓; (c) three-layer decomposition: **(I)** the generic `Z¹ → H¹` reduction (fully specified O-work, design §2: `graphPullback_shift_mem_B2` = genericized `RepIndependence.graphPullback_sub_mem_B2`, `iotaB_add_mem_B2` (no `hH2`!), `QZero_add_vCob`, `vCobHom`-range card `#V` via `vCob_injective` at `V^{C₀} = 0` from {`hsimple`,`hfaith`,`Nontrivial YC`,`ContSurj`}, `Fintype.sum_fiberwise` orbit-sum → new leaf `GQ2/GaussZReduction.lean`); **(II)** the (83)-evaluation per source = **split ticket P-16d6e4a** (row below); **(III)** the pinning — landed.  Dead ends checked: §6.2-orbit pinning on `H¹` (not a `C`-module), lift-count reading of `zeroCount(Q⁰)` (obstruction quadratic in `c`, no half-torsor affinity), shared-datum dodges.  O-close scope = design §5 items 1–2   **LANDED (Opus O-close)**: **layer (I)** `GQ2/GaussZReduction.lean` (all theorems std-3, tree 8711): `iotaB_add_mem_B2` (ι_Γ absorbs a B²-shift, no `#H²=2` needed), **`graphPullback_shift_mem_B2`** (the generic-Γ form of `RepIndependence.graphPullback_sub_mem_B2` — its `AbsGalQ2`-bound `innerConj`/`kappa0_cocycle` re-proved in a clean context, continuity reused from `graphCob_mem_B2`), **`QZero_add_vCob`** (Q⁰ is B¹-invariant), `vCobHom`/`vCobRange`, **`hfix_of_simple`** (the `V^{C₀}=0` freeness from {hsimple, hfaith, Nontrivial C₀, ρ′-surj}), `QZeroBar` (the descended form on Z¹⧸B¹ via `Quotient.liftOn′`), **`gaussZ_reduction`** (`∑ᶠ_c sign(Q⁰ c) = #V·∑ᶠ_{Z¹⧸B¹} sign(Q̄⁰)` — `Finset.sum_comp` + coset-fibre card `= #V` via `AddMonoidHom.ofInjective`), and `card_quotient_vCobRange` (`#(Z¹⧸B¹)=#V` from `hZcard` + Lagrange).  **Ledger reshape** (`GQ2/Prop89Close.lean`): dropped the wrong-shaped `hG0indep`, added `(G0 : ℤ)` + `hGaussZA`/`hGaussZF` (via the new `GaussZResidue` abbreviation in `Phase140Assembly.lean`, matching `phase140_from_residues`'s `hGaussZ` arg verbatim) + `[Nontrivial YC]`; capstone now imports `RStageGammaA` (already root, allowlisted) for the `GammaA` `𝔽₂`-action.  **Remaining = P-16d6e4a** (the (83)-evaluation `∑_{Z¹⧸B¹} sign(Q̄⁰) = G0`, row below). |
| P-16d6e4a | B: **the (83)-evaluation** (the P-12 seam surfacing in the (140) lane) — per source, an explicit `𝔽₂`-iso `e : (Z¹⧸B¹) ≃+ V`-model with `Q̄⁰ = (pinned form) ∘ e` (`q̄` unramified / `qDouble q̄ (U•·)` ramified).  Routes (design §3): **local** — explicit wild/tame cocycle coordinates + `ι_Γ∘graphPullback`-evaluation via the landed invariant-map machinery (`LocalLiftingDuality`, B6); the unramified case kills `H¹_ur` (`V^F = V^{C₀} = 0`); **candidate** — the §6.3–6.5 finite-word ledger through `WordCohBridge` + P-16c4 technology (sequence AFTER P-16d6e5's degree-2 bridge).  **Fallback is legitimate** (the repo's own (83)-precedent): keep `hGaussZA`/`hGaussZF` on the `prop_8_9` ledger and close P-16 modulo them, this ticket remaining open — exactly how §6.2 deferred Prop 6.5 | ⭐⭐⭐ | **F**-design → O-close (per source) | P-16d6e4 (I) ✓-when-landed; local: B6 machinery ✓; candidate: P-16d6e5 | ⊆ {B6, B7} (local); ∅ expected (candidate, word-side) | ◐ **F-DESIGN COMPLETE + bridge (C) LANDED (Opus, 2026-07-08)** — `docs/p16d6e4a-evaluation-design.md` (authoritative).  **Pivotal find (survey)**: the LOCAL source needs NO (83) — `prop_6_18_unramified`/`prop_6_18_ramified` (BOTH sorry-free, `UnramifiedModel.lean:538`/`DeepPart.lean:1428`) already compute `zeroCount(Q0loc)` on `H¹(G_ℚ₂,V) = 2^{2m−1} ∓ 2^{m−1}`, so `gaussSum = ∓2^m = G0` via `gaussSum_eq`.  The local discharge is a **transport** (my `QZero`/`VCocycle` ↔ paper `Q0loc`/`H1`) ⊗ `prop_6_18` — no new deep math: (A) `VCocycle ≃+ Z1` [⚠ SHARED — the e3 agent is building it in `Phase140Local.lean`; land once, don't duplicate], (B) `vCobRange ↦ B1`, **(C) `iotaB = iotaF∘H2ofFun` — LANDED** (`GQ2/IotaBridge.lean` `iotaB_eq_iotaF_of_injective`, std-3, parametrized on `iotaF` injectivity; the remaining `iotaF_injective` = `mapCoeff2_injective` degree-2 infra), (D)/(E) `∑ sign(Q0loc) = gaussSum → prop_6_18` [trivial].  Hypothesis supply at e7: `hfac` tame-factorization (c3-G0 package), form data (`En`), un/ram split (`c tameTau`, source-independent ⟹ shared `G0`).  **CANDIDATE (`Γ_A`) is the genuine remaining seam**: no `prop_6_18`-analog over `Γ_A`; needs (83)-for-Γ_A (word ledger, after P-16d6e5) or the ledger fallback.  **O-close** = the transport (gated on e3's shared bridge) + `mapCoeff2_injective` + hypothesis supply. |
| P-16d6e5 | B: **Γ_A (136) residues** (the anticipated `P-16d6e-A` split) — **`hZcount_A`** (nearly assembled per `docs/p16d6a-handoff.md` §3: `z1Equiv` + `prop_5_15` clause 2 + `RCocycle ≃ Z¹` + `blockRChar_card`) and **`hsep_hom_A`**, which needs the two named §5-lane gaps: **(i)** the (2,0)-corollary — a right-nondegenerate pairing of equinumerous finite elementary 2-groups is perfect (finite `𝔽₂` lin. alg.); **(ii)** the **twisted degree-2 word↔continuous bridge** — extend `WordCohBridge`'s degree-≤1 `z1Equiv` and `WordCoh2`'s central-`𝔽₂` `CentExt`/relator-evaluation foundation to `R`-coefficients (the P-16c lane's technology; `HalfTorsorGammaA.exists_nonzero_varCoc_gammaA` shows the working pattern).  Then `blockStageR136` (✓, hypothesis-threaded) closes `stageR136_A`.  Fallback if (ii) runs heavy: promote `hsep_hom_A` to the `prop_8_9` hypothesis ledger (plan doc §1) and keep this ticket open — the vacuous-`DR` branch and everything else still closes | ⭐⭐⭐ | **F**-design (the degree-2 bridge architecture) → O-close | P-16d6a ✓, P-16c ✓ | ∅ expected (word-side) | ☑ **CLOSED (Fable 5, 2026-07-07)** — **`hsep_hom_gammaA` PROVED; `GQ2/RStageGammaA.lean` sorry-free, REMOVED from `SORRY_ALLOWLIST`; `hsep_hom_gammaA` AND `stageR136_gammaA_of_hcard` `#print axioms` = std-3, NO B-axioms** (`lake build` green 8662).  Route as landed (design doc §5 has the full record): L4 runs frame-abstractly — `push_tameRel`/`push_wildRel` (relators die along ANY continuous `Γ_A →ₜ* G'`, no surjectivity) + `redValues_eq_of_coverLift` over a bare `CentralCover` (corrections are central 2-torsion by the structure fields; landed L1 evaluates both reduced relator values to `r̄₁`; `pair_coverMap`+`zsign` convert to `d(v₁)=d(v₂)`); `sep_word` then yields `d1Fun (markC θ) x = (ofMul v₁, ofMul v₂)`; L5's general correction is the `WordLift` multiplication hom (`mulW`/`projW`/`baseW`, `corrected_tameValue`/`corrected_wildValue`/`d1Fun_base_change`), corrected values die by `hR2` (involutions ⇒ no orientation bookkeeping); descent = `lift_of_relatorFree_marking` (closure-subgroup admissibility + `Marking.descend` + `toHom_hom_univMarking_map`).  Earlier status (superseded) — `GQ2/RStageGammaA.lean` (root-imported, allowlisted, builds green): **`htriv_gammaA` ✓, `hZcount_gammaA` ✓ PROVED** (`z1Equiv` + `prop_5_15` cl.2 + `RCocycle ≃ Z¹` + `blockRChar_card`, std-3), **`stageR136_gammaA_of_hcard` ✓** (the (136) assembly, threading `hcard_A` — decoupled from e6), and the full **marking-route word package L1–L4** (gap (i) = `wTrace`/`sep_word` ✓; gap (ii) dissolved into the relator-correction route: `tameValue_correction`/`wildValue_correction` + `corrMark` calculus ✓, `d1Fun` closed forms ✓).  **ONE sorry left: `hsep_hom_gammaA` (:635)** — the L4/L5 boundary-frame assembly (recipe: `docs/p16d6e-handoff.md` §2, design `docs/p16d6e5-plan.md`).  Remove from `SORRY_ALLOWLIST` when it fills. |
| P-16d6e6 | B: **Γ_A (140) residues + M-lift counts** — **`hH2_A` zero-edge variant** (⚠ `card_H2_gammaA_eq_two` carries a `NoDescent` crutch from its variation-class construction; the zero-edge case needs a nonzero continuous `H²(Γ_A,𝔽₂)`-class WITHOUT it — word-level `#H2w = 2` is free from `prop_5_15` at trivial coefficients, the transport is exactly e5's bridge (ii)); **`hsep_A`/`hpartial_A`/`hZcard_A`** (the `prop_5_15`/`IsSelfDual` analogs of e3's clauses — degree-(1,1) pairing + card clauses + the (2,0)-corollary); **`κ_M^A`** (`#MLifts = \|M_B\|²` — the d6d pattern at `Γ_A`: `z1Equiv` + `prop_5_15` clause 2 + the `MLifts ≃ Z¹` torsor + `Nonempty` via `#H²`-splitting; **independently startable — degree-1 only**); **`κ_I^A`** (NOTE: `hGaussZ` is P-16d6e4/e4a's via `GaussZResidue`, not e6's); plus (post-e4-design) the `hGaussZA` ledger discharge via P-16d6e4a's candidate route — `hG0indep` is DROPPED by the e4 reshape (`docs/p16d6e4-gauss-design.md` §4) | ⭐⭐⭐ | O (after e5's bridge (ii); `κ_M^A`/`hZcard_A` startable now) | P-16d6e2 ✓, P-16d6e5 (the H²-transport parts only) | ⊆ {B9?} (audit at close) | ◐ **`hH2_A` DONE (Opus 2026-07-07) — the `NoDescent` crutch is discharged**: `GQ2/CardH2GammaA.lean` (std-3, NO B-axioms, `lake build` green 8663) proves **`card_H2_gammaA` : `Nat.card (H2 GammaA (ZMod 2)) = 2` UNCONDITIONALLY** via a concrete `NoDescent` witness — the central extension `𝔽₂ → D₈ → 𝔽₂²` (`DihedralGroup 4 ↠ DihedralGroup 2`, `z = r 2`) with `T = M = ⟨s̄⟩`, `q ≡ 0`: `T` is in the (whole) radical, and `r 1` swaps the two `𝔽₂`-lifts `sr 0 ↔ sr 2 = (sr 0)·z`, so neither line complementing `⟨z⟩` in `p⁻¹(T)` is normal (`datum_noDescent`, all finite facts `decide`d).  Fed into `LedgerGammaA.card_H2_gammaA_eq_two` with a surjection `ρ : Γ_A ↠ 𝔽₂²/⟨s̄⟩` (`Marking.descend` of an order-2 marking).  **Also lands the hypothesis-free `stageR136_gammaA`** (drops the `hcard_A` arg of e5's `stageR136_gammaA_of_hcard`; bridges `GA ≡ GammaA` via `card_H2_gammaA_unit`).  **Also `hZcard_A` DONE (Opus 2026-07-07, `GQ2/Phase140GammaA.lean` `hZcard_gammaA`, std-3, NO B-axioms)**: `#Z¹_{Γ_A,ρ'}(V) = #V²` — the `Γ_A` twin of `Phase140Local.hZcard_local` with the candidate count (`VCocycle ≃ Z¹` bridge + `z1Equiv` + `prop_5_15` clause 2 instead of `card_Z1_eq`; `#fixedPts=1` via `card_fixedPts_elemDual_eq_one_of_nontrivial`, `hnt` required). **Also `hsep_A` DONE (Fable 5, 2026-07-07, `GQ2/Phase140GammaA.lean` `hsep_gammaA`, std-3, NO B-axioms, sorry-free)**: the `(T^∨)^C`-separation at `Γ_A` by the **marking route** (the local `prop_5_16` `cup20` route has no `Γ_A` analog): per-character `𝔽₂`-covers `B/ker χ ↠ B/T` (`charCover`, the T-stage `scalarCover` mirror) + the direct `B²`-extraction lift (`exists_lift_charCover`) + e5's `redValues_eq_of_coverLift`/`sep_word`/`WordLift`-correction machinery + the T-stage descent `mlift_of_relatorFree_marking` (the **corestriction trick**: `g_Q` is non-surjective, so the classified `F₄`-hom of the `J̄`-marking descends through `N_A`-kernel-transfer-by-subtype-injectivity to a surjective `ḡ : Γ_A ↠ J̄` feeding `Pro2Core`).  Remaining e6: `κ_M^A` — P-17i M-stage lane (CLOSED there per increment 3); `κ_I^A`/`hGaussZA` (e4/e4a route); `hpartial_A` (mirror of e3's still-open `hpartial_local`). |
| P-16d6e7 | B: **witness + `RecursionInputs` + final splice** (`GQ2/Prop89Close.lean`) — `by_cases hex : ∃ l ≠ zeroDR` (¬hex: (137)–(140) vacuous, `DT := PUnit`, junk covers — only the two `stageR136` fields are live); hex-branch: `DT := ↥(TCharC (En.radData l₀ h₀))` (per-(l,h) types are DEFEQ — `radData`'s `T`/`hT` fields are the same literal terms for every `l`), `phase l h ζ := dite` on the descent-∃ (`phaseChi En l h (descentOf hN) ζ`, else the trivial cover `centralCoverOfCocycle 0 …`), `μ := #V·μ₀` (μ₀: per-ρ constant via `tcocycle_mu_indep (κ_M, κ_I)`, l-indep via `tcocycle_card_l_indep`, source-shared via the twin duality VALUES — e3/e6 outputs), `G0 :=` the ledger-threaded value (e4 reshape: `−2^m` unramified / `+2^m` ramified, via `hGaussZA`/`hGaussZF`; `hG0indep` dropped); `inpF := ⟨stageR136_local ✓, half139_local ✓, phase140_from_residues @ e3+e4⟩`, `inpA := ⟨stageR136_A @ e5, half139_via_radData ✓ + lemma_8_6_gammaA ✓ + κ_M^A @ e6, phase140_from_residues @ e6+e4⟩`; `exact prop_8_9_of …`; **remove `Prop89Close.lean` from `SORRY_ALLOWLIST`**; close P-16d6e → P-16d6 → P-16d → **P-16** on the board.  ⚠ elaboration risks pre-analyzed (session 2026-07-08): the DT-defeq sharing across `(l,h)`, the `dif_pos`-reduction of the dite-phase against the eq140-hypothesis's own descent-proof (proof-irrelevance), the μ-existential plumbing — **write the sorried skeleton FIRST** to validate all plumbing before the residue lanes land | ⭐⭐ | **F**-skeleton → O-close | P-16d6e3, P-16d6e4, P-16d6e5 (or ledger-fallback), P-16d6e6 | B6, B7, B9 | ◐ **SKELETON LANDED (Fable, 2026-07-08 session 3)** — `prop_8_9`'s assembly is PLUMBING-COMPLETE, `lake build` green (8676), **exactly 2 sorries, both Γ_A/e6-gated**.  All three pre-analyzed elaboration risks VALIDATED: **(a)** DT-defeq across `(l,h)` ✓ — `TCharC (En.radData l h) = TCharC (En.radData l₀ h₀)` is `rfl`, the cross-λ `ζ`-typing in `phaseFamily` elaborates; ⚠ GOTCHA: `simpa … using h140` FAILS the final cross-λ defeq match (transparency wall) — use `simp only […]` then bare `exact h140`; **(b)** `dif_pos`-reduction ✓ — `phaseFamily_pos := dif_pos hN` rewrites under the finsum binder, proof-irrelevant in the stored witness; **(c)** μ-plumbing ✓ — `muZero` read at λ₀, `hμ := (tcocycle_card_l_indep _ …).trans (tcocycle_card_local …)`, defeq-accepted.  NEW helpers in `Prop89Close.lean`: `descentOf` (∃→`Descent` choose-unpack; the `radData_noDescent_iff` `Iff.rfl` alignment holds on the nose), `trivialPhaseCover` (zero-cocycle junk cover), `phaseFamily`, `phaseFamily_pos`, `muZero`.  **LIVE**: the ¬hex branch entirely; `hRK`/`hR2` discharged internally (`lemma_7_2` at `T.piY`/`F.alpha`, per plan-ledger) ✓; `hfgA` internal ✓; `hnt` from `hfaith`+`[Nontrivial YC]` ✓; both (136) stages (`RStageLocal.stageR136_local`, `CardH2GammaA.stageR136_gammaA` — NOTE the file-namespace qualifiers); **the ENTIRE local (G_ℚ₂) bundle sorry-free** (`half139_local` + `phase140_local` with `hGaussZF l h` threaded).  **SORRIED (2)**: half139_A (needs e6 `hMcountM_A`; `lemma_8_6_gammaA` ✓ supplies `hlem86M` via `half139_via_radData`) + phase140_A (needs e6's `phase140_gammaA` mirror — `hZcard_gammaA` ✓; `hsep_A`/`hpartial_A`/`tcocycle_card_gammaA` open; consume with `phaseFamily_pos` + the shared `hμ`-transport + `hGaussZA l h`, verbatim as the local branch).  CLOSE checklist (on e6): swap the 2 sorries, `lake build`, `#print axioms prop_8_9` ≤ {std-3, B6, B7, B9} + e6's trace, remove `Prop89Close.lean` from `SORRY_ALLOWLIST` (`scripts/check_axioms.sh:24`), close e7 → e → d6 → d → **P-16**. |
| P-17 | B: §9 — induction on `\|L_Y\|` ⇒ **Thm 4.2 proof** — **decomposed → P-17a–i** (`docs/section9-extraction.md`) | ⭐⭐⭐ | **F** design, O finish | P-11–P-16 | **B1, B3c**, B6, B7, B7′, B8, B9 *(budget +B1: the `hfg` discharge for `G_ℚ₂` at P-17i is the first consumption of `absGalQ2_isTopologicallyFinitelyGenerated`; +B3c via `prop_3_10_local_marked` in the terminal case — flagged, no census change)* | ◐ **F-half ☑ (Fable 2026-07-06 = P-17a)**: `GQ2/SectionNine.lean` skeleton (6 sorried interfaces + relocated `thm_4_2`), design note `docs/section9-extraction.md`, sub-tickets P-17b–i below.  ⚠️ **Two reviewed statement actions**: (1) **`thm_4_2` amended** with `(hE2 : ∀ e : E, e ^ 2 = 1)` — the θ-descent (`lemma_7_3`, whose paper form carries exactly this hypothesis) and the terminal case's odd-kill both need the decoration target elementary-2; §10 consumes Thm 4.2 at `E = 0` only, so downstream-harmless; (2) **`thm_4_2`/`thm_4_2_stratum` relocated** `BoundaryFrame.lean → SectionNine.lean` (proof needs §§5–9 machinery; moved-out pattern) — `BoundaryFrame.lean` now **sorry-free & off the allowlist**, `SectionNine.lean` takes its slot.  Found proved-and-ready: 9.1 (`coprime_fiber_product`), 9.2-core (`oddOrder_twoQuotient_split`), `lemma_7_3`, `lemma_7_1_dual`, P-25's `prop_3_10_*` marked isos.  Lemma 9.3 needs **no §9 statement** (absorbed in P-16d2's obstruction datum). |
| P-17a | B: §9 design — statement skeleton + extraction note + sub-ticket DAG | ⭐⭐ | **F** | P-16d1–d5 | (statements) | ☑ **DONE (Fable 2026-07-06)** — see P-17 parent row. |
| P-17b | B: §9.1 terminal case — `terminal_count_eq` — **decomposed → P-17b1–b3** (a naive dive hits a missing foundation: no result yet that a finite quotient of `Ttame` is 2-nilpotent) | ⭐⭐⭐ | O | P-17a | ∅ (B3c/B8 turned out unneeded) | ☑ **DONE (Opus 2026-07-06)** — **`terminal_count_eq` fully proved, std-3** (all of b1/b2/b3 ☑; `GQ2/SectionNine.lean`, library green 8671, gate census 15).  Foundation: `coprime_card_of_odd_pTwo` + `scalarStack_centralized_of_coprime` + `isScalarStack.normal`.  See P-17b3 below for the (144)-correspondence proof; **B3c/B8 were not needed** (axiom trace is pure std-3). |
| P-17b1 | B: Lemma 9.2 structure — the odd normal lift `Ñ ◁ Y` (odd, centralizes `L_Y`, unique) + the fibre-product iso `Y ≅ H ×_{H₂} Q` (`Q := Y/Ñ` a 2-group), given tame 2-nilpotency (P-17b2).  Tools all ready: `scalarStack_centralized_of_coprime` ✓, `oddOrder_twoQuotient_split` (SZ) ✓, `coprime_fiber_product` (9.1) ✓, **`tame_two_nilpotent` (P-17b2) ✓** | ⭐⭐⭐ | O | P-17b2 ✓ | ∅ | ☑ **DONE (Opus 2026-07-06)** — **`lemma_9_2_core` proved, std-3, sorry-free** (`GQ2/SectionNine.lean`; library green 8670, gate census 15).  Output bundle: `∃ Ñ, ∃ _ : Ñ.Normal, Odd(card Ñ) ∧ IsPGroup 2 (Y⧸Ñ) ∧ Ñ ⊓ L_Y = ⊥ ∧ ⁅Ñ,L_Y⁆ = ⊥ ∧ Ñ.map π_Y = M ∧ Ñ ⊔ L_Y = comap π_Y M` — the fibre-product pieces P-17b3 feeds to `coprime_fiber_product`.  Two supporting theorems: **`sz_odd_complement`** (SZ inside `↥P` for `P = comap π_Y M`, via `exists_right_complement'_of_coprime` + subtype transport of `IsComplement'`) and **`head_two_nilpotent`** (`H` 2-nilpotent from the frame: `gen_ttame_quotient` + `tame_relation` + `tame_two_nilpotent`).  **Key idea for `Ñ ◁ Y`**: `Ñ` = the odd-order elements of `P` (`⁅Ñ,L_Y⁆=⊥` + coprime orders ⟹ direct product), manifestly conjugation-stable.  **Unblocks P-17b3.** |
| P-17b2 | B: **tame 2-nilpotency** — a finite quotient `H` of `Ttame` has `O²(H)` of odd order (`H = ⟨s,t \| tˢ=t²⟩`, `t` odd via `Tame.tame_odd_order` ✓, `⟨t⟩ ◁ H`, `H/⟨t⟩` cyclic ⟹ `H` metacyclic 2-nilpotent).  Lemma 3.1's structural content; new tame-group infra (Foundations-adjacent) | ⭐⭐⭐ | **F**+O | — | ∅ | ☑ **DONE (Opus 2026-07-06)** — **`tame_two_nilpotent` proved, std-3, sorry-free** (`GQ2/SectionNine.lean`, foundation section; whole library green 8670, gate all-pass census 15).  Conclusion `∃ N, ∃ _ : N.Normal, Odd (card N) ∧ IsPGroup 2 (H ⧸ N)` — exactly `oddOrder_twoQuotient_split`'s interface.  Two reusable abstract lemmas landed alongside: **`exists_normal_odd_pow_mem_of_cyclic`** (finite cyclic `Q` has a normal odd `Q₀ = ⟨g^{2ᵃ}⟩` — the odd part — with `∀ x, ∃ k, x^{2ᵏ} ∈ Q₀`; via `orderOf_pow'` + `ordProj`/`ordCompl`) and **`exists_normal_odd_twoQuotient_of_cyclic_quotient`** (odd-normal `C` + cyclic `H/C` ⟹ 2-nilpotent; `N = comap (mk' C) Q₀`, `card N = card C · card Q₀` via `index_comap_of_surjective`, 2-quotient by lifting the membership form).  Tame case = `⟨t⟩` odd+normal (`Tame.*`) + `H/⟨t⟩` cyclic (image of `s`). **Unblocks P-17b1.** |
| P-17b3 | B: (144) correspondence + assembly — `BoundaryLifts b F T ≃ {g : Π ↠ Q // qg = α₂, θ̄g = ψ̄}` per source (universal property + the full `ℤ₂`-marking pinning `H₂`-compatibility, not just parity), source-independence via `prop_3_10_*` ✓, ⟹ `terminal_count_eq` (θ kills odd `Ñ` via `hE2`).  Inputs ready: `lemma_9_2_core` + `head_two_nilpotent` (P-17b1) ✓, `coprime_fiber_product` ✓, `prop_3_10_*` ✓ | ⭐⭐⭐ | O | P-17b1 ✓ | ∅ | ☑ **DONE (Opus 2026-07-06)** — **`terminal_count_eq` proved, std-3, sorry-free** (`GQ2/SectionNine.lean`; library green 8671, gate census 15; `#print axioms terminal_count_eq` = `[propext, Classical.choice, Quot.sound]` exactly).  **(A) `boundaryLifts_equiv_qlifts`** — the (144) correspondence `BoundaryLifts b F T ≃ QLifts …` (source-generic), via `qliftHom` (fibre-product reconstruction through `L92.fibreMulEquiv`) + a **2nd `coprime_fiber_product`** for surjectivity.  **(B) `qlifts_equiv_commonLifts`** — source-independence `QLifts Γ b ≃ CommonLifts` (on `Π`, source-free), via the **maximal-pro-2 universal property** (`compPro2Equiv` = `maxProPHomEquiv` transported by `pro2Iso`) + `b`-surjectivity + `compat*` (H-condition indexed by `∂bd`, θ-condition on `Π`).  **`ker_pro2A`** (`ker pro2A = proPKernel` from `prop_3_10_gammaA` + `topGen_gammaA` generator agreement).  Supporting `L92` infra: `fibreMulEquiv`, `thetaBarQ` (θ descends via `odd_subgroup_le_ker_of_expTwo` — where `hE2` enters), `mkCH`, `discreteTopology_Q`.  **B3c/B8 NOT needed** (confirmed by the pure-std-3 axiom trace): `head_factors_through_nuT`/`Zhat.zpowHat` avoided entirely — the H-condition rides `b`- and `nuT`/`nuTwo`-surjectivity, not a marked iso.  **Deviations**: `terminal_count_eq` gains `[CompactSpace AbsGalQ2] [TotallyDisconnectedSpace AbsGalQ2]` (the codebase convention for `AbsGalQ2`; the §9 induction/`thm_4_2` will thread them — no callers yet); `SectionNine` now imports `GQ2.SectionThreeMarked` (for `prop_3_10_gammaA`). |
| P-17c | B: §9 concrete frame — fill `blockFrame` (quotient targets via `lemma_7_3` ✓ θ-descent + `hE2`; `DR` = the `card_DR` subtype itself; scalar covers `Y/R' ↠ Y/R`, centrality of `z` via the trivial `C`-action on `R/R' ≅ ℤ/2`) + spec lemmas per the work order | ⭐⭐⭐ | O | P-17a | ∅ | ☑ **DONE** (commit `cd4ad4f`, sorry-free std-3) — `blockFrameImpl` in `GQ2/BlockFrameImpl.lean` (193 ln, term-mode anon-constructor so fields are defeq-accessible via `unfold blockFrameImpl`); `blockFrame := blockFrameImpl T Blk hE2`. (Board row was stale; corrected by Opus 2026-07-06.) |
| P-17d | B: §9 concrete enrichment — fill `blockEnrichment` — **DECOMPOSED → d1/d2/d3** (Opus 2026-07-06; scoped against real interfaces).  **📋 FULL HANDOFF: `docs/p17d2-handoff.md`** (d1+d2a+d2c DONE in the two untracked scratch files `GQ2/ScratchP17d{1,2}.lean`; field-by-field `blockEnrichment` assembly recipe; d2b+d3 remaining).  **Two findings**: (i) `blockEnrichment` **must gain `(cH : ContinuousMonoidHom Ttame H) (hcH : Surjective cH)`** — `prop_7_4` needs the tame quotient (`lam_sq_vanish`, SectionSeven:2600); benign (no callers; `prop_8_9_aux` carries `F`, so the induction passes `F.alpha`).  (ii) `lemma_7_1_head : R ≤ K⊓S` ✓ ⟹ `MB/TBsub ≅ P/S`, so set **`Vmod := Additive(P/S)`** (where `prop_7_4`'s `qbar` and `blockAction` live natively).  Concrete frame (`blockFrameImpl`): `YB=Y/R`, `MB=K.map(mk' R)`, `TBsub=((K⊓S)⊔R).map(mk' R)=(K⊓S).map(mk' R)`, `DR`=index-≤2 normal ≤ R, per-λ `scalarCover` (`z=mk' l r₀`, `ker p=⟨z⟩`). | ⭐⭐⭐ | O | P-17c ✓, P-17e ✓ | ∅ | ☑ **CLOSED (Opus 2026-07-06; committed `b7f47cf` 2026-07-07)** — `SectionNine.blockEnrichment` LANDED via all of d1/d2a/d2b/d2c/d3 (build-tree modules `GQ2/Block{Descent,Char,FormFields,Enrichment}.lean`, root-imported; scratch deleted; `SectionNine.lean` stub → pointer).  `lake build GQ2.BlockEnrichment` green (8672); `#print axioms blockEnrichment` = **pure std-3** (the `kappa0_exists`/P-17e sorry is gone — P-17e closed).  Sig gains `F : BoundaryFrame H E` (`cH := F.alpha`).  The earlier "◐ decomposed / uncommitted" state is resolved. |
| P-17d1 | B: **descent structure** — `Vmod := Additive(Blk.P ⧸ Blk.S.subgroupOf Blk.P)` + instances (`addV`/`finV`); `actV : DistribMulAction (Y/K) Vmod` from `BlockModule.blockAction Blk.S Blk.P` (Y-conjugation on P/S) descended through `Y↠Y/K` (`K` acts trivially: `[K,P] ≤ [P,P] ≤ S`); `descend : MB →* Multiplicative Vmod` (`k·R ↦ k·S`, well-def as `R ≤ S`) + `descend_surj`/`descend_ker` (`= TBsub`)/`descend_conj`.  Self-contained (no `prop_7_4`).  **BUILD RECIPE (Opus 2026-07-06 scoping)**: ① **linchpin `commutator_P_le_S : ⁅Blk.P,Blk.P⁆ ≤ Blk.S`** — `⁅P,P⁆⊔S` is Y-normal, `S ≤ · ≤ P`; by `Blk.chief` = `S` or `P`; not `P` else `P/S` (nontrivial 2-group: `hP2` from `B.h2L`, `hQnt` from `hSP` — pattern already at SectionSeven:283–297) is **perfect**, impossible for a nontrivial finite 2-group (nilpotent ⟹ `⁅Q,Q⁆ < ⊤`; via p-group `center_nontrivial`/`IsPGroup.isNilpotent`+derived-series, or a `↠ ℤ/2` quotient). ② **`CommGroup (P/S)`** via `{ (inferInstance:Group ·) with mul_comm := … }` from ① (copy `letI` pattern at **SectionSeven:1321–1332**). ③ `AddCommGroup(Additive(P/S))` free. ④ **`DistribMulAction Y (Additive(P/S))`**: `blockAction` is a `MulAction`; `smul_add`/`smul_zero` hold since `y•` = the group hom `QuotientGroup.map(conjHom …)`. ⑤ **descend to `Y/K`** via `DistribMulAction.compHom` along `mk' K` — `K` acts trivially by `[K,P] ≤ ⁅P,P⁆ ≤ S` (①+`K≤P`). ⑥ `descend`/props: quotient maps (`R ≤ S` from `lemma_7_1_head`).  **PROGRESS (Opus 2026-07-06): ①–④ + the descent's key fact DONE & verified** in `GQ2/ScratchP17d1.lean` (banked WIP, not yet spliced): `commutator_P_le_S` ①, `blockPS_commGroup` ②, `blockActVY : DistribMulAction Y (Additive(P/S))` ④ (`{ blockAction … with smul_zero := map_one, smul_add := map_mul }`), `blockK_smul_eq` (`k∈K ⟹ k • q = q`, ⑤'s content).  **UPDATE (Opus 2026-07-06): ⑤ DONE too** — `blockActV : DistribMulAction (Y/K) (Additive(P/S))` verified (`compHom` is the wrong direction — no `Y/K →* Y`; built manually via `Quotient.liftOn'` + `blockK_smul_eq` for well-def, laws by `QuotientGroup.induction_on`).  **So the whole module structure `Vmod`/`addV`/`finV`/`actV` is DONE** (5 verified defs in `ScratchP17d1.lean`; `finV` = `inferInstance`).  **DONE (Opus 2026-07-06): ⑥ `descend` + all 3 props complete** — the whole of d1 is verified `std-3` clean (`#print axioms` = `[propext, Classical.choice, Quot.sound]` for `blockDescend_conj`/`_ker`/`_surjective`/`commutator_P_le_S`/`blockActV`) in `GQ2/ScratchP17d1.lean` (banked, not yet spliced; 18 verified decls = 5 module + 13 descend).  Route actually taken (cleaner than the `quotientKerEquivRange`+transport plan — **corestrict**, avoiding any subgroup-equality transport): `blockKappa := ((mk' R)∘K.subtype).codRestrict MB … : ↥K ↠ ↥MB` (`blockKappa_surjective` by `Subgroup.mem_map`); `blockAlpha := (mk'(S.subgroupOf P))∘(inclusion K≤P) : ↥K →* P/S`; `blockKappa_ker_le_alpha` (`[k]_R=1 ⟹ k∈R≤S ⟹ [k]_S=1`); `blockDescend := (lift blockAlpha _) ∘ (quotientKerEquivOfSurjective blockKappa _).symm`; **characterizing identity `blockDescend_kappa : descend∘κ' = α`** (via `kerLift_mk`=`rfl`) drives all 3 props.  `blockDescend_surjective` (`Blk.gen : K⊔S=P` + `Subgroup.mul_normal`); `blockDescend_ker` (`descend m=1 ↔ m∈TBsub`, both `↔ (k:Y)∈S` using `Subgroup.comap_map_eq`+`sup_eq_left.mpr (lemma_7_1_head)` collapsing `TBsub=(K⊓S).map(mk' R)`); `blockDescend_conj` (equivariance `descend(b·m·b⁻¹)=π_BC(b)•descend(m)`, additive form: lift `m`,`b` to `K`/`Y`-reps, both sides reduce to `⟦y k y⁻¹⟧_S` via `blockAction_smul_mk`+`conjHom`, closes by `congr 2`+proof-irrel; `blockPiBC`/`blockActV_mk'`/`blockActVY_mk` helpers).  Note ① used `IsPerfect.not_isNilpotent` (`namespace Group`) + `IsPGroup.isNilpotent`.  **Instance note for splice**: the standalone scratch supplies `[(Blk.R).Normal] [(Blk.S.subgroupOf Blk.P).Normal] [(Blk.K).Normal]` as section-variable instance binders (needed because they appear in the `Y⧸R`/`Y⧸K`/`P/S` **types**); at the real splice these are fixed by the frame `RF`, so the binders disappear (supply via `haveI := Blk.hR/hS/hK`).  **Next: splice into `blockEnrichment` (with the `cH/hcH` sig extension) as part of d2/d3 assembly.** | ⭐⭐ | O | P-17c | ∅ | ☑ DONE |
| P-17d2 | B: **form fields** — **DECOMPOSED → d2a/d2b/d2c** (Opus 2026-07-06, scoped against `prop_7_4`/`mForm_of_qbar`/`QuadraticFp2`).  **CORRECTED FINDING (2026-07-06)**: `hquad`/`hns` ARE derivable from `prop_7_4`'s *output* alone (`qbar`+hspec+nonzero+Y-inv) — **no `SectionSeven` privates needed**; both live in a **new file consuming `prop_7_4`'s output** (NOT edited into co-owned `SectionSeven`).  The polar form collapses: `β(⟦a⟧,⟦b⟧)=qbar(ab)+qbar a+qbar b = λ([b,a])` (single conjugated commutator, via `(ab)²(a²b²)⁻¹=a[b,a]a⁻¹`), so **biadditivity needs only `hlam_hom`+`hlam_conj`** (commutator identity `[w,uv]=[w,u]·u[w,v]u⁻¹`), NOT the vanish lemmas.  `hns` uses public `Blk.chief` (radical-submodule/simplicity) + `lemma_7_1_dual`.  **d2a ☑ + d2c ☑ DONE** (all `std-3`, `GQ2/ScratchP17d2.lean`); only **d2b** (assembly of `qbar`/`q`/`hrad`/`hTzero`/`hqbar`/`hinv`/`hq`) remains, best done with the d3 splice. | ⭐⭐⭐ | O | P-17d1 | ∅ | ☑ **CLOSED (Opus 2026-07-06; committed `b7f47cf`)** — d2a/d2b/d2c all landed: `blockLam` + `blockQbar`/`blockQ`/`hq`/`hqbar`/`hrad`/`hTzero`/`hinv` + `hquad`/`hns`, in build-tree `GQ2/BlockChar.lean` + `GQ2/BlockFormFields.lean` (consolidated from the deleted scratch), all std-3, consumed by `SectionNine.blockEnrichment` (P-17d3 ✓). |
| P-17d2a | B: **`blockLam` character** — `blockLam B l : ↥Blk.R → 𝔽₂ := if (r:Y)∈l then 0 else 1` (index-2 indicator of `l≤R`), the `lam` input to `prop_7_4`/`mForm_of_qbar`.  **DONE (Opus 2026-07-06)**, verified `std-3` in `GQ2/ScratchP17d2.lean` (banked): `blockLam_hom` (additive, via `EvensKahn.mul_mem_iff_of_index_two` on `(l.subgroupOf R).index=2`), `blockLam_conj` (Y-conj-inv, via `l.Normal`), `blockLam_ne` (nonzero, via `SetLike.exists_of_lt`), `relIndex_two_of_le` (DR `≤2`+proper ⟹ `=2`, reusing the `BlockFrameImpl:99` pattern), `blockLam_eq_zero_iff`.  Needs `open scoped Classical` (noncomputable `if`).  Splice: instantiate `l:=l.1`, `hidx` from `relIndex_two_of_le` + `l.2.2.2`, `l.1≠R` from `h:l≠zeroDR` via `Subtype.ext`. | ⭐ | O | — | ∅ | ☑ DONE |
| P-17d2b | B: **`qbar`/`q`/`hrad`/`hTzero`/`hinv` + `hq`/`hqbar` wiring** — mostly assembly of existing lemmas: `qbar l h := (prop_7_4 π hπ hkerπ cH hcH B hRN hsq (blockLam …) …).choose` (+ hspec/nonzero/`hinv`=3rd conjunct); `q l h := (mForm_of_qbar B (mk' R) ker_mk' (blockLam …) … qbar hspec).choose` (+ `hrad`/`hTzero` clauses).  `hqbar : q m = qbar (toAdd (descend m))` — couples `mForm` value-clause (`qM(π_B k)=lam(k²)`) with `hspec` (`lam(k²)=qbar(mk⟨k⟩)`) through d1's `blockDescend` (`descend([k]_R)=[k]_S`).  `hq` (cover square `x̃²=z^{q}`) — **frame-specific**, from `blockFrameImpl`'s `scalarCover` (`z=mk' l r₀`, `z_sq`, `ker_eq`); done at the splice.  Needs the `cH/hcH` sig extension.  Best done together with the d3 assembly. | ⭐⭐ | O | P-17d2a, P-17d1 | ∅ | ☑ **field lemmas DONE (Opus 2026-07-06)** — all in `GQ2/ScratchP17d2b.lean` (untracked scratch, olean built), **std-3 verified** (`#print axioms` ⊆ `{propext,Classical.choice,Quot.sound}`, no sorryAx/no B-axioms; **`prop_7_4` is now itself std-3** — `lam_sq_vanish` was discharged since the handoff). Stated over `l : BlockDR T Blk` (=`RF.DR` defeq) + `hlne : l.1 ≠ Blk.R` (=`l ≠ RF.zeroDR`), carrying `(cH : ContinuousMonoidHom Ttame H)(hcH)`. **Defs**: `blockProp74`/`blockQbarRaw` (Prop 7.4 `.choose`), `blockMForm`/`blockQ` (`mForm_of_qbar` `.choose`), `blockQbar` (`Vmod=Additive(P/S)` form), `blockScalarCover` (=`RF.scalarCover`). **Field lemmas**: `blockHspec`/`blockHne`/`blockHinvRaw`/`blockHval`/`blockHrad`/`blockHTzero` (direct `.choose_spec` clauses), `blockHquad`/`blockHns` (P-17d2c packaging `isQuadraticFp2_of_mul`/`nonsingular_of_mul`), `blockHinv` (`IsInvariant (Y/K)` via `blockActV_mk'`+`blockActVY_mk`), `blockHqbar` (couples `blockHval`+`blockHspec` through `blockDescend_kappa`), `blockHq` (cover square, via `blockScalarCover_p`+`blockZ_pow_lam`+`eq_one_or_z_of_mem_ker`; key trick: ascribe the `k`-lift to the cover type `set kk : (…).cover := mk' l.1 k` to avoid `HMul cover (Y⧸l)` mismatch). **Field-fit VALIDATED** by `example`s: `blockQ` fits the `q` field and `blockHq` fits the `hq` field on the nose (all `RF.DR`/`RF.MB`/`RF.scalarCover` defeqs hold). **`cH` source for the sig extension = `F.alpha`** (`F : BoundaryFrame H E`, `F.alpha : ContinuousMonoidHom Ttame H` + `F.alpha_surjective`; §9 caller carries `F`). **Remaining (→ P-17d3)**: the actual `blockEnrichment` record splice (drop these into the fields via `fun l h => …`, wrap the d1 `descend`/`descend_conj` ofAdd/toAdd glue), `dat`/`hdat` via `kappa0_exists` (needs `hsimple`/`htame`, rides P-17e), and the `blockEnrichment` signature gaining `cH/hcH` (or `F`). |
| P-17d2c | B: **`hquad`/`hns`** (the new math) — new file consuming `prop_7_4` output (NOT `SectionSeven`).  **`hquad` DONE (Opus 2026-07-06)**, verified `std-3` in `GQ2/ScratchP17d2.lean` (banked): `comm_mem_R_of_K` (`[b,a]∈R` via `a[b,a]a⁻¹=(ab)²(a²b²)⁻¹`+`hsq`); `blockQbar_beta` (**`β=λ([b,a])`**, the linchpin); `blockQbar_map_zero` (`qbar 1=0`); `blockQbar_polar_add` (**biadditivity**, via `β` + commutator identity `[w,uv]=[w,u]·u[w,v]u⁻¹` + `hlam_hom`/`hlam_conj` — NO vanish lemmas); `isQuadraticFp2_of_mul` (generic `CommGroup`-form→`IsQuadraticFp2 (·∘toMul)` packaging); helpers `mkK_mul`/`exists_K_rep`.  Splice: `hquad l h := isQuadraticFp2_of_mul qbar (blockQbar_map_zero …) (blockQbar_polar_add …)` with `G=P/S` (d1's `blockPS_commGroup`).  **`hns` DONE too (Opus 2026-07-06)**, verified `std-3` in the same scratch: instead of `RadP : Subgroup ↥P` + `map`/`comap` correspondence, built the radical **directly as `radSub : Subgroup Y`** = `{y∈P \| ∀q∈P, βP(y,q)=0}` via a total extension `qbP y := if y∈P then qbar⟦y⟧ else 0` and `betaP y q := qbP(yq)+qbP y+qbP q` (avoids all dependent-membership-in-carrier pain).  Pieces: `qbP`/`qbP_mem`/`mkP_mul`; `beta_conj` (`β(g•a,g•b)=β(a,b)` from `hinv`); `betaP`/`betaP_biadd`/`betaP_one`; `radSub`+`radSub_le_P`/`S_le_radSub`/`radSub_normal`; `blockQbar_nonsingular_mul` (contradiction: `chief`⟹`radSub=P`⟹`β≡0`⟹`qbar` additive `hadd`); `additive_qbar_absurd` (**endgame**: `Xk:={k∈K \| qbar⟦k⟧=0}` Y-normal via `hinv`, `R≤Xk` via `R≤S`⟹`⟦r⟧=1`, `[K:Xk]=2` via `Subgroup.index_eq_two_iff` + witness `k₁` with `qbar⟦k₁⟧=1` — NO hom/`index_ker` machinery — contradicting `lemma_7_1_dual`); `nonsingular_of_mul` (generic mul→`Nonsingular(·∘toMul)` packaging).  Splice: `hns l h := nonsingular_of_mul qbar (blockQbar_nonsingular_mul …)` with `G=P/S`.  **ENTIRE d2c (hquad+hns) COMPLETE**, ~21 decls, all `std-3`, NO `SectionSeven` edits, NO new axioms. | ⭐⭐⭐ | O | P-17d2a | ∅ | ☑ DONE |
| P-17d3 | B: **κ⁰ discharge + assembly** — `hsimple : IsSimpleModTwo (Y/K) Vmod` (`Blk.chief` + `nontrivial_action`); `htame : ActsThroughTame (Y/K) Vmod` (head `H` via `Y/K ↠ Y/L_Y ≅ H`, `[K,P]≤S` + `FoxH.lemma_5_12` ✓ + `gen_ttame_quotient` ✓ + `tame_relation` ✓); `dat`/`hdat` from `kappa0_exists` (rides P-17e sorry); assemble `blockEnrichment`. | ⭐⭐⭐ | O | P-17d1, P-17d2, P-17e | ∅ | ☑ **CLOSED — `SectionNine.blockEnrichment` LANDED (Opus 2026-07-06)**, full library green (8684 jobs), `check_axioms` pass (census **15**, unchanged). `#print axioms SectionNine.blockEnrichment` = std-3 + `sorryAx` — the **only** `sorryAx` flows through `kappa0_exists` (P-17e), no B-axioms; **`blockHsimple`/`blockHtame` are pure std-3**. Landed as build-tree modules (scratch files consolidated + deleted): **`GQ2/BlockDescent.lean`** (d1 descent), **`GQ2/BlockChar.lean`** (d2a `blockLam` + d2c quad/nonsing packaging), **`GQ2/BlockFormFields.lean`** (d2b `blockQ`/`blockQbar`/`blockHq`/…), **`GQ2/BlockEnrichment.lean`** (d3: `blockHsimple`, `blockHtame`, `blockKappa0`, and `blockEnrichment` itself — in `namespace SectionNine` to keep the FQN, sitting downstream of `SectionNine` since it consumes `kappa0_exists`/`ActsThroughTame`). The sorried stub was removed from `SectionNine.lean` (pointer comment left; no callers); `GQ2.lean` gains `import GQ2.BlockEnrichment`. **`hsimple`**: `blockPS_exp2` (exp-2 via `exists_K_rep`+`k²∈R≤S`) + `Blk.chief` under the `W ↦ WP ≤ ↥P ↦ X ≤ Y` correspondence (`X` Y-normal via `blockActVY_mk`). **`htame`**: `blockLY_smul_eqY` (`L_Y/K` normal-2 acts trivially, `FoxH.lemma_5_12`) ⟹ `blockActLY` (descend Y-action to `Y/L_Y`) ⟹ transport to head via `e : Y/L_Y ≃* H` (`MulEquiv.ofBijective` of the `T.piY`-lift) + `DistribMulAction.compHom`; generators `F.alpha tameSigma/tameTau` (`gen_ttame_quotient`+`tame_relation`). **Signature: `F : BoundaryFrame H E`** (`cH := F.alpha`, `hcH := F.alpha_surjective`, tame generators). **⇒ P-17d + P-17d1/d2/d2a/d2b/d2c all fully closed.** Only `sorryAx` remaining is `kappa0_exists` (P-17e). |
| P-17e | B: κ⁰ base-class existence — `kappa0_exists` = **paper Lemma 6.3** (simple tame `V`; statement **amended** with `hsimple`/`htame`, deviation 4 in `docs/section9-extraction.md`) | ⭐⭐⭐ | O (F-escalation **resolved**) | P-17a | ∅ | ☑ **CLOSED via P-17e5 (2026-07-06): `kappa0_exists` PROVED std-3 (no sorryAx, no B-axioms) — final assembly in `GQ2/KappaNormalForm.lean`, see the P-17e5 row.**  ◐-history: **RESTATED + core proved** (escalated Opus → **resolved Fable, 2026-07-06**; `docs/p17e-kappa0-scoping.md`). **Verdict hardened:** the P-17a general form was **false**, not just unproved — a datum ⟺ a lift of `ρ : C → O(q)` through `1 → V^∨ → Aut(E_f) → O(q) → 1` (Lemma 6.1's own equivalence, `E_f` extraspecial `2^{1+2n}`), and for `C = O(q)` that extension is **non-split** (Griess, Pacific J. Math. 48 (1973), = `H²(O^ε_{2n}(2), V) ≠ 0`) for large `n`; small ranks split, so no tiny counterexample. Existence is the paper's **Lemma 6.3** (simple self-dual **tame** `V`), whose hypotheses the amendment restores: `hsimple : FoxH.IsSimpleModTwo C V` (existing predicate) + `htame : ActsThroughTame C V` (new def, the `tame_two_nilpotent` `(hgen,hrel)` interface + surjective compat `π`). Self-duality is implied by `hns`+`hinv`; statement now TRUE and **dischargeable at the sole consumer P-17d** (`hsimple` ⟸ `Blk.chief`+`nontrivial_action`; `htame` ⟸ head factorization via `[K,P] ≤ S` + the proved `FoxH.lemma_5_12`); datum-choice independence downstream is `repIndep` ✓ (Lemma 6.4), so ∃-form is right. **Landed (std-3, Ax ∅, `GQ2/SectionNine.lean`):** `isEquivariantFactorSet_of_invariant`, `_of_biadditive_invariant`, `IsEquivariantFactorSet.add`, `.comap` (pullback (77)), `.comapHom` (reduction to the tame image), `kappa0_exists_of_split`, `ActsThroughTame`, restated `kappa0_exists` (sorried). Library green (8672-ish), census 15. **Remaining → sub-tickets:** P-17e1 split pair (unramified branch: `H_V` odd via `central_pow2_smul_trivial` ✓ + Maschke or direct SZ — start here; ramified: **Lemma 6.11** projectivity, the long pole), P-17e2/e3 orbit data ((75)/(76) via the biadditive entry ✓; Lemma 6.2 involution datum), P-17e5 invariant normal form on `𝔽₂[H]^N`.  **Decomposed → P-17e1–e5 (rows below).** |
| P-17e1 | B: κ⁰ **odd/unramified case** — datum existence when the acting group has odd order (the ramified branch is now its own ticket P-17e4) | ⭐⭐⭐ | F+O | P-17e | ∅ | ☑ **DONE** (Opus, 2026-07-06, std-3, `GQ2/SectionNine.lean`; library green 8671, census 15). **Realisation:** the odd case needs **no embedding/Maschke/orbit data at all** — just *average the bilinear form*.  Two theorems, both **sorry-free, Ax ∅**: **`exists_biadditive_refinement`** (a char-2 `IsQuadraticFp2` form on a finite exp-2 module is the diagonal `f v v = q v` of a biadditive `f` — via Mathlib `QuadraticMap.toBilin`'s upper-triangular companion, packaging `q` as a `QuadraticMap (ZMod 2) V (ZMod 2)` with companion the bundled `polar q`, mirroring `Transgression.bflat_bijective`); and **`kappa0_exists_of_odd`** (`Odd (Nat.card H)` ⟹ datum exists with `m = 0`: `f v w := ∑_{h} f₀(h•v)(h•w)`, invariant by reindexing `h ↦ h·g`, diagonal `∑_h q(h•v) = ∑_h q(v) = \|H\|·q(v) = q(v)` since `\|H\|` odd `= 1` in `𝔽₂`, closed by `isEquivariantFactorSet_of_biadditive_invariant` ✓).  This IS the vanishing of `H²(odd, 2-torsion)` in elementary averaged-cochain form.  Fed to `kappa0_exists`'s unramified lane via `IsEquivariantFactorSet.comapHom` ✓.  **The ramified branch** (`H_V` has a nontrivial 2-part, averaging fails, needs Lemma 6.11 projectivity) is **P-17e4**; `unram_odd`/`quotient_average` in `SectionSeven` give `Odd` in the unramified case for the P-17i wiring. |
| P-17e2 | B: κ⁰ **orbit data (square/free)** — `squareOrbitDatum`/`freeOrbitDatum` are `IsEquivariantFactorSet` for their square maps (75)/(76) | ⭐⭐ | O | P-17e | ∅ | ☑ **DONE** (Opus, 2026-07-06, std-3, `GQ2/SectionNine.lean`; library green 8671, census 15). **`isEquivariantFactorSet_squareOrbitDatum`** and **`isEquivariantFactorSet_freeOrbitDatum`**, both **sorry-free, Ax ∅**, via the (now polar-free) entry point `isEquivariantFactorSet_of_biadditive_invariant`: each reduces to biadditivity (`show` the coordinate form since `RegRep N` is a type synonym, then `finsum_add_distrib` with `Set.toFinite`) + permutation-invariance (`finsum_comp_equiv` along `Equiv.mulLeft c⁻¹`; the free case reassociates `c⁻¹·(h·ḡ)` first).  **Bonus refactor:** `isEquivariantFactorSet_of_biadditive_invariant` now **derives `hpolar` internally** (`polar q v w = f(v+w)(v+w)+f v v+f w w = f v w+f w v` in char 2), so it takes only biadditivity + diagonal + invariance; `kappa0_exists_of_odd` updated to match.  **The involution datum** (Lemma 6.2, nonzero `m`) is **P-17e3**. |
| P-17e3 | B: κ⁰ **involution datum** — `invOrbitDatum` (Lemma 6.2, eqs (67)–(70)/(73)) is `IsEquivariantFactorSet` with its **explicit nonzero `m`** (orientation bookkeeping via `⟨g⟩`-coset transversals `R_g`).  Harder than e2 (`m ≠ 0`, `hquad`/(60) need the transversal ledger); `lemma_6_15`'s (105) already exercises this datum, so some infra may be reusable | ⭐⭐⭐ | O | P-17e | ∅ | ☑ **DONE & std-3 (Fable 2026-07-06)** — **`GQ2/InvolutionDatum.lean`** committed **sorry-free** (`827cc17`), `#print axioms` = std-3 (**Ax ∅**), imports only `OrbitData`.  `isEquivariantFactorSet_invOrbitDatum` proves **all 8 `IsEquivariantFactorSet` fields** of Lemma 6.2 incl. the nonzero-`m` identities (71) `m_quad` / (72) `m_mul`.  Reusable transversal infra: `piEquiv` (π_c bijection), `zpowers_sq_dichotomy` (⟨ḡ⟩={1,ḡ}), `coset_out_decomp` (**67**), `square_term_out` (ḡ²=1 shift-invariance of x_a·x_{aḡ}), `piEquiv_mul`/`piElt_mul` (**74** `π_{cd}=π_d∘π_c`), `epsFun_mul` (**74** `ε_{cd}=ε_c+ε_d∘π_c`, 4-case g-exponent bash).  Statement needs `gbar*gbar=1`.  Build-graph wiring (import from the κ⁰ orbit-sum, P-17e5) is that ticket's step. |
| P-17e4 | B: κ⁰ **ramified split embedding** — Lemma 6.11, = the discharge of the **shared paper node `lemma_6_11`** in `GQ2/RegularSummand.lean` (created under P-15f1; also feeds `lemma_6_17_dim`) | ⭐⭐⭐ | F | P-17e | ∅ | ☑ **CLOSED 2026-07-06 (Fable) — `lemma_6_11` FULLY PROVED: `#print axioms` = exactly std-3 (no `sorryAx`, no B-axioms); `GQ2/RegularSummand.lean` sorry-free & OFF the `SORRY_ALLOWLIST`; whole library green (8679), census 15, guard green.**  **Final increment — the involution leaf `involution_fixedPoints_sq_le` DISCHARGED** by an explicit **𝔽₂-rational trace element** (recorded deviation from the paper's `𝔽̄₂` weight-orbit route, and simpler than the handoff's étale-factor plan — no idempotents, no semilinear algebra, no transversal-choice induction): with `t := c τ` (odd order `m`, `⟨t⟩ ⊴ C`) and `ω = g₀^{2^{s−1}}`, conjugation gives `ωtω⁻¹ = t^q`, `q² ≡ 1 (mod m)`.  *Case `t^q = t`*: `ω` lies in the 2-torsion of the **abelian** centralizer `C_C(⟨t⟩)` (abelian: `⟨t⟩ ≤ Z(D)` + `D/⟨t⟩` ↪ cyclic `C/⟨t⟩`, via `MonoidHom.isMulCommutative_of_isCyclic_of_ker_le_center`) — a normal 2-subgroup of `C`, which acts trivially by orbit-counting (`IsPGroup.exists_fixed_point_of_prime_dvd_card_of_fixed_point`) + simplicity (`two_torsion_of_centralizer_eq_one`, the O₂-linchpin of Rk 6.12) — contradicting `hfaith` + `ω ≠ 1`.  *Case `t^q ≠ t`*: `g := gcd(q−1,m)` is a **unitary divisor** (`coprime_sub_one_div_gcd`: for a prime `p` dividing both `q−1` and `m/g`, `p^e ∥ m` divides `(q−1)(q+1)` but `p ∤ q+1` since `m` odd, so `p^e ∣ g` — factorization bookkeeping), hence on `u := t^g` of order `r := m/g` mult-by-`q` is a **fixed-point-free involution of `(ZMod r)∖{0}`** (fpf = unit cancellation, `ZMod.isUnit_iff_coprime`); every `t^k ≠ 1` has zero fixed space (**`fixedPoints_zpowers_tame_eq_zero`** — generalizes `fixedPoints_tame_inertia_eq_zero`: conjugates of `t^k` are powers of `t^k`, `⊤` killed by `hfaith` instead of `hram`), so the geometric sum vanishes (**`sum_range_orderOf_smul_eq_zero`**), and for `ω`-fixed `v` the transversal sum `w := Σ_{k ≠ 0, k.val < (qk).val} u^{k.val}•v` (val-comparison picks one element of each pair `{k, qk}` — canonical, no choice, no induction) satisfies `w + ω•w = Σ_{k≠0} u^{k.val}•v = v`; so `ker(1+ω) ⊆ range(1+ω)` and `#V^ω ^2 ≤ #ker·#range = #V` by first-iso counting (`AddSubgroup.card_eq_card_quotient_mul_card_addSubgroup` + `QuotientAddGroup.quotientKerEquivRange`).  **Statement amendment (documented)**: `involution_fixedPoints_sq_le` gained `hs1 : 1 ≤ s` — the leaf was FALSE as previously stated for `s = 0` (`ω = g₀^0 = 1`; counterexample: the Frobenius group `F₂₁ = C₇⋊C₃` acting on `𝔽₈` is ramified simple faithful with trivial Sylow-2, `#V^ω ^ 2 = #V² > #V`); the sole consumer `card_fixedPoints_pow_le_of_ramified` (statement UNCHANGED) now case-splits `s = 0`, where the bound is plain subtype counting.  Doc updates: file-header status → PROVED, `sylow_free_of_ramified`/`lemma_6_11`/EquivariantLift docstrings de-sorried, `HomCounting.lean` header note refreshed, allowlist entry swapped for a removal note.  New gotchas banked: `Nat.dvd_sub'`→`Nat.dvd_sub`, `Nat.odd_iff_not_even`→`Nat.not_even_iff_odd` (renames); `commutative_of_cyclic_center_quotient` deprecated → `MonoidHom.isMulCommutative_of_isCyclic_of_ker_le_center` (returns `IsMulCommutative`; extract with `.is_comm.comm`); `rw [← huord]` under a `ZMod r`-typed binder is a dependent-motive trap — instantiate `pow_mod_orderOf` in a standalone `have` and rewrite THAT with `huord`; `set q := …` blocks `obtain ⟨k, rfl⟩` substitution (let-bound fvar) — destructure to `hk` and `rw [hk]`; `IsPGroup.exists_fixed_point_of_prime_dvd_card_of_fixed_point` takes the action type EXPLICITLY; `rw [hD] at ha'` is motive-unsafe when `a : ↥D` occurs — pass `a.2` directly (set-fvar is defeq-transparent).  (◐-history: **REDUCED TO THE WEIGHT-ORBIT KERNEL** (Fable, 2026-07-06, `GQ2/RegularSummand.lean`; library green 8671, census 15).  **Landed, sorry-free, std-3 / Ax ∅:** **`regular_summand_of_subgroup_summand`** — the paper's "Sylow criterion" as a concrete odd-index **relative trace**: a `P`-equivariant split pair `(j, q)` into a regular `𝔽₂[P]`-module with `Odd P.index` yields the `C`-level pair, via `ι v n g := j (g⁻¹ • v) n 1` and `ρ F := ∑ᶠ_{gP ∈ C/P} g • q (F(g·—))` (coset summand well-defined by `Quotient.lift` + `q`-equivariance; `ρ∘ι = [C:P] • id = id` by odd index + 2-torsion; equivariance by reindexing `C ⧸ P` along `x ↦ h⁻¹ • x`).  No induced-module formalism — all function-level.  **`lemma_6_11` is now ASSEMBLED** (Sylow 2-subgroup + `Sylow.not_dvd_index` odd index + the trace): its `sorryAx` enters only through the isolated kernel **`sylow_split_pair_of_ramified`** (new sorried statement)  **Kernel re-posed (Opus, same day):** the sole remaining sorry is now the clean, standard statement **`sylow_free_of_ramified`** — *`V\|_P` is equivariantly `≃+` a regular module `𝔽₂[P]^r`*, i.e. free of rank `r` over `𝔽₂[P]`; `sylow_split_pair_of_ramified` is now **proved from it** (`j := φ`, `q := φ⁻¹`, retraction-equivariance by `φ`-injectivity, `q∘j=id` by `symm_apply_apply`).  So the residual `sorryAx` is exactly the weight-orbit freeness — a clean 𝔽₂[P]-module statement (Mathlib-shaped), not the bespoke split-pair conditions.  The freeness itself remains the genuine long pole (Clifford over `𝔽̄₂` + descent; limited Mathlib support). — the paper's pp. 29–30 weight-orbit argument (base-change to `𝔽̄₂`, Clifford at a nontrivial inertia character, 1-dim weight lines, `P`-stabilizer trivial by 2-power-scalar + faithfulness ⟹ `P` free on the weight lines ⟹ regular basis, descend to `𝔽₂`) — the true long pole, F-class rep theory.  Gotchas banked: dependent-motive `rw` on `g'` (occurs in the membership proof) — use `congrArg (F n)` + `group` instead; annotate `↥P`-binders in inner lambdas (elaborator drifts to `C`).  **Kernel toolbox landed (Fable, same day, std-3, Ax ∅):** `quotient_zpowers_isCyclic_of_tame` (`C/⟨t⟩` cyclic, generated by `s̄`), **`isCyclic_of_isPGroup_two_of_tame`** (every 2-subgroup of a tame-generated group is cyclic — coprime-kills the `⟨t⟩`-intersection, embed in the cyclic quotient via `isCyclic_of_injective`; discharges step 1 of the plan), **`fixedPoints_tame_inertia_eq_zero`** (`V^{⟨t⟩} = 0` on a ramified simple module — the `t`-fixed `AddSubgroup` is `C`-stable via normality + `MulAction.stabilizer` zpow-closure).  New gotcha banked: `rcases hz with rfl \| rfl` on `z ∈ {s,t}` can `subst` **`t` itself** (rewriting the whole context) — use `rw [h2]` in the second branch instead.  File gained `import GQ2.Tame` (was not in the closure; no cycle).  **Counting criterion LANDED (Fable, same day, std-3, Ax ∅): `free_of_card_fixedPoints_pow_le`** — over a **cyclic 2-group** `P`, a finite 2-torsion module with `#V^P ^ \|P\| ≤ #V` is equivariantly `≃+ 𝔽₂[P]^r` (the reverse bound is automatic — Jordan filtration — so the hypothesis pins every block full).  Fully constructive, ~620 new lines, no `𝔽̄₂`, no structure theorem, no finiteness in the splitting: `ν := γ+1` has `ν^{2^s} = 0` and group-sum `= ν^{2^s−1}` (squaring induction, no Lucas); given a full-depth `v₀`, a functional `λ` with `λ(ν^{2^s−1}v₀)=1` makes `T := φ∘j` (coefficient map ∘ orbit map) `= 1 + (μ+1)B` with `(μ+1)^{2^s}=0` ⟹ **explicit geometric-series inverse** ⟹ `ρ := T⁻¹∘φ` retracts `j`, one regular block splits equivariantly, recurse (bound inherited); no full-depth vector ⟹ kernel-filtration bound collapses `V = 0`.  **`sylow_free_of_ramified` is now PROVED** from it (cyclic Sylow toolbox + tame relation transported along `c`); the residual sorry is the pure **counting inequality `card_fixedPoints_pow_le_of_ramified`** (`#V^P ^ \|P\| ≤ #V` on a ramified simple faithful module) — no ∃-iso content left.  Paper cross-checked (pp. 29–30 read this session): the criterion replaces the two Lean-hostile steps (Clifford induction over `𝔽̄₂`; faithfully-flat projectivity descent) by their rational counting shadow.  𝔽₂-rational plan for the inequality banked in the file section docstring: étale `𝔽₂[⟨τ⟩]` factor fields + single-`C`-orbit faithfulness (cyclic `⟨τ⟩` has characteristic subgroups) + the O₂-linchpin (a `P`-stabilizer element acting `K`-linearly centralizes `⟨τ⟩` ⟹ abelian normal closure ⟹ nontrivial normal 2-subgroup ⟹ `FoxH.lemma_5_12` vs faithfulness) + finite-field semilinear fixed-point descent (additive Hilbert 90 / normal basis) — a recorded deviation from the paper (which uses 1-dimensional `𝔽̄₂`-weight lines); de-risks the remainder from 'Clifford over `𝔽̄₂`, poor Mathlib support' to explicit finite-field linear algebra.  New gotchas banked: numeral `rw` motive failure at `(2 : Module.End (ZMod 2) M)` (the polymorphic `one_add_one_eq_two` pattern catches the `2` in `ZMod 2`'s instance chain — pin with an explicitly-typed `have` first); `Fin.cons` on a constant family needs `(α := fun _ => M)` everywhere; a single `rw [AddEquiv.apply_symm_apply]` rewrites all identical occurrences (a second copy errors).  **Reduced to the involution (Opus, 2026-07-06, std-3, Ax ∅ for the reduction): `card_fixedPoints_pow_le_of_half` + Jordan-increment concavity land.**  The counting bound `card_fixedPoints_pow_le_of_ramified` is now PROVED from a strictly smaller leaf `involution_fixedPoints_sq_le` (`#V^ω ^ 2 ≤ #V` for the involution `ω = g₀^{2^{s-1}}` of the cyclic Sylow-2), via the standard elementary-abelian / Chouinard reduction, all constructive: **`finrank_ker_pow_succ`** (`dim ker ν^{k+1} = dim ker ν^k + dim(im ν^k ⊓ ker ν)`, rank-nullity for `ν^k : ker ν^{k+1} → im ν^k ⊓ ker ν`) ⇒ **`finrank_ker_pow_concave`** (the increment `dim(im ν^k ⊓ ker ν)` is antitone by `finrank_mono` on `im ν^{k+1} ≤ im ν^k`); two `ℕ`-lemmas **`seq_double_le`** (concavity's automatic `b(2m) ≤ 2 b m`) and **`seq_first_increment_le`** (two equal `m`-term antitone sums are termwise equal ⇒ all increments flatten, `Finset.sum_eq_sum_iff_of_le` + squeeze); and **`card_fixedPoints_pow_le_of_half`** wiring them to `b k := dim ker(nuOp g₀)^k` with the card↔finrank bridge (`Module.card_eq_pow_finrank`, `ZMod.card`), `b(2^s) = dim V` (`nuOp_pow_card_eq_zero`), the freshman `nuOp(g₀^{2^t}) = (nuOp g₀)^{2^t}` (`genOp_pow` + `add_pow_two_pow_of_two_eq_zero`), and the ω-fixed-points ↔ `ker` count.  So `#V^ω ^ 2 ≤ #V` (the involution) + concavity force `2^s·dim V^P = dim V`, i.e. the full bound.  `#print axioms`: the reduction is Ax ∅; `sorryAx` enters `lemma_6_11` only through `involution_fixedPoints_sq_le`.  **The residual leaf is now the single-involution case** — quadratic descent (additive Hilbert 90) rather than a cyclic-group weight-orbit analysis; a much smaller finite-field-linear-algebra target.  New gotcha banked: bare `nuOp`/`genOp` in a `have`/`show` TYPE need an explicit `: Module.End (ZMod 2) V` ascription (V-inference leaves the `SMulCommClass` instance stuck on a metavariable); `omega` refuses nonlinear `2^s * b 1` vs `b 1 * 2^s` — `rw [mul_comm]` first.  Library green (8679), census 15, guard green.  Backups in scratchpad.  ⚠ **e5 note**: `kappa0_exists`'s `htame` gives abstract `(s,t)`; `lemma_6_11` wants `c : Ttame →ᶜ C` — the e5 assembly needs the finite-presentation → marking bridge (Ttame universal property) or a statement alignment. |
| P-17e5 | B: κ⁰ **normal form + final assembly** — an `H`-invariant quadratic map on `𝔽₂[H]^N` is a sum of orbit polynomials `S_j`/`C_{j,k,g}`/`E_{j,g}` (coefficients constant on orbits of unordered degree-2 coordinate monomials; free coordinate action ⟹ pair-stabilizers trivial or order-2-swap), then assemble the summands (`IsEquivariantFactorSet.add` ✓) through `kappa0_exists_of_split` ✓ ⟹ **closes `kappa0_exists`**.  The analytic heart | ⭐⭐⭐ | F+O | P-17e1, P-17e2, P-17e3, P-17e4 | ∅ | ☑ **CLOSED 2026-07-06 (Fable, same session as the P-17e4 closure) — `kappa0_exists` PROVED: `#print axioms GQ2.SectionNine.kappa0_exists` = exactly std-3 (no `sorryAx`, no B-axioms); whole library green (8686), census 15, guard green.**  Own file **`GQ2/KappaNormalForm.lean`** (~1250 ln; imported by `SectionNine`, whose splice unpacks `htame`/`hsimple`, transports invariance/simplicity along the surjection, calls `kappa0_exists_tame`, and pulls back by `comapHom`).  **Architecture** (deviation-lite, documented in the file header): (1) `PermW H K` = `𝔽₂[H]^K` as a **reducible** synonym with the blockwise left-translation `DistribMulAction` (keyed on the raw `Fin K → H → ZMod 2`; safe — no competing instance can exist for abstract `H` — and `lemma_6_11`'s split pair lands there definitionally); (2) **normal form** `exists_datum_of_invariant_quadratic`: instead of the paper's full `S_j`/`C_{j,k,g}`/`E_{j,g}` orbit-polynomial sum, ONE invariant biadditive refinement `f(F,G) = Σ F_{n,x} G_{m,y} φ(n,m,x⁻¹y)` from the relative-position coordinates `β n m u := polar Q (e_{n,1}) (e_{m,u})` with a half-choice `χ` (`Fintype.equivFin`-order on the pairs `{(n,m,u),(m,n,u⁻¹)}`), possible exactly OFF the involution locus; the ordered-pair **expansion lemma** `quadratic_eq_double_sum` (`Q F = ΣΣ F_p F_{p'} f₀(p,p')` for any diagonal/polar-compatible kernel — no `Sym2`) gives `f`'s diagonal; (3) **involution corrections**: `exists_invBlock_datum` transports P-17e3's `invOrbitDatum` at `G := H, N := ⊥` along `mk' ⊥` (`comapHom` + `MulEquiv.ofBijective`) and the block projection (`comap`) — package = datum + `IsQuadraticFp2` + basis-diagonal 0 + polar = `u`-pairing indicator (finsum-single extraction over the `⟨ḡ⟩`-coset space); `Bad := {(n,u) \| u² = 1, u ≠ 1, β n n u = 1}`, `Q' := Q + Σ_{Bad} E_{n,u}` kills exactly the obstructed coordinates, and `Q' + ΣE = Q` in char 2 (`datum_add` + `exists_datum_finset_sum`); (4) **assembly** `kappa0_exists_tame` over an abstract finite tame pair: 2-torsion **derived** from `hns` + simplicity + `Nontrivial` (the 2-torsion subgroup is stable; `⊥` ⟹ `\|V\|` odd ⟹ every polar pairing vanishes — Cauchy + card-smul), faithful-image reduction via `MulAction.toPermHom` into `Equiv.Perm V` with a bespoke `letI` `DistribMulAction` on `↥range` (fields via range-membership; NO quotient groups, NO `AddAut` — 2026-mathlib's `toAddAut` lands in `Multiplicative (AddAut V)`), then the dichotomy: `t̂` trivial ⟹ `t̂ = 1` ⟹ `\|Ĥ\|` odd (a Cauchy involution commutes with `t̂ = 1` and dies by P-17e4's `two_torsion_of_centralizer_eq_one`) ⟹ averaging (`datum_of_odd` copy); else ramified ⟹ `lemma_6_11_of_tame_pair` ⟹ split pair ⟹ normal form at `qW := q ∘ ret` ⟹ `datum_of_split`.  **The banked e5 design flag is RESOLVED by statement alignment**: `RegularSummand.lean` gained `_of_tame_pair` generalizations of the whole `lemma_6_11` chain (abstract `(sg, t, hgen, hrel)` in place of `Ttame`-markings; the original `Ttame` statements are unchanged 3-line wrappers, still std-3).  **Private copies** of the §9 assembly layer (of_invariant/of_biadditive/add/comap/comapHom/of_split/of_odd/refinement) live in the file — SectionNine imports us, so importing it back would cycle; promotion to a shared low file stays deferred per the scoping note.  Gotchas banked: `Nat.odd_iff_not_even` → `Nat.not_even_iff_odd`; `simp only []` normalizes `a = a` conjuncts to `True` and breaks a following `if_pos ⟨rfl, …⟩` (use `show` with the beta-reduced `ite` instead); numeral-`2` `rw` in `ZMod 2` inside cast-heavy goals trips `CharP`/field-instance motives — use the `linear_combination (norm := (ring_nf; simp [CharTwo.two_eq_zero]))` idiom; `rw` on `gbar` under `(H⧸⊥)⧸zpowers gbar`-typed binders is a dependent-motive trap (`congr 1` the factor); `choose` over the subtype `{p // p ∈ Bad}` cleanly avoids partial choice functions; identifiers may not contain `Σ`.  Shared-file hygiene: the `SectionNine` splice/import, the `GQ2.lean` registration line, and this board row were **staged against HEAD** (git plumbing) so the parallel P-17d session's uncommitted `blockEnrichment` move does not ride this commit. |
| P-17f | B: §9.2 `M`-stage partition — `mStage_partition` (`LiftsOver`-fibration ✓ d3-pattern + `partition137_of` image-stratification machinery; `mult` as hypothesis, numerics at P-17i) | ⭐⭐ | O | P-17a | ∅ | ☑ **DONE & verified std-3** (Opus, 2026-07-05) — `mStage_partition` in `GQ2/SectionNine.lean` **sorry-free**; `lake build GQ2` green (8670); `#print axioms mStage_partition = std-3` (**Ax ∅** confirmed). **Route:** (A) the trivial `hmult`-collapse `∑_ρ #LiftsOver(ρ) = mult·e(T_C)` (`Finset.sum_const`); (B) `Mset := {m : Γ→ᶜ B // IsBoundaryLift b F T_B m ∧ (π_BC∘m surj)}` fibres over the `C`-image ρ (fibre `≃ LiftsOver ρ` via `sigmaFiberEquiv` + `RF.isBoundaryLift_of_over`), giving `Nat.card Mset = ∑_ρ #LiftsOver`; (C) `Mset` stratifies by exact image `range m` into the `C`-onto strata (`partition137_of` machinery minus the scalar-cover `∃g`: `cmhCodRestrict`/`cmhInclude`, `C`-missing + head-missing strata empty), giving `Nat.card Mset = ∑ᶠ_{J.map π_BC=⊤} exactImageCountOn b F T_B J`. Both banked gotchas respected (`set` the stratification `Finset`; no `range=J` rw in dependent hyps). **NOTE (worktree):** the proof lives in the untracked, §9-batch-owned `SectionNine.lean` (deps `RadicalEdgeBridge`/`AffineTLift`/`FrameEnrichment` also untracked) — left as a working-tree change for the batch commit; backup in scratchpad. **Unblocks P-17i** (`R = ⊥` lane). |
| P-17g | B: Lemma 9.4 bounds — state+prove (145)/(148)/(153) against `blockFrame` (cover-generic pullback-stratum kernel bounds; `4 ≤ \|K\|` from `dim V ≥ 2` via `nontrivial_action`) | ⭐⭐ | O | P-17c | ∅ | ☑ **DONE & verified std-3 (Opus, 2026-07-06)** — new own file **`GQ2/BlockFrameBounds.lean`** (commit `ef44890`), **sorry-free**; all 6 deliverables `#print axioms = std-3` (**Ax ∅**), gate green (census 15). `card_LB_mul`/`card_LB_lt` (\|L_B\|·\|R\|=\|L_Y\|, strict when R≠⊥), `card_LC_lt` (K≠⊥ from `gen`+`S<P`); **`four_le_card_K`** (\|K\|=2 ⟹ K central ⟹ [Y,P]≤S vs `nontrivial_action`, reusing `lemma_7_1_dual`'s `hcommP`); **(148)** `card_stratum_LB_lt` (double cover ×2 vs the index-2 `J∩L_B<L_B`, ÷\|R\| via R≠⊥); **(153)** `card_stratum_LC_lt` (⊆p⁻¹(L_C)=2\|L_C\| vs `4≤\|K\|`). Cover-generic; stated against `blockFrameImpl` ⟹ applies to `blockFrame` by defeq. Helpers: `card_comap_of_surjective`/`card_map_mk'_mul`/`card_subgroupOf_eq_inf`/`centralCover_card_comap`/`_le_two_mul_card_map`. **Build-graph wiring** (import from `SectionNine`/`GQ2.lean`) + the blockFrame delegation left for the P-17 lead. **Feeds P-17i.** |
| P-17h | B: §9.3 recursion solver — `count_eq_of_closedRecursion` (pure ℤ-arithmetic on the boxed system: cancel `8`, `2`, `2·#D_T ≠ 0`, `#D_R ≠ 0`; case split target-side) | ⭐⭐ | O | P-17a | ∅ | ☑ **DONE & verified std-3** (Opus, 2026-07-06) — `count_eq_of_closedRecursion` in `GQ2/SectionNine.lean` **sorry-free**; `lake build GQ2` green (8670); `#print axioms = std-3` (**Ax ∅**). **Chain (all mechanical):** `mJOn` agree (eq138 + `hpull` ⟹ `8·mJ₁ = 8·mJ₂` via `finsum_mem_congr`, `omega`-cancel `8`; `dite` both branches) ⟹ `zBC` agree (`by_cases` on the descent-∃ `∃N…`, target-side identical for both sources: eq139 ⟹ cancel `2` by `omega`; eq140 ⟹ RHS agree via `hTC`+`hphase`+`finsum_congr`, cancel `2·#D_T ≠ 0` by `mul_left_cancel₀`) ⟹ `mB` agree (`l=0`: `mB=e(T_B)`=`hTB`; else eq137 ⟹ `add_right_cancel` after `hzBC`+`hsum`) ⟹ `e(T)` agree (eq136 RHS agree via `hmB`+`hTB`, cancel `#D_R ≠ 0` — nonempty via `zeroDR`). **Worktree:** in the §9-batch-owned untracked `SectionNine.lean`; left as working-tree change for the batch commit; scratchpad backup refreshed. **Feeds P-17i** (inductive lane). |
| P-17i | B: §9 master induction — prove `thm_4_2` (strong induction on `Nat.card T.LY`; terminal/`R=⊥`/`R≠⊥` lanes; discharge `hfg` [Γ_A: P-03 ✓; `G_ℚ₂`: **B1**], `hscalar` [`lemma_8_2_*` ✓], `M`-multiplicities [`prop_5_15` ✓/`prop_5_16_bundle` ✓], `hphase` via `lemma_8_3` ✓ at the phase covers + (153)-IH).  ⚠-P-16d6-coordination **resolved** (`prop_8_9` already returns `0 < Nat.card DT`, `Prop89Close.lean:75`) | ⭐⭐⭐ | O | P-17b–h, P-16c, P-16d2, P-16d6 | B1 + transitive (B3c, B6, B7, B7′, B8, B9) | ◐ **CLAIMED (Opus 2026-07-07) — INCREMENT 1 LANDED (sequencing B of `docs/p17i-handoff.md`): induction scaffold + terminal lane + full M-stage lane assembly.**  `thm_4_2` now runs the `suffices`-helper + `Nat.strong_induction_on Nat.card ↥T.LY` (IH over *all* smaller instance-generalized targets — the handoff's highest-risk item, validated), terminal lane closed by `terminal_count_eq`; **M-stage lane fully assembled**: `hhead` case split (degenerate head ⟹ both counts `0`), `mStage_partition` ×2 at `blockFrameImpl` with `mult = \|M_B\|²`, `hfg` = P-03 / **B1**, IH at `TC` ((145c)) + proper strata, `⊤`-stratum transport (`R=⊥ ⟹ π_B` iso), finsum `⊤`-split + cancellation.  New leaf **`GQ2/MStageCount.lean`** (imported by `SectionNine`; **sorry-free**): `exactImageCount_congr`, `exactImageCountOn_top`, `exactImageCount_TB_of_R_bot`, `exactImageCount_eq_zero_of_not_headSurj`, `card_stratum_mStage_lt` (the (146)-analogue stratum bound, valid for **every** `R` — `card_stratum_LB_lt` needs `R≠⊥`), **`liftsOver_card_local`** (`#LiftsOver(ρ)=\|M_B\|²` for `G_ℚ₂` — the `Enrichment`-free `hMcountM_local` analogue on `π_BC`/`Y_C` with `surjInv` for `Quotient.out`; std-3+B6+B7, rest std-3).  `#print axioms thm_4_2` = std-3 + sorryAx + **B1 (first real consumption) + B6 + B7**; downstream (`SectionTen`/`Sources`, `BlockEnrichment`) green; census 15.  **Remaining: (i) `hmultA`** — the `Γ_A` `#LiftsOver = \|M_B\|²` count, the ONE sorry inside the M-lane (= P-16d6e's shared `Γ_A` `#MLifts`; route: `hZcount_gammaA` pattern (`z1Equiv` + `prop_5_15` clause 2 + the source-independent `hfix`, already inline in `liftsOver_card_local`) at the module `M_B`, + `LiftsOver`-nonemptiness via `#H2w = 1 ⟹` word-`d¹`-surjectivity relator correction — the NEW e5 word-lift calculus `push_*Rel`/`corrected_*Value`/`d1Fun_base_change` (38d321e) is exactly this toolkit); **(ii) the R-stage lane** (gated: P-16d6e assembly + P-17d).  Instance-binder decision (documented in `thm_4_2`'s docstring): only `[CompactSpace AbsGalQ2] [TotallyDisconnectedSpace AbsGalQ2]` added (= `terminal_count_eq`'s, = `eq_154`'s section); `GammaA`'s triple + `IsTopologicalGroup AbsGalQ2` verified globally inferable (`GammaA : ProfiniteGrp`; mathlib Krull), so `prop_8_9`'s six binders self-discharge at the R-lane call site.**  **INCREMENT 2 LANDED (Opus 2026-07-07): M-stage lane assembly COMPLETE.**  New leaf `GQ2/MStageCountGammaA.lean` (registered, allowlisted): `RecursionFrame.liftsOver_card_gammaA` (the `Γ_A` `#LiftsOver(ρ)=\|M_B\|²`, `hmultA`'s content) = `liftsOver_card_gammaA_of_nonempty` (**PROVED std-3, no B-axioms**: candidate-duality Z¹ count `z1Equiv`+`prop_5_15` clause 2 at module `M_B` × `hfix=1` via `lemma_7_1_dual`, then the source-generic Z¹-torsor bridge) ∘ `liftsOver_nonempty_gammaA` (**the ONE remaining M-stage sorry**). `thm_4_2`'s M-stage `hmultA` now delegates to it, so the whole M-lane is assembly-complete; SectionNine's only remaining sorry is the R-stage lane.  **Nonemptiness scoping (decisive):** no degree-2 word↔continuous bridge exists for `Γ_A` (`WordCoh2` is `𝔽₂`-only), so the local `#H²=1⟹coboundary` route is unavailable — `liftsOver_nonempty_gammaA` must port `RStageGammaA.hsep_hom_gammaA` + the L5 descent from `(π_B,R)` to `(π_BC,M_B)` (set-lift marking → relator values in `M_B` → trace-span forces correction since `(M_B^∨)^C=0` → `lift_of_relatorFree_marking` descent).  Opus-appropriate (execution, architecture pinned by two existing Γ_A templates; no degree-2 bridge to build).  Commits ecf6979 (incr 1: scaffold+terminal+M-lane skeleton) + 26576cd (incr 2: count).  **INCREMENT 3 LANDED (Opus 2026-07-07): `liftsOver_nonempty_gammaA` PROVED — the M-stage lane is FULLY CLOSED.**  `GQ2/MStageCountGammaA.lean` now **sorry-free** (removed from `SORRY_ALLOWLIST`): `liftsOver_card_gammaA` (`hmultA`) = std-3, no B-axioms.  Two new pieces: **`descend_piBC`** (the `π_B`→`π_{BC}` port of `RStageGammaA`'s private `lift_of_relatorFree_marking` L5 descent, kernel `M_B` 2-torsion via `MB_elem`) and **`liftsOver_nonempty_gammaA`** — the set-lift→correct→descend of `hsep_hom_gammaA`, but SIMPLER: `(M_B^∨)^{Y_C}=0` (`hfix`) makes `H2w(markC ρ,M_B)` a singleton (`prop_5_15` clause 1 + `hfix`), so the relator pair is an unconditional `d¹`-coboundary via `Subsingleton (H2w)` — no `obs`/`blockRObstructionData`/`sep_word` needed.  `thm_4_2` M-lane (terminal + `R=⊥`) is now sorry-free; **only the R-stage (`R≠⊥`) lane remains** (gated on P-16d6e + P-17d).  `#print axioms thm_4_2` = std-3 + sorryAx (R-stage only) + B1 + B6 + B7; census 15.  **Coordination:** commit 13081bf flags this as `κ_M^A owned by P-17i lane` — P-16d6e can consume `liftsOver_card_gammaA` for its `Γ_A` M-lift count.  Commit efe0dea. |
| P-18 | B: Lemma 10.1 (tame-frame exhaustion) + eq. (154) ⇒ `main_surjection_count` (consumes the **amended** `thm_4_2` at `E = 0` — `hE2` trivially discharged).  **PLANNED (Fable 2026-07-07): `docs/p18-plan.md`** (paper §10 pp. 47–48 read; all interfaces verified against source).  **Key design resolution**: Lemma 10.1 fixes `L = O₂(G)` (the 2-core) — ONE target `𝒴_G` for all `f`, only the tame frame `α : Ttame ↠ G/O₂(G)` varies; `f(W) ≤ O₂(G)` automatically (pro-2 wild image is a normal 2-subgroup), so cells = `BoundaryLifts` sets verbatim and **no Möbius/poset induction is needed** (the earlier ⭐⭐⭐-risk assessment is retracted).  `BoundaryMaps` already bundles the **F-side hypotheses** (`tameF_surjective`, `wild_isProP`, `wild_isMax`); `frameMap = (α∘pr₁, ψ̄∘pr₂)` definitionally, so the `E=0` framing condition IS the fixed-frame condition.  **Decomposed → P-18a–e** (rows below); **a–d unblocked NOW** (P-09 ✓/P-10 ✓ enter via `prop_3_14_proved` ✓); only e's axiom trace rides `thm_4_2` (P-17i) — P-18 runs to completion in parallel with P-17i, carrying `sorryAx` through the allowlisted `SectionNine` sorry until it closes. | ⭐⭐ | O | P-09 ✓, P-10 ✓, P-17i (consumed at e only) | (all of Track B) | ◐ **decomposed (Fable 2026-07-07)**; sub-rows open |
| P-18a | B: **§10 design + statements** — `docs/section10-extraction.md` (deviation ledger: sigma-equiv encoding of "unique frame"; `E₀` trivial-decoration choice (`PUnit` vs `Multiplicative (ZMod 1)`); `twoCore` as sSup; hypothesis-form Γ-generic 10.1) + `GQ2/SectionTen.lean` skeleton: `E₀`, `twoCore G` def + **sorried** `twoCore_normal`/`twoCore_isPGroup`/`le_twoCore`, builders `tameTarget G : MarkedTarget (G⧸twoCore G) E₀ G` (`LY := twoCore G`, `piY := mk'`, `thetaY := 1`) + `tameFrame α : BoundaryFrame … E₀` (`psiBar := 1`) + index `TameFrames G := {α : ContinuousMonoidHom Ttame (G⧸twoCore G) // Surjective α}`, **sorried** `lemma_10_1 : ContSurj Γ G ≃ (α : TameFrames G) × BoundaryLifts b (tameFrame α) (tameTarget G)` (over `htame`/`hwild` hyps) + `card_contSurj_eq` + `eq_154`; `SORRY_ALLOWLIST += SectionTen.lean` (same commit); decide the `Statement.lean` splice direction (import-cycle check — plan §6.3, transplant fallback). | ⭐⭐ | **F** | — | (statements) | ☑ **CLOSED 2026-07-07 (Fable)** — `GQ2/SectionTen.lean` LANDED (root-imported, allowlisted; full library green 8701, gate pass): `twoCore` def + sorried props, `E₀ := PUnit`, `tameTarget`/`tameFrame`/`TameFrames`/`tameCoord`, sorried `lemma_10_1`/`card_contSurj_eq`/`eq_154`, and **`main_surjection_count'` PROVED modulo `eq_154`** (`#print axioms` = std-3 + sorryAx, verified — the end-to-end (154)→`prop_2_3` wiring type-checks, validating every statement shape/instance).  Extraction doc `docs/section10-extraction.md` (9 deviations; **load-bearing: `Statement.lean` is UPSTREAM of the tower** ⟹ P-18e = statement-move + `main_presentation` hypothesis-form + the `AbsGalQ2` instance-binder amendment; same geometry note recorded for P-19's `main_presentation_literal`).  Gotchas banked: `⟨rfl⟩`-discreteness fails for abstract `[DiscreteTopology G]` (use `discreteTopology_iff_forall_isOpen` + `isOpen_coinduced`); `exactImageCount`'s context needs `[IsTopologicalGroup Γ]`.  **P-18b/c/d all unblocked.** |
| P-18b | B: **the 2-core layer** — prove `twoCore_normal` (sSup of normals), **`twoCore_isPGroup`** (join of two normal 2-subgroups is a normal 2-subgroup: `Subgroup.quotientInfEquivProdNormalQuotient` + card-extension, or the `Subgroup.mul_normal` card-divisibility fallback; then finite induction up the sSup) + `le_twoCore` (`le_sSup`) + the **pro-2 image bridge** `IsProP 2 K → continuous hom to finite discrete → IsPGroup 2 (image)` (reuse the `piBd_isProP`→`IsPGroup` extraction shape, `BoundaryConstruction.lean:182`; `IsProP` def `MaxProP.lean:47`).  Pure finite group theory (Mathlib has NO `pCore`), zero arithmetic; ideal parallel ticket. | ⭐⭐ | O | P-18a | ∅ | ☑ **CLOSED 2026-07-07 (Opus)** — all three proved std-3 in `GQ2/SectionTen.lean` (build green, gate pass); much shorter than scoped: **`twoCore_isPGroup`** = one-liner `Sylow.sSup_of_normal` (mathlib already has "sSup of normal p-subgroups is a p-group" — needs `[Finite G]`, added; harmless, `tameTarget` always has it), **`twoCore_normal`** = `Subgroup.sSup_normal`, **`isPGroup_map_of_isProP`** = restrict `f` to `↥K`, ker is open (cont. hom to discrete: `(isOpen_discrete _).preimage`), package as `OpenNormalSubgroup`, apply `IsProP`, transfer `K⧸ker ≃* range = K.map f` via `quotientKerEquivRange` + `IsPGroup.of_equiv` + `MonoidHom.range_comp`/`range_subtype`.  **P-18c/P-18d unblocked** (both consume these). |
| P-18c | B: **Γ-generic Lemma 10.1** — the equiv + count form over `(b, htame : Surjective (pr₁∘b), hwild : IsProP 2 (ker (pr₁∘b)))`: forward `f ↦ ⟨α_f, f⟩` (`f(ker(pr₁∘b)) ≤ twoCore G` via the P-18b bridge + `Subgroup.Normal.map` + `le_twoCore`; `α_f` descends `π∘f`, continuous by discreteness, surjective); disjointness from `htame`; `TameFrames G` finiteness (`gen_ttame_quotient` `Prop32.lean:134` + the t.f.g.-hom-finiteness machinery, `finite_boundaryLifts` pattern); `Nat.card_sigma` ⟹ `card_contSurj_eq : Nat.card (ContSurj Γ G) = ∑ᶠ α, exactImageCount b (tameFrame α) (tameTarget G)` (cell finiteness from `hfg`). | ⭐⭐ | O | P-18a, P-18b | ∅ | ☑ **CLOSED 2026-07-07 (Fable)** — `lemma_10_1` + `card_contSurj_eq` PROVED, **`#print axioms` BOTH = `{propext, Classical.choice, Quot.sound}`** (P-18b landed mid-session, so no sorryAx even transits).  Architecture: `Equiv.sigmaFiberEquiv (inducedFrame …)` — the fibration over the total map `f ↦ α_f` IS partition+uniqueness, per-fiber `Equiv.subtypeEquivRight` (`frameOf f = α ⟺ IsBoundaryLift`, both directions via the descent spec `inducedHom_tameCoord` = `liftOfRightInverse_comp_apply`; `E₀`-component by `Subsingleton.elim`); descent `inducedHom` via `MonoidHom.liftOfSurjective` (kernel condition = `map_wildKer_le_twoCore` = P-18b bridge + `Normal.map` + `le_twoCore`); continuity via `Continuous.isClosedMap.isQuotientMap` (compact source ↠ Hausdorff `Ttame`) + `continuous_iff` + `Continuous.congr`; count via `Nat.card_sigma` + `finite_boundaryLifts` + new `instance Finite (TameFrames G)` from new `ttame_tfg` (`topGen_ttame` Finset-form) + `finite_continuousMonoidHom`.  **Statement amendments (extraction doc deviation 10)**: `lemma_10_1` += `[CompactSpace Γ]` (α_f-continuity NEEDS the quotient-map property — not derivable otherwise), `card_contSurj_eq` += `[CompactSpace Γ] [TotallyDisconnectedSpace Γ]`; `omit [IsTopologicalGroup Γ]` on 10.1+helpers.  Free at both P-18e call sites (sources profinite) but **P-18e note: `eq_154` must bind the `GammaA` instance pair too**.  Gates: LSP clean (only `eq_154`'s sorry left in file), `lake build GQ2.SectionTen` green (8669 jobs), census 15, guard green.  P-18e now waits only on P-18d |
| P-18d | B: **per-source hypotheses** — **F-side FREE** (`B.tameF_surjective` + `B.wild_isProP` are `BoundaryMaps` clauses; `pr₁∘bF = tameF` is `rfl` via `bF_apply_coe`).  **A-side (⚠ the one unknown)**: (i) `tameA_surjective` from the generator clauses (`tameA_sigma`/`tameA_tau` hit `tameSigma`/`tameTau`) + `gen_ttame_quotient` + closed-range/compactness; (ii) `IsProP 2 (ker B.tameA)` — dig the witness (`tameA := phiA`, `BoundaryMapsWitness.lean:468`/`BoundaryConstruction`): if `phiA` descends through `Γ_A/wildCore ≅ Ttame` then `ker = wildCore` and **`isProP_wildCore`** (`AdmissibleLimit.lean:374`, P-04 ✓) closes it; **fallback (owner sign-off)**: amend `BoundaryMaps` with symmetric A-side clauses, discharged in `BoundaryMapsWitness` from the same material.  **Cross-benefit**: (i) also discharges `prop_8_9_of`'s `hheadA`/`hheadF` — **coordinate with P-16d6e** (it needs exactly these). | ⭐⭐ | O | P-18a | ∅ | ☑ **CLOSED 2026-07-07 (Opus)** — new leaf `GQ2/SectionTenSources.lean` (root-imported, sorry-free, my subtree green 8670, gate pass): all four `htame`/`hwild` for `bA`/`bF`.  **A-side was NOT an "unknown" — the witness supplies it, NO `BoundaryMaps` amendment**: `phiA_surjective` (Prop32 ✓) gives `htame`, and `ker φ_A = W_A` (**new `ker_phiA`**, ⊆ via injectivity of `psiW = φ_A/W_A` = the proved Prop-3.2 iso `tameAEquiv`, ⊇ via `wildPartB_le_ker_phiA`) + `isProP_wildPart` (P-04) gives `hwild`.  F-side generic from the `BoundaryMaps` fields (`tameCoord B.bF = B.tameF` via `bF_apply_coe`).  Axioms: F-side std-3; A-side std-3 + {B3c,B5,B8,B10′} (the `boundaryMapsWitness`/prop_3_14 set), no `sorryAx`.  **eq_154 (P-18e) must use `boundaryMapsWitness`** (A-side facts are witness-specific), carrying `[CompactSpace/TotallyDisconnectedSpace AbsGalQ2]`.  Cross-benefit `hheadA`/`hheadF` for P-16d6e: `tameA_surjective`(=`phiA_surjective`)/`tameF_surjective` are now packaged as `tameCoord_b{A,F}_surjective`. |
| P-18e | B: **assembly + splice** — `eq_154 G : Nat.card (ContSurj GammaA G) = Nat.card (ContSurj AbsGalQ2 G)` via `card_contSurj_eq` ×2 (`B₀ := boundaryMapsWitness`) + `thm_4_2 B₀ (tameFrame α) (tameTarget G) hE2` per α (consumed DIRECTLY — sorryAx flows from the allowlisted SectionNine sorry until P-17i, then auto-cleans); `main_surjection_count := eq_154 ▸ prop_2_3` (shapes verified: `contSurjCount` def + `Prop23.lean:207`); **`Statement.lean` off the `SORRY_ALLOWLIST`** (line 49 is its only sorry) + `GQ2.lean` wiring.  Unblocks **P-19** (= the ~15-line `main_presentation GammaA gammaA_topologicallyFinitelyGenerated absGalQ2_isTopologicallyFinitelyGenerated (fun G _ _ _ _ => prop_2_3)` instantiation).  Axiom audit at close (plan §5: std-3 + witness B3c/B5/B8/B10′ + thm_4_2's B1/B6/B7/B7′/B8/B9). | ⭐ | O | P-18b, P-18c, P-18d (+P-17i for the clean trace, non-gating) | B1 + Track B (audit) | ✅ **CLOSED (Opus, 2026-07-07, `628f9fc`).** `eq_154` + `main_surjection_count'` PROVED in **`GQ2/SectionTenSources.lean`** (NOT `SectionTen`: `eq_154`'s A-side is witness-specific — `Γ_A` tame surjectivity `phiA_surjective` — and `boundaryMapsWitness` is downstream; statement-move pattern). `eq_154` = `card_contSurj_eq` at `bA`/`bF` (per-source P-18d hyps) rewriting each `Nat.card (ContSurj …)` as the tame-frame exact-image sum, then `thm_4_2 boundaryMapsWitness (tameFrame α) (tameTarget G) hE2` equating them frame-by-frame (`hE2` trivial on `E₀ = PUnit`). **`SectionTen.lean` now sorry-free → OFF the allowlist.** **`Statement.lean`**: `main_surjection_count` retired to a comment-pointer, `main_presentation` → **hypothesis-form** (gains `hcount : ∀ G, contSurjCount G = admissibleCount G`, P-19 supplies from `main_surjection_count'`; no current consumers — `main_presentation_literal` is a P-19 sorry) → **sorry-free → OFF the allowlist**. `#print axioms eq_154` = std-3 + `sorryAx` (via the allowlisted §9 `thm_4_2` until P-17i) + declared {`dyadicOrientation`, `localReciprocity`, `peripheralCyclotomicAction`, `tameQuotient`, B1 `absGalQ2_isTopologicallyFinitelyGenerated`}. Full-library build green (8714), gate green (census 15). **Unblocks P-19** (`main_presentation_literal`: pass `main_surjection_count'` as `hcount` + instantiate at `GammaA`). |
| P-19 | Assembly: `main_presentation_literal` via `main_presentation` — **assessed straightforward (Opus 2026-07-07)**: `main_presentation` is already proved; all plug-ins exist (`prop_2_3` ✓ exact shape, `gammaA_topologicallyFinitelyGenerated` ✓, B1, `GammaA : ProfiniteGrp` instances); ~15 lines once P-18e lands. | ⭐ | O | P-02 ✓, P-03 ✓, P-05 ✓, P-18 | B1 + Track B | ✅ **CLOSED (Opus, 2026-07-07, `e743f81`).** **THE LITERAL THEOREM 1.2**: `GQ2.main_presentation_literal : Nonempty (ContinuousMulEquiv GammaA AbsGalQ2)` PROVED in new leaf **`GQ2/PresentationLiteral.lean`** (registered in `GQ2.lean`) = `main_presentation` @ `Γ_A` with `hΓA := prop_2_3` (exact-shape `Γ_A` count) + `hcount := SectionTen.main_surjection_count'` (P-18e, the `G_ℚ₂` count) + the two f.g. witnesses. **Statement-move** (P-18e pattern): `prop_2_3`/`main_surjection_count'` are downstream of the upstream `GammaA.lean`, so `GammaA.lean`'s sorried `main_presentation_literal` is retired to a comment-pointer (GammaA keeps 2 other sorries → stays allowlisted). `#print axioms` = std-3 + `sorryAx` (via the allowlisted §9 `thm_4_2` until P-17i) + declared {`dyadicOrientation`, `localReciprocity`, `peripheralCyclotomicAction`, `tameQuotient`, B1}. `lake build GQ2.PresentationLiteral` green (8671), gate green (census 15). **Now the ONLY sorry between here and the unconditional literal Theorem 1.2 (mod B-axioms) is `thm_4_2` = P-17i.** |
| P-20 | Meta: review packet v3 — interior-node statements + App. D certificate diff + **the citation-faithfulness classification table (from P-22, now in `docs/review-packet.md` §2)**, at statement freeze | ⭐ | O | P-06, P-11, P-12, P-14, P-22 | — | ☑ **CLOSED (Opus, 2026-07-05) — doc-only, zero code changes.** `docs/review-packet.md` → **v3**: (i) new **§4 interior-node statements** (locator table: 30 paper nodes → Lean decls + file + proof status) with **§4.1 statement-amendments-beyond-the-paper** — the review-critical Q2 targets, verbatim: `lemma_6_17_dim`'s load-bearing self-dual-form package (**false without it** — `C₇⋊C₃/𝔽₈` ⇒ `#H¹=8` not a square, P-15f1 user-approved 2026-07-05), `lemma_6_8`'s isotypic `hVU`/`hrank` (flagged genuine gap), `lemma_6_6`'s paper-silent 2-power monodromy hyp (`ω=[[1,1],[0,1]]/𝔽₂²` ⇒ `−8≠4`), + 6.17_vanish/6.18_unram/6.14/6.21 `hc`/`κ⁰_q` amendments; (ii) new **§5 App. D certificate diff** — per-node `#print axioms` (verified against a green build) for all 9 App. D edges + top-level/§3 nodes, with the bundle caveat and the honesty check (every leaf's consumers; B2 zero-consumer re-confirmed). **Diffs surfaced**: Prop 1.1 = `{B3c,B8}` (not B3,B4,B5,B7′ — B3c⊇marked-B4, B5/B7′ at `lemma_3_5_hilbert_ledger`); Prop 5.15 = **std-3** (§C "uses B6" discharged at `prop_5_16`); Prop 3.2-local = `{B10}` (AxiomLedger header "→B5" is pre-B10 stale — noted, not code-edited). **(iv) B11a/B11b Serre citations LINE-CHECKED** vs `references/Serre -- Local fields.pdf` (GTM 67): **B11a** = Ch. XIV §2 Prop. 7 (iii) `(a,b)ᵥ=1 ⟺ b∈N_{K(a^{1/n})}`, symbol `inv_K(a∪b)` (p.208–209); **B11b** = Ch. V §2 Prop. 3 + Cor./Rem. 1 (`N(Uⁿ_L)=Uⁿ_K`; finite residue ⇒ `U_K=NU_L`, p.82) — resolves the P-23-deferred item; propagated to `literature-axioms.md` §D/§E ("all thirteen source-verified") + `-onepage.md`. P-22 4-tier table carried into v3. **Committed** review-packet.md + literature-axioms{,-onepage}.md only; this board row left uncommitted (co-owned, parallel work in tree). Gate green (census 13). |
| P-21 | Foundations: ℤ₂-powering on pro-2 groups — `maxPro2(ℤ̂) ≅ ℤ₂` (the `ι`-seam), `zpowZtwo` + unit/odd-power bijectivity, `IsProP 2 ℤ₂ˣ` + `η`-injectivity, pro-2 Frattini criterion | ⭐⭐⭐ | **F** | — | ∅ | ☑ 2026-07-03 (`GQ2/ZtwoPowering.lean` (i)–(iii) + `GQ2/FrattiniCriterion.lean` (iv); all std-3, zero sorries). **Unblocks P-07 η-leg, all three P-08 legs' infra, `prop_3_10` `ι`-seam** |
| P-22 | Meta: axiom documentation pass — adversarial axioms review recs 1/3/4/6 (`docs/adversarial-axioms-review.md`): B8 citation → "Stix **plus** cyclotomic surjectivity" (statement KEPT — user decision 2026-07-04), B2 → "available/unused" label, B3c → composite-interface reclassification, 4-way classification table | ⭐ | O | — | — (doc-only; census 13 unchanged) | ☑ CLOSED (Opus, 2026-07-05) all four deliverables landed, **doc-only, zero axiom-statement changes** (`git diff GQ2/Foundations/Axioms.lean` = docstrings/comments only; the tree's sole axiom-statement change is P-23's approved B11 split). (i) **B8** `peripheralCyclotomicAction` docstring = **Composite** (Stix §3.3/Def 37 **+** cyclotomic surjectivity, feeders B2 globally / B5's `χ_cyc∘rec=(·)⁻¹` locally named; all-units form kept, image-weakening declined). (ii) **B2** `cyclotomicCharacter_two_surjective` = **available/unused** — **re-verified zero consumers** (grep 2026-07-05: only the decl, the `AxiomLedger` label-map, and docstrings; no proof consumes it). (iii) **B3c** `dyadicOrientation` = **composite interface** (Labute Thm 4(2) values + Demushkin-dualizing = cyclotomic-character local fact + normalized B4 iso; **subsumes a marked B4**). (iv) **4-tier classification table** in `docs/review-packet.md` §2 (direct B1/B6/B7/B7′/B10 · classical+encoding B4/B5/B9 · composite B3c/B8/B11a/B11b · unused B2), cross-ref'd from `Axioms.lean` header + `AxiomLedger.lean` tiers; P-20 row already carries it. Mostly landed by the P-23 session; this pass **finished + reconciled the B11-split staleness**: fixed `review-packet.md` counts (twelve→thirteen leaves, "three"→four composites), added a B3c-composite cross-ref + a "B11a/B11b pending-P-20 / eleven-of-thirteen verified" honesty fix to `literature-axioms.md`, and a census-13 note to `literature-axioms-onepage.md`. Gate: `check_axioms` all-pass (census 13, no native_decide, sorries in allowlist). Did NOT commit. |
| P-23 | Foundations: B11 split into named leaves — adversarial review rec 2 (**census change APPROVED**, user 2026-07-04): `hilbertSymbol_normCriterion_finiteDyadic` + `unramifiedQuadratic_units_are_norms` + isolated spectral-norm bridge; old `dyadicNormCriterion` re-derived as a SAME-NAME theorem (zero consumer churn) | ⭐⭐ | O | **P-15e capstone landed** | (census 12→13) | ✅ **CLOSED 2026-07-04 (Fable)** — B11 split landed, census **12→13**, affected scope green.  `axiom dyadicNormCriterion` → two classical leaf axioms `hilbertSymbol_normCriterion_finiteDyadic` (B11a) + `unramifiedQuadratic_units_are_norms` (B11b) in `GQ2/Foundations/Axioms.lean`; the spectral-norm "unramified" proxy isolated as **`def IsUnramifiedQuadraticSpectral`** (a named project convention, **not** an axiom — the review's "riskiest piece": bridge route (c), neither provable-in-repo nor axiomatized, just named + flagged, so it adds zero proof-theoretic strength).  `dyadicNormCriterion` re-derived as a **same-name theorem** `⟨B11a k htriv, fun a δa hδa hunram u hu => B11b k a δa hδa hunram u hu⟩`, statement byte-for-byte unchanged ⟹ **zero consumer churn** (`HilbertLedger`'s `.1`/`.2` projections untouched).  Verified: `lake build GQ2.HilbertLedger GQ2.SectionSix` green (8629); `check_axioms.sh` census **13 (= expected)**, placement OK, no `native_decide`; `#print axioms` — `dyadicNormCriterion` = std-3 + {B11a, B11b} (no `sorryAx`), and both consumers `evensNorm_deepUnit_vanish` = `SectionSix.lemma_6_16` = std-3 + {B9, B11a, B11b} (the declared B9+B11 column, with B11 now expanding to its two leaves).  Updated same commit: `EXPECTED_AXIOMS 12→13` (`scripts/check_axioms.sh`) + `AxiomLedger.bAxioms` (B11a/B11b tags, `dyadicNormCriterion` now a tracked consumer) + Axioms.lean docstrings/B-index/tier + rules line + `review-packet.md` §2 (table split, tier, amendment log).  **(iv) DEFERRED → P-20**: exact Serre display numbers (LF XIV §2 / V §2) stay "pending PDF verification" — the split preserves the pre-split axiom's citations verbatim, so this is citation-audit work independent of the split.  **Unrelated pre-existing guard red** (not P-23): `GQ2/TrivialSelfDual.lean:157` sorry — a paused agent's WIP scratch, not imported by `GQ2.lean`, red before this change. |
| P-24 | Meta: guard hardening — untracked-file WARN (tracked FAIL unchanged) + scratch convention in `step2-plan.md` — adversarial review rec 5 (the `WF72.lean` incident, since self-resolved; that P-15g prototype is now spliced into `SectionSeven` and the scratch DELETED 2026-07-04 — no live scratch remains) | ⭐ | O | — | — | ☑ **CLOSED 2026-07-05 (Opus)** — all three deliverables in one guard commit. **(i)** `scripts/check_axioms.sh`: each per-file scan (stray-axiom / sorry / native_decide) now routes hits by `git ls-files` membership — **tracked** files FAIL (unchanged), **untracked** (uncommitted) files print a clearly-labeled `WARN` block and do **not** fail the gate (the guard certifies the *committed* library; a parallel session's mid-flight scratch under `GQ2/` must not block everyone's commits). Fail-safe: if git membership can't resolve, every file is treated tracked (strict). Applied to all three per-file scans (not just sorry) — same rationale. **(ii)** scratch convention = `step2-plan.md` conventions item 6 (throwaway prototypes → session scratchpad or gitignored repo-root `scratch/`, not `GQ2/`); `/scratch/` added to `.gitignore`. **(iii)** self-test PASSED: untracked plant with a code `axiom`+`sorry`+`native_decide` → 3 WARNs + **exit 0**; `git add`-ing it (tracked in index) → 3 FAILs + **exit 1**; removed → green. Also corrected the stale `SORRY_ALLOWLIST` `SectionThreeMarked` comment (line-93 note): only the two ν-surjectivities were removed by P-08/P-09/P-10; `prop_3_10_local_marked`+`prop_3_14` REMAIN (tracked P-25), why the file stays allowlisted. Guard green (census 13); `WF72.lean` untouched. Board close is a working-tree edit pending the shared board's next commit (tickets.md carries heavy parallel work — deliberately not swept into the guard commit). |
| P-25 | B: §3 boundary construction — Prop 3.10 (`Γ_A` half + local/Cor 3.12 half) + Prop 3.14 `Nonempty BoundaryMaps` (the orphaned `SectionThreeMarked.lean` sorries) | ⭐⭐⭐ | O | P-09, P-10, P-21 | B5, B8, B3c, B10′ | ☑ **CLOSED 2026-07-06 (Opus)** — all three §3-marked sorries proved + spliced; `SectionThreeMarked` and `BoundaryMapsWitness` **sorry-free**.  (1) `prop_3_10_gammaA` (word collapse, std-3).  (2) `prop_3_10_local_marked` (prop_1_1 ∘ Nielsen `d0PiEquiv`, std-3+B3c+B8).  (3) `prop_3_14`: full 21-field `BoundaryMaps` (kit `fiberProductExists`/`proPKernel_image_ge`/`ker_nuT_le_proPKernel`/`compatA`/`surjA` std-3) + P-25b reduction (density+`units_gen`, std-3) + the two atoms discharged by **B10′** (B10 strengthened in place to `OrientedTameQuotient`, user-approved, census 13 unchanged; Neukirch ANT V (6.2)/V (1.2) verified in `references/`).  **`#print axioms prop_3_14` = std-3 + B3c + B5 + B8 + B10′ — NO sorryAx**: the `(B : BoundaryMaps)` hypothesis of §§4–8 is fully discharged.  Sub-tickets P-25a (rigidity) SHELVED (well-posedness certification, off critical path), P-25c/d/e SUPERSEDED by B10′ (`docs/p25-tame-reciprocity-plan.md` has the full derivation analysis + why-blocked record).  Build 8665 green, guard green. |

## Proof-state census — the remaining `sorry`s (audit 2026-07-05; **12** — P-25 assembled `prop_3_14`, its blanket sorry replaced by the isolated `tame_reciprocity`)

Authoritative snapshot of every bare `sorry` in the compiled tree (`lake build GQ2` green, 8652 jobs;
`check_axioms.sh` all-pass, census 13, no `native_decide`).  Each is an honest open obligation — no
*proved* theorem depends on `sorryAx` except by explicitly taking the sorried result as a hypothesis
(notably `(B : BoundaryMaps)`, discharged only by `prop_3_14` below).

**Apex — final assembly:**
- `GQ2.main_surjection_count` (`Statement.lean:49`) — Theorem 1.2, surjection-count form → **P-18** ☐
- `GQ2.main_presentation_literal` (`GammaA.lean:286`) — literal `Γ_A ≅ G_ℚ₂` → **P-19** ☐

**§4 keystone:**
- `thm_4_2` (`BoundaryFrame.lean:465`) — Theorem 4.2 → **P-17** ☐ (via the §9 induction)

**Active (agents in flight):**
- `prop_5_15` (`FoxHeisenberg.lean:2971`) → **P-13f** ◐ — part (i) `trivialSelfDual` done; parts (ii) simple
  factors + (iii) dévissage remain (`lemma_5_11` now available to it).
- `lemma_6_17_dim` / `lemma_6_17_vanish` / `prop_6_18_unramified`
  (`SectionSix.lean:862/883/917`) → **P-15f1/f2/f3** ◐ (`prop_6_18_ramified` PROVED mod 6.17,
  statement moved to `DeepPart.lean`).
- `lemma_8_6_gammaA` / `lemma_8_6_local` / `prop_8_9` (`SectionEight.lean:1390/1399/1592`) → **P-16a–d** ◐.

**§3 boundary construction — now tracked as ⟶ P-25 ◐** (was UNOWNED; fell through P-09/P-10 closure):
- ~~`prop_3_10_gammaA`~~ — **DONE 2026-07-04 (P-25, std-3)**: marked iso `Γ_A(2) ≅ Π`, spliced.
- ~~`prop_3_10_local_marked`~~ — **DONE 2026-07-05 (P-25, std-3 + B3c + B8)**: local anabelian iso
  `(Π, ν₂) ≅ (G_{ℚ₂}(2), ν_ur)` = `prop_1_1` ∘ the by-hand Nielsen iso `d0PiEquiv` (`D₀ ≅ Π`); spliced.
- ~~`prop_3_14 : Nonempty BoundaryMaps`~~ — **ASSEMBLED 2026-07-05 (P-25)**: the full 21-field
  `BoundaryMaps` bundle is built + spliced (`GQ2/BoundaryMapsWitness.lean`; `SectionThreeMarked` now
  **sorry-free**, off the allowlist), so `(B : BoundaryMaps)` for §§4–8 is discharged **modulo one
  isolated `sorry`** = `tame_reciprocity` (`BoundaryMapsWitness.lean`): `ι(ν_t(tameF g)) = ν_ur(toAb g)`,
  the tame↔reciprocity **orientation bridge** — genuine arithmetic (wild inertia unramified-trivial;
  B10's tame `equiv` is unoriented and B5's `ν_ur` is independent).  **Needs a B10-strengthening or a
  `tame_reciprocity` axiom — census decision, FLAGGED TO USER** (do not add unilaterally).  Everything
  else is std-3: the kit (`fiberProductExists`, `proPKernel_image_ge`, `ker_nuT_le_proPKernel`), `compatA`,
  `surjA`, and both tame/pro-2 quotient maps + kernels.  `compatF`/`surjF` reduce to `tame_reciprocity`.

## Per-ticket acceptance criteria

Common to all: `lake build GQ2` green; `scripts/check_axioms.sh` green (allowlist edits per the
rules above); `#print axioms` of every new theorem ⊆ std-3 ∪ the **Ax** column; docstrings carry
paper eq./§ cross-references; new files, own board row only.

- **P-01** ☑: `docs/STATUS.md` regenerated to the current tree (it stops at 2026-07-02); a script or
  Lean file (`scripts/axiom_ledger.*` or `GQ2/AxiomLedger.lean`) that reports, for every theorem in
  `GQ2`, its non-std-3 axioms, diffable against the per-ticket **Ax** declarations.  Re-run
  instructions in the file header.
  *Done.* `GQ2/AxiomLedger.lean` — a `run_cmd` metaprogram over the elaborated environment
  (`Lean.collectAxioms` per decl; robust to `namespace`/`private`, which the shell
  `.claude/tools/lean4/check_axioms.sh` cannot see).  Re-run: `lake env lean GQ2/AxiomLedger.lean`
  (header documents it; **not** imported by `GQ2.lean`, so `lake build GQ2` never runs it).  Output:
  per-B-axiom consumer lists (diff vs App. D §C), the `sorryAx` gap map, and an **ALARM** count for
  any other non-standard axiom.  Current run: 613 tracked decls, 601 at std-3, ALARM 0; only B7 has
  consumers (its 6 Euler stress tests), gap map = 6 (the 3 root sorries + 3 transitive).
  `docs/STATUS.md` rewritten as a per-layer ledger (axiom layer, 3-sorry gap map, def-layers
  T-05…T-18, proved infra), with verify-from-scratch commands.
- **P-02** ☑: sorry replaced; **zero sorries in `Reconstruction.lean`**, allowlist entry removed,
  `exists_contSurj_of_card_le`/`reconstruction_of_equinum`/`reconstruction` all `#print axioms` =
  std-3 (`lean_verify`'d), `check_axioms.sh` green (census 10).  Level sets nonempty+finite via
  `contSurj_quotient_nonempty_finite`; the compatible family via `konigFunctor : OpenNormalSubgroup
  (ProfiniteGrp.of R) ⥤ Type` + `nonempty_sections_of_finite_cofiltered_system` (König), as the
  recipe said.  **Deviation from the recipe's assembly step**: instead of building a cone over
  `ProfiniteGrp.diagram R` and `isLimitCone.lift`, the section is assembled by an **elementary**
  embedding `e : R ↪ ∏_U R/U` (closed embedding, `R` compact/`Q` Hausdorff) + **two Cantor
  intersections** (`IsCompact.nonempty_iInter_of_directed_nonempty_isCompact_isClosed`): one in `R`
  realizes each compatible family as `e r` (⇒ `ψ := e⁻¹∘Φ` a continuous hom), one in `S` gives `ψ`
  surjective.  **Why**: the `ProfiniteGrp.diagram` objects carry the discrete `⊥` topology, not
  `R/U`'s coinduced (equal but not defeq) topology, and `(diagram R).obj U` won't even synthesize a
  `TopologicalSpace` when used as a bare `ContinuousMonoidHom` codomain — so the categorical
  cone/lift route fights Mathlib's `CompHausLike`/`FiniteGrp` coercions at every step.  The
  product-embedding route stays in natural quotient topologies throughout.  Reusable spinoffs (top
  of the file, `open CategoryTheory`): `projMap`(+`_mk`/`_surjective`/`_id`/`_comp_apply`) and
  `konigFunctor` — for **P-04/P-05** (also over `OpenNormalSubgroup (ProfiniteGrp.of _)`).
- **P-03**: `FreeProfiniteGroup X` is t.f.g. for finite `X` (the generators' images topologically
  generate — density of the free group in its completion); t.f.g. passes to `profiniteQuotient`;
  instantiate: `GammaA` t.f.g. in the exact `∃ s : Finset _, …` form `main_presentation` consumes.
  *Done (`GQ2/FinitelyGenerated.lean`, std-3):* predicate `IsTopologicallyFinGen G` (unfolds to the
  raw `main_presentation` form); `IsTopologicallyFinGen.of_surjective` (transfer along a continuous
  surjection — `DenseRange.topologicalClosure_map_subgroup`); `isTopologicallyFinGen_freeProfiniteGroup`
  (finite `X` — `ProfiniteGrp.ProfiniteCompletion.denseRange` + `FreeGroup.closure_range_of`);
  `gammaA_isTopologicallyFinGen` + `gammaA_topologicallyFinitelyGenerated` (the raw `∃`-form P-19 feeds
  to `main_presentation`).
- **P-04** ☑: (i) `tameRelator`/`wildRelator` of `univMarking` lie in `NA` (each dies in every
  admissible quotient — `map_*Relator_eq_one_iff` + `IsAdmissibleU`); (ii) the closed normal
  closure of `{x₀, x₁}`-images in `Γ_A` is pro-2 (`IsProP 2`, via `MaxProP.lean`'s
  characterization: every open normal subgroup of `Γ_A` pulls back to an admissible-dominated one —
  the Lemma 2.1 subdirect-closure argument).  Design note documenting the limit argument.
  *Done (`GQ2/AdmissibleLimit.lean`; every theorem `#print axioms` = std-3; no allowlist changes —
  pure proof ticket).*  Deliverables: (i) `tameRelator_mem_NA`/`wildRelator_mem_NA` +
  `quotientMk_*Relator_eq_one` (relations (5)/(6) hold in `Γ_A`); (ii) `wildCore` (the closed
  normal closure `⟨⟨x₀,x₁⟩⟩`, on the raw quotient `F₄ ⧸ NA` ≡ᵈᵉᶠ `GammaA`) with
  `isProP_wildCore : IsProP 2 wildCore`.  **P-05 interface**: `isAdmissibleU_iff_NA_le`
  (admissible opens = opens above `N_A`) and `generates_univMarking_map` (generation is automatic
  in every finite quotient — density of `FreeGroup` in its completion).  Engine (design note in
  the module docstring): `isAdmissibleU_top` (trivial quotient admissible — nonvacuity without
  App. B) + `isAdmissibleU_inf` (Lemma 2.1 subdirect closure, elementwise 2-power form
  `isPGroup_normalClosure_image_inf`) ⇒ directedness ⇒ compactness domination
  (`exists_isAdmissibleU_le`, the `proPKernel`-style argument) ⇒ `isAdmissibleU_of_NA_le`
  (Lemma 2.2 pushforward).  Lean gotcha recorded: state `wildCore` on `F₄ ⧸ NA`, not
  `Subgroup GammaA` — mixing the `↥GammaA` and raw-quotient spellings breaks instance search
  (`Membership`-instance mismatch), while everything is defeq on the raw type.
- **P-05** ☑: the bijection `ContSurj GammaA G ≃ {t : Marking G // t.Admissible}` for finite discrete
  `G`: forward = push `univMarking` (admissible by P-04 + Lemma 2.2 `map_admissible`); backward =
  `quotientLift` along `NA_le_ker` (T-21); round-trips via `univMarking_map_toHom` and topological
  generation (P-03).  `Nat.card` conclusion in exactly `main_presentation`'s `hΓA` form.
  *Done (`GQ2/Prop23.lean`; zero sorries; `prop_2_3` and all helpers `#print axioms` = std-3).*
  Headline: `prop_2_3 : Nat.card (ContSurj GammaA G) = admissibleCount G` via
  `contSurjEquivAdmissible` (assembled from named halves `Marking.push`/`Marking.descend` with
  `push_admissible`, `descend_surjective`, `push_descend`, `descend_push`).  Key new lemma:
  `admissible_of_NA_le_ker` — the **converse of `NA_le_ker`** (surjective + `N_A ≤ ker` ⇒ pushed
  marking admissible, via P-04's `isAdmissibleU_of_NA_le` + Lemma 2.2 transfer along
  `quotientKerEquivOfSurjective`); together they characterize admissible markings of `G` as the
  continuous surjections `F₄ ↠ G` killing `N_A`.  **Deviations from this sketch**: (i) t.f.g.
  (P-03) is *not needed* — the round-trip uses the **uniqueness half of the universal property**
  (`Marking.toHom_univMarking_map`: every `f : F₄ ⟶ P` is `toHom` of its own pushed marking;
  CMH-level form `toHom_hom_univMarking_map` via `ConcreteCategory.ofHom`, whose constructor is
  the public route — `ProfiniteGrp.Hom.mk` is private); (ii) the equiv is stated on `F₄ ⧸ NA`
  (defeq `GammaA`, the P-04 spelling convention), `prop_2_3` itself on `GammaA`.  Lean gotchas:
  prove uniqueness by `rw [Marking.toHom, Equiv.symm_apply_eq]` then rewrite
  `homEquiv_apply` **before** `fin_cases i <;> rfl` (otherwise `rfl` unfolds the completion
  adjunction — deterministic timeout), and keep the equiv's fields as *named* lemmas — inline
  `by`-blocks inside the `Equiv` literal retrigger the same timeout via nested-proof abstraction.
- **P-06**: sorried statements, faithful to the paper's §3 (read the PDF; extract also 3.7/3.8,
  whose precise content the step-1 docs never recorded), phrased against the existing def-layers
  (`Reciprocity` bundle for eq. (13), `HilbertSymbol`/`Kummer` for 3.5, `PeripheralAction` for
  3.6, `Tame.lean` for the tame side, `DyadicPresentation`+`Orientation` for Prop 1.1's target).
  Design note mapping each statement to its paper display.  Allowlist entries added.
  *Done (`GQ2/SectionThree.lean` — 10 sorries — plus `GQ2/SectionThreeMarked.lean` — 5 — both
  allowlisted; design note `docs/section3-extraction.md`).*  Statements: Prop 1.1 (`prop_1_1`,
  lift-read `ν_ur`-rows, `R : LocalReciprocity`-parametrized); Prop 3.2 split
  (`prop_3_2_gammaA` generator-pinned; `prop_3_2_local` = `Nonempty LocalTameQuotient`, wild
  inertia encoded as the maximal closed normal pro-2 subgroup per Lemma 3.3); eq. (11) bundle
  (`BDecomposition`, `b_decomposition`); Lemma 3.5 residue (`lemma_3_5_marked_abelianization`,
  `lemma_3_5_hilbert_ledger`, `lemma_3_5_injective` — the (13) rows are step-1-proved in
  `Reciprocity.lean`); Lemma 3.7 (`lemma_3_7`); Prop 3.8 (`prop_3_8_lift`,
  `prop_3_8_classification`).  **Absorptions**: Lemma 3.4 = B4 + B3c + the B3b no-axiom
  decision; Lemma 3.6 = B8 verbatim.  **P-11 handoff taken**: Prop 3.10/3.14 stated against
  `BoundaryFrame.lean` (`prop_3_10_gammaA`, `prop_3_10_local_marked`, `nuT_surjective`,
  `nuTwo_surjective`, `prop_3_14 : Nonempty BoundaryMaps`) in `SectionThreeMarked.lean`
  (separate file: it imports the P-11 layer, committed `f4f911e`, while the core file rests
  on step-1 modules only).  **Escalation for P-09** (rule 1, pre-authorized in its bullet): `prop_3_2_local`
  needs the classical tame-quotient description of `G_ℚ₂`, which no census axiom covers —
  census discussion before P-09; options in the design note.  P-10 infra flagged:
  `IsProP 2 (Multiplicative ℤ₂)` descent + the `Ztwo ≅ Multiplicative ℤ₂` bridge.
  *Escalation resolved (same day, user decision):* **B10** (`GQ2.tameQuotient`, NSW (7.5.3)
  Iwasawa; defs `GQ2/TameQuotient.lean`) added, census 10 → 11; maximality (Lemma 3.3) kept
  as P-09's theorem (`LocalTameQuotient extends TameQuotientData`); `Ttame` deduped onto the
  P-11 layer (`SectionThree`'s copy removed; `tame_relation` now in `TameQuotient.lean`).
- **P-07/P-08/P-09/P-10**: the P-06 statements proved, allowlist entries removed, axiom sets per
  the **Ax** column (this *is* the App. D certificate check for §3).  P-09's `Γ_A` side should
  consume `Tame.lean`'s Lemma 3.1 as-is; if the local side needs more than the B5 bundle exposes,
  that is a design escalation (rule 1), not a bundle edit.
  *P-09 done (`GQ2/Prop32.lean`, ~1250 lines, zero sorries).*  `prop_3_2_gammaA` **proved at
  std-3 — no axioms at all** (classifier `F₄ → T_tame` kills `N_A` via the ω₂ word-ledger
  collapse at odd-order `τ̄`; mutually inverse maps by generator density); `prop_3_2_local`
  **proved at std-3 + B10 exactly** (the declared Ax was an upper bound; B5 unused): the
  maximality of the B10 wild subgroup via `eq_bot_of_normal_two_images` — the paper's
  Lemma 3.3 argument with concrete Fermat levels `G_m = C_{2^{2^m}−1} ⋊ C_{2^m}`
  (faithful inverse-squaring `SemidirectProduct`, trivial center, `ord(2 mod e_m) = 2^m` by
  the Fermat-number trick), a procyclic level-comparison in `Q = T_tame/⟨⟨τ⟩⟩` (cyclic
  `#K`-torsion lemma `mem_of_orderOf_dvd_card`), and the odd∧2-power kill on inertia.  Also
  proved: `nuT_surjective`, `nuTwo_surjective` (their sorried statements deleted from
  `SectionThreeMarked.lean`), the reusable density pack (`topGen_*`, `monoidHom_eq_of_topGen`),
  and `isPGroup_map_of_isProP`.  The two `prop_3_2` sorries are deleted from
  `SectionThree.lean` (which is now **sorry-free** — P-07 closed the last one and removed the
  `SectionThree.lean` allowlist entry 2026-07-03).
  **Commit note**: `Prop32.lean` imports the P-04/P-07 dedup layer (`wildCore`,
  `wildPart_eq_closure`) still uncommitted in `SectionThree.lean`/`AdmissibleLimit.lean` —
  commit together with (or after) those.  Gotcha for the record: `continuous_quot_mk` pins
  the generic quotient topology into instance goals; use `QuotientGroup.continuous_mk` when
  downstream needs `QuotientGroup` instances (`IsTopologicalGroup (G ⧸ N)` etc.).
- **P-07** ◐ (partial): `lemma_3_5_hilbert_ledger` **proved** (six `hilbertSymbol_dyadic`
  evaluations; `#print axioms` = std-3 + B7′, matching the **Ax**), and the missing
  `import GQ2.Foundations.Axioms` added to `SectionThree.lean` (P-08 needs it too).  **⚠ D₀ fixed to
  pro-2 (structural correction, `GQ2/DyadicPresentation.lean`)**: `D0` was `profinitePresentation
  {d0Relator}` — a *full* profinite group (it surjects onto `ℤ/3`: `A,S↦0,Y↦1` kills the relator),
  so `topAbelianization D0 ≅ ℤ/2 × Ẑ × Ẑ`, making `b_decomposition`'s `ℤ/2 × ℤ₂ × ℤ₂` target
  *unprovable* and **axiom B4** (`maxProQuotient 2 AbsGalQ2 ≅ D0`) non-faithful (pro-2 ≅
  non-pro-2 — a latent inconsistency).  Now `D0 := maxProPQuotient 2 D0Full` (pro-2), with
  `d0FullA/S/Y : D0Full` + `d0Full_relation` and `d0A/S/Y := maxProPMk 2 D0Full d0Full…`;
  `d0_relation` and `d0A/S/Y : D0` unchanged, so `Orientation`/`SectionThree`/B4 recompile
  unaffected (validated: `lake build GQ2.Orientation GQ2.Foundations.Axioms` green).  **Consumers
  (P-10) note**: `D0` is now genuinely pro-2.  **Remaining (blocked on new infra)**:
  `b_decomposition`, `lemma_3_5_injective`, `lemma_3_5_marked_abelianization` need a
  **pro-2-abelianization layer** (free pro-2 abelian `≅ ℤ₂^X`, `topAbelianization` congruence along
  a `ContinuousMulEquiv`, the explicit `D₀^ab ≅ ℤ/2×ℤ₂×ℤ₂` basis change) — absent from repo+Mathlib,
  a large foundational build (arguably its own infra ticket).
- **P-10** ◐ (partial — the provable-now part): `GQ2/PropOneOne.lean` (new file, `lake build GQ2`
  green, both key theorems `lean_verify`'d = std-3; **`prop_1_1`'s `SectionThree.lean` sorry is
  untouched**, so no allowlist change yet).  Delivered the design-note §1.1 descent infrastructure:
  `isProP_two_multPadicInt : IsProP 2 (Multiplicative ℤ₂)` (every open subgroup of `ℤ₂` contains
  `span{2ⁿ}` — `PadicInt.norm_le_pow_iff_mem_span_pow` — so `2ⁿ` uniformly annihilates the quotient;
  reusable, also for P-09's `nuTwo`) and the **`ν_ur`-descent** `nuUrBar`/`nuUrBar_maxProPMk`/
  `nu_ur_toAb_eq_of_maxProPMk_eq` (`ν_ur ∘ toAb` factors through `maxProPMk 2 G_{ℚ₂}` via
  `maxProPHomEquiv`) — the well-definedness `prop_1_1`'s `ν_ur`-rows read off `maxProPMk`.
  **`prop_1_1` itself remains blocked**: its assembly composes B4's iso with Lemma 3.5's marked
  abelianization (**P-07**, in progress) and Prop. 3.8's automorphism lift (**P-08**, ⚠ escalated —
  needs the new "ℤ₂-powering on pro-2 groups" foundations ticket, `docs/section3-extraction.md`
  §Escalations 4).  When those land, `prop_1_1` closes by: take `e₀` from B4, correct its marking by
  the Prop. 3.8 lift of the (orientation-preserving, by B3c) automorphism `φ = e₀^{ab}∘e'` matching
  Lemma 3.5's `e'`, then the `ν_ur`-rows follow from `nuUrBar_maxProPMk` + `Reciprocity.lean`'s
  eq. (13) rows.  So P-10 is unblocked-modulo-P-07/P-08, not design-blocked.
- **P-11**: the §4 objects as Lean structures (boundary frame, marked target, the two exact-image
  counts) + `theorem thm_4_2 : … := sorry`, with a design note justifying every encoding choice
  against the paper's §4 text; the `Γ_A`-side count must be *definitionally* the finite object §5
  computes with.  This is the step-2 keystone — over-document rather than over-abstract.
  *Done (`GQ2/BoundaryFrame.lean`; design note = module docstring; proved layer at std-3 with
  **zero** B-axioms).*  Shipped: `Ttame` (§3 display), `PiBd` = paper's `Π` (Prop 3.10 eq. (20),
  relator `x₀^{σ²}x₀[x₁,σ]` — conjugation by `σ` **squared**; pro-2 presentation encoded as
  `maxProPQuotient 2 ∘ profinitePresentation`, the Δ/T-12 pattern), `Ztwo := maxProPQuotient 2
  Zhat`, markings `nuT`/`nuTwo` via new `presentationLift` helper + T-05 universal property, with
  **proved** generator-value stress tests (`nuT_tameSigma` etc.); `boundarySubgroup`/`Boundary` =
  eq. (26) as the closed equalizer subgroup; `BoundaryFrame` (28) with `frameMap` β;
  `MarkedTarget` (Def 4.1, `[Finite Y]` carried — implied by paper) + `stratum`;
  `IsBoundaryLift`/`BoundaryLifts`/`exactImageCount` (29) on `ContSurj` (Nat.card convention;
  `finite_boundaryLifts` under t.f.g.).  **Key decision**: the (27) epimorphisms are a hypothesis
  bundle `BoundaryMaps` (B5/B6-style) — `Γ_A`-side pinned rigidly by 8 generator equations
  (Prop 3.10/3.14 proof), `G_ℚ₂`-side intrinsically (Lemma 3.3 2-core kernel for tame;
  `ker = proPKernel 2` for pro-2) + ν-compat + joint surjectivity; `thm_4_2` quantifies over
  witnesses (faithful per 3.14's "may be chosen" + §4's "once and for all"; residual-slack risk
  flagged in-file for P-17/P-20).  `thm_4_2_stratum` (second clause) is *derived*, fixing §8's
  consumption shape.  **Handoffs**: P-06 states Prop 3.10/3.14 against these defs (instantiation
  = P-09/P-10); P-12 consumes `IsBoundaryLift` + may promote `presentationLift`; P-16's
  `X_Γ(C)` = `BoundaryLifts`; P-17 removes the sorry.
- **P-12/P-14**: same statement-first discipline for §5 / §§6–7 (P-14 discovers §7's actual
  content at extraction — the step-1 docs only ever cite 6.x and 8.x).
  *P-12 done (`GQ2/FoxHeisenberg.lean`, namespace `GQ2.FoxH`; proved layer std-3, zero B-axioms).*
  Defs: `Marking.tameValue/wildValue` (relations (5)/(6) as elements, `= 1 ↔ Rel` proved);
  `WordLift A C` (paper's `(u,g)(v,h) = (u+g•v, gh)`, own instances — no `Multiplicative`
  wrappers); the word complex (30)/(31): `d0` proved-additive, `d1Fun` = the literal
  A-coordinates of the evaluated relator values at `liftMarking`, with **Fox linearity
  `d1Fun_add` as a named sorried obligation** (bundled `d1` built on it; `Z1w/H0w/H1w/H2w`
  ContCoh-shaped total defs); `ElemDual` (T-14 recipe, contragredient action); `HeisLift`
  (§5.2 Heisenberg ⋊, group laws proved incl. the char-2 center); `mixedB` = traced `β_t+β_w`;
  `stokesEval`/`expMod2` (general 5.7 form on Mathlib `FreeGroup`); `dualEval` + `fixedPts`;
  `x0Supported`, `IsSimpleModTwo`, `IsSelfDual` (the (56)-package: card numerics +
  ∃-descended nondegenerate pairing).  Sorried statements (15, all tagged P-13):
  `d1Fun_add`, `d1Fun_comp_d0`, 5.6, 5.7 (38)/(39), 5.8 (41)/(42), 5.11 (2-of-3 for
  `IsSelfDual`), 5.12, 5.13 (split/ramified shapes + (54) pairings), 5.15, 5.16 (T-14-style
  cup perfectness + `#H²(𝔽₂)=2` line clause; **B6/B7 enter only in its proof**).  Proved
  stress: `d1Fun_tame` (the (34) tame row in closed form — validates the whole convention
  stack), `expMod2_fgTame` ((0,1,0,0) exponent vector), `cor_5_17_card` (5.17-numerics wiring).
  **Deviations flagged in-module**: 5.10 carried as 5.8+5.6 component identities (no
  `HomologicalComplex`); 5.11 in quasi-iso-consequence form (cone (49) = proof device); 5.1
  absorbed by `powOmega2` encoding; **5.17's adjoint-boundary (58) deferred** (needs
  connecting-map infra in both theories — owner P-13, shape arbitrated by P-17); 5.2/5.3/5.4/
  5.14 + Remark 5.9's GL₂(𝔽₂) regression test = P-13 proof layer.
  *P-14 done (`GQ2/SectionSix.lean` + `GQ2/SectionSeven.lean`; sorry-free def-layers
  `GQ2/QuadraticFp2.lean` + `GQ2/Corestriction.lean`; design note + full display map + deviation
  ledger in `docs/section67-extraction.md`).*  25 sorried statements (18 §6 + 7 §7, all tagged
  P-15; verified std-3 + `sorryAx` only — no B-axiom leakage; proved infra at std-3):
  `graphPullback_mem_Z2` (6.1/(62)), 6.6, 6.8, 6.9×2 (unram/ram via (83)'s branches), 6.13×2
  (`D₈` fibre extension of the `decide`-checked `twoPointExt` + (96) against `evensNormH2`),
  6.15×3 ((103)/(104)/(105) per orbit type on `RegRep`, canonical `Quotient.out` transversals;
  (105) absorbs (100)), 6.16 (deep-unit Evens norm — `IsDeepUnit` via the spectral norm on
  `ℚ̄₂`, `‖b‖ < 1`; unramifiedness = index-2 fixing subgroups + equal norm value groups), 6.17×2
  (`deepPart`; `#X₊² = #H¹`; `Q⁰_loc`-vanishing), **6.18×2 (the headline Gauss counts on
  `Q0loc`)**, 6.14 ((102) via `FactorSet.comap` + `mapCoeff1`), 6.21 (consequence/splitting
  form), 6.22 ((121) cochain-exact mod explicit coboundary); §7: `exists_minimalBlock`,
  7.1×3 (radical/head-as-`R ≤ K⊓S`/dual-as-no-index-2), 7.2 (tame head via `Ttame`-marked
  surjection), 7.3, 7.4 (`∃ q̄_λ` bundled with spec — §8's Gauss-sum form).  Key encodings:
  `ι_F = inv_ℚ₂` = `iotaF D` through the `𝔽₂ ≅ μ₂` bridge (B6's `D.inv`, statements
  parametrized over `D : TateDuality 2` — statement layer axiom-free); ramified/unramified =
  `c tameTau`-action through `ρ = c ∘ B.tameF` (`BoundaryMaps`); `SemiProd` = same
  no-`Multiplicative` carrier convention as P-12's `WordLift` (independent convergence);
  democratic `arf`.  Trimmed with flags: 6.5→P-12 seam, 6.19→P-16 assembly, 6.4+6.7/6.10/6.11
  = P-15 proof-internal.
- **P-13/P-15/P-16**: proofs; each lemma cites its display number; `decide`-style finite
  verifications welcome where the paper's objects are literally finite (e.g. 6.13's two-point
  `D₈` class), `native_decide` still banned.
- **P-17**: the strong induction assembled exactly as §9 states it (Lemma 9.4 strict decrease as
  the termination measure; regimes 9.1/9.2/9.3 as separate lemmas); `thm_4_2`'s sorry removed.
- **P-18**: `main_surjection_count`'s sorry removed; `Statement.lean` untouched except the proof
  body (statement frozen since T-21 review).
- **P-19**: `main_presentation_literal`'s sorry removed by instantiating `main_presentation` at
  `GammaA` with P-03 + P-05 + B1 + P-18; **zero sorries repo-wide**, allowlist empty, census 10.
- **P-20**: `docs/review-packet-v2.md` → v3: every interior-node statement quoted with its paper
  display + the certificate table (per-node `#print axioms` vs App. D row), handed for human
  review before Wave-3 proofs begin.  Also carries **P-22's citation-faithfulness classification
  table** (`docs/review-packet.md` §2, four tiers: direct / classical+encoding / composite /
  unused) into the packet, so the human reviewer sees which leaves are composite interfaces
  (B3c, B8, B11) before checking them.
- **P-21** (the foundations ticket requested by P-08's escalation — `docs/section3-extraction.md`
  §Escalations 4; opened by user decision 2026-07-03): `GQ2/ZtwoPowering.lean`, **no axioms, no
  sorries, everything std-3** (pure infra; Ax = ∅).  Scoped phases, partial landing legible:
  **(i) the projection + seam iso** — `zhatProjTwo : ℤ̂ → Multiplicative ℤ₂` (defined as
  `(ofAdd 1) ^ᶻ ·`, so the T-06 anchors come free), surjectivity, `ker = proPKernel 2 ℤ̂`, and
  `ztwoEquivPadic : ContinuousMulEquiv (maxProPQuotient 2 Zhat) (Multiplicative ℤ₂)` pinned by
  `maxProPMk (ofInt 1) ↦ ofAdd 1` — **this is the `ι` of `prop_3_10_local_marked`**
  (`Ztwo := maxProPQuotient 2 Zhat` defeq) and the T-12 nice-to-have `maxPro2(ℤ̂) ≅ ℤ₂`;
  **(ii) `zpowZtwo`** — `x^u` for `u ∈ ℤ₂`, `x` in any pro-2 `P` (`IsProP 2 P` hypothesis), via
  `maxProPHomEquiv` through (i); anchors (`= x^n` on `ℤ`, exponent-additivity, composition
  `(x^u)^v = x^{uv}`, naturality, **uniqueness** of a continuous hom `ℤ₂ → P` by its value at
  `1`) and **bijectivity of `x ↦ x^u` for `u ∈ ℤ₂ˣ`**, specialized to odd integer powers
  (P-08's cube roots);
  **(iii) the `η`-facts** — `IsProP 2 ℤ₂ˣ` (uniform annihilation: `‖u^{2^k}−1‖ ≤ 2^{-(k+1)}` by
  ultrametric factoring, no binomials) and **`η`-injectivity**: `‖η−1‖ = 2⁻² ⇒ u ↦ η^u`
  injective (exact-level squaring + (ii)-bijectivity; no closed-subgroup classification) — the
  shared P-07 (`lemma_3_5_injective`) / P-08 (`prop_3_8_classification`) prerequisite, with the
  `η = (−3)⁻¹` anchor;
  **(iv) pro-2 Frattini/Burnside** ("surjective on `G/Φ(G)` ⇒ surjective") — phase 2 of this
  ticket (route sketched in the module docstring: Mathlib `frattini` + finite `p`-groups
  nilpotent + maximal-⇒-normal-index-`p`); its only consumers (`lemma_3_7`/`prop_3_8_lift`
  surjectivity legs) are still blocked on the escalation's (a) HNN gap, so (i)–(iii) land first.
  *Phases (i)–(iii) done (2026-07-03):* `GQ2/ZtwoPowering.lean`, imported in `GQ2.lean`;
  `lake build GQ2` + `check_axioms.sh` green; key theorems `lean_verify`'d = std-3
  (`ker_zhatProjTwo`, `zpowZtwo_zpowZtwo`, `pow_bijective_of_odd`, `isProP_two_unitsPadicInt`,
  `zpowZtwo_injective_of_exact_level`).  Deviations from the sketch, all documented in-file:
  the density workhorse is `multPadicIntHom_ext` (via Mathlib's
  `DenseRange.addChar_eq_of_eval_one_eq` — continuous homs out of `ℤ₂` are pinned by their
  value at `1`), which makes every algebraic law of `zpowZtwo` a one-liner; the `ker ⊆`
  direction of (i) identifies any finite 2-group quotient map of `ℤ̂` with a mod-`2^k` powering
  (`powZModTwoHom`, the finite-order `ℤ₂`-powering gadget) by `funext_ofInt`; part (iii)'s
  level-tracking is **divisibility-with-unit-witnesses** (`2^{k+1} ∣ u^{2^k} − 1`;
  `η^{2^k} − 1 = 2^{k+2}·unit`), no real-norm induction — norms appear once, at the
  `Units`-topology ball bridge; the `η`-hypothesis is the algebraic `η − 1 = 4a` (`a ∈ ℤ₂ˣ`,
  ⟺ `v₂(η−1) = 2`), with the `(−3)⁻¹` instance `zpowZtwo_injective_neg_three_inv` as the
  direct P-07/P-08 consumable.  Bonus: `zpowHat_eq_zpowZtwo` (T-06 `ẑ`-powers factor through
  `ℤ₂` on pro-2 groups — the `ω₂`-word bridge) and the `TotallyDisconnectedSpace ℤ₂ˣ/ℤ₂ᵐᵒᵖ`
  instances (Mathlib had compactness/T2 for units but not these).
  *Phase (iv) done (same day):* `GQ2/FrattiniCriterion.lean` (imported in `GQ2.lean`;
  `lake build GQ2.FrattiniCriterion` + guard green; `lean_verify` = std-3 on
  `coatom_index_of_pGroup`, `eq_top_of_forall_not_le_index_p`,
  `surjective_of_forall_index_p_quotient_surjective`).  **Index-`p`-detection form** — no
  `Φ(K)` object: finite ingredients `coatom_normal_of_pGroup` (via `IsPGroup.isNilpotent` +
  `Group.normalizerCondition_of_isNilpotent` + `Subgroup.NormalizerCondition.normal_of_coatom`,
  all Mathlib) and `coatom_index_of_pGroup` (the coatom quotient is simple by the subgroup
  correspondence, abelian since its centre is nontrivial-normal, so `ℤ/p` by
  `IsSimpleGroup.prime_card`); profinite side: `eq_top_of_forall_map_eq_top` (closed subgroup
  with full image in every open-normal quotient is dense, via
  `exist_openNormalSubgroup_sub_open_nhds_of_one` — no T2 needed), then pull a coatom above the
  image back (`Finite.to_isCoatomic` + `comap`; index preserved by
  `index_comap_of_surjective`).  Hom forms take `[T2Space K]` only (closed range).  Note at
  landing time the *aggregate* `lake build GQ2` was transiently red in `GQ2/SectionSeven.lean`
  (P-15's in-flight edits, unrelated); `GQ2.FrattiniCriterion`/`GQ2.ZtwoPowering` build green
  in isolation.
- **P-22** (opened from `docs/adversarial-axioms-review.md` recs 1/3/4/6; user decisions
  2026-07-04 — B8: keep the statement, fix the citation; B2: keep, relabel): **docstring/docs
  edits only — no axiom statement may change** (gate: `git diff GQ2/Foundations/Axioms.lean`
  touches comment lines only; census stays 12).  Deliverables: (i) `peripheralCyclotomicAction`
  (B8) docstring reframed as **composite** — Stix (decomposition group acts on cusp inertia
  through the cyclotomic character) **plus** a cyclotomic-surjectivity input (the
  `aut : ℤ_[2]ˣ → …`-for-every-unit form is the strengthening); name the available feeders
  explicitly (B2 globally; the B5-derived local surjectivity via `χ_cyc(rec u) = u⁻¹`, cf.
  P-07's `units_gen`/`markedHom_bijective` machinery) and record that the weakened
  cyclotomic-image form was considered and declined (downstream churn) — review §1 + fixes §1.
  (ii) `cyclotomicCharacter_two_surjective` (B2) docstring gains the honest label — **available
  but currently unused** (grep 2026-07-04: zero consumers; retained as B8's citation companion /
  future B8-elimination route) — review §4.  (iii) `dyadicOrientation` (B3c) reclassified as a
  **composite interface**: Labute orientation values + (Demushkin dualizing character = local
  cyclotomic character) + choice of a normalized B4 iso; note it **subsumes a marked B4**
  (downstream Ax lists citing B3c need not add B4) — review §3.  (iv) The 4-way
  **classification table** (direct: B1, B6, B7, B7′, B10 · classical + encoding choices: B4,
  B5, B9 · composite/project interface: B3c, B8, B11 · available-unused: B2) added to
  `docs/review-packet.md` (new section) and cross-referenced from `Axioms.lean`'s header;
  amend **P-20**'s row so packet v3 carries the table — review §6.  (v)
  `GQ2/AxiomLedger.lean` comment tags + `docs/literature-axioms*.md` updated where they echo
  the old citation stories.  Closed rows' history (e.g. P-08's "B2 turned out unneeded") stays
  untouched.  Gate: `lake build GQ2` green, guard green (census 12).
- **P-23** ✅ **DONE 2026-07-04 (Fable) — see the P-23 row above for the close record; outcome: bridge isolated as a `def IsUnramifiedQuadraticSpectral` (census 12→13), not axiomatized; (iv) citation-number verification deferred to P-20.** (review rec 2; **census change pre-approved** by user 2026-07-04 — the split route,
  not the bridge-doc route): **hard-sequenced — start only when P-15e's Tier-4 capstone
  (`evensNorm_deepUnit_vanish`) is committed and its row shows it landed** (its owner actively
  consumes B11 in uncommitted `HilbertLedger.lean`).  Deliverables: (i) replace
  `axiom dyadicNormCriterion` with named leaves `hilbertSymbol_normCriterion_finiteDyadic`
  (`[a] ∪ [b] = 0 ↔ ∃ x y, b = x² − a·y²` over each finite dyadic base — Serre's norm
  criterion) and `unramifiedQuadratic_units_are_norms` (unramified quadratic ⟹ every base unit
  is a norm); the repo's **spectral-norm proxy** ("unramified = equal spectral-norm value
  groups") is the risky translation layer — first TRY proving the bridge in-repo; if not
  provable, isolate it as a third leaf explicitly flagged *project convention, not a cited
  theorem* (review fixes §2).  (ii) Re-derive the **exact current `dyadicNormCriterion`
  statement as a theorem with the same name**, so `HilbertLedger.lean` and all consumers need
  **zero edits**; re-verify their `#print axioms` = std-3 + exactly the new leaves.  (iii)
  Census 12→13 (bridge proved) or 14 (bridge axiomatic): update the rules line above +
  `EXPECTED_AXIOMS` in `scripts/check_axioms.sh` (precise-staged, hot file) +
  `AxiomLedger.lean` tags + `docs/review-packet.md` §2 history + P-22's classification table,
  same commit (B10/B11-precedent wording).  (iv) Resolve the docstring's "citation display
  numbers pending PDF verification" against the Serre PDF in `references/`.  Gate:
  whole-library build green; guard green at the new census; P-15e's landed theorems re-verified
  unchanged.
- **P-24** (review rec 5): the review-time failure (`GQ2/WF72.lean:239` sorry) **self-resolved**
  (2026-07-04: the sorry is gone and the guard is green) — `WF72.lean` is P-15g's live
  prototype and **must not be touched**; this ticket removes the failure *mode*, not the file.
  Deliverables: (i) `scripts/check_axioms.sh` (precise-staged, hot): the per-file scan
  distinguishes **tracked** files (current behavior — FAIL) from **untracked** ones (`git
  ls-files` membership): untracked violations print a clearly-labeled **WARN** block and do not
  fail the gate (rationale: the gate certifies the committed library; a parallel session's
  mid-flight scratch must not block everyone's commits — review fixes §5).  (ii) Scratch
  convention documented in `step2-plan.md`'s conventions: prototypes importing repo modules go
  in the session scratchpad or a root `scratch/` (gitignored), not `GQ2/`; if under `GQ2/`
  anyway, expect WARN noise.  (iii) Self-test: a planted untracked sorry file WARNs without
  failing; a tracked violation still FAILs (test, then delete the plant).  Gate: guard green on
  the current tree; the script diff reviewed against parallel allowlist edits (reconstruct from
  HEAD).
